---
phase: 04.5-llm-provider-abstraction-real-cloud-dynamic-ollama
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/tests/unit/llm/__init__.py
  - backend/tests/unit/llm/conftest.py
  - backend/tests/unit/llm/test_protocol_conformance.py
  - backend/tests/unit/llm/test_ollama_provider.py
  - backend/tests/unit/llm/test_openai_provider.py
  - backend/tests/unit/llm/test_anthropic_provider.py
  - backend/tests/unit/llm/test_factory.py
  - backend/tests/integration/test_cloud_providers_real.py
autonomous: true
requirements:
  - REQ-llm-provider-abstraction
tags:
  - llm-provider
  - test-scaffolding
  - dependency-management

must_haves:
  truths:
    - "uv add succeeds for langchain-openai and langchain-anthropic; uv.lock regenerates with both packages and shared langchain-core>=1.3.2 peer."
    - "backend/tests/unit/llm/ exists as a test package and pytest collects (but skips) the failing stubs without import errors."
    - "Cloud-acceptance test file exists and is correctly skipped by pytest when OPENAI_API_KEY and ANTHROPIC_API_KEY are absent."
    - "LLMProvider Protocol does NOT inherit from BaseChatModel — it's a structural Protocol (annotated as future truth in the stubs)."
    - "BoundProvider is what bind_tools returns; do not conflate it with LLMProvider (annotated as future truth in the stubs)."
    - "API keys are session-scoped only; never log them; never persist them server-side (annotated as future truth in the stubs)."
  artifacts:
    - path: backend/pyproject.toml
      provides: "langchain-openai + langchain-anthropic listed in [project] dependencies"
      contains: "langchain-openai"
    - path: backend/tests/unit/llm/__init__.py
      provides: "Test package marker enabling per-provider unit-test layout"
    - path: backend/tests/unit/llm/conftest.py
      provides: "Shared httpx response builder (_mock_ollama_tags_response) reused by Ollama provider tests"
    - path: backend/tests/unit/llm/test_protocol_conformance.py
      provides: "Stub asserting all four (Ollama/OpenAI/Anthropic + factory) classes satisfy the Protocols (xfail until Wave 1+2 land)"
    - path: backend/tests/unit/llm/test_ollama_provider.py
      provides: "Failing stubs for OllamaProvider.validate_config (unreachable, model_not_installed) and list_models (sorted unique ids)"
    - path: backend/tests/unit/llm/test_openai_provider.py
      provides: "Failing stubs for OpenAIProvider.validate_config (missing_api_key when no key)"
    - path: backend/tests/unit/llm/test_anthropic_provider.py
      provides: "Failing stubs for AnthropicProvider.validate_config (missing_api_key when no key)"
    - path: backend/tests/unit/llm/test_factory.py
      provides: "Failing stubs for LLMProviderFactory.build dispatch + key-precedence (D-08)"
    - path: backend/tests/integration/test_cloud_providers_real.py
      provides: "Gated cloud acceptance test stub with module-level pytest.mark.skipif on OPENAI_API_KEY / ANTHROPIC_API_KEY"
  key_links: []
---

<objective>
Wave 0 scaffolding for Phase 4.5. Add the two new dependencies (langchain-openai, langchain-anthropic) and create the failing test stubs that subsequent waves implement against.

Purpose: Per VALIDATION.md and `<deep_work_rules>`, every later task must be able to point its `<verify><automated>` at a real test file. This plan creates those files with `pytest.skip("Wave N implements")` markers so collection succeeds and downstream waves replace the skip with the real assertion.

Output: Two new dependencies in pyproject.toml + uv.lock; one new test package (backend/tests/unit/llm/) with stubs for Protocol conformance + per-provider unit tests + factory tests; one integration test file with the gated cloud-acceptance skip pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase canonical context — read these in full before starting
@.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-CONTEXT.md
@.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-RESEARCH.md
@.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-PATTERNS.md
@.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-VALIDATION.md

# Existing analogs (read before scaffolding the test files)
@backend/tests/unit/test_provider_probe.py
@backend/tests/integration/test_session_probe.py
@backend/tests/conftest.py
@backend/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: uv add langchain-openai and langchain-anthropic; preserve langchain-ollama</name>
  <files>backend/pyproject.toml, backend/uv.lock</files>
  <read_first>
    - backend/pyproject.toml (current dependency list — line-by-line; preserve exact ordering and version pins for everything except the two additions)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-RESEARCH.md §"Standard Stack" (versions verified against PyPI 2026-05-16: langchain-openai 1.2.1, langchain-anthropic 1.4.3; both pin langchain-core>=1.3.2,<2.0.0 — confirms co-installability)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-RESEARCH.md §"Package Legitimacy Audit" (both packages are [ASSUMED] approved, published by langchain-ai org)
    - CLAUDE.md §"Package management" (uv exclusively — never pip, poetry, or conda)
  </read_first>
  <action>
    Run `cd backend && uv add langchain-openai langchain-anthropic` from the worktree root. Per RESEARCH.md §"Standard Stack" the bump from langchain-ollama 1.0.0 → 1.1.0 is OPTIONAL and not done in this plan; leave langchain-ollama at its current pinned version (`>=0.2.0`) unless uv resolves a higher version automatically. Do NOT remove `langchain` (umbrella) or `langgraph` — RESEARCH.md §"Open Questions" #1 defers their removal to Phase 6.

    After the add, verify the new dependencies appear in `[project] dependencies` of `backend/pyproject.toml` and that `backend/uv.lock` regenerated cleanly (no errors, no manual edits).

    Project skill /fastapi confirms uv is the canonical install tool here — do not invoke pip/poetry.
  </action>
  <verify>
    <automated>cd backend && uv sync && uv tree | grep -E "^(langchain-openai|langchain-anthropic)" | wc -l | grep -q 2</automated>
  </verify>
  <acceptance_criteria>
    - Source: `backend/pyproject.toml` `[project] dependencies` array contains both `"langchain-openai>=1.2.0"` (or whatever version uv resolves to from the latest 1.x) and `"langchain-anthropic>=1.4.0"` (or latest 1.x).
    - Source: `backend/pyproject.toml` STILL contains `langchain-ollama>=0.2.0` (or higher) — line not removed.
    - CLI: `cd backend && uv sync` exits 0.
    - CLI: `cd backend && uv run python -c "import langchain_openai, langchain_anthropic, langchain_ollama; print('ok')"` prints `ok` and exits 0.
    - Source: `backend/uv.lock` mentions both `name = "langchain-openai"` and `name = "langchain-anthropic"` blocks.
    - Source: `backend/pyproject.toml` does NOT contain a hand-edited `slow` marker change (the marker handling is Phase 4.4's concern, not this phase).
  </acceptance_criteria>
  <done>Both packages resolvable via uv; `uv tree` shows them; existing langchain-ollama still resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Create backend/tests/unit/llm/ test package with shared fixtures and per-provider stubs</name>
  <files>
    backend/tests/unit/llm/__init__.py,
    backend/tests/unit/llm/conftest.py,
    backend/tests/unit/llm/test_protocol_conformance.py,
    backend/tests/unit/llm/test_ollama_provider.py,
    backend/tests/unit/llm/test_openai_provider.py,
    backend/tests/unit/llm/test_anthropic_provider.py,
    backend/tests/unit/llm/test_factory.py
  </files>
  <read_first>
    - backend/tests/unit/test_provider_probe.py (the canonical source for httpx mocking + AAA test naming + `_mock_ollama_tags_response` helper; lines 1-88 are the analog template)
    - backend/tests/conftest.py (shared `auth_headers` + `mock_flight_client` + `mock_llm_provider` fixtures — do NOT duplicate these in the new conftest)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-PATTERNS.md §"Backend tests — backend/tests/unit/llm/test_ollama_provider.py (new)" (verbatim shape for how the migration test file should look; copy the imports + helper + skeleton)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-VALIDATION.md (Wave 0 list — these are the exact files to create)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-RESEARCH.md §"Validation Architecture — Wave 0 Gaps" (canonical list of stub names + test names)
    - ~/.claude/rules/python/testing.md (pytest markers, AAA pattern)
    - ~/.claude/rules/common/testing.md §"Test Naming" (descriptive names: `test_validate_config_returns_unreachable_on_connect_error`, not `test_1`)
  </read_first>
  <action>
    Create the package marker `backend/tests/unit/llm/__init__.py` (empty file — mirrors `backend/tests/unit/__init__.py`).

    Create `backend/tests/unit/llm/conftest.py`. Provide ONE shared helper exported as a fixture: `mock_ollama_tags_response(model_names: list[str]) -> MagicMock` that returns a MagicMock(spec=httpx.Response) with status_code=200, json() returning `{"models": [{"name": n, "model": n} for n in model_names]}`, and a no-op `raise_for_status`. Pattern is verbatim from `backend/tests/unit/test_provider_probe.py:24-30` (the existing `_mock_ollama_tags_response` helper). Do NOT redefine `auth_headers` — it lives in the parent `backend/tests/conftest.py` and is auto-picked up.

    Create `backend/tests/unit/llm/test_protocol_conformance.py` containing:
    - module-level `pytestmark = pytest.mark.unit`
    - one stub test `test_protocols_are_runtime_checkable` that imports nothing from `app.llm` (which doesn't exist yet) and uses `pytest.skip("Wave 1 implements LLMProvider + BoundProvider Protocols")` as the body — when Wave 1 lands, the skip is removed and the test asserts `isinstance(provider, LLMProvider)` for each provider class
    - placeholder docstrings citing RESEARCH.md §"Pattern 1: Two-Protocol Shape" so the executor in Wave 1 has the contract spelled out

    Create `backend/tests/unit/llm/test_ollama_provider.py`:
    - module-level `pytestmark = pytest.mark.unit`
    - imports the helper fixture from conftest (`mock_ollama_tags_response`)
    - four test stubs, each with `pytest.skip("Wave 2 implements OllamaProvider")` body:
      - `test_validate_config_returns_unreachable_on_connect_error` (mirrors test_provider_probe.py::test_probe_ollama_unreachable_on_connect_error)
      - `test_validate_config_returns_unreachable_on_timeout` (mirrors test_probe_ollama_timeout)
      - `test_validate_config_returns_model_not_installed_when_tag_missing` (mirrors test_probe_ollama_model_not_installed)
      - `test_validate_config_returns_none_when_model_present` (mirrors test_probe_ollama_success)
      - `test_list_models_returns_sorted_unique_ids_from_api_tags` (NEW — covers the dynamic-discovery success criterion)

    Create `backend/tests/unit/llm/test_openai_provider.py`:
    - module-level `pytestmark = pytest.mark.unit`
    - three test stubs (skip body):
      - `test_validate_config_returns_missing_api_key_when_key_is_none`
      - `test_validate_config_returns_missing_api_key_when_key_is_empty_string`
      - `test_validate_config_returns_none_when_key_present`

    Create `backend/tests/unit/llm/test_anthropic_provider.py`: mirror the OpenAI shape with provider name `anthropic`.

    Create `backend/tests/unit/llm/test_factory.py`:
    - module-level `pytestmark = pytest.mark.unit`
    - four test stubs (skip body):
      - `test_factory_builds_ollama_provider_for_provider_name_ollama`
      - `test_factory_builds_openai_provider_for_provider_name_openai`
      - `test_factory_builds_anthropic_provider_for_provider_name_anthropic`
      - `test_factory_uses_payload_api_key_over_settings_env_key` (D-08 precedence)
      - `test_factory_falls_back_to_settings_env_key_when_payload_key_is_none` (D-08 fallback path)
      - `test_factory_raises_value_error_for_unknown_provider`

    Every file: keep imports minimal (do NOT import `app.llm.*` yet — those modules land in Wave 1+2). Use `pytest.skip("Wave N implements ...")` as the test body so collection passes today.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/llm -q --co 2>&1 | grep -c "test_" | (read n; test "$n" -ge 14)</automated>
  </verify>
  <acceptance_criteria>
    - Source: `backend/tests/unit/llm/__init__.py` exists (zero-byte file is acceptable).
    - Source: `backend/tests/unit/llm/conftest.py` defines a fixture `mock_ollama_tags_response` (decorated with `@pytest.fixture`) returning a callable or factory.
    - Source: each of the five test files (`test_protocol_conformance.py`, `test_ollama_provider.py`, `test_openai_provider.py`, `test_anthropic_provider.py`, `test_factory.py`) declares `pytestmark = pytest.mark.unit` at module scope.
    - Behavior: `cd backend && uv run pytest tests/unit/llm -q` exits 0 with all tests SKIPPED (not failed, not errored). Skip reasons mention "Wave 1 implements" or "Wave 2 implements" or "Wave 3 implements".
    - Behavior: `cd backend && uv run pytest tests/unit/llm --co -q` lists ≥14 collected test ids (5 ollama + 3 openai + 3 anthropic + 6 factory + 1 protocol = at least 18; floor of 14 to allow planner-discretion combining).
    - Source: NO test file imports from `app.llm.*` (the package doesn't exist yet); imports are limited to `pytest`, `httpx`, `unittest.mock`.
    - Source: every test name uses snake_case behavior-first naming; no `test_1`, `test_foo`, or other low-information names.
  </acceptance_criteria>
  <done>Test package exists, all stubs are SKIPPED (not failed) under default pytest run; Wave 1+2 will replace skip bodies with real assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Create the gated cloud-acceptance integration test stub</name>
  <files>backend/tests/integration/test_cloud_providers_real.py</files>
  <read_first>
    - backend/tests/integration/test_session_probe.py (analog file — module-level `pytestmark = pytest.mark.integration`, TestClient pattern; skip pattern is the only addition)
    - backend/tests/e2e/README.md (policy: real-LLM tests do NOT belong in tests/e2e; they go in tests/integration with a skipif gate per phase 4.5)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-RESEARCH.md §"Code Examples — Pytest Skip Pattern for Real Cloud Acceptance Tests" (the canonical pattern; file lives in tests/integration/ NOT tests/e2e/)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-VALIDATION.md §"Wave 0 Requirements" (this file is mandated)
    - ROADMAP success criterion #6: "Happy-path acceptance test exists for each real cloud provider, gated on the corresponding API key being present in the test environment (skipped in default PR CI)."
  </read_first>
  <action>
    Create `backend/tests/integration/test_cloud_providers_real.py` containing two test stubs:
    - `test_openai_provider_real_chat_turn` — function-level decorator `@pytest.mark.skipif(not os.getenv("OPENAI_API_KEY"), reason="OPENAI_API_KEY not set")`; stub body `pytest.skip("Wave 5 implements real OpenAI acceptance")`.
    - `test_anthropic_provider_real_chat_turn` — function-level decorator `@pytest.mark.skipif(not os.getenv("ANTHROPIC_API_KEY"), reason="ANTHROPIC_API_KEY not set")`; stub body `pytest.skip("Wave 5 implements real Anthropic acceptance")`.

    Module level: `pytestmark = pytest.mark.integration`. Function-level skipif decorators are used (NOT a module-level skipif) so that the OpenAI test still runs even if only OPENAI_API_KEY is set (and vice versa).

    Imports limited to: `os`, `pytest`. No `app.llm.*` imports — Wave 5 wires those.

    Add a module-level docstring citing RESEARCH.md §"Pytest Skip Pattern" and `backend/tests/e2e/README.md` policy that real-LLM tests live in tests/integration, not tests/e2e.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/test_cloud_providers_real.py -q 2>&1 | tee /tmp/04.5-01-task3.out && grep -E "skipped|deselected" /tmp/04.5-01-task3.out</automated>
  </verify>
  <acceptance_criteria>
    - Source: `backend/tests/integration/test_cloud_providers_real.py` exists.
    - Source: file declares `pytestmark = pytest.mark.integration` at module level.
    - Source: file defines exactly two top-level test functions: `test_openai_provider_real_chat_turn` and `test_anthropic_provider_real_chat_turn`.
    - Source: each test has its OWN function-level `@pytest.mark.skipif(not os.getenv("<KEY>"), reason="...")` decorator (not module-level), so they can be skipped independently.
    - Behavior: with no API keys set, `cd backend && uv run pytest tests/integration/test_cloud_providers_real.py -q` exits 0 with both tests SKIPPED (skipif fires before the inner `pytest.skip("Wave 5 ...")` body matters).
    - Source: file does NOT import from `app.llm.*` (those modules don't exist in Wave 0).
    - Source: docstring cites `backend/tests/e2e/README.md` and the placement rationale.
  </acceptance_criteria>
  <done>Cloud acceptance test file exists; both tests skipped cleanly when env vars are absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds dependencies and test scaffolding only. The trust boundaries are unchanged from Phase 4.4 (auth at HTTP edge, untrusted user input crosses into FastAPI route handlers, outbound localhost traffic to Ollama).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.5-01-SC | Tampering | uv add langchain-openai / langchain-anthropic | mitigate | Both packages [ASSUMED] approved in RESEARCH.md §"Package Legitimacy Audit"; published by canonical `langchain-ai` GitHub org; verified co-installable in /tmp scratch project on 2026-05-16. Slopcheck unavailable at research time — installation in this worktree functions as a registry-existence smoke test. |
| T-04.5-01-02 | Information Disclosure | new test files containing fake API keys | accept | Test stubs use literal `"sk-bogus"` strings only (never a real key); skipif gate ensures real-key tests never run in default CI (which would expose key presence in logs). |
</threat_model>

<verification>
- `cd backend && uv sync` exits 0; both new packages resolve.
- `cd backend && uv run pytest tests/unit/llm -q` exits 0 with all tests skipped.
- `cd backend && uv run pytest tests/integration/test_cloud_providers_real.py -q` exits 0 with both tests skipped.
- `cd backend && uv run pytest -q` (full suite) still exits 0 — the new skips do not break the existing 4.2/4.3 test suite.
- `cd backend && uv run mypy app --strict` exits 0 (no app changes; this is a sanity check).
</verification>

<success_criteria>
- pyproject.toml lists langchain-openai and langchain-anthropic; uv.lock regenerates.
- backend/tests/unit/llm/ contains 1 conftest + 5 test files, all collected and skipped.
- backend/tests/integration/test_cloud_providers_real.py contains 2 skipif-gated stubs.
- The full test suite still passes (`just test` exits 0).
</success_criteria>

<output>
Create `.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-01-SUMMARY.md` recording: dep versions resolved, count of stubs created, any deviations from PATTERNS.md analogs.
</output>
