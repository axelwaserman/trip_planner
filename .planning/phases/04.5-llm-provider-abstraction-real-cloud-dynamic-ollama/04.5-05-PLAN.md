---
phase: 04.5-llm-provider-abstraction-real-cloud-dynamic-ollama
plan: 05
type: execute
wave: 2
depends_on:
  - 04.5-02
files_modified:
  - backend/app/llm/providers/anthropic.py
  - backend/tests/unit/llm/test_anthropic_provider.py
autonomous: true
requirements:
  - REQ-llm-provider-abstraction
tags:
  - llm-provider
  - anthropic
  - cloud-provider

must_haves:
  truths:
    - "AnthropicProvider is a REAL implementation, not a stub — it wraps langchain_anthropic.ChatAnthropic."
    - "AnthropicProvider.validate_config does key-presence ONLY (no outbound API call by default per D-13). Returns MISSING_API_KEY when the key is None or empty after strip."
    - "AnthropicProvider must check api_key truthiness BEFORE constructing ChatAnthropic (Pitfall 2: ChatAnthropic.anthropic_api_key is a REQUIRED SecretStr — instantiating with None raises Pydantic ValidationError)."
    - "bind_tools is the only place ChatAnthropic gets constructed. It runs AFTER validate_config has succeeded — the factory + ChatService.create_session ordering in Plan 06 enforces this."
    - "AnthropicProvider.list_models returns a curated list (D-04: cloud providers do not perform live discovery in 4.5)."
  artifacts:
    - path: backend/app/llm/providers/anthropic.py
      provides: "AnthropicProvider class implementing LLMProvider Protocol"
      contains: "class AnthropicProvider:"
      min_lines: 35
    - path: backend/tests/unit/llm/test_anthropic_provider.py
      provides: "Real assertions for validate_config (None/empty/valid) + list_models curated"
  key_links:
    - from: backend/app/llm/providers/anthropic.py
      to: backend/app/llm/protocol.py
      via: "implements LLMProvider via duck typing"
      pattern: "from app\\.llm\\.protocol import BoundProvider"
    - from: backend/app/llm/providers/anthropic.py
      to: backend/app/llm/errors.py
      via: "from app.llm.errors import ProbeError, ProbeErrorCode"
      pattern: "from app\\.llm\\.errors import"
---

<objective>
Implement AnthropicProvider — the second REAL cloud provider via API key (ROADMAP success criterion #3). Wraps langchain_anthropic.ChatAnthropic; bind_tools delegates to the SDK; validate_config does key-presence only by default.

Purpose: Symmetric with OpenAIProvider, but with one critical difference (Pitfall 2): ChatAnthropic requires a non-None api_key at CONSTRUCTION time (Pydantic SecretStr is required, not Optional). So bind_tools must run AFTER validate_config has confirmed the key is present. The factory + ChatService.create_session ordering in Plan 06 enforces this; this plan's AnthropicProvider includes a defensive assert in bind_tools as the inner-loop guarantee.

Output: AnthropicProvider class + 4 unit tests replacing the Wave 0 skips. The factory wiring lands in Plan 06; the gated real-API acceptance test lands in Plan 09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-CONTEXT.md
@.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-RESEARCH.md
@.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-PATTERNS.md

@backend/app/llm/protocol.py
@backend/app/llm/errors.py
@backend/app/services/provider_probe.py
@backend/tests/unit/test_provider_probe.py
@backend/tests/unit/llm/test_anthropic_provider.py
</context>

<interfaces>
<!-- Contracts the executor needs. -->

From backend/app/llm/protocol.py (Plan 02 output):
- LLMProvider Protocol with get_provider_name, validate_config, bind_tools, list_models.
- BoundProvider Protocol with ainvoke + astream.

From backend/app/llm/errors.py (Plan 02 output):
- ProbeErrorCode.MISSING_API_KEY = "missing_api_key"
- ProbeError(error: ProbeErrorCode, message: str, hint: str)

From langchain_anthropic 1.4.3 (verified in RESEARCH.md):
- ChatAnthropic(model: str, api_key: SecretStr, temperature: float, max_tokens: int) — api_key is REQUIRED (SecretStr, not Optional). Constructing with None raises Pydantic ValidationError BEFORE any user code can intercept it. This is Pitfall 2.
- Constructor accepts a plain str (Pydantic SecretStr coerces).
- ChatAnthropic.bind_tools(tools: list[BaseTool]) returns Runnable[LanguageModelInput, AIMessage] (same shape as ChatOpenAI).

From RESEARCH.md curated cloud lists (D-04):
- Anthropic: ["claude-3-5-sonnet-20241022", "claude-3-5-haiku-20241022", "claude-3-opus-20240229"] — matches Settings.get_available_providers()["anthropic"]["models"].
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app/llm/providers/anthropic.py implementing AnthropicProvider</name>
  <files>backend/app/llm/providers/anthropic.py</files>
  <read_first>
    - backend/app/services/provider_probe.py lines 90-103 (the 4.2 missing-key message + hint shape; Anthropic title-cases provider name)
    - backend/app/llm/protocol.py (LLMProvider + BoundProvider Protocols)
    - backend/app/llm/errors.py (ProbeError + ProbeErrorCode)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-PATTERNS.md §"backend/app/llm/providers/anthropic.py" (verbatim shape — including the assert before ChatAnthropic construction)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-RESEARCH.md §"Code Examples — Constructing ChatAnthropic" + §"Pitfall 2: ChatAnthropic Requires a Non-Empty api_key at Construction"
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-CONTEXT.md D-04, D-08, D-09, D-13
    - CLAUDE.md (mypy strict; tunable thresholds on Settings)
  </read_first>
  <behavior>
    - validate_config returns None when self._api_key is a non-empty string.
    - validate_config returns ProbeError(MISSING_API_KEY) when self._api_key is None or empty-after-strip.
    - validate_config makes NO outbound HTTP calls — purely local key-presence check (D-13).
    - bind_tools constructs ChatAnthropic(model=self._model, api_key=self._api_key) ONLY when self._api_key is truthy. The defensive assert protects against direct callers who skip validate_config (Pitfall 2).
    - list_models returns a curated 3-element list verbatim.
    - get_provider_name returns "anthropic".
  </behavior>
  <action>
    Create backend/app/llm/providers/anthropic.py:

    Imports: `from collections.abc import Sequence`, `from langchain_core.tools import BaseTool`, `from langchain_anthropic import ChatAnthropic`, `from app.llm.errors import ProbeError, ProbeErrorCode`, `from app.llm.protocol import BoundProvider`.

    class AnthropicProvider:
    - __init__(self, model: str, api_key: str | None) -> None: stores both. NO Settings dependency.
    - get_provider_name(self) -> str: returns "anthropic".
    - async validate_config(self) -> ProbeError | None: if not self._api_key: return ProbeError(error=MISSING_API_KEY, message="Anthropic needs an API key.", hint="Set ANTHROPIC_API_KEY on the backend, or paste a key in /settings/providers."). Otherwise return None.
    - bind_tools(self, tools: Sequence[BaseTool]) -> BoundProvider: include the line `assert self._api_key is not None  # Pitfall 2: ChatAnthropic requires non-None api_key at construction; validate_config gates this`. Then construct llm = ChatAnthropic(model=self._model, api_key=self._api_key); return llm.bind_tools(list(tools)). Add `# type: ignore[return-value]` with inline justification (Runnable structurally satisfies BoundProvider). Note: the bare `assert` is acceptable here per the precondition pattern; an inline `# nosec B101` comment is unnecessary unless bandit is wired up — but a comment explaining the precondition is mandatory.
    - async list_models(self) -> list[str]: returns ["claude-3-5-sonnet-20241022", "claude-3-5-haiku-20241022", "claude-3-opus-20240229"].

    Module docstring documents: (a) D-13 — validate_config is key-presence only; (b) D-04 — curated cloud list; (c) Pitfall 2 — ChatAnthropic.anthropic_api_key is a required SecretStr (NOT Optional). validate_config MUST run before bind_tools so the SDK never sees None. The assert is a precondition guard; the route layer + factory + ChatService.create_session sequence in Plan 06 is where the actual ordering invariant is enforced.

    Do NOT call os.environ.get(...) anywhere.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "
from app.llm.providers.anthropic import AnthropicProvider
from app.llm.protocol import LLMProvider
p = AnthropicProvider(model='claude-3-5-sonnet-20241022', api_key='sk-ant-test')
assert isinstance(p, LLMProvider)
assert p.get_provider_name() == 'anthropic'
import asyncio
result = asyncio.run(p.validate_config())
assert result is None
p_no_key = AnthropicProvider(model='claude-3-5-sonnet-20241022', api_key=None)
result_missing = asyncio.run(p_no_key.validate_config())
assert result_missing is not None and result_missing.error == 'missing_api_key'
print('ok')
" && cd backend && uv run mypy app/llm --strict</automated>
  </verify>
  <acceptance_criteria>
    - Source: backend/app/llm/providers/anthropic.py defines class AnthropicProvider with __init__(self, model: str, api_key: str | None) -> None.
    - Source: AnthropicProvider implements all FOUR LLMProvider members.
    - Source: bind_tools method body contains a defensive assertion that self._api_key is not None BEFORE the ChatAnthropic() call (verifiable by grep `assert self._api_key is not None` in the bind_tools body).
    - Source: validate_config makes NO httpx call.
    - Source: list_models returns exactly 3 strings (claude-3-5-sonnet-20241022, claude-3-5-haiku-20241022, claude-3-opus-20240229) matching Settings.get_available_providers()["anthropic"]["models"].
    - Source: file does NOT import os; does NOT read os.environ.
    - Source: file does NOT import settings from app.config.
    - Behavior: isinstance(AnthropicProvider("claude-3-5-sonnet-20241022", "sk-ant-x"), LLMProvider) is True.
    - Behavior: validate_config(api_key=None) returns ProbeError(MISSING_API_KEY).
    - Behavior: validate_config(api_key="sk-ant-x") returns None.
    - CLI: `cd backend && uv run mypy app/llm --strict` exits 0.
  </acceptance_criteria>
  <done>AnthropicProvider exists, satisfies the LLMProvider Protocol, validate_config is key-presence only, bind_tools includes the Pitfall 2 precondition guard.</done>
</task>

<task type="auto">
  <name>Task 2: Replace skip stubs in tests/unit/llm/test_anthropic_provider.py with real assertions</name>
  <files>backend/tests/unit/llm/test_anthropic_provider.py</files>
  <read_first>
    - backend/tests/unit/llm/test_anthropic_provider.py (the Wave 0 skips)
    - backend/tests/unit/llm/test_openai_provider.py (Plan 04 sibling — the test shape is identical except for the model + hint string)
    - backend/tests/unit/test_provider_probe.py::test_probe_cloud_missing_key (4.2 analog)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-PATTERNS.md §"backend/app/llm/providers/anthropic.py"
    - ~/.claude/rules/python/testing.md, ~/.claude/rules/common/testing.md
  </read_first>
  <action>
    Replace the body of each test in backend/tests/unit/llm/test_anthropic_provider.py:

    - test_validate_config_returns_missing_api_key_when_key_is_none: Arrange: AnthropicProvider(model="claude-3-5-sonnet-20241022", api_key=None). Act: await validate_config(). Assert: result.error == ProbeErrorCode.MISSING_API_KEY; "ANTHROPIC_API_KEY" in result.hint or "/settings/providers" in result.hint.

    - test_validate_config_returns_missing_api_key_when_key_is_empty_string: Arrange: api_key="". Same assertions.

    - test_validate_config_returns_none_when_key_present: Arrange: api_key="sk-ant-test". Assert result is None.

    - test_list_models_returns_curated_anthropic_list: Arrange: provider with any key. Act: await list_models. Assert: result == ["claude-3-5-sonnet-20241022", "claude-3-5-haiku-20241022", "claude-3-opus-20240229"] (or a more lenient assertion that "claude-3-5-sonnet-20241022" in result and len == 3).

    Add one regression test for the Pitfall 2 guarantee:
    - test_bind_tools_does_not_construct_chat_anthropic_when_validate_config_was_skipped: Arrange: AnthropicProvider(model="claude-3-5-sonnet-20241022", api_key=None). Act: with pytest.raises(AssertionError): provider.bind_tools([]). Assert: AssertionError is raised (the precondition guard fires). This proves bind_tools never instantiates ChatAnthropic with None — preventing the Pydantic ValidationError that would otherwise leak from inside the SDK.

    Imports: `import pytest`, `from app.llm.errors import ProbeErrorCode`, `from app.llm.providers.anthropic import AnthropicProvider`. No httpx mocking required.

    Remove all `pytest.skip(...)` calls.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/llm/test_anthropic_provider.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `cd backend && uv run pytest tests/unit/llm/test_anthropic_provider.py -q` exits 0 with all tests PASSING.
    - Source: NO `pytest.skip(...)` calls remain.
    - Source: tests import from `app.llm.providers.anthropic` and `app.llm.errors`.
    - Source: at least 5 tests in the file (3 validate_config + 1 list_models + 1 bind_tools precondition).
    - Behavior: the bind_tools precondition test asserts an AssertionError is raised when api_key=None — proving Pitfall 2 protection.
    - CLI: `cd backend && uv run pytest tests/unit/llm -q` (full unit/llm suite) exits 0.
  </acceptance_criteria>
  <done>AnthropicProvider has real unit-test coverage including the Pitfall 2 regression guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → api.anthropic.com | bind_tools later constructs ChatAnthropic which speaks to api.anthropic.com. The api_key crosses this boundary embedded in HTTPS x-api-key header. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.5-05-01 | Information Disclosure | api_key in AnthropicProvider repr/str | mitigate | self._api_key is a private attribute; no __repr__/__str__ override exposes it. ChatAnthropic internally wraps it as Pydantic SecretStr (verified in RESEARCH.md). The provider class never logs it. |
| T-04.5-05-02 | Information Disclosure | api_key in Pydantic ValidationError stack trace if bind_tools called with None | mitigate | The defensive assert in bind_tools fires BEFORE ChatAnthropic construction, raising AssertionError instead of letting Pydantic produce a ValidationError that includes the field path. RESEARCH.md Pitfall 2 explicit. |
| T-04.5-05-03 | Spoofing | Empty/whitespace key bypass | mitigate | validate_config rejects None AND empty-after-strip via `if not self._api_key`. |
| T-04.5-05-04 | Tampering | Provider name impersonation | accept | Same mitigation as Plan 04 — factory match-dispatches on a fixed Literal. |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/unit/llm/test_anthropic_provider.py -q` exits 0 (5 tests pass).
- `cd backend && uv run mypy app/llm --strict` exits 0.
- `cd backend && uv run python -c "from app.llm.providers.anthropic import AnthropicProvider; p = AnthropicProvider('claude-3-5-sonnet-20241022', None); import asyncio; r = asyncio.run(p.validate_config()); assert r.error == 'missing_api_key'; print('ok')"` prints `ok`.
- The Pitfall 2 regression guard test passes: bind_tools raises AssertionError when api_key=None.
</verification>

<success_criteria>
- AnthropicProvider class exists, implements LLMProvider via duck typing.
- validate_config returns MISSING_API_KEY for None and empty-string keys; None for valid keys; makes no HTTP call.
- bind_tools has a defensive assert protecting against Pitfall 2.
- list_models returns the curated 3-element list.
- 5 unit tests pass; mypy strict passes.
</success_criteria>

<output>
Create `.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-05-SUMMARY.md` recording: tests passing, Pitfall 2 regression test in place.
</output>
