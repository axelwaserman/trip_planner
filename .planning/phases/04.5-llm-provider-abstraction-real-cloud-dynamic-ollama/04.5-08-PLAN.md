---
phase: 04.5-llm-provider-abstraction-real-cloud-dynamic-ollama
plan: 08
type: execute
wave: 5
depends_on:
  - 04.5-07
files_modified:
  - frontend/src/components/ProviderCard.tsx
  - frontend/src/pages/SettingsProviders.tsx
  - frontend/src/components/ChatInterface.tsx
  - frontend/src/App.tsx
  - frontend/src/components/__tests__/ProviderCard.test.tsx
  - frontend/src/pages/__tests__/SettingsProviders.test.tsx
autonomous: false
requirements:
  - REQ-llm-provider-abstraction
tags:
  - frontend
  - settings-page
  - chakra-ui

must_haves:
  truths:
    - "/settings/providers route exists in App.tsx; reachable from a Settings link in the chat header (sidebar component is OUT of orchestrator scope)."
    - "SettingsProviders page renders three ProviderCards (Ollama, OpenAI, Anthropic) per UI-SPEC; LM Studio is OUT of orchestrator scope."
    - "Each ProviderCard reads/writes the localStorage key 'provider_settings' (D-21 schema) — the same shape useChat reads in Plan 07."
    - "Selecting 'Use this provider' on a card writes provider_settings with the chosen provider+model+key/base_url, then navigates to /app where useChat picks up the new selection on its next mount (D-02: switching provider creates a new session)."
    - "ChatInterface header shows the active-model badge ({provider} · {model}) per D-23; clicking the badge navigates to /settings/providers. The old in-header ProviderSelector dropdown is REMOVED (D-19 + D-23)."
    - "API key inputs are type='password' with show/hide toggle, autoComplete='off' (UI-SPEC); helper text says 'Stored in this browser only. Never sent to our server.'"
    - "Color/typography/spacing tokens come from frontend/src/theme/index.ts; do NOT invent new tokens (UI-SPEC §'Executor handoff notes')."
  artifacts:
    - path: frontend/src/components/ProviderCard.tsx
      provides: "Composable card for one provider entry on the settings page"
      contains: "function ProviderCard"
      min_lines: 80
    - path: frontend/src/pages/SettingsProviders.tsx
      provides: "Settings page rendering 3 ProviderCards per UI-SPEC"
      contains: "function SettingsProviders"
    - path: frontend/src/App.tsx
      provides: "/settings/providers route registered behind RequireAuth; legacy /app and /login routes preserved"
      contains: "/settings/providers"
    - path: frontend/src/components/ChatInterface.tsx
      provides: "Header gains active-model badge; in-header ProviderSelector dropdown is removed; the SelectorErrorBanner is preserved (re-used verbatim per UI-SPEC §'Executor handoff notes' #2)"
      contains: "Settings"
  key_links:
    - from: frontend/src/pages/SettingsProviders.tsx
      to: frontend/src/components/ProviderCard.tsx
      via: "renders three ProviderCards (Ollama, OpenAI, Anthropic)"
      pattern: "<ProviderCard"
    - from: frontend/src/App.tsx
      to: frontend/src/pages/SettingsProviders.tsx
      via: "react-router-dom v7 Route path='/settings/providers' element={<RequireAuth><SettingsProviders /></RequireAuth>}"
      pattern: "/settings/providers"
    - from: frontend/src/components/ChatInterface.tsx
      to: /settings/providers
      via: "active-model badge is a button that navigate(/settings/providers) on click; the page-header Settings link uses Link"
      pattern: "useNavigate"
---

<objective>
Build the user-facing settings surface: a /settings/providers page with one ProviderCard per provider (Ollama, OpenAI, Anthropic — three providers per orchestrator scope) and a ChatInterface header that shows the active-model badge. Picking a provider on the settings page writes to the localStorage key Plan 07 wired up; on next mount of /app, useChat picks up the new selection and creates a fresh session.

Purpose: This is the user-visible deliverable for the phase — without it, the dynamic-discovery + cloud-provider work has no UI affordance. This plan implements UI-SPEC.md §"Component contracts" for ProviderCard + the page shell. The Sidebar component from UI-SPEC is OUT of orchestrator scope — a single Settings nav link in the chat header replaces it.

Output: ProviderCard component + SettingsProviders page + extended App.tsx routing + modified ChatInterface header. Manual UAT checkpoint at the end (the ONE checkpoint in this plan) — visual + interaction verification per UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-CONTEXT.md
@.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-RESEARCH.md
@.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-PATTERNS.md
@.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-UI-SPEC.md

@frontend/src/pages/Login.tsx
@frontend/src/components/auth/LoginForm.tsx
@frontend/src/components/chat/SelectorErrorBanner.tsx
@frontend/src/components/chat/UserMenu.tsx
@frontend/src/components/ChatInterface.tsx
@frontend/src/components/ProviderSelector.tsx
@frontend/src/lib/auth.ts
@frontend/src/lib/providerErrors.ts
@frontend/src/hooks/useChat.ts
@frontend/src/App.tsx
@frontend/src/theme/index.ts
</context>

<interfaces>
<!-- Contracts the executor needs. Project skill /chakra-ui is highly relevant — use Chakra v3 primitives only. -->

From frontend/src/pages/Login.tsx (the editorial-direction analog — Grid layout, Fraunces display heading, eyebrow + accent underline pattern):
- Grid templateColumns={{ base: '1fr', lg: '1.4fr 1fr' }} with bg.canvas / bg.surface tokens.
- Heading as="h1" fontFamily="display" with clamp() responsive size.
- Box as="span" eyebrow with accent.solid + 0.04em letter-spacing + uppercase + 13px.

From frontend/src/components/auth/LoginForm.tsx lines 76-128 (the Field.Root form pattern + show/hide password toggle):
- Field.Root invalid={...} with Field.Label htmlFor, Input id, Field.ErrorText role="alert" aria-live="polite".
- Show/hide toggle: IconButton with Eye / EyeOff lucide icons.
- Submit button: <Button colorPalette="accent" type="submit" loading={isLoading}>.

From frontend/src/components/chat/SelectorErrorBanner.tsx (RE-USE VERBATIM):
- Renders a ProviderErrorView prop with message + hint + inline code chips. UI-SPEC §"Executor handoff notes" #2 mandates re-use; do NOT recreate the visual treatment.

From frontend/src/lib/providerErrors.ts (Plan 07 output):
- ProviderErrorView interface; mapProbeError function.
- The F5 code 'invalid_api_key' is wired but no 4.5 endpoint emits it.

From frontend/src/lib/auth.ts:
- apiFetch wrapper that handles 401 redirects.

From frontend/src/App.tsx (current 22 lines — extend with the new route):
- Existing routes: /login, /app (protected), /, *.

From .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-UI-SPEC.md §"ProviderCard" (the component contract — copy + states + layout):
- Vertical stack inside Box bg=bg.surface borderWidth=1px borderColor=border.subtle borderRadius=lg p=6.
- Layout: eyebrow + status pill / provider name h2 / subtitle / optional Base URL field / optional API Key field with show/hide / Model select / Refresh + Test buttons row / Use this provider button.
- Status pill states: Ready (accent), Needs setup (muted), Unreachable (danger).

From UI-SPEC §"Copywriting Contract" (locked copy):
- Page title: "Providers"; eyebrow: "SETTINGS"; intro paragraph verbatim.
- Per-provider subtitles, helper texts, button labels — all locked.

From UI-SPEC §"Color":
- bg.canvas (60% dominant), bg.surface (30% secondary), accent.solid (10% reserved for primary CTA + status dots), danger.solid (errors only). NO new tokens.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ProviderCard component with Field.Root form pattern + Vitest spec</name>
  <files>
    frontend/src/components/ProviderCard.tsx,
    frontend/src/components/__tests__/ProviderCard.test.tsx
  </files>
  <read_first>
    - frontend/src/components/auth/LoginForm.tsx lines 76-128 (Field.Root + Input + show/hide toggle pattern — copy verbatim for the API key field)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-UI-SPEC.md §"ProviderCard (new — one per provider)" (the layout + states + interaction shape)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-UI-SPEC.md §"Copywriting Contract" (the locked subtitle/helper/label strings)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-UI-SPEC.md §"Iconography" (Eye/EyeOff for password toggle, RefreshCw for Refresh, Plug for Test connection — though Test connection button is wired BUT non-functional in 4.5; the /test endpoint is deferred)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-PATTERNS.md §"frontend/src/pages/SettingsProviders.tsx (NEW)"
    - frontend/src/theme/index.ts (the locked token surface — accent.solid, bg.canvas, etc.)
    - .claude/skills/chakra-ui/SKILL.md (active project skill — Chakra v3 component idioms)
    - ~/.claude/rules/typescript/coding-style.md (interface for props; named function components; no React.FC)
    - ~/.claude/rules/web/coding-style.md (file organization; PascalCase components; kebab-case CSS)
  </read_first>
  <action>
    Create frontend/src/components/ProviderCard.tsx — a presentational + state-managing card component for one provider entry.

    Define interface ProviderCardProps:
    - kind: 'ollama' | 'openai' | 'anthropic'
    - settings: {selected: {provider: string; model: string}; ollama: {base_url: string; models: string[]}; openai: {api_key: string; model: string}; anthropic: {api_key: string; model: string}} — the full ProviderSettings shape from Plan 07
    - onSave: (kind: 'ollama' | 'openai' | 'anthropic', updated: ProviderSettings) => void — fires when user clicks "Use this provider"

    Component structure:
    - <Box bg="bg.surface" borderWidth="1px" borderColor="border.subtle" borderRadius="lg" p="6"> (UI-SPEC layout).
    - Header row: eyebrow ("LOCAL" for ollama; "CLOUD" for openai/anthropic) + status pill (Ready/Needs setup/Unreachable; default to "Needs setup" since live probing is out of scope for the card itself; "Ready" when the saved settings have base_url for local OR api_key for cloud).
    - Heading as="h2" fontFamily="display" fontSize="24px": locked copy — "Ollama" / "OpenAI" / "Anthropic".
    - Subtitle text fg.secondary 15px — UI-SPEC locked copy: "Open-source models running on your machine. No key needed." (Ollama) / "gpt-4o, gpt-4o-mini, o-series. Bring your own key." (OpenAI) / "Claude 3.5 Sonnet, Haiku, Opus. Bring your own key." (Anthropic).
    - For Ollama: Field.Root with Field.Label "Base URL" + Input type="url" defaultValue="http://localhost:11434"; Field.HelperText "Where the daemon listens. Default works for most local setups."
    - For OpenAI/Anthropic: Field.Root with Field.Label "API Key" + Input type="password" autoComplete="off" + IconButton with Eye/EyeOff toggle (mirror LoginForm.tsx:98-111); Field.HelperText "Stored in this browser only. Never sent to our server."
    - Field.Root with Field.Label "Model" + native <select> populated from a hardcoded curated list per provider:
      - Ollama: settings.ollama.models if non-empty, else an empty-state option `<option disabled>No models discovered. Run \`ollama pull qwen3:4b\`, then refresh in Settings.</option>` (Plan 08b adds the live Refresh button that populates settings.ollama.models from `POST /api/providers/refresh`). DO NOT ship a hardcoded `["qwen3:4b"]` fallback — ROADMAP SC#2 requires no model list to be hard-coded, and Plan 06b's GET /api/providers now serves the cache directly.
      - OpenAI: ["gpt-4o", "gpt-4o-mini", "gpt-4-turbo", "o1-mini", "o3-mini"].
      - Anthropic: ["claude-3-5-sonnet-20241022", "claude-3-5-haiku-20241022", "claude-3-opus-20240229"].
    - Action row at bottom right: <Button colorPalette="accent">Use this provider</Button>. On click: build the updated ProviderSettings object (selected = {provider: kind, model: chosen}; per-provider entry updated with current form values); call onSave(kind, updated).

    Refresh button (RefreshCw icon, ghost style) and Test connection button (Plug icon, cloud-only, ghost style) are documented in UI-SPEC but the underlying endpoints are OUT of orchestrator scope. Do NOT include either button in this plan — keep the surface clean. A future phase wires them up.

    Use Chakra v3 named imports from '@chakra-ui/react': Box, Heading, Text, Stack, Field, Input, Button, IconButton, NativeSelect (or native <select> styled to match Input — UI-SPEC mentions native select; mirror existing ProviderSelector.tsx:121-138 pattern). Lucide icons: Eye, EyeOff (for the password toggle).

    The Save button writes to localStorage AND calls onSave so the parent page can navigate.

    Create frontend/src/components/__tests__/ProviderCard.test.tsx. Test cases:
    - `renders Ollama variant with Base URL field and no API Key field` — render <ProviderCard kind="ollama" ... />; assert document has a Base URL label; assert no API Key label.
    - `renders OpenAI variant with API Key field type=password and no Base URL field` — render kind="openai"; assert API Key label and input type='password'; assert no Base URL label.
    - `renders Anthropic variant with API Key field` — same as OpenAI.
    - `clicking show/hide toggle on API Key field flips input type between password and text` — render OpenAI; query the toggle button by its aria-label; click it; assert input.type === 'text'; click again; assert input.type === 'password'.
    - `clicking Use this provider calls onSave with the updated ProviderSettings shape` — render Ollama with default settings; change base_url input value; click "Use this provider"; assert mock onSave called with ('ollama', expect.objectContaining({selected: {provider: 'ollama', model: ...}})).

    Imports: `import { describe, it, expect, vi } from 'vitest'`, `import { render, screen, fireEvent } from '@testing-library/react'`, the Chakra v3 ChakraProvider for the test wrapper if needed (check existing test setup in frontend/src/test-setup.ts).
  </action>
  <verify>
    <automated>cd frontend && npm test -- ProviderCard --run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: frontend/src/components/ProviderCard.tsx exports a named function `ProviderCard` (NOT default export — matches LoginForm.tsx convention).
    - Source: ProviderCard accepts a `kind: 'ollama' | 'openai' | 'anthropic'` prop (string literal union — NOT including 'lmstudio' since LM Studio is out of scope).
    - Source: API key fields use `type="password"` AND `autoComplete="off"` (verifiable via grep).
    - Source: helper texts match UI-SPEC verbatim — grep `Stored in this browser only. Never sent to our server.` returns at least one hit.
    - Source: ProviderCard does NOT render a "Refresh models" button (out of scope) or a "Test connection" button (out of scope) — grep for 'Refresh' and 'Test connection' returns zero hits in this file.
    - Source: file does NOT invent new theme tokens — uses only existing tokens (bg.canvas, bg.surface, accent.solid, fg.primary, fg.secondary, border.subtle, danger.solid). Grep for `oklch(` returns zero hits in ProviderCard.tsx.
    - Behavior: `cd frontend && npm test -- ProviderCard --run` exits 0 with ≥5 tests pass.
    - CLI: `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>ProviderCard renders 3 variants per UI-SPEC; password show/hide works; onSave fires with updated ProviderSettings; tsc strict passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SettingsProviders page + register /settings/providers route + Vitest spec</name>
  <files>
    frontend/src/pages/SettingsProviders.tsx,
    frontend/src/App.tsx,
    frontend/src/pages/__tests__/SettingsProviders.test.tsx
  </files>
  <read_first>
    - frontend/src/pages/Login.tsx (the editorial Grid + heading shell — copy the layout direction)
    - frontend/src/components/ProviderCard.tsx (Task 1 output)
    - frontend/src/components/auth/RequireAuth.tsx (the auth wrapper for protected routes — same pattern wraps SettingsProviders)
    - frontend/src/App.tsx (current 22 lines — add the new Route)
    - frontend/src/lib/auth.ts (apiFetch — used to GET /api/providers if needed; for v1 we read from localStorage instead since GET /api/providers is unchanged in 4.5)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-UI-SPEC.md §"Copywriting Contract — Page-level" (Page title "Providers", eyebrow "SETTINGS", intro paragraph verbatim)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-PATTERNS.md §"frontend/src/pages/SettingsProviders.tsx (NEW)" (Grid + Heading shell)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-CONTEXT.md D-19 (route name) + D-20 (login lands on / not on settings)
    - .claude/skills/chakra-ui/SKILL.md
  </read_first>
  <action>
    Create frontend/src/pages/SettingsProviders.tsx:

    Component structure:
    - <Grid minH="100dvh" templateColumns={{ base: '1fr', lg: '1fr' }}> — single-column layout (Sidebar component is out of scope; UI-SPEC's two-column shape collapses to one column without it).
    - <GridItem bg="bg.canvas" px={{ base: '8', lg: '16' }} py={{ base: '12', lg: '16' }}>:
      - <Box as="span" eyebrow> SETTINGS (mirrors Login.tsx:21-29 with accent.solid + 0.04em + uppercase + 13px + 500 weight + mb=6).
      - <Heading as="h1" fontFamily="display" fontSize="clamp(40px, 4vw + 16px, 48px)" lineHeight="0.95" letterSpacing="-0.02em" color="fg.primary"> Providers (mirrors Login.tsx:31-43 but at the smaller "utility page" size from UI-SPEC §"Typography").
      - <Text mt="6" maxW="36ch" color="fg.secondary" fontSize="15px"> the locked intro paragraph: "Pick where the agent thinks. Local providers run on your machine; cloud providers send messages to OpenAI or Anthropic. Keys you paste here live in this browser only — never on our server."
      - <Stack gap="6" mt="8" maxW="640px">:
        - ProviderCard kind="ollama" settings={settings} onSave={handleSave}
        - ProviderCard kind="openai" settings={settings} onSave={handleSave}
        - ProviderCard kind="anthropic" settings={settings} onSave={handleSave}
      - <Box mt="8"> back-to-chat link: <Link to="/app">Back to chat</Link> (use react-router-dom v7 Link).

    State + handlers:
    - useState<ProviderSettings>(...) initialized from localStorage.getItem('provider_settings') (with the same migration-from-llm_provider_config logic as Plan 07 useChat — the page can either re-implement it or import a shared helper; recommend extracting to a frontend/src/lib/providerSettings.ts helper IF the migration touches the same logic in two places, otherwise inline is acceptable for v1).
    - handleSave(kind, updated): localStorage.setItem('provider_settings', JSON.stringify(updated)); useNavigate()('/app'). The navigation triggers a fresh useChat mount on /app, which reads the updated settings and creates a new session.

    Imports: Box, Grid, GridItem, Heading, Stack, Text from '@chakra-ui/react'; Link, useNavigate from 'react-router-dom'; ProviderCard from '../components/ProviderCard'.

    Modify frontend/src/App.tsx — add the new route inside the existing Routes block:
    ```
    <Route
      path="/settings/providers"
      element={<RequireAuth><SettingsProviders /></RequireAuth>}
    />
    ```
    Existing routes (/login, /app, /, *) stay UNCHANGED. The "/" redirect to /app per D-20 stays — the user is NOT redirected to settings on login.

    Create frontend/src/pages/__tests__/SettingsProviders.test.tsx. Test cases:
    - `renders three provider cards (Ollama, OpenAI, Anthropic) — not LM Studio` — render <SettingsProviders /> within MemoryRouter; query for headings "Ollama", "OpenAI", "Anthropic"; assert all present; assert NO "LM Studio" heading present.
    - `renders the Providers display heading and SETTINGS eyebrow` — assert document has "Providers" heading and "SETTINGS" eyebrow text.
    - `renders the locked intro paragraph` — assert text content includes "Keys you paste here live in this browser only — never on our server."
    - `clicking Use this provider on a card writes to provider_settings localStorage` — render in MemoryRouter; click "Use this provider" on the Ollama card; assert localStorage.setItem was called with key 'provider_settings' (use vi.spyOn(Storage.prototype, 'setItem')).
    - `clicking Use this provider navigates to /app` — render with a custom history/MemoryRouter; click; assert location is /app (use react-router-dom's testing helpers — see existing Login.test.tsx if present for the pattern).

    Imports: `import { describe, it, expect, vi } from 'vitest'`, `import { render, screen, fireEvent } from '@testing-library/react'`, `import { MemoryRouter, Route, Routes } from 'react-router-dom'`, `import { SettingsProviders } from '../SettingsProviders'`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- SettingsProviders --run && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source: frontend/src/pages/SettingsProviders.tsx exports `SettingsProviders` as a named function.
    - Source: page renders THREE ProviderCard instances (one each for ollama, openai, anthropic) — verifiable by grep `<ProviderCard` returning 3 hits in the file.
    - Source: page does NOT render a `<ProviderCard kind="lmstudio" ...>` (out of scope).
    - Source: page renders the locked intro paragraph verbatim — grep `Keys you paste here live in this browser only` returns at least one hit.
    - Source: frontend/src/App.tsx adds a Route for path="/settings/providers" wrapped in <RequireAuth>.
    - Source: App.tsx STILL has the / → /app Navigate, the * → /app Navigate, and the /login route — D-20 unchanged.
    - Behavior: `cd frontend && npm test -- SettingsProviders --run` exits 0 with ≥5 tests pass.
    - Behavior: `cd frontend && npm run build` exits 0 (production build succeeds).
    - CLI: `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Page exists at /settings/providers; renders three ProviderCards; Save flow writes localStorage and navigates to /app; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Modify ChatInterface header — add active-model badge + Settings nav link; remove old in-header dropdown</name>
  <files>frontend/src/components/ChatInterface.tsx</files>
  <read_first>
    - frontend/src/components/ChatInterface.tsx (current 240 lines — surgical edits to lines 88-117 of the header; preserve lines 118-239 of the messages + input)
    - frontend/src/components/chat/SelectorErrorBanner.tsx (PRESERVE its usage in the header — UI-SPEC §"Executor handoff notes" #2 mandates re-use)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-UI-SPEC.md §"ChatInterface modifications" (the locked badge shape: Box bg=bg.canvas borderWidth=1px borderColor=border.subtle borderRadius=full px=3 py=1 fontSize=13px color=fg.secondary; click → /settings/providers; tooltip on hover)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-CONTEXT.md D-23 (badge always visible) + D-19 (settings nav)
    - frontend/src/components/ProviderSelector.tsx (the old in-header dropdown — REFERENCE only; the file is NOT deleted in this plan — Plan 09 deletes orphan components if needed; this plan only stops importing it)
  </read_first>
  <action>
    Modify frontend/src/components/ChatInterface.tsx:

    Imports:
    - REMOVE `import { ProviderSelector } from './ProviderSelector'` (line 8). The ProviderSelector file is NOT deleted by this plan — Plan 09 handles orphan cleanup. The component just stops being imported.
    - ADD `import { Link } from 'react-router-dom'`.
    - ADD `import { Cpu, Settings as SettingsIcon } from 'lucide-react'` (Cpu icon for the active-model badge; SettingsIcon for the nav link — both per UI-SPEC §"Iconography").

    Header block (lines 88-117) — rewire:
    - The outer <Box bg="white" borderBottom="1px" borderColor="gray.200" p={4}> stays but the inner content changes.
    - First row (existing Flex justify="space-between" align="center" mb={2}):
      - Left: existing <Box> with title "Trip Planning Assistant" + subtitle "Ask me anything about planning your trip!" — UNCHANGED.
      - Right: REPLACE the <ProviderSelector .../> block with a stack of two elements:
        - The active-model badge (D-23): <Box as="button" onClick={() => navigate('/settings/providers')} bg="bg.canvas" borderWidth="1px" borderColor="border.subtle" borderRadius="full" px="3" py="1" fontSize="13px" color="fg.secondary" display="inline-flex" alignItems="center" gap="2" title={`This conversation is using ${currentProvider} · ${currentModel}. Switch in Settings.`}><Cpu size={14} />{currentProvider} · {currentModel}</Box> (use useNavigate from 'react-router-dom').
        - The Settings nav: <Link to="/settings/providers"><IconButton aria-label="Settings"><SettingsIcon size={20} /></IconButton></Link>. **Note:** Plan 08b removes this IconButton in favor of a single settings entry point via the Sidebar; it lives here in 08 only because 08 ships before Sidebar exists. Don't relocate or restyle it — 08b deletes it outright.
    - Second row (existing Flex justify="space-between" align="center"):
      - REMOVE the <Text fontSize="xs" color="gray.500">Using: {currentProvider} / {currentModel}</Text> line (the badge replaces it).
      - KEEP {username && <UserMenu username={username} />}.
    - SelectorErrorBanner (line 116) is PRESERVED verbatim — error rendering still works for the F1-F4 codes that POST /api/chat/session emits.

    Strip the now-unused state variable + handlers:
    - The `selectorError` state + setSelectorError + handleSelectorChange + handleRetry exist because of the old dropdown. With the dropdown gone, the only error path is providerError (from useChat hook). REMOVE selectorError + setSelectorError + handleSelectorChange + handleRetry; pass `activeError = providerError` directly to <SelectorErrorBanner /> and `onRetry={retryProvider}`.

    Run `cd frontend && npx tsc --noEmit` and fix any unused-import warnings. Run `cd frontend && npm run build` to confirm the production build passes.

    Document in the SUMMARY.md that ProviderSelector.tsx is now an orphan but NOT deleted — Plan 09 handles orphan cleanup if it's still unreferenced after the gated tests land.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source: frontend/src/components/ChatInterface.tsx does NOT import ProviderSelector — grep `from './ProviderSelector'` returns zero hits.
    - Source: ChatInterface.tsx does NOT render `<ProviderSelector` — grep returns zero hits.
    - Source: ChatInterface.tsx renders the active-model badge — grep `currentProvider} · ${currentModel}` (or equivalent template-literal match for the badge content) returns at least one hit.
    - Source: ChatInterface.tsx renders a Settings link to /settings/providers — grep `/settings/providers` returns at least one hit.
    - Source: ChatInterface.tsx PRESERVES the SelectorErrorBanner usage — grep `<SelectorErrorBanner` returns at least one hit.
    - Source: removed state variables (selectorError, handleSelectorChange, handleRetry) are no longer referenced — grep `selectorError` and `handleSelectorChange` return zero hits.
    - CLI: `cd frontend && npx tsc --noEmit` exits 0.
    - CLI: `cd frontend && npm run build` exits 0.
    - Behavior: existing ChatInterface tests (if any in `frontend/src/components/__tests__/` from 4.2) STILL pass after the rewire — they may need minimal updates if they exercised the dropdown; document in SUMMARY.
  </acceptance_criteria>
  <done>Header rewired with badge + Settings nav; old dropdown stripped; tsc + build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual UAT — verify the settings surface renders and the save-and-navigate loop works</name>
  <files>(no files modified — manual UAT only)</files>
  <action>Operator runs the steps in <how-to-verify> below.</action>
  <verify>
    <human-check>Operator confirms each numbered step in <how-to-verify> matches the expected result.</human-check>
  </verify>
  <done>Operator types "approved" — see <resume-signal>.</done>
  <what-built>
    Plan 08 ships the user-visible settings surface. Backend (Plans 02-06) accepts the new payload; Plan 07 wired useChat to send it; this plan delivers the page that lets the user pick a provider + model + key, plus a chat-header active-model badge and a Settings nav link. Manual UAT confirms the visual + interaction loop end-to-end.
  </what-built>
  <how-to-verify>
    1. Start the stack: `just backend` in one terminal, `just frontend` in another. Open http://localhost:5173/login and sign in (default `admin/admin`).
    2. After login, you should land on `/app` (chat surface). Confirm the chat header shows: title "Trip Planning Assistant", an active-model badge reading roughly `ollama · qwen3:4b`, a gear/Settings icon, and the username pill on the bottom row.
    3. Click the badge OR the Settings icon. You should navigate to `/settings/providers`.
    4. The settings page should render: eyebrow "SETTINGS", display heading "Providers", an intro paragraph mentioning "browser only — never on our server", THREE provider cards stacked vertically (Ollama / OpenAI / Anthropic — confirm there is NO "LM Studio" card), and a "Back to chat" link at the bottom.
    5. On the Ollama card: confirm there is a "Base URL" field (no API key field). Default value should be `http://localhost:11434`. Confirm the Model select is present.
    6. On the OpenAI card: confirm there is an "API Key" field of type=password (the dots, not the chars). Click the eye toggle next to the input — the chars should reveal; click again — they should hide. Confirm autocomplete is OFF (browser should not offer to autofill).
    7. On the Anthropic card: confirm same shape as OpenAI (API Key field with show/hide).
    8. Type a value into the OpenAI API Key field. Pick a model from the OpenAI Model select. Click "Use this provider".
    9. You should navigate back to `/app`. The active-model badge in the chat header should now read `openai · <chosen-model>`.
    10. If the OpenAI key you typed is gibberish: send a chat message. Backend should return a 400 ProbeError (MISSING_API_KEY if the key was empty/whitespace; otherwise the chat will actually try to call OpenAI and fail with a different error — that's fine, F5 INVALID_API_KEY is reserved but not emitted in 4.5). The error should render as a SelectorErrorBanner.
    11. Open browser devtools → Application → Local Storage → http://localhost:5173. Confirm a key `provider_settings` exists with the JSON shape: `{selected: {provider: "openai", model: "..."}, ollama: {...}, openai: {api_key: "...", model: "..."}, anthropic: {...}}`. Confirm the legacy key `llm_provider_config` is NOT present (or was removed by the migration).
    12. Reload the page (F5). Confirm the active-model badge still reads `openai · <model>` — settings persist via localStorage.
    13. Pick Ollama, click "Use this provider", reload — badge should read `ollama · ...`.
  </how-to-verify>
  <resume-signal>
    Type "approved" to mark Plan 08 complete and unlock Plan 09 (Wave 5: cloud acceptance tests + provider_probe.py deletion). If any step above fails, describe the symptom + which step number — Plan 08 will be revised.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM → localStorage | API keys typed into the form land in localStorage in plain text (v1 trust model). |
| browser DOM → backend POST /api/chat/session | The localStorage-stored key travels as a JSON body field on the next session-create. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.5-08-01 | Information Disclosure | API key visible on screen via show toggle | accept | UI-SPEC mandates the show/hide toggle for usability (paste verification). The toggle defaults to hidden. Phase 8 CSP hardens against XSS exfiltration. |
| T-04.5-08-02 | Information Disclosure | API key in form autofill / password manager | mitigate | autoComplete="off" on the API key field; password manager will not offer to save it. Field type=password ensures browser does not show the value in keystroke logs. |
| T-04.5-08-03 | Tampering | Cross-origin POST to /settings/providers | mitigate | RequireAuth wrapper redirects unauth'd users to /login; backend POST /api/chat/session requires JWT (Plan 06). |
| T-04.5-08-04 | Spoofing | A second tab modifying localStorage out from under the chat tab | accept | Cross-tab settings broadcast is explicitly out of scope (CONTEXT.md Deferred Ideas). The chat tab will use whatever localStorage value is read on its next mount. |
</threat_model>

<verification>
- `cd frontend && npm test -- ProviderCard SettingsProviders providerErrors useChat --run` exits 0.
- `cd frontend && npx tsc --noEmit` exits 0.
- `cd frontend && npm run build` exits 0.
- Manual UAT (Task 4 checkpoint) confirms the visual + interaction loop.
</verification>

<success_criteria>
- ProviderCard component renders the three provider variants per UI-SPEC.
- SettingsProviders page renders the three cards + heading + intro paragraph + back link.
- /settings/providers route registered behind RequireAuth.
- ChatInterface header shows the active-model badge + Settings nav; old dropdown stripped.
- All Vitest specs pass; tsc strict + production build green.
- Manual UAT signed off.
</success_criteria>

<output>
Create `.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-08-SUMMARY.md` recording: tests passing, manual UAT outcome, any deviations from UI-SPEC (e.g. if a Sidebar mini-version was inlined), any orphan components flagged for Plan 09.
</output>
