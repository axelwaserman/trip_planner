---
plan: 04.5-02
phase: 04.5-llm-provider-abstraction-real-cloud-dynamic-ollama
status: complete
completed: 2026-05-17
tasks_completed: 3
tasks_total: 3
files_changed: 5
---

# Plan 04.5-02 Summary — Contract Layer

## Outcome

Landed the contract layer the rest of Phase 4.5 plugs into:
- Two `@runtime_checkable` Protocols (`LLMProvider`, `BoundProvider`).
- Relocated wire-level error taxonomy (`ProbeErrorCode` + `ProbeError`) into
  `app/llm/errors.py`, with a new fourth `INVALID_API_KEY` member reserved per
  RESEARCH.md §"Error Contract".
- Per-session DTO (`SessionLLMConfig`, frozen dataclass, four fields per D-24).
- Factory skeleton (`LLMProviderFactory.__init__` only — `build()` raises
  `NotImplementedError` until Plan 06).
- Extended `SessionCreateRequest` with `base_url` + `api_key` fields and
  Pydantic validators (SSRF allowlist + 256-char key length cap).

## Files created / modified

| Path | Action | Notes |
|------|--------|-------|
| `backend/app/llm/__init__.py` | created | Package marker |
| `backend/app/llm/errors.py` | created | Relocated `ProbeErrorCode` (4 members) + `ProbeError` |
| `backend/app/llm/protocol.py` | created | `LLMProvider` (4 members) + `BoundProvider` (2 members) |
| `backend/app/llm/factory.py` | created | `SessionLLMConfig` + `LLMProviderFactory` skeleton |
| `backend/app/models.py` | modified | New fields + validators; rewired `ProbeErrorCode` import |

`backend/app/services/provider_probe.py` was intentionally **not modified** —
plan explicitly defers its deletion to Plan 09 so the existing route layer
keeps working until Plan 06 rewires it.

## Verification

- `uv run mypy app --strict` → clean (18 source files).
- Plan 02 verify command (existing tests unaffected by import rewire):
  `uv run pytest tests/unit/test_flight_models.py tests/integration/test_session_probe.py tests/unit/test_provider_probe.py -q` →
  **23 passed** in 0.19s.
- All three task-level inline verify commands pass:
  - Task 1: `ProbeErrorCode` has 4 members; `INVALID_API_KEY = "invalid_api_key"`.
  - Task 2: `LLMProvider` has `bind_tools`; `BoundProvider` has only `ainvoke` + `astream`; `SessionLLMConfig` is a frozen dataclass.
  - Task 3: All happy paths and all four ValidationError paths (length cap,
    non-allowlisted host, AWS metadata IP, file:// scheme) pass.

## Threat-register dispositions met

| Threat ID | Disposition | Evidence |
|-----------|-------------|----------|
| T-04.5-02-01 (SSRF via `base_url`) | mitigate | `_validate_base_url` enforces scheme ∈ {http, https} AND hostname ∈ {localhost, 127.0.0.1, host.docker.internal}. AWS metadata IP `169.254.169.254` is rejected. |
| T-04.5-02-02 (DoS via long `api_key`) | mitigate | `_strip_and_bound_api_key` caps `len(stripped) ≤ 256`. |
| T-04.5-02-03 (`api_key` in OpenAPI) | accept | Field description explicitly states "Stored in session memory only — never logged or persisted." Future Phase 8 ships the structlog scrubber. |
| T-04.5-02-04 (`ProbeErrorCode` wire stability) | accept | Values verbatim. Module docstring warns future contributors against renaming. |
| T-04.5-02-05 (provider-name injection) | mitigate | Plan 02 establishes `SessionLLMConfig` dataclass; Plan 06 dispatches via `match` against a fixed `Literal` set. |

## Deviations

**Recovery from stalled executor:**

The original executor agent (background subagent) stalled mid-task 2 after
writing `protocol.py` and `factory.py` but before committing. The orchestrator:

1. Rescued task 2 files from the stalled worktree to a temp directory.
2. Verified they matched the plan's acceptance criteria byte-for-byte
   (4 members on `LLMProvider`, 2 on `BoundProvider`, frozen dataclass,
   `NotImplementedError` skeleton, `mypy --strict` clean, both verify commands pass).
3. Merged task 1's clean commit (`feat(04.5-02): create app/llm package with
   relocated ProbeErrorCode + ProbeError`) from the stalled branch.
4. Removed the stalled worktree.
5. Committed task 2 files (`fd8ae66`) and task 3 changes (`e9fdb55`) on the
   phase branch directly — no functional difference from a clean executor run.

The recovery is recorded here per the GSD deviation contract; downstream
verification will see four commits on this plan instead of three (the extra
one is the merge commit from task 1's worktree branch).

No deviation from the plan's substance, acceptance criteria, or PATTERNS.md
shape.

## Next

- Plan 04.5-03 (Wave 2): `OllamaProvider` — implements the relocated `LLMProvider`
  Protocol; adds dynamic `/api/tags` discovery for `list_models`.
- Plan 04.5-04 (Wave 2): `OpenAIProvider`.
- Plan 04.5-05 (Wave 2): `AnthropicProvider`.
- Plan 04.5-06 (Wave 3): wires the factory `build()` body, ChatService,
  lifespan, and the extended `POST /api/chat/session` route.
