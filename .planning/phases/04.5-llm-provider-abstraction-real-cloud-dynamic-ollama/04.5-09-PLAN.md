---
phase: 04.5-llm-provider-abstraction-real-cloud-dynamic-ollama
plan: 09
type: execute
wave: 7
depends_on:
  - 04.5-08
files_modified:
  - backend/tests/integration/test_cloud_providers_real.py
  - backend/tests/unit/test_provider_probe.py
  - backend/app/services/provider_probe.py
  - backend/app/services/__init__.py
autonomous: true
requirements:
  - REQ-llm-provider-abstraction
tags:
  - llm-provider
  - cloud-acceptance-tests
  - cleanup
  - deletion

must_haves:
  truths:
    - "Real cloud acceptance tests exist for OpenAI and Anthropic, gated on OPENAI_API_KEY and ANTHROPIC_API_KEY env vars (ROADMAP success criterion #6)."
    - "Real cloud tests are SKIPPED in default PR CI (no API keys present in the integration job env) — they only run when the secrets are wired in a dedicated job."
    - "app/services/provider_probe.py is DELETED — its logic has migrated to app/llm/providers/{ollama,openai,anthropic}.py and its types live in app/llm/errors.py (D-12)."
    - "Old test_provider_probe.py is DELETED — the equivalent assertions live in app/llm/test_{ollama,openai,anthropic}_provider.py (Wave 2 outputs)."
    - "Full backend test suite continues to pass after the deletion — no orphan imports."
  artifacts:
    - path: backend/tests/integration/test_cloud_providers_real.py
      provides: "Real assertions replacing the Wave 0 skip stubs: one happy-path test per cloud provider, gated by skipif"
      contains: "from app.llm.providers.openai import OpenAIProvider"
  key_links:
    - from: backend/tests/integration/test_cloud_providers_real.py
      to: backend/app/llm/providers/openai.py
      via: "imports OpenAIProvider; constructs with os.environ['OPENAI_API_KEY']"
      pattern: "from app\\.llm\\.providers\\.openai import OpenAIProvider"
    - from: backend/tests/integration/test_cloud_providers_real.py
      to: backend/app/llm/providers/anthropic.py
      via: "imports AnthropicProvider; constructs with os.environ['ANTHROPIC_API_KEY']"
      pattern: "from app\\.llm\\.providers\\.anthropic import AnthropicProvider"
---

<objective>
Two cleanup deliverables:

1. **Real cloud acceptance tests** (ROADMAP success criterion #6) — replace the Wave 0 skip-stubs in tests/integration/test_cloud_providers_real.py with real assertions that prove OpenAIProvider and AnthropicProvider work end-to-end against the real cloud APIs. Tests skip when the corresponding env var is absent (default CI behavior); the test bodies use minimal token usage (one-word reply, max_tokens=1 where applicable).

2. **Delete the legacy probe module** (D-12) — remove app/services/provider_probe.py + its unit test (tests/unit/test_provider_probe.py). The full migration from "free-standing probe function" to "per-provider class with validate_config" is now complete; keeping the legacy module would violate the move-not-copy rule.

Purpose: The real cloud tests are the empirical proof that REQ-llm-provider-abstraction is met — without them the phase ships unverified. The deletion is the architectural-tax payment: leaving provider_probe.py around invites future contributors to add new logic to the wrong place.

Output: Two real cloud test bodies (gated by skipif); two deleted files; updated app/services/__init__.py if needed; full backend suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-CONTEXT.md
@.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-RESEARCH.md
@.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-PATTERNS.md

@backend/app/llm/providers/openai.py
@backend/app/llm/providers/anthropic.py
@backend/app/services/provider_probe.py
@backend/tests/unit/test_provider_probe.py
@backend/tests/integration/test_cloud_providers_real.py
@backend/tests/integration/test_session_probe.py
@backend/app/services/__init__.py
@backend/app/models.py
@backend/app/api/routes/routes.py
</context>

<interfaces>
<!-- Contracts the executor needs. -->

From backend/app/llm/providers/openai.py (Plan 04 output):
- OpenAIProvider(model: str, api_key: str | None) with validate_config (key-presence) + bind_tools (constructs ChatOpenAI).
- bind_tools returns a Runnable that exposes ainvoke + astream (BoundProvider).

From backend/app/llm/providers/anthropic.py (Plan 05 output):
- AnthropicProvider(model: str, api_key: str | None) — same shape, defensive assert in bind_tools (Pitfall 2).

From langchain_core.messages (verified in RESEARCH.md):
- HumanMessage(content=str) — the user-side message constructor.
- AIMessage.content — the assistant reply (string or list of content blocks).

From .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-RESEARCH.md §"Pytest Skip Pattern for Real Cloud Acceptance Tests":
- pytestmark = [pytest.mark.integration, ...] (module-level integration marker).
- Per-test skipif decorator on the function (NOT module-level skipif), so OpenAI runs even if only OPENAI_API_KEY is set and Anthropic skips (and vice versa).

From RESEARCH.md §"Runtime State Inventory" — files that import provider_probe and need to be unchanged AT THIS POINT:
- backend/app/api/routes/routes.py — Plan 06 already rewired the import to app.llm.errors; no work here.
- backend/app/models.py — Plan 02 already rewired the import; no work here.
- backend/tests/integration/test_session_probe.py — Plan 06 already rewired the monkeypatch; no work here.

The remaining importer is backend/tests/unit/test_provider_probe.py — this file IS deleted by this plan because the equivalent tests already exist in tests/unit/llm/test_{ollama,openai,anthropic}_provider.py (Wave 2 outputs).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement real cloud acceptance tests in tests/integration/test_cloud_providers_real.py</name>
  <files>backend/tests/integration/test_cloud_providers_real.py</files>
  <read_first>
    - backend/tests/integration/test_cloud_providers_real.py (the Wave 0 skip stubs from Plan 01 — replace each function body)
    - backend/app/llm/providers/openai.py (the OpenAIProvider class)
    - backend/app/llm/providers/anthropic.py (the AnthropicProvider class)
    - backend/tests/e2e/README.md (CI policy: real-LLM tests live in tests/integration with skipif gate, NOT in tests/e2e)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-RESEARCH.md §"Pytest Skip Pattern for Real Cloud Acceptance Tests" (the canonical test shape — module-level integration marker, function-level skipif, one-word "pong" assertion)
    - ROADMAP success criterion #6: "Happy-path acceptance test exists for each real cloud provider, gated on the corresponding API key being present in the test environment (skipped in default PR CI)."
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-RESEARCH.md §"Assumptions Log A2" (Anthropic max_tokens=1 produces a billable single-token response — acceptable for a smoke test; document in the test docstring)
  </read_first>
  <action>
    Replace the body of each test in backend/tests/integration/test_cloud_providers_real.py:

    Module-level setup (preserve from Plan 01): `pytestmark = pytest.mark.integration`, plus the imports at module top. Remove the `pytest.skip("Wave 5 implements ...")` calls and add the real bodies.

    Test 1: test_openai_provider_real_chat_turn — function-level decorator stays @pytest.mark.skipif(not os.getenv("OPENAI_API_KEY"), reason="OPENAI_API_KEY not set").
    Body (AAA pattern):
    - Arrange: provider = OpenAIProvider(model="gpt-4o-mini", api_key=os.environ["OPENAI_API_KEY"]). Assert await provider.validate_config() is None.
    - Act: bound = provider.bind_tools([]) (empty tools — pure chat turn). result = await bound.ainvoke([HumanMessage(content="Reply with just the single word: pong")]). Note: bound.ainvoke is async per the BoundProvider Protocol.
    - Assert: isinstance(result.content, str) AND "pong" in result.content.lower(). Use a tolerant assertion — the model may add punctuation.
    - Docstring: "End-to-end smoke test against api.openai.com. Gated on OPENAI_API_KEY. Costs ~1-3 tokens per run."

    Test 2: test_anthropic_provider_real_chat_turn — function-level decorator @pytest.mark.skipif(not os.getenv("ANTHROPIC_API_KEY"), reason="ANTHROPIC_API_KEY not set").
    Body (AAA):
    - Arrange: provider = AnthropicProvider(model="claude-3-5-haiku-20241022", api_key=os.environ["ANTHROPIC_API_KEY"]). Pick claude-3-5-haiku for cost (cheapest tier). Assert await provider.validate_config() is None.
    - Act: bound = provider.bind_tools([]). result = await bound.ainvoke([HumanMessage(content="Reply with just the single word: pong")]).
    - Assert: result.content is non-empty (Anthropic returns content as a list of content blocks; access result.content[0].text or string-coerce — verify the actual shape against langchain_anthropic 1.4.3). Use a tolerant assertion: assert "pong" in str(result.content).lower().
    - Docstring: "End-to-end smoke test against api.anthropic.com. Gated on ANTHROPIC_API_KEY. Costs a small handful of tokens per run; uses claude-3-5-haiku for the cheapest tier."

    Imports: `import os`, `import pytest`, `from langchain_core.messages import HumanMessage`, `from app.llm.providers.openai import OpenAIProvider`, `from app.llm.providers.anthropic import AnthropicProvider`.

    Document at the top of the file that these tests live in tests/integration/ (NOT tests/e2e/) per the policy in backend/tests/e2e/README.md.

    Note: these tests are async functions; pytest-asyncio's auto mode (already enabled via pyproject.toml) handles them — no @pytest.mark.asyncio decorator needed.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/test_cloud_providers_real.py -q -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: backend/tests/integration/test_cloud_providers_real.py imports `from app.llm.providers.openai import OpenAIProvider` and `from app.llm.providers.anthropic import AnthropicProvider`.
    - Source: file imports `from langchain_core.messages import HumanMessage`.
    - Source: each test has its own function-level `@pytest.mark.skipif(not os.getenv("<KEY>"), ...)` decorator (still per-function, not module-level — so the two tests are independent).
    - Source: file does NOT contain pytest.skip("Wave N implements...") calls — all bodies are real now.
    - Behavior: with neither env var set, `cd backend && uv run pytest tests/integration/test_cloud_providers_real.py -q` exits 0 with both tests SKIPPED (skipif fires).
    - Behavior: with both env vars set to real keys, the tests run and PASS — verified by an operator in a dedicated CI job. (CI does not have the keys; this is a manual smoke-test step.)
    - CLI: `cd backend && uv run mypy app --strict` exits 0 (no app changes here, but the test file uses real types, so mypy checking the test file should also pass — run `uv run mypy tests/integration/test_cloud_providers_real.py --strict` and confirm).
  </acceptance_criteria>
  <done>Cloud acceptance tests have real bodies; skipif gate works as documented; manual run with real keys passes (validation step is in the verification block, not in CI).</done>
</task>

<task type="auto">
  <name>Task 2: Delete app/services/provider_probe.py + tests/unit/test_provider_probe.py; verify no remaining importers</name>
  <files>backend/app/services/provider_probe.py, backend/tests/unit/test_provider_probe.py, backend/app/services/__init__.py</files>
  <read_first>
    - backend/app/services/provider_probe.py (the file being deleted — 103 lines; confirm Plan 02 + Plan 06 have already migrated all references)
    - backend/tests/unit/test_provider_probe.py (the file being deleted — 88 lines; confirm Plan 03 created equivalent tests in tests/unit/llm/test_ollama_provider.py and tests/unit/llm/test_openai_provider.py)
    - backend/app/services/__init__.py (verify whether it re-exports anything from provider_probe)
    - backend/app/api/routes/routes.py (Plan 06 should have already rewired the import)
    - backend/app/models.py (Plan 02 should have already rewired the import)
    - backend/tests/integration/test_session_probe.py (Plan 06 should have already rewired the monkeypatch)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-CONTEXT.md D-12 (the legacy module is DELETED, not kept as a back-compat shim)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-RESEARCH.md §"Runtime State Inventory" §"Module deletion" (the canonical importers list)
  </read_first>
  <action>
    Pre-flight check: `cd backend && grep -rn "provider_probe" app tests --include="*.py"` to enumerate all remaining references. Expected hits: zero in app/, zero in tests/ (after Plans 02 + 06 + 07 land). If any hits remain, surface them to the orchestrator BEFORE deletion — they indicate a missing rewire from earlier plans.

    Pre-flight check 2: `cd backend && grep -rn "ProbeErrorCode\|probe_provider\|ProbeError" app tests --include="*.py" | grep -v 'app/llm/' | grep -v 'tests/unit/llm/'` — should show zero hits in non-llm-package files (the only places that legitimately use these symbols are the new app/llm/ + tests/unit/llm/ directories, plus models.py and routes.py which already rewired their imports to app.llm.errors).

    Pre-flight check 3 (NEW — closes plan-checker warning #9): `cd backend && grep -c provider_probe app/services/__init__.py` MUST return 0 BEFORE the deletion proceeds. If non-zero, services/__init__.py still re-exports from provider_probe and needs to be edited to remove the re-export line. This catches the case where a stale `from .provider_probe import ...` line in __init__.py would silently break `from app.services import ProbeErrorCode` callers post-deletion.

    Delete backend/app/services/provider_probe.py.

    Delete backend/tests/unit/test_provider_probe.py.

    Inspect backend/app/services/__init__.py — if it re-exports anything from provider_probe (e.g. `from .provider_probe import ProbeErrorCode`), strip that line. If the file is empty after the strip, leave it as an empty package marker (the services/ subpackage still hosts other services in 4.2 — keep the marker).

    Run the full backend test suite to confirm no orphan imports: `cd backend && uv run pytest -q`.

    Run mypy strict to catch any residual type errors: `cd backend && uv run mypy app --strict`.

    Run ruff to catch unused-import warnings: `cd backend && uv run ruff check app tests`.
  </action>
  <verify>
    <automated>cd backend && uv run pytest -q && uv run mypy app --strict && uv run ruff check app tests</automated>
  </verify>
  <acceptance_criteria>
    - Source: backend/app/services/provider_probe.py does NOT exist after this task. `test -f backend/app/services/provider_probe.py` returns non-zero (file absent).
    - Source: backend/tests/unit/test_provider_probe.py does NOT exist. `test -f backend/tests/unit/test_provider_probe.py` returns non-zero.
    - Source: `cd backend && grep -rn "provider_probe" app tests --include="*.py"` returns zero hits (no surviving import or string reference).
    - Source: backend/app/services/__init__.py is either empty or contains only __all__ definitions or unrelated re-exports — `grep -c provider_probe backend/app/services/__init__.py` returns 0.
    - Behavior: `cd backend && uv run pytest -q` exits 0 (full suite green).
    - CLI: `cd backend && uv run mypy app --strict` exits 0.
    - CLI: `cd backend && uv run ruff check app tests` exits 0 (no unused imports).
    - Behavior: `cd backend && uv run pytest tests/integration/test_session_probe.py -q` STILL exits 0 (the existing 4.2 integration test continues to pass — Plan 06 rewired its monkeypatch; the wire contract is preserved).
  </acceptance_criteria>
  <done>Legacy probe module deleted; equivalent coverage lives in app/llm/; full suite green; mypy + ruff clean.</done>
</task>

<task type="auto">
  <name>Task 3: Optionally clean up orphan ProviderSelector + frontend stale imports if Plan 08 left any</name>
  <files>frontend/src/components/ProviderSelector.tsx, frontend/src/components/__tests__/ProviderSelector.test.tsx</files>
  <read_first>
    - frontend/src/components/ProviderSelector.tsx (the dropdown component; Plan 08 stopped IMPORTING it but did not delete it)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-08-SUMMARY.md (if Plan 08 produced a summary, check whether it flagged ProviderSelector as an orphan — Plan 08's docs say it should)
    - .planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-CONTEXT.md (frontend file `ProviderSelector.tsx` — listed under "Removed in 4.5; logic relocates to the settings page" in Canonical Refs)
  </read_first>
  <action>
    Pre-flight check: `cd frontend && grep -rn "ProviderSelector" src --include="*.tsx" --include="*.ts"` to enumerate remaining references. After Plan 08, the ONLY hits should be in:
    - frontend/src/components/ProviderSelector.tsx (the file itself).
    - frontend/src/components/__tests__/ProviderSelector.test.tsx (its test, if it exists).

    If there are any other importers in production code, STOP and surface to the orchestrator — Plan 08 did not finish.

    If ProviderSelector is genuinely orphaned (only the file + its test reference it), DELETE both:
    - frontend/src/components/ProviderSelector.tsx
    - frontend/src/components/__tests__/ProviderSelector.test.tsx (if it exists)

    Run the full frontend suite to confirm nothing breaks: `cd frontend && npm test --run`.
    Run tsc strict: `cd frontend && npx tsc --noEmit`.
    Run production build: `cd frontend && npm run build`.

    If ProviderSelector is still imported anywhere unexpected, document that in the SUMMARY and DO NOT delete — surface for triage.
  </action>
  <verify>
    <automated>cd frontend && npm test --run && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source: `cd frontend && grep -rn "ProviderSelector" src --include="*.tsx" --include="*.ts"` returns ZERO hits AFTER this task (file gone + no remaining importer).
    - Behavior: `cd frontend && npm test --run` exits 0.
    - CLI: `cd frontend && npx tsc --noEmit` exits 0.
    - CLI: `cd frontend && npm run build` exits 0.
    - Conditional: if Plan 08 left ProviderSelector imported somewhere unexpected, the executor STOPS, documents in SUMMARY, and does NOT delete the file. The orchestrator triages.
  </acceptance_criteria>
  <done>Orphan frontend dropdown deleted; full frontend suite + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → api.openai.com | Real cloud test sends a HumanMessage to api.openai.com via langchain_openai's ChatOpenAI; the api_key crosses HTTPS Authorization. |
| backend → api.anthropic.com | Real cloud test sends a HumanMessage with max_tokens=1 to api.anthropic.com via langchain_anthropic's ChatAnthropic; the api_key crosses HTTPS x-api-key. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.5-09-01 | Information Disclosure | API key in CI logs | mitigate | skipif gate ensures the tests do not run in default CI (where the env vars are absent). When they DO run in a dedicated CI job, the env var is provided as a GitHub Actions secret which is automatically masked in logs. The test code itself never logs the key; it passes os.environ[KEY] directly into the provider constructor. |
| T-04.5-09-02 | Denial of Service / Cost | Real test runs every PR | mitigate | skipif gate prevents this — default CI env has no keys. Operator manually runs the gated job when validating a release. |
| T-04.5-09-03 | Tampering | Deletion of provider_probe.py breaks downstream code | mitigate | Pre-flight grep enumerates all importers BEFORE deletion. If any remain, deletion is aborted and the orchestrator triages. |
| T-04.5-09-04 | Information Disclosure | Anthropic max_tokens=1 still produces a billable single-token response | accept | RESEARCH.md Assumption A2 — acceptable for a smoke test; the cost is negligible. The test docstring documents the cost characteristic so operators understand what they're spending when they run it. |
</threat_model>

<verification>
- `cd backend && uv run pytest -q` exits 0 (full backend suite, with cloud tests SKIPPED).
- `cd backend && uv run mypy app --strict` exits 0.
- `cd backend && uv run ruff check app tests` exits 0.
- `cd frontend && npm test --run` exits 0.
- `cd frontend && npx tsc --noEmit && npm run build` exits 0.
- Manual cloud smoke test (operator-only, when keys are present): `cd backend && OPENAI_API_KEY=sk-... uv run pytest tests/integration/test_cloud_providers_real.py::test_openai_provider_real_chat_turn -v` exits 0 with the test PASSING. Same for Anthropic with ANTHROPIC_API_KEY.
- `find backend/app/services/provider_probe.py backend/tests/unit/test_provider_probe.py 2>/dev/null` returns nothing — both deleted.
</verification>

<success_criteria>
- Real cloud acceptance tests for OpenAI + Anthropic exist in tests/integration/test_cloud_providers_real.py with skipif gates.
- app/services/provider_probe.py + tests/unit/test_provider_probe.py deleted.
- Optional: frontend/src/components/ProviderSelector.tsx orphan-deleted if no longer referenced.
- Full backend + frontend test suites green (with cloud tests skipped in default CI).
- mypy strict + ruff + tsc + production build all pass.
</success_criteria>

<output>
Create `.planning/phases/04.5-llm-provider-abstraction-real-cloud-dynamic-ollama/04.5-09-SUMMARY.md` recording: cloud test skip behavior, files deleted, manual cloud smoke-test outcome (if performed by operator), any orphans flagged for follow-up.
</output>
