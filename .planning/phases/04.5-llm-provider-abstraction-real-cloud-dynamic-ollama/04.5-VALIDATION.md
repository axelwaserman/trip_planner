---
phase: 4.5
slug: llm-provider-abstraction-real-cloud-dynamic-ollama
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-16
---

# Phase 4.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Filled by the planner from `04.5-RESEARCH.md` ## Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x (backend) + Vitest 1.x (frontend) |
| **Config file** | `backend/pyproject.toml` (`[tool.pytest.ini_options]`) and `frontend/vitest.config.ts` |
| **Quick run command** | `cd backend && uv run pytest tests/unit -q` |
| **Full suite command** | `just check && just test` |
| **Estimated runtime** | ~30s unit, ~3 min full |

---

## Sampling Rate

- **After every task commit:** `cd backend && uv run pytest tests/unit -q`
- **After every plan wave:** `just check && just test-unit && just test-integration`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds for unit; 180 seconds for integration

---

## Per-Task Verification Map

> Filled by the planner — every task in every PLAN.md gets a row here. Status updated by execute-phase.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| _planner-fills_ | _01_ | _1_ | REQ-llm-provider-abstraction | _—_ | _—_ | _unit_ | _command_ | _❌ W0_ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `backend/tests/unit/test_llm_provider_protocol.py` — stubs for REQ-llm-provider-abstraction (Protocol contract tests)
- [ ] `backend/tests/unit/providers/` — package directory with `__init__.py` for per-provider unit tests
- [ ] `backend/tests/integration/test_cloud_providers_real.py` — gated cloud acceptance tests (skipif missing API key)
- [ ] `backend/tests/conftest.py` — shared fixtures: `respx_mock`, `monkeypatch_env`, sample provider configs
- [ ] `frontend/src/components/__tests__/` — Vitest specs for the provider-error 400 surfacing in the UI

*If none: "Existing infrastructure covers all phase requirements."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| OpenAI `/v1/models` 401 wire shape (validate_config returns INVALID_API_KEY) | REQ-llm-provider-abstraction | Endpoint status code semantics not 100% confirmed in research (MEDIUM confidence) | `curl -s -o /dev/null -w "%{http_code}\n" -H "Authorization: Bearer sk-INVALID" https://api.openai.com/v1/models` should print `401` |
| Real Ollama daemon at OLLAMA_BASE_URL returns expected `/api/tags` payload | REQ-llm-provider-abstraction | Local-only endpoint, not reachable in CI | `curl -s "${OLLAMA_BASE_URL}/api/tags" \| jq '.models \| length'` |

*If none: "All phase behaviors have automated verification."*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s (unit) / 180s (integration)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
