---
plan: 04.5-09
phase: 04.5-llm-provider-abstraction-real-cloud-dynamic-ollama
status: complete
completed: 2026-05-18
tasks_completed: 2
tasks_total: 3
files_changed: 3
---

# Plan 04.5-09 Summary — Cloud-acceptance tests + legacy probe deletion

## Outcome

Two cleanup deliverables landed; the third optional task is deferred pending
Plan 08b's frontend changes settling.

| # | Task | Status |
|---|------|--------|
| 1 | Real cloud acceptance tests (OpenAI + Anthropic) replacing Wave 0 stubs | ✅ complete |
| 2 | Delete `app/services/provider_probe.py` + `tests/unit/test_provider_probe.py` | ✅ complete |
| 3 | Optional: delete orphan `frontend/src/components/ProviderSelector.tsx` | ⏸ deferred |

## Files created / modified / deleted

| Path | Action | Notes |
|------|--------|-------|
| `backend/tests/integration/test_cloud_providers_real.py` | modified | Wave 0 stubs replaced with real bodies; gated by per-function `@pytest.mark.skipif` |
| `backend/app/services/provider_probe.py` | **deleted** | Logic relocated to `app/llm/providers/*` (Wave 2) and `app/llm/errors.py` (Plan 02) |
| `backend/tests/unit/test_provider_probe.py` | **deleted** | Equivalent coverage now in `tests/unit/llm/test_{ollama,openai,anthropic,lmstudio}_provider.py` |

## Commits

- `a9ad023` test(04.5-09): replace cloud-acceptance Wave 0 stubs with real bodies
- (next) chore(04.5-09): delete legacy provider_probe.py + its unit tests (D-12)

## Verification

**Task 1 (cloud tests):**
- `uv run pytest tests/integration/test_cloud_providers_real.py -q -v` → **2 skipped** (no env vars, expected)
- `uv run mypy tests/integration/test_cloud_providers_real.py --strict` → clean
- With keys set, both tests run real — operator-driven smoke test (CI deliberately doesn't run them)

**Task 2 (deletion):**
- Pre-flight grep enumerated all `provider_probe` references; the only functional importer was `tests/unit/test_provider_probe.py` itself (deleted by this plan).
- Surviving non-deleted occurrences are: `Settings.provider_probe_timeout_seconds` (config field — unrelated) and docstring mentions of relocation history.
- Backend suite: **182 passed, 2 skipped** (down from 187 by exactly the 5 deleted tests; equivalent assertions live in the new `tests/unit/llm/` files).
- `uv run mypy app --strict` → clean (23 source files)
- `uv run ruff check app tests` → 1 pre-existing TC001 in `app/chat.py:25` (introduced by Plan 06 commit `52a34a7`, captured in `deferred-items.md` by Plan 02b — unchanged here).

## Threat-register dispositions met

| Threat ID | Disposition | Evidence |
|-----------|-------------|----------|
| T-04.5-09-01 (API key in CI logs) | mitigate | `skipif` gate prevents tests running in default CI; when run in a dedicated job, env var comes from a GitHub Actions secret which is masked in logs. Test code never logs the key. |
| T-04.5-09-02 (DoS / cost from PR-time runs) | mitigate | `skipif` gate prevents auto-run on every PR. |
| T-04.5-09-03 (deletion breaks downstream) | mitigate | Pre-flight grep enumerated all importers; zero functional importers remained outside the deleted test file. |
| T-04.5-09-04 (Anthropic single-token billing) | accept | Per RESEARCH.md A2 — negligible cost. Test docstring documents the cost characteristic. |

## Recovery from stalled executor

The originally-spawned executor agent for Plan 09 (`worktree-agent-a57624d7ad99ee5f0`)
stalled (#3212 stream-idle-timeout watchdog) before doing any work AND was
forked from a stale base (`f47c40a`, Phase 4.4 commit) — its branch listed every
Phase 4.5 file as deleted. Merging it would have wiped the entire phase.

The orchestrator detected this via `git diff --diff-filter=D --name-only` showing
~60 file deletions, force-removed the bad worktree without merging, and executed
Plan 09's two main tasks inline on the phase branch:

1. Rewrote `tests/integration/test_cloud_providers_real.py` with the canonical
   pattern from RESEARCH.md (function-level `skipif`, AAA test bodies, tolerant
   assertions for the `langchain_anthropic` list-of-blocks content shape).
2. `git rm`'d the two legacy probe files after pre-flight verified zero
   functional importers.

Both task verify commands pass; threat-register dispositions are met identically.

## Deferred — Task 3 (frontend ProviderSelector orphan)

`frontend/src/components/ProviderSelector.tsx` still exists in the tree as an
orphan (no production importers; only the file itself + a docstring reference
in `frontend/src/lib/auth.ts:11`).

**Why deferred:** Plan 08b is running in parallel as the orchestrator
finalizes Wave 6, and the agent is rebuilding the app-shell `Sidebar` /
provider-card surface. If Plan 08b's continuation re-introduces a usage of
`ProviderSelector` (unlikely given Plan 08's UAT polish dropped the cloud
cards), deleting it now would race against that change. Safer to defer the
orphan-deletion check until Plan 08b merges.

**Follow-up action:** After Plan 08b lands and the wave 6 worktrees merge, the
orchestrator (or a Plan 09 fix-up commit) should run:

```bash
cd frontend && grep -rn "ProviderSelector" src --include="*.tsx" --include="*.ts"
```

If the only hits are the file itself + the docstring in `auth.ts`, both can be
deleted (the docstring reference is informational, not a functional import).

## Next

Wave 7 — phase-level verification, code review, and completion.
