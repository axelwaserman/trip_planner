---
phase: 04.7-error-handling-streamevent-hierarchy
plan: 04
type: execute
wave: 3
depends_on:
  - 04.7-02
  - 04.7-03
files_modified:
  - frontend/src/hooks/useChat.ts
  - frontend/src/components/ToolExecutionCard.tsx
  - frontend/src/components/ChatInterface.tsx
  - frontend/src/hooks/__tests__/useChat.test.ts
  - frontend/src/components/__tests__/ToolExecutionCard.test.tsx
  - frontend/src/types/chat.ts
autonomous: false
requirements:
  - REQ-error-handling-feedback
  - REQ-streamevent-hierarchy

must_haves:
  truths:
    - "useChat consumes ChatStreamEvent via a switch (event.type) — every legacy event.type === 'X' chain is replaced with discriminator narrowing."
    - "On ErrorEvent with retryable=true, useChat updates the last tool_execution message with errorEvent metadata so ToolExecutionCard can render an inline error + Retry button."
    - "On ErrorEvent with retryable=false and error_code=session_error, useChat shows a Chakra toast and silently re-creates a session via the existing initSession path (D-12)."
    - "On ErrorEvent with retryable=false and any other error_code, useChat shows a Chakra toast — the chat remains usable for the next user message."
    - "useChat exposes retryLastTool: () => Promise<void> that POSTs /api/chat/retry with the current sessionId and feeds the SSE stream through the same callback as sendMessage."
    - "ToolExecutionCard renders three states: executing (spinner, blue), completed (✓, green), error (✗, red); error state shows the ErrorEvent.message and an optional Retry button when onRetry is provided."
    - "ToolExecutionCard's existing arguments + result Collapsible sections continue to work unchanged in the executing/completed branches."
  artifacts:
    - path: frontend/src/hooks/useChat.ts
      provides: "Discriminator-narrowed SSE handler with toast + retry routing; new updateToolError helper; retryLastTool entry point."
      contains: "switch (event.type)"
    - path: frontend/src/components/ToolExecutionCard.tsx
      provides: "Three-state UI (executing / completed / error) with retry button; new isLoading / errorEvent / onRetry props."
      contains: "errorEvent"
    - path: frontend/src/types/chat.ts
      provides: "ToolExecutionData extended with optional errorEvent field so the component can read it from the message store."
      contains: "errorEvent?:"
  key_links:
    - from: frontend/src/hooks/useChat.ts
      to: frontend/src/lib/toaster.ts
      via: "import { toaster } from '../lib/toaster'; toaster.create({ ... })"
      pattern: "toaster\\.create\\("
    - from: frontend/src/hooks/useChat.ts
      to: "/api/chat/retry"
      via: "apiFetch('/api/chat/retry', { method: 'POST', body: JSON.stringify({ session_id }) })"
      pattern: "/api/chat/retry"
    - from: frontend/src/components/ToolExecutionCard.tsx
      to: frontend/src/types/chat.ts
      via: "import type { ErrorEvent } from '../types/chat'"
      pattern: "import type.*ErrorEvent"
---

<objective>
Wire the Plan 03 types and Plan 02 retry endpoint into the live UI: rewrite the SSE event handler in useChat as a switch (event.type) discriminator narrowing, route ErrorEvent payloads to inline (retryable) state on the active ToolExecutionCard or to a Chakra toast (non-retryable), implement retryLastTool, and expand ToolExecutionCard to render an executing / completed / error tri-state with a Retry button. Conclude with a manual UAT checkpoint that exercises the full happy path + tool error + session error + retry flow in the browser.

Purpose: Closes REQ-error-handling-feedback for the user-visible behaviour. Honors CONTEXT.md D-06, D-09, D-10, D-11, D-12.

Output:
- frontend/src/hooks/useChat.ts — switch-narrowed handler; toaster + initSession on session_error; updateToolError + retryLastTool callbacks.
- frontend/src/components/ToolExecutionCard.tsx — new props (isLoading, errorEvent, onRetry); error-state header + body; Retry button; existing happy-path UI preserved.
- frontend/src/components/ChatInterface.tsx — passes errorEvent + onRetry to ToolExecutionCard rows.
- frontend/src/types/chat.ts — ToolExecutionData gains an optional errorEvent?: ErrorEvent field.
- frontend/src/hooks/__tests__/useChat.test.ts — extended with retryable=true / retryable=false / session_error coverage and a retryLastTool test.
- frontend/src/components/__tests__/ToolExecutionCard.test.tsx — extended with isLoading / errorEvent / onRetry render assertions.
- MANUAL UAT CHECKPOINT — the user verifies the full flow in the browser before this plan is marked complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.7-error-handling-streamevent-hierarchy/04.7-CONTEXT.md
@.planning/phases/04.7-error-handling-streamevent-hierarchy/04.7-RESEARCH.md
@.planning/phases/04.7-error-handling-streamevent-hierarchy/04.7-PATTERNS.md
@.planning/phases/04.7-error-handling-streamevent-hierarchy/04.7-02-SUMMARY.md
@.planning/phases/04.7-error-handling-streamevent-hierarchy/04.7-03-SUMMARY.md
@CLAUDE.md
@frontend/src/hooks/useChat.ts
@frontend/src/components/ToolExecutionCard.tsx
@frontend/src/components/ChatInterface.tsx
@frontend/src/types/chat.ts
@frontend/src/lib/toaster.ts
@frontend/src/lib/parseSSE.ts
@frontend/src/hooks/useSSEStream.ts
@frontend/src/hooks/__tests__/useChat.test.ts
@frontend/src/components/__tests__/ToolExecutionCard.test.tsx
@frontend/src/lib/auth.ts
@frontend/src/lib/chatSessionStore.ts

<interfaces>
<!-- Contracts available after Plans 02 + 03 have shipped. -->

From frontend/src/types/chat.ts (Plan 03):
- export type ChatStreamEvent = ContentEvent | ThinkingEvent | ToolCallEvent | ToolResultEvent | ErrorEvent
- export interface ErrorEvent has type: 'error', error_code, message, retryable, optional tool_name and raw_detail, required session_id
- export interface ToolExecutionData has callMetadata + optional resultMetadata; this plan EXTENDS it with optional errorEvent

From frontend/src/lib/toaster.ts (Plan 03):
- export const toaster = createToaster({ placement: 'top-end', overlap: true })
- toaster.create({ title, description?, type, duration? }) call shape

From backend/app/api/routes/routes.py (Plan 02):
- POST /api/chat/retry accepts {session_id: string}; returns 404 if missing/cross-user, 422 if no last_tool_invocation; otherwise SSE stream identical in event shapes to /api/chat.

From frontend/src/hooks/useChat.ts (current shape — refactor target):
- The readSSEStream callback at lines ~556-588 currently uses chained if (event.type === 'X') guards.
- updateToolResult helper at lines 509-538 walks back to find the last tool_execution message and updates resultMetadata.
- The UseChatReturn interface at lines 22-37 must gain retryLastTool: () => Promise<void>.
- initSession at lines 229-275 is the existing session-create path; the session_error branch invokes it via the existing loadProviderSettings + resolveSelection helpers.

From frontend/src/lib/auth.ts:
- apiFetch(url, init) handles JWT injection + 401 redirect; reuse for /api/chat/retry POST.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useChat switch narrowing + toast routing + updateToolError + retryLastTool + tests</name>
  <files>
    frontend/src/hooks/useChat.ts
    frontend/src/types/chat.ts
    frontend/src/hooks/__tests__/useChat.test.ts
  </files>
  <read_first>
    frontend/src/hooks/useChat.ts
    frontend/src/types/chat.ts
    frontend/src/lib/toaster.ts
    frontend/src/lib/auth.ts
    frontend/src/lib/chatSessionStore.ts
    frontend/src/hooks/useSSEStream.ts
    frontend/src/hooks/__tests__/useChat.test.ts
    .planning/phases/04.7-error-handling-streamevent-hierarchy/04.7-PATTERNS.md
    .planning/phases/04.7-error-handling-streamevent-hierarchy/04.7-03-SUMMARY.md
  </read_first>
  <behavior>
    - The SSE event handler is a single switch (event.type) block with cases content / thinking / tool_call / tool_result / error — every chained if is gone.
    - case error: if event.retryable is true, call updateToolError(event) to attach the ErrorEvent to the last tool_execution message; if false, call toaster.create with title=event.message, type='error', duration=5000. If event.error_code === 'session_error' (always retryable=false per backend D-12), additionally call initSession with the current selection via the existing loadProviderSettings + resolveSelection helpers — the toast already informs the user; the silent re-init recovers the chat.
    - updateToolError(errorEvent) mirrors updateToolResult: walk backwards through prev.messages to find the last role === 'tool_execution', immutably update its toolExecution.errorEvent field. If no tool_execution exists, fall back to surfacing the toast (already done by the caller).
    - retryLastTool() returns Promise<void>; refuses if sessionId === null or sessionSnapshot.isStreaming === true; otherwise POSTs to /api/chat/retry with {session_id: sessionId} via apiFetch. On response.ok, drives readSSEStream(response.body, ...) with the same callback shape as sendMessage. On non-2xx, toaster.create({ title: 'Could not retry', type: 'error' }) and return.
    - The UseChatReturn interface gains retryLastTool: () => Promise<void> and useChat returns it.
    - ToolExecutionData in frontend/src/types/chat.ts gains an optional errorEvent?: ErrorEvent field. Message shape is unchanged (toolExecution is already optional).
    - Existing useChat.test.ts cases continue to pass; new cases:
      1. ErrorEvent retryable=true updates the last tool_execution message's toolExecution.errorEvent field (no toast spied).
      2. ErrorEvent retryable=false fires toaster.create (mocked).
      3. ErrorEvent error_code='session_error' fires toaster.create AND triggers a follow-up POST /api/chat/session call (the silent re-init path).
      4. retryLastTool POSTs to /api/chat/retry with {session_id} and feeds events through readSSEStream — assert that on retry, a tool_call event leads to a new tool_execution message in the store.
  </behavior>
  <action>
    Edit frontend/src/types/chat.ts:
    - Extend ToolExecutionData to include an optional errorEvent?: ErrorEvent field. ErrorEvent is already exported from the file by Plan 03.
    - Do not modify any other interface.

    Edit frontend/src/hooks/useChat.ts:

    1. Imports — add at the top alongside existing imports:
       - import { toaster } from '../lib/toaster'
       - import type { ChatStreamEvent, ErrorEvent } from '../types/chat'

    2. Inside sendMessage (next to updateToolResult), add updateToolError. It is a closure over submitSessionId and follows the updateToolResult pattern: walk messages backwards, find the last role==='tool_execution', and immutably set msg.toolExecution.errorEvent.

    3. Replace the entire await readSSEStream callback body (the existing chained if-guards) with a switch (event.type) block. Cases:
       - case 'content': call markFirstChunk(); appendContentChunk(event.chunk); set isStreamingAssistant = true. Note: event.chunk is REQUIRED (not optional) on the new ContentEvent — drop the truthy guard, but still skip empty strings if needed.
       - case 'thinking': call markFirstChunk(); appendThinkingChunk(event.chunk); set isStreamingThinking = true.
       - case 'tool_call': call markFirstChunk(); appendToolCall(event.tool_name, event.tool_args).
       - case 'tool_result': call updateToolResult(event.tool_result, event.elapsed_ms); set isStreamingAssistant = false.
       - case 'error': branch on event.retryable. retryable=true → updateToolError(event). retryable=false → toaster.create({title: event.message, type: 'error', duration: 5000}); if event.error_code === 'session_error', read loadProviderSettings() ?? DEFAULT_PROVIDER_SETTINGS, build merged with selected.provider/model from current state, call resolveSelection, void initSession with the resolved fields.
       Drop the legacy 'done' branch — the new union has no 'done' discriminator. Drop the legacy if (event.type === 'error') { throw new Error(...) } block — replaced by case 'error'.

    4. Extract the switch body into a named helper handleStreamEvent(event, helpers) so retryLastTool can reuse it without duplication. helpers carries the closures (markFirstChunk, appendContentChunk, appendThinkingChunk, appendToolCall, updateToolResult, updateToolError, isStreamingAssistant/isStreamingThinking refs, plus the toaster + initSession callbacks). Do this BEFORE writing retryLastTool so both call sites converge on the same handler.

    5. Add retryLastTool as a useCallback at hook scope:
       - Refuses when sessionId is null OR getSessionSnapshot(sessionId).isStreaming is true.
       - Captures submitSessionId = sessionId, sets isStreaming + isAwaitingFirstChunk to true on the store entry.
       - apiFetch('/api/chat/retry', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({ session_id: submitSessionId }) }).
       - On !response.ok or no response.body: toaster.create({title: 'Could not retry', type: 'error', duration: 5000}); return.
       - Otherwise: await readSSEStream(response.body, (event) => handleStreamEvent(event, helpers)).
       - finally: setSession(submitSessionId, prev => ({...prev, isStreaming: false, isAwaitingFirstChunk: false})).
       - Dependency array: [sessionId, currentProvider, currentModel, initSession].

    6. Add retryLastTool to the UseChatReturn interface and to the returned object.

    Edit frontend/src/hooks/__tests__/useChat.test.ts:
    - vi.mock('../lib/toaster', () => ({ toaster: { create: vi.fn() } })).
    - Add the four test cases described in the behavior list. For test 4, mock apiFetch (or globalThis.fetch — read the existing test file first to see which surface the existing tests mock) to resolve a Response whose body is a ReadableStream of data: ... lines.
    - Use AAA pattern with descriptive names like test_use_chat_retryable_error_updates_tool_execution_message etc.

    Run cd frontend && npx tsc --noEmit && npm test -- useChat.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm test -- useChat 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/types/chat.ts ToolExecutionData interface contains errorEvent?: ErrorEvent.
    - frontend/src/hooks/useChat.ts contains the literal substring "switch (event.type)" and a case 'error': arm.
    - frontend/src/hooks/useChat.ts contains "toaster.create(" inside the case 'error' branch.
    - frontend/src/hooks/useChat.ts contains "apiFetch('/api/chat/retry'" and exports retryLastTool through the returned object.
    - The legacy if (event.type === 'error') { throw new Error( block is removed.
    - cd frontend && npx tsc --noEmit exits 0.
    - cd frontend && npm test -- useChat exits 0 with the four new tests passing.
    - Mocked toaster.create is called for the retryable=false test cases and not for the retryable=true test (assert via vi.mocked(toaster.create).toHaveBeenCalledTimes / toHaveBeenCalledWith).
  </acceptance_criteria>
  <done>
    useChat is switch-narrowed, error-routed, and exposes retryLastTool. Tests pass. ToolExecutionCard wiring lands in Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ToolExecutionCard three-state UI + ChatInterface wiring + tests</name>
  <files>
    frontend/src/components/ToolExecutionCard.tsx
    frontend/src/components/ChatInterface.tsx
    frontend/src/components/__tests__/ToolExecutionCard.test.tsx
  </files>
  <read_first>
    frontend/src/components/ToolExecutionCard.tsx
    frontend/src/components/__tests__/ToolExecutionCard.test.tsx
    frontend/src/components/ChatInterface.tsx
    frontend/src/hooks/useChat.ts
    frontend/src/types/chat.ts
    .planning/phases/04.7-error-handling-streamevent-hierarchy/04.7-PATTERNS.md
  </read_first>
  <behavior>
    - ToolExecutionCardProps interface: { callMetadata: ToolCallMetadata; resultMetadata?: ToolResultMetadata; isLoading?: boolean; errorEvent?: ErrorEvent; onRetry?: () => void }.
    - hasError = !!errorEvent; isComplete = !!resultMetadata && !hasError; isExecuting = !isComplete && !hasError.
    - Color tokens: error path uses red.50 bg / red.200 border / red.800 text / red accent. Existing green (complete) / blue (executing) tokens unchanged.
    - Header icon: Spinner when isExecuting; ✓ when isComplete; ✗ when hasError.
    - When hasError, render an error block immediately below the arguments Collapsible: red-tinted Box containing Text with errorEvent.message and, when onRetry is provided, a Button labelled "Retry" that calls onRetry on click.
    - The existing Result section (the {isComplete && resultMetadata && ...} block) MUST not render when hasError is true.
    - The existing Arguments Collapsible MUST continue to render in all three states.
    - ChatInterface (the parent that maps Message rows to ToolExecutionCard) passes errorEvent={msg.toolExecution?.errorEvent} and onRetry={msg.toolExecution?.errorEvent?.retryable ? retryLastTool : undefined} so the Retry button only appears for retryable errors. retryLastTool comes from useChat().
    - Tests:
      1. renders Spinner + blue tokens when no resultMetadata and no errorEvent (executing).
      2. renders ✓ + green tokens + Result section when resultMetadata present and no errorEvent.
      3. renders ✗ + red tokens + error message + Retry button when errorEvent + onRetry present; clicking Retry calls onRetry.
      4. renders ✗ + red tokens + error message but NO Retry button when errorEvent present without onRetry.
      5. error path does NOT render the Result Collapsible even when both resultMetadata and errorEvent are set.
  </behavior>
  <action>
    Edit frontend/src/components/ToolExecutionCard.tsx:

    1. Imports — add `import type { ErrorEvent } from '../types/chat'` alongside existing imports. If TypeScript reports a clash with the global lib.dom.d.ts ErrorEvent type, alias the import as `ErrorEvent as ErrorEventData` and use ErrorEventData in the props/component. Read the existing import block first to decide.

    2. Extend the props interface to include isLoading?: boolean, errorEvent?: ErrorEvent, onRetry?: () => void.

    3. Replace the state-derivation block (currently isComplete + bgColor/borderColor/textColor/accentColor at lines 88-92) with a three-state derivation: hasError, isComplete (gated on !hasError), color tokens with the red branch added. Reuse the existing accentColor pattern.

    4. Update the header to render exactly one icon per state: Spinner when isExecuting, ✓ Text when isComplete, ✗ Text when hasError.

    5. Insert a new error block AFTER the Arguments Collapsible block and BEFORE the existing isComplete && resultMetadata block. Wrap the existing result block in {!hasError && isComplete && resultMetadata && (...)} so it stays hidden in error state. The error block JSX:
       - Outer Box: bg="red.50" rounded="md" borderWidth="1px" borderColor="red.200" p={2} mt={2}.
       - Text: fontSize="sm" color="red.700" containing errorEvent.message; mb={onRetry ? 2 : 0} so spacing collapses when no button.
       - Conditional Button: only when onRetry provided. Use size="xs" colorScheme="red" variant="outline" onClick={onRetry} with the literal label "Retry".

    6. Do NOT touch the FlightResultsTable component or the markdown render block — those stay verbatim.

    Edit frontend/src/components/ChatInterface.tsx:
    - Find the JSX block where the component maps over messages and renders ToolExecutionCard for role === 'tool_execution' rows. Read the file first to confirm the exact pattern (it likely passes callMetadata + resultMetadata directly).
    - Add to the props passed: errorEvent={msg.toolExecution?.errorEvent} and onRetry={msg.toolExecution?.errorEvent?.retryable ? retryLastTool : undefined}.
    - Pull retryLastTool from useChat() — read the existing destructuring of useChat() at the top of the component.
    - DO NOT add a new component or restructure — this is a targeted set of additional props on the existing JSX element.

    Edit frontend/src/components/__tests__/ToolExecutionCard.test.tsx:
    - Add the five test cases described in the behavior list. Use @testing-library/react render + screen.getByRole('button', { name: /retry/i }) for the click assertion (with userEvent or fireEvent — match the existing test file's convention; read first).
    - For test 5, supply both resultMetadata and errorEvent; assert that the result-section trigger ("Show full results" or whatever the existing test asserts) is NOT in the document via screen.queryByText.

    Run cd frontend && npx tsc --noEmit && npm test. The full frontend suite must be green before the UAT checkpoint.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm test 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ToolExecutionCard.tsx contains errorEvent in the props interface and a "Retry" Button conditional on onRetry.
    - frontend/src/components/ToolExecutionCard.tsx wraps the existing isComplete && resultMetadata block in a !hasError && ... guard.
    - frontend/src/components/ChatInterface.tsx contains retryLastTool destructured from useChat() and passes errorEvent + onRetry to ToolExecutionCard.
    - cd frontend && npx tsc --noEmit exits 0.
    - cd frontend && npm test exits 0 with the five new ToolExecutionCard tests passing.
    - cd frontend && npm run build exits 0.
  </acceptance_criteria>
  <done>
    ToolExecutionCard renders all three states with the Retry control wired to the retry endpoint via useChat. Build is green; full frontend suite is green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: MANUAL UAT — full Phase 4.7 user flow in the browser</name>
  <what-built>
    - Backend: discriminated StreamEvent union + ErrorEvent on tool errors / stream errors / session errors + POST /api/chat/retry endpoint.
    - Frontend: switch-narrowed useChat, Chakra Toaster wired in App.tsx, ToolExecutionCard with executing/completed/error tri-state and Retry button.
    - Plans 01–04 must all be green; full backend pytest suite + full frontend npm test suite passing.
  </what-built>
  <how-to-verify>
    Run the stack locally: `just backend` in one terminal, `just frontend` in another. Log in via /login (use AUTH_USERS=user1:pass1 or whatever the env-seed contains).

    1. Happy path — verify nothing regressed:
       a. Pick Ollama + qwen3:4b (or any locally-installed model). Send "Search for flights from JFK to LAX on December 15, 2026 for 1 passenger".
       b. Confirm the ToolExecutionCard appears with a blue spinner, then turns green with ✓ once the tool returns. The flight results table renders below the summary line.
       c. Confirm content streams to the assistant bubble after the tool result.

    2. Tool error → retryable inline error + Retry button:
       a. With dev tools open (Network tab), set the backend's MockFlightAPIClient to raise APIError(retryable=True). The simplest way: temporarily edit `backend/app/tools/flight_client.py` `MockFlightAPIClient.search_flights` to raise `APITimeoutError(message="simulated outage")` at the top. Restart `just backend`.
       b. Send the same flight query. Confirm the ToolExecutionCard turns red with ✗, displays "Tool search_flights failed: simulated outage" (or similar), and shows a Retry button.
       c. Click Retry. Confirm a second SSE stream begins, the same tool_execution row reappears with a fresh spinner, and either succeeds (if you reverted the override) or shows the error again.
       d. Revert the MockFlightAPIClient override and restart backend.

    3. Tool error → non-retryable toast (no inline retry):
       a. Edit the override to raise `APIClientError(message="bad request param", retryable=False)` instead. Restart backend.
       b. Send the flight query. Confirm a Chakra toast appears (top-right) with "Tool search_flights failed: bad request param". The ToolExecutionCard turns red with ✗ but does NOT show a Retry button (because retryable=false routes to toast, not inline; per D-10 the inline path is retryable-only).
       c. Revert the override and restart backend.

    4. Session error → toast + silent reconnect:
       a. With the app open and a working session, manually delete the session via `curl -X DELETE -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/chat/session/<session_id>` (read the session_id from the URL or the network tab).
       b. Send a message in the UI. Confirm: a toast appears with "Session not found or expired" (or the configured message), the chat continues to function, and the next message creates a fresh session (visible in the Network tab as a POST /api/chat/session followed by the chat POST).

    5. Stream error path:
       a. Force a stream error by setting an invalid OPENAI_API_KEY in the user's settings, then switching to OpenAI provider. Send a message.
       b. Confirm: an error toast appears with "Sorry, something went wrong. Please try again." and the chat remains usable. (Or, reproduce by killing the Ollama daemon mid-stream — same expected behavior.)

    6. Type-narrowing sanity in dev tools:
       a. Open the browser console. Inspect the `messages` array via React DevTools or a temporary `console.log` in useChat. Confirm tool_execution messages with errors carry a populated `toolExecution.errorEvent` field with `error_code`, `message`, and `retryable` properties.

    7. Type-check + build green:
       a. Run `just check` and confirm clean (lint + format-check + mypy + tsc + eslint).
       b. Run `just test` (full backend) and `cd frontend && npm test` (full frontend) and confirm green.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 verification steps pass. Type "issues: ..." with a description if anything fails.</resume-signal>
  <files>backend/app/, frontend/src/ — manual UAT verifies the integrated stack from Plans 01-04 (no new file produced; the user's browser session is the artifact).</files>
  <action>This is a checkpoint task — Claude pauses here and waits for the user to perform the 7-step verification described in the how-to-verify block. No code changes occur in this task; the user runs the stack (just backend + just frontend), exercises each error path, and types the resume signal. Reference: CONTEXT.md D-09 (retry replays full LLM turn), D-10 (toast vs inline routing rule), D-12 (silent reconnect on session_error).</action>
  <verify>
    <human-check>User reports "approved" after completing all 7 steps in the how-to-verify block. If user reports "issues: ...", create a new gap-closure plan via /gsd:plan-phase --gaps before proceeding.</human-check>
  </verify>
  <acceptance_criteria>
    - User has visually verified all 7 steps in a browser session.
    - User explicitly types "approved" to mark the plan complete.
  </acceptance_criteria>
  <done>
    The Phase 4.7 user-visible flow is verified end-to-end. The plan is ready for /gsd:verify-work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE wire → useChat handler | Untrusted JSON narrowed by parseSSELine (Plan 03 guard); discriminator is checked at switch entry. |
| useChat → toaster.create | event.message is shown verbatim — backend ErrorEvent.message is a static string per Plan 02. |
| useChat → POST /api/chat/retry | apiFetch handles JWT injection + 401 redirect; backend enforces ownership at the route boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.7-10 | Information Disclosure | ToolExecutionCard error message + raw_detail rendering | mitigate | Render only errorEvent.message inline; do NOT render raw_detail in the production UI (CONTEXT.md deferred ideas). raw_detail stays available in dev tools / future debug-mode-only render. |
| T-04.7-11 | Cross-Site Scripting | errorEvent.message contains user-controlled or upstream content | accept | Backend message strings are static / templated server-side; React's default text-node rendering escapes content. No dangerouslySetInnerHTML. |
| T-04.7-12 | Tampering | Retry button issues arbitrary tool calls | accept | Retry replays only the last_tool_invocation already authorized for this session. Backend enforces ownership; no new tool-args injection surface. |
</threat_model>

<verification>
- cd frontend && npx tsc --noEmit passes.
- cd frontend && npm test passes (useChat + ToolExecutionCard + parseSSE + existing).
- cd frontend && npm run build produces a clean bundle.
- cd backend && uv run pytest tests/ -x passes (Plans 01–02 + this plan's expectations).
- just check passes (lint + format + types).
- The 7-step UAT in Task 3 is approved by the user.
</verification>

<success_criteria>
- The Phase 4.7 user flow is fully visible and working in the browser: tool errors render inline with a Retry button (retryable) or as a toast (non-retryable); session errors silently reconnect with a toast notification; stream errors surface as toasts; the chat remains usable through every error path.
- All Phase 4.7 must-haves across all four plans are satisfied.
- The frontend type system enforces discriminated narrowing — adding a new event type to the union forces a compile error in the switch.
- The full test suite is green and ready for /gsd:verify-work.
</success_criteria>

<output>
Create `.planning/phases/04.7-error-handling-streamevent-hierarchy/04.7-04-SUMMARY.md` when done.
</output>
