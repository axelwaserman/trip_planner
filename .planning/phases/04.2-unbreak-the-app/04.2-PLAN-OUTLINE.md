---
phase: 04.2
slug: unbreak-the-app
type: outline
created: 2026-05-15
---

# Phase 4.2 — Plan Outline

| Plan ID | Objective | Wave | Depends On | Requirements |
|---------|-----------|------|------------|--------------|
| 01-test-stubs | Create the 8 Wave-0 failing test stubs (4 backend, 4 frontend) listed in VALIDATION.md so every downstream task has an `<automated>` gate. Includes `backend/tests/unit/test_auth_me.py`, `backend/tests/unit/test_provider_probe.py`, `backend/tests/integration/test_auth_flow.py`, `frontend/src/lib/__tests__/auth.test.ts`, `frontend/src/components/auth/__tests__/RequireAuth.test.tsx`, `frontend/src/components/auth/__tests__/LoginForm.test.tsx`, `frontend/src/lib/__tests__/providerErrors.test.ts`, `frontend/src/components/chat/__tests__/SelectorErrorBanner.test.tsx`. | 0 | [] | REQ-login-page, REQ-llm-provider-ui-fix |
| 02-be-auth-and-probe | Backend: rename auth router mount to `/api/auth` (Pitfall 1 fix — updates `OAuth2PasswordBearer(tokenUrl=...)`, existing auth integration tests, and renames `/users/me` → `/api/auth/me` per D-17 + RESEARCH §A6/A7). Add `backend/app/services/provider_probe.py` with `probe_provider()` covering Ollama liveness (httpx 1.5s timeout) + cloud env-var presence per D-07/D-18. Add `SessionCreateError` Pydantic model to `models.py`. | 1 | [01-test-stubs] | REQ-login-page, REQ-llm-provider-ui-fix |
| 03-fe-auth-foundation | Frontend foundation: `frontend/src/lib/auth.ts` (`getToken`/`setToken`/`clearToken` + `apiFetch` with `Authorization: Bearer` injection and 401 → `clearToken` + `window.location.assign('/login?flash=session_expired&from=...')` per D-01/D-02/D-04). `frontend/src/lib/providerErrors.ts` mapping `error_code` → F1–F4 copy from UI-SPEC. Wire `<BrowserRouter>` in `main.tsx` and install `react-router-dom@^7.15.1` + `lucide-react` per D-14. Add Chakra theme `frontend/src/theme/index.ts` with the 12 OKLCH tokens from UI-SPEC. | 1 | [01-test-stubs] | REQ-login-page, REQ-llm-provider-ui-fix |
| 04-be-session-probe-wire | Wire `probe_provider()` into `POST /api/chat/session` in `backend/app/api/routes/routes.py`: probe before allocating the session; on probe failure raise `HTTPException` with status 502 (`ollama_unreachable`) or 400 (`model_not_installed` / `missing_api_key`) and structured `detail={error,message,hint}` per D-18 + RESEARCH Pattern 5. Make `create_session` `async` if needed; integration test `tests/integration/test_session_probe.py` round-trips a 502 with `monkeypatch`-faked `httpx.ConnectError`. | 2 | [02-be-auth-and-probe] | REQ-llm-provider-ui-fix |
| 05-fe-login-and-routing | Frontend login + routing surfaces: `frontend/src/components/auth/RequireAuth.tsx` (D-15 — checks `auth_token`, fires `apiFetch('/api/auth/me')` on mount, redirects with `?from=` per RESEARCH Code Examples §"Boot-time /users/me check"). `frontend/src/components/auth/LoginForm.tsx` + `pages/Login.tsx` (D-10/D-11/D-12/D-13 — editorial split-screen per UI-SPEC Surface 1, Chakra `Field.Root`/`Field.Label`/`Field.ErrorText`, password Eye/EyeOff toggle, `POST /api/auth/token` form-urlencoded, inline 400 error). `frontend/src/components/auth/SessionExpiredFlash.tsx` (UI-SPEC Surface 1 right panel item 2 — reads `?flash=session_expired`, replaceState clears). `frontend/src/components/chat/UserMenu.tsx` (D-05 + UI-SPEC §"Logged-in user + logout" — username + `LogOut` icon button). Wire `<Routes>` in `App.tsx`. Update `ChatInterface.tsx` header to render `<UserMenu>`. | 2 | [03-fe-auth-foundation] | REQ-login-page |
| 06-fe-selector-error-banner | Frontend selector error wiring + manual UAT: `frontend/src/components/chat/SelectorErrorBanner.tsx` (UI-SPEC Surface 2 §"Selector error banner" — `danger.muted` bg, `AlertCircle` icon, inline `<code>` snippets, Retry button, `aria-live="polite"`). Rewire `ProviderSelector.tsx` to use `apiFetch('/api/providers')` and surface F4 via callback (drop the `console.error`). Migrate `frontend/src/hooks/useChat.ts` from bare `fetch` to `apiFetch`; parse structured probe-error body and emit it to the parent so the banner renders F1–F3 (RESEARCH Code Examples §"useChat — exact change required"). Wire banner into `ChatInterface.tsx` header. Closes with manual-UAT checkpoint covering golden path + F1 (kill Ollama) + F3 (unset OPENAI_API_KEY) + logout per VALIDATION §"Manual-Only Verifications". | 3 | [04-be-session-probe-wire, 05-fe-login-and-routing] | REQ-login-page, REQ-llm-provider-ui-fix |
