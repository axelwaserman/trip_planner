---
phase: "04.2"
plan: "03"
plan_name: "fe-auth-foundation"
subsystem: frontend
tags: [auth, theme, router, chakra]
key-files:
  created:
    - frontend/src/lib/auth.ts
    - frontend/src/lib/providerErrors.ts
    - frontend/src/theme/index.ts
  modified:
    - frontend/src/main.tsx
    - frontend/src/test-setup.ts
    - frontend/index.html
    - frontend/package.json
    - frontend/package-lock.json
metrics:
  tasks_completed: 2
  commits: 3
  test_count_delta: +12 (auth.test.ts +7, providerErrors.test.ts +5 — converted from todo to real tests)
---

# Plan 04.2-03 Summary — fe-auth-foundation

## Outcome

Created the frontend auth foundation Plan 05 builds on. `lib/auth.ts` ships
`getToken/setToken/clearToken` (localStorage key `auth_token`, D-01) plus the
`apiFetch` wrapper that injects `Authorization: Bearer <jwt>` (D-04) and on 401
clears the token, navigates to `/login?flash=session_expired&from=<path>`, and
throws `Error('Session expired')` so callers' `.catch()` runs (D-02). `lib/providerErrors.ts`
ships the F1-F4 mapping (`mapProbeError`) plus the `PROVIDERS_FETCH_FAILED` constant.
Added a custom Chakra `system` with the 11 OKLCH tokens, fonts, and radii from UI-SPEC,
wired `BrowserRouter` into `main.tsx`, and added Fraunces + Inter font preloads to
`index.html`.

## Commits

| Task | SHA | Message |
|------|-----|---------|
| 1 (RED) | 96e721a | test(04.2-03): add failing tests for auth + providerErrors helpers |
| 1 (GREEN) | bf66519 | feat(04.2-03): add lib/auth.ts + lib/providerErrors.ts |
| 2 | 306eb53 | feat(04.2-03): add Chakra theme system, BrowserRouter, font preloads |

## Deviations

**Recovery from background-agent stall.** The Wave 1 background executor for this plan
hit Claude Code's 600s SSE stream watchdog after committing the RED tests (96e721a).
The orchestrator merged the worktree branch and completed the GREEN step + Task 2 inline.
No logic deviations; only delivery mechanism changed.

**Test setup fix (additive).** vitest 4 + jsdom 29 do not ship a usable `localStorage`
out of the box (only an unconfigured `--localstorage-file` warning). Added a small
in-memory Storage shim to `frontend/src/test-setup.ts` so all current and future tests
get a consistent Storage API. The pre-existing `useChat.test.ts` localStorage mock still
works alongside the global shim.

## Self-Check: PASSED

- `npm test -- --run` → 48 passed, 22 todo (Wave 0 stubs that later plans light up)
- `npm run build` → bundle built clean; 1570 modules; tsc -b clean
- `npx tsc --noEmit` → clean

## Acceptance Criteria

- [x] `frontend/src/lib/auth.ts` exports `getToken`, `setToken`, `clearToken`, `apiFetch`
- [x] `frontend/src/lib/providerErrors.ts` exports `mapProbeError`, `PROVIDERS_FETCH_FAILED`,
      `ProviderErrorView`, `ProviderErrorCode`
- [x] `apiFetch` body contains `Authorization` + `Bearer` (grep confirms)
- [x] `npm test -- src/lib/__tests__/auth.test.ts src/lib/__tests__/providerErrors.test.ts --run` → 12 passed
- [x] `frontend/package.json` lists `react-router-dom` and `lucide-react`
- [x] `frontend/src/theme/index.ts` exports `system` and contains the bg.canvas OKLCH token
- [x] `frontend/src/main.tsx` imports `{ BrowserRouter }` and `{ system } from './theme'`,
      no longer references `defaultSystem`
- [x] `frontend/index.html` contains `fonts.googleapis.com`, `<title>Trip Planner</title>`
- [x] `npm run build` exits 0

## Hands-off to Plan 05

Plan 05 imports `getToken/setToken/apiFetch` from `lib/auth` for the LoginForm POST and
the `/api/auth/me` boot probe in App, then adds `<Routes>` for `/login` + `/` (wrapped
in `<RequireAuth>`) inside the existing BrowserRouter.
