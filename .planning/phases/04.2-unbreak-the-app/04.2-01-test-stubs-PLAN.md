---
phase: "04.2"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/unit/test_auth_me.py
  - backend/tests/unit/test_provider_probe.py
  - backend/tests/integration/test_auth_flow.py
  - frontend/src/lib/__tests__/auth.test.ts
  - frontend/src/components/auth/__tests__/RequireAuth.test.tsx
  - frontend/src/components/auth/__tests__/LoginForm.test.tsx
  - frontend/src/lib/__tests__/providerErrors.test.ts
  - frontend/src/components/chat/__tests__/SelectorErrorBanner.test.tsx
autonomous: true
requirements:
  - REQ-login-page
  - REQ-llm-provider-ui-fix

must_haves:
  truths:
    - "Every Wave 1+ task has a pre-written failing test that turns green when the implementation lands"
    - "Backend unit test stubs are discovered by pytest and fail with XFAIL or skip markers — not errors"
    - "Frontend test stubs are discovered by Vitest and fail with skip/todo markers — not import errors"
  artifacts:
    - path: "backend/tests/unit/test_auth_me.py"
      provides: "Failing stub for GET /api/auth/me (D-17 route to be added in Plan 02)"
    - path: "backend/tests/unit/test_provider_probe.py"
      provides: "Failing stubs for probe_provider() covering ollama unreachable / model not installed / cloud missing key"
    - path: "backend/tests/integration/test_auth_flow.py"
      provides: "Failing integration stub for POST /api/auth/token → GET /api/auth/me round-trip"
    - path: "frontend/src/lib/__tests__/auth.test.ts"
      provides: "Failing stubs for apiFetch token injection + 401 redirect"
    - path: "frontend/src/components/auth/__tests__/RequireAuth.test.tsx"
      provides: "Failing stubs for RequireAuth redirect + ?from= preservation"
    - path: "frontend/src/components/auth/__tests__/LoginForm.test.tsx"
      provides: "Failing stubs for LoginForm submit success + 400 error"
    - path: "frontend/src/lib/__tests__/providerErrors.test.ts"
      provides: "Failing stubs for mapProbeError F1-F4 copy mapping"
    - path: "frontend/src/components/chat/__tests__/SelectorErrorBanner.test.tsx"
      provides: "Failing stubs for SelectorErrorBanner render + Retry click"
  key_links:
    - from: "backend/tests/unit/test_provider_probe.py"
      to: "backend/app/services/provider_probe.py"
      via: "import — will import probe_provider from this path once Plan 02 creates it"
      pattern: "from app.services.provider_probe import probe_provider"
    - from: "frontend/src/lib/__tests__/auth.test.ts"
      to: "frontend/src/lib/auth.ts"
      via: "import — will import getToken/setToken/clearToken/apiFetch once Plan 03 creates it"
      pattern: "from '../auth'"
---

<objective>
Create 8 failing test stub files (4 backend, 4 frontend) that define the behavioral contracts every
subsequent plan must satisfy. Stubs fail NOW so Wave 1+ executors have automated gates that turn green
as they implement.

Purpose: Nyquist compliance for Phase 4.2 — every behavioral task has a pre-written automated test.
Without Wave 0 stubs, executors have no immediate feedback loop and ship untestable code.

Output:
- 8 test files committed with @pytest.mark.xfail or pytest.skip() (backend) and vi.todo() / test.skip()
  (frontend) so the suite stays green while the implementations are absent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.2-unbreak-the-app/04.2-CONTEXT.md
@.planning/phases/04.2-unbreak-the-app/04.2-RESEARCH.md
@.planning/phases/04.2-unbreak-the-app/04.2-VALIDATION.md
@.planning/phases/04.2-unbreak-the-app/04.2-UI-SPEC.md

<interfaces>
<!-- Key contracts Wave 0 stubs must import against (all will be created by Plans 02-06). -->
<!-- Stubs use xfail/skip so missing imports do not crash the suite. -->

backend/tests/conftest.py exports:
  - auth_headers: dict[str, str] fixture — creates Bearer token for "admin" user via create_access_token({"sub": "admin"})
  - mock_flight_client: MockFlightAPIClient fixture
  - mock_llm_provider: MagicMock(spec=BaseChatModel) fixture

backend/app/services/provider_probe.py (DOES NOT EXIST YET — Plan 02 creates it):
  - probe_provider(provider: str, model: str) -> ProbeError | None
  - ProbeError.error: Literal["ollama_unreachable", "model_not_installed", "missing_api_key"]
  - ProbeError.message: str
  - ProbeError.hint: str

backend/app/api/routes/auth.py (WILL BE MODIFIED by Plan 02):
  - Current route: GET /users/me at auth.py:195
  - Plan 02 renames mount to /api/auth prefix → endpoint becomes GET /api/auth/me
  - Current token route: POST /token → becomes POST /api/auth/token

frontend/src/lib/auth.ts (DOES NOT EXIST YET — Plan 03 creates it):
  - getToken(): string | null
  - setToken(token: string): void
  - clearToken(): void
  - apiFetch(input: string, init?: RequestInit): Promise<Response>
    - injects Authorization: Bearer <jwt> on all /api/* calls
    - on 401: clearToken() + window.location.assign('/login?flash=session_expired&from=...')

frontend/src/lib/providerErrors.ts (DOES NOT EXIST YET — Plan 03 creates it):
  - ProviderErrorCode: 'ollama_unreachable' | 'model_not_installed' | 'missing_api_key' | 'providers_fetch_failed'
  - ProviderErrorView: { code, message, hint, inlineCode: string[] }
  - mapProbeError(body: BackendProbeError, context): ProviderErrorView
  - PROVIDERS_FETCH_FAILED: ProviderErrorView

frontend/src/components/auth/RequireAuth.tsx (DOES NOT EXIST YET — Plan 05 creates it):
  - RequireAuth({ children: ReactNode }): JSX.Element
  - Reads getToken(), fires apiFetch('/api/auth/me') on mount
  - Redirects to /login?from=<encoded-path> when not authenticated

frontend/src/components/auth/LoginForm.tsx (DOES NOT EXIST YET — Plan 05 creates it):
  - LoginForm(): JSX.Element
  - Posts to POST /api/auth/token (form-urlencoded)
  - On 200: setToken(access_token) + navigate(from ?? '/')
  - On 400: renders "Username or password is incorrect." under form

frontend/src/components/chat/SelectorErrorBanner.tsx (DOES NOT EXIST YET — Plan 06 creates it):
  - SelectorErrorBanner({ error: ProviderErrorView | null, onRetry: () => void }): JSX.Element | null
  - Renders danger.muted banner with AlertCircle icon + message + Retry button
  - aria-live="polite", role="status"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend test stubs (unit + integration)</name>
  <files>
    backend/tests/unit/test_auth_me.py
    backend/tests/unit/test_provider_probe.py
    backend/tests/integration/test_auth_flow.py
  </files>
  <read_first>
    - backend/tests/conftest.py — auth_headers fixture, create_access_token pattern
    - backend/tests/integration/test_auth_routes.py — full example of client fixture, auth_headers usage, POST /token + GET /api/providers pattern; also POST /token path will become /api/auth/token after Plan 02
    - backend/app/api/routes/auth.py — current route paths (/token, /users/me); understand what Plan 02 will rename
    - backend/pyproject.toml — [tool.pytest.ini_options] for markers, asyncio_mode
  </read_first>
  <action>
    Create three backend test stub files. All stubs use pytest.mark.xfail(strict=False, reason="implementation not yet landed") so the suite stays green.

    File 1 — backend/tests/unit/test_auth_me.py:
    - Mark module with @pytest.mark.unit
    - Import: from fastapi.testclient import TestClient; from app.api.main import app; from app.api.routes.auth import create_access_token
    - client fixture: TestClient(app) context manager, same shape as test_auth_routes.py
    - test_auth_me_returns_401_without_token: GET /api/auth/me (new path after Plan 02 renames); assert status 401
    - test_auth_me_returns_200_with_valid_token: GET /api/auth/me with auth_headers; assert status 200 and body has "username" key
    - Both tests: @pytest.mark.xfail(strict=False, reason="GET /api/auth/me not yet mounted — Plan 02")

    File 2 — backend/tests/unit/test_provider_probe.py:
    - Mark module with @pytest.mark.unit
    - Import: import httpx; import pytest; from unittest.mock import AsyncMock, patch
    - Conditional import for the not-yet-existing module: wrap in try/except ImportError, set probe_provider = None, ProbeError = None
    - test_probe_ollama_unreachable_on_connect_error: monkeypatch httpx.AsyncClient.get to raise httpx.ConnectError; call probe_provider("ollama", "qwen3:4b"); assert result.error == "ollama_unreachable". xfail.
    - test_probe_ollama_model_not_installed: monkeypatch httpx.AsyncClient.get to return a mock response with {"models": [{"name": "other:7b", "model": "other:7b"}]}; call probe_provider("ollama", "qwen3:4b"); assert result.error == "model_not_installed". xfail.
    - test_probe_ollama_success: monkeypatch httpx.AsyncClient.get to return {"models": [{"name": "qwen3:4b", "model": "qwen3:4b"}]}; assert probe_provider("ollama", "qwen3:4b") is None. xfail.
    - test_probe_cloud_missing_key: monkeypatch settings so openai_api_key is None; call probe_provider("openai", "gpt-4o-mini"); assert result.error == "missing_api_key". xfail.
    - test_probe_ollama_timeout: monkeypatch httpx.AsyncClient.get to raise httpx.TimeoutException("timed out"); assert result.error == "ollama_unreachable". xfail.

    File 3 — backend/tests/integration/test_auth_flow.py:
    - Mark module with @pytest.mark.integration
    - Import: from fastapi.testclient import TestClient; from app.api.main import app
    - client fixture: same as test_auth_routes.py
    - test_login_and_get_me_round_trip: POST /api/auth/token (new path) with username=admin password=admin; assert 200, extract access_token; GET /api/auth/me with Authorization: Bearer <token>; assert 200 and body["username"] == "admin". xfail(reason="POST /api/auth/token not yet mounted — Plan 02").
    - test_get_me_rejects_expired_token: use a manually crafted token with exp in the past (build via create_access_token with expires_delta=timedelta(seconds=-1)); GET /api/auth/me; assert 401. xfail.
  </action>
  <verify>
    <automated>cd /Users/axel/code/trip_planner/backend && uv run pytest tests/unit/test_auth_me.py tests/unit/test_provider_probe.py tests/integration/test_auth_flow.py -v --no-header 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 3 files exist. pytest collects the tests and reports them as XFAIL (not ERROR). No import errors crash the collection.
  </done>
  <acceptance_criteria>
    - backend/tests/unit/test_auth_me.py exists and contains def test_auth_me_returns_401_without_token and def test_auth_me_returns_200_with_valid_token
    - backend/tests/unit/test_provider_probe.py exists and contains def test_probe_ollama_unreachable_on_connect_error, test_probe_ollama_model_not_installed, test_probe_ollama_success, test_probe_cloud_missing_key, test_probe_ollama_timeout
    - backend/tests/integration/test_auth_flow.py exists and contains def test_login_and_get_me_round_trip and def test_get_me_rejects_expired_token
    - `uv run pytest tests/unit/test_auth_me.py tests/unit/test_provider_probe.py tests/integration/test_auth_flow.py -v` exits 0 (all xfail, no errors)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Frontend test stubs (auth lib + components)</name>
  <files>
    frontend/src/lib/__tests__/auth.test.ts
    frontend/src/components/auth/__tests__/RequireAuth.test.tsx
    frontend/src/components/auth/__tests__/LoginForm.test.tsx
    frontend/src/lib/__tests__/providerErrors.test.ts
    frontend/src/components/chat/__tests__/SelectorErrorBanner.test.tsx
  </files>
  <read_first>
    - frontend/src/hooks/__tests__/useChat.test.ts — established Vitest test patterns: vi.stubGlobal for fetch, localStorage mock, describe/it/expect, MemoryRouter wrapper
    - frontend/src/lib/__tests__/parseSSE.test.ts — pure function test pattern; import syntax; vi.fn() usage
    - frontend/vite.config.ts — test config section (environment: 'jsdom', globals: true)
    - frontend/package.json — available test dependencies: vitest, @testing-library/react, jsdom; verify @testing-library/user-event presence
  </read_first>
  <action>
    Create 5 frontend test stub files. All use test.todo() for the not-yet-implemented cases so Vitest reports them as "todo" (not failures). The describe blocks must import the target modules via conditional try/catch so missing modules do not crash collection.

    File 1 — frontend/src/lib/__tests__/auth.test.ts:
    - Import: import { describe, it, expect, vi, beforeEach } from 'vitest'
    - Use dynamic import wrapped in try/catch for getToken, setToken, clearToken, apiFetch from '../auth'
    - beforeEach: localStorage.clear(); vi.restoreAllMocks()
    - test.todo('getToken returns null when localStorage is empty')
    - test.todo('setToken stores the value under key auth_token')
    - test.todo('clearToken removes auth_token from localStorage')
    - test.todo('apiFetch injects Authorization: Bearer header when token is present')
    - test.todo('apiFetch does not add Authorization header when token is absent')
    - test.todo('apiFetch calls clearToken and redirects to /login?flash=session_expired when response is 401')

    File 2 — frontend/src/components/auth/__tests__/RequireAuth.test.tsx:
    - Import: import { describe, it, expect, vi, beforeEach } from 'vitest'; import { render } from '@testing-library/react'; import { MemoryRouter, Routes, Route } from 'react-router-dom'
    - beforeEach: localStorage.clear()
    - test.todo('RequireAuth redirects to /login when no token is stored')
    - test.todo('RequireAuth redirects to /login?from=<encoded-path> preserving the original path')
    - test.todo('RequireAuth renders children when token is valid and /api/auth/me returns 200')
    - test.todo('RequireAuth redirects to /login when /api/auth/me returns 401')

    File 3 — frontend/src/components/auth/__tests__/LoginForm.test.tsx:
    - Import: import { describe, it, expect, vi, beforeEach } from 'vitest'; import { render, screen, waitFor, fireEvent } from '@testing-library/react'; import { MemoryRouter, Routes, Route } from 'react-router-dom'
    - beforeEach: localStorage.clear(); vi.restoreAllMocks()
    - test.todo('LoginForm renders username and password fields with Sign in button')
    - test.todo('LoginForm calls POST /api/auth/token with form-urlencoded body on submit')
    - test.todo('LoginForm stores access_token in localStorage and navigates to / on 200 response')
    - test.todo('LoginForm renders inline error "Username or password is incorrect." on 400 response')
    - test.todo('Password field toggles between type=password and type=text when eye icon is clicked')

    File 4 — frontend/src/lib/__tests__/providerErrors.test.ts:
    - Import: import { describe, it, expect } from 'vitest'
    - Dynamic import for mapProbeError, PROVIDERS_FETCH_FAILED from '../providerErrors'
    - test.todo('mapProbeError returns F1 copy with ollama serve inlineCode for ollama_unreachable')
    - test.todo('mapProbeError returns F2 copy with model and ollama pull <model> inlineCode for model_not_installed')
    - test.todo('mapProbeError returns F3 copy with ENV_VAR inlineCode for missing_api_key')
    - test.todo('PROVIDERS_FETCH_FAILED has code providers_fetch_failed and empty inlineCode array')

    File 5 — frontend/src/components/chat/__tests__/SelectorErrorBanner.test.tsx:
    - Import: import { describe, it, expect, vi } from 'vitest'; import { render, screen, fireEvent } from '@testing-library/react'
    - Dynamic import for SelectorErrorBanner from '../SelectorErrorBanner'
    - test.todo('SelectorErrorBanner renders null when error prop is null')
    - test.todo('SelectorErrorBanner renders the error message and a Retry button when error is provided')
    - test.todo('SelectorErrorBanner calls onRetry when Retry button is clicked')
    - test.todo('SelectorErrorBanner has aria-live=polite and role=status on the container')

    Also create the __tests__ directories if they do not exist. Use mkdir -p for frontend/src/components/auth/__tests__ and frontend/src/components/chat/__tests__.
  </action>
  <verify>
    <automated>cd /Users/axel/code/trip_planner/frontend && npm test -- --run 2>&1 | tail -30</automated>
  </verify>
  <done>
    All 5 files exist. Vitest collects them and reports all cases as "todo" (or skipped). No import errors. Exit code 0.
  </done>
  <acceptance_criteria>
    - All 5 test files exist at the specified paths
    - frontend/src/components/auth/__tests__/ directory exists
    - frontend/src/components/chat/__tests__/ directory exists
    - `npm test -- --run` exits 0 with no import errors — all new tests show as "todo" or "skip", not "fail"
    - Each file contains at least 4 test.todo() calls covering the behavior described in the VALIDATION.md Wave 0 rows
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → app module | Test stubs import from app modules; no secrets or live I/O in stubs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-W0-01 | Information Disclosure | test_provider_probe.py — hardcoded credentials in test assertions | accept | Tests use the default admin:admin seed; no real secrets. Tests are in backend/tests/ which is not shipped to production. |
| T-42-W0-02 | Tampering | xfail stubs could be committed as "passing" if strict=False is omitted | mitigate | All xfail stubs use strict=False so they report XPASS (not PASS) when unexpectedly passing — executor cannot silently skip implementing the feature. |
</threat_model>

<verification>
Both tasks above run within 30 seconds each. Final gate: pytest collects all backend stubs with zero ERRORs (only XFAILs); Vitest collects all frontend stubs with zero FAILs (only todos).

```bash
cd /Users/axel/code/trip_planner/backend && uv run pytest tests/unit/test_auth_me.py tests/unit/test_provider_probe.py tests/integration/test_auth_flow.py -v
cd /Users/axel/code/trip_planner/frontend && npm test -- --run
```
</verification>

<success_criteria>
8 test files exist. pytest exits 0 (all xfail). Vitest exits 0 (all todo). No import errors in either suite. VALIDATION.md Wave 0 checklist rows W0-be-1 through W0-fe-5 are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-unbreak-the-app/04.2-01-SUMMARY.md`
</output>
