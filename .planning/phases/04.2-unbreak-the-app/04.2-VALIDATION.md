---
phase: 4.2
slug: unbreak-the-app
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-15
---

# Phase 4.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Backend framework** | pytest 8.x + pytest-asyncio (markers: `unit`, `integration`, `e2e`, `slow`) |
| **Frontend framework** | Vitest 4.x + @testing-library/react + jsdom |
| **Backend config** | `backend/pyproject.toml` (`[tool.pytest.ini_options]`) |
| **Frontend config** | `frontend/vite.config.ts` (Vitest section) |
| **Quick run command** | `just test-unit` (backend) / `cd frontend && npm test -- --run` (frontend) |
| **Full suite command** | `just test` (excludes slow/e2e) |
| **E2E command (excluded from CI)** | `just test-e2e` |
| **Estimated runtime — unit (be+fe)** | ~25–40 seconds |
| **Estimated runtime — full default suite** | ~60–90 seconds |

---

## Sampling Rate

- **After every task commit:** Run the closest-scoped unit suite for the file changed (e.g. `cd backend && uv run pytest tests/unit/test_auth_probe.py -v`).
- **After every plan wave:** Run `just test` (backend default) and `cd frontend && npm test -- --run` (frontend).
- **Before `/gsd-verify-work`:** `just check` + `just test` must both be green; manual UAT on the golden path.
- **Max feedback latency:** ≤ 90 seconds for the full default suite.

---

## Per-Task Verification Map

> The planner fills this in once PLAN.md tasks have IDs. Wave 0 rows below are the seed expectations the planner must honour — every other row is filled after PLAN.md is written.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| W0-be-1 | 00 | 0 | REQ-login-page | T-AUTH-RATE-LIMIT (deferred per ADR-009) | rejects expired/invalid JWT with 401 | unit | `cd backend && uv run pytest tests/unit/test_auth_me.py -v` | ❌ W0 | ⬜ pending |
| W0-be-2 | 00 | 0 | REQ-llm-provider-ui-fix | T-PROVIDER-MISCONFIG | `POST /api/chat/session` returns structured `{error,message,hint}` body for F1–F3 | unit | `cd backend && uv run pytest tests/unit/test_provider_probe.py -v` | ❌ W0 | ⬜ pending |
| W0-be-3 | 00 | 0 | REQ-llm-provider-ui-fix | T-OLLAMA-TIMEOUT | probe respects 1.5s timeout; surfaces `ollama_unreachable` on `httpx.ConnectError`/`TimeoutException` | unit | `cd backend && uv run pytest tests/unit/test_provider_probe.py::test_ollama_timeout -v` | ❌ W0 | ⬜ pending |
| W0-be-4 | 00 | 0 | REQ-login-page | — | integration test: `POST /token` then `GET /api/auth/me` round-trip with the issued JWT | integration | `cd backend && uv run pytest tests/integration/test_auth_flow.py -v` | ❌ W0 | ⬜ pending |
| W0-fe-1 | 00 | 0 | REQ-login-page | — | `apiFetch()` injects `Authorization: Bearer`; 401 clears token + redirects to `/login` | unit | `cd frontend && npm test -- src/lib/__tests__/auth.test.ts --run` | ❌ W0 | ⬜ pending |
| W0-fe-2 | 00 | 0 | REQ-login-page | — | `<RequireAuth>` redirects unauthenticated users; preserves `?from=` deep link | unit | `cd frontend && npm test -- src/components/auth/__tests__/RequireAuth.test.tsx --run` | ❌ W0 | ⬜ pending |
| W0-fe-3 | 00 | 0 | REQ-login-page | — | `<LoginForm>` calls `POST /token`, stores JWT, navigates on success; renders inline error on 401 | unit | `cd frontend && npm test -- src/components/auth/__tests__/LoginForm.test.tsx --run` | ❌ W0 | ⬜ pending |
| W0-fe-4 | 00 | 0 | REQ-llm-provider-ui-fix | — | `mapProviderError(error_code)` returns the F1–F4 copy from UI-SPEC for each taxonomy code | unit | `cd frontend && npm test -- src/lib/__tests__/providerErrors.test.ts --run` | ❌ W0 | ⬜ pending |
| W0-fe-5 | 00 | 0 | REQ-llm-provider-ui-fix | — | `<SelectorErrorBanner>` renders F1–F4 with Retry button; banner persists until selector change or successful retry | unit | `cd frontend && npm test -- src/components/chat/__tests__/SelectorErrorBanner.test.tsx --run` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

> Planner: extend this table with one row per non-Wave-0 task. Tasks with **only** type-check / lint / build verification (no behaviour assertion) must declare a Wave 0 dependency in `read_first` so they ride a sibling test's automated command.

---

## Wave 0 Requirements

- [ ] `backend/tests/unit/test_auth_me.py` — covers `GET /api/auth/me` (D-17 — reuse-vs-rename of `/users/me`)
- [ ] `backend/tests/unit/test_provider_probe.py` — covers Ollama probe success / unreachable / model-not-installed / cloud missing-key
- [ ] `backend/tests/integration/test_auth_flow.py` — `POST /token` → `GET /api/auth/me` integration round-trip
- [ ] `frontend/src/lib/__tests__/auth.test.ts` — `apiFetch()`, `getToken()`, `setToken()`, `clearToken()`, 401 redirect
- [ ] `frontend/src/components/auth/__tests__/RequireAuth.test.tsx` — protected-route guard + `?from=` query
- [ ] `frontend/src/components/auth/__tests__/LoginForm.test.tsx` — submission, success, failure, password toggle
- [ ] `frontend/src/lib/__tests__/providerErrors.test.ts` — error-code → copy mapping
- [ ] `frontend/src/components/chat/__tests__/SelectorErrorBanner.test.tsx` — banner render + Retry behaviour
- [ ] Reuse existing `auth_headers` fixture in `backend/tests/conftest.py` — no new framework install needed

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Golden path: open `/`, get redirected to `/login`, sign in with `AUTH_USERS` creds, see chat, complete one Ollama-backed turn | REQ-login-page + REQ-llm-provider-ui-fix | Visual + LLM-backed end-to-end behaviour with a live Ollama; not worth a Playwright run for this phase (deferred to 4.3 per RESEARCH.md §"Test split") | `just backend` + `just frontend`; navigate to `localhost:5173`, expect `/login` redirect, sign in, expect chat redirect, send "hello" and watch the SSE stream |
| Provider misconfig F1: stop Ollama, reload chat, pick `ollama:llama3.2`, expect F1 banner with Retry | REQ-llm-provider-ui-fix | Requires actually killing the Ollama daemon | `pkill -f ollama` (or stop the desktop app), reload `localhost:5173`, pick `ollama:<any>`, expect "Cannot reach Ollama at localhost:11434…" banner |
| Provider misconfig F3: unset `OPENAI_API_KEY`, pick `openai:gpt-4o-mini`, expect F3 banner | REQ-llm-provider-ui-fix | Requires env var manipulation in the running backend | Restart backend with `OPENAI_API_KEY=""`, pick `openai:<any>`, expect "OpenAI API key not configured…" banner |
| Logout flow: click logout, expect `/login`, expect `auth_token` cleared from `localStorage` | REQ-login-page | Visual + storage assertion | DevTools → Application → Local Storage; click logout, verify `auth_token` is gone |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (8 new test files + reused conftest fixture)
- [ ] No watch-mode flags (`--watch`, `--watchAll`) anywhere in commands
- [ ] Feedback latency < 90s for full default suite
- [ ] `nyquist_compliant: true` set in frontmatter once Wave 0 stubs land

**Approval:** pending
