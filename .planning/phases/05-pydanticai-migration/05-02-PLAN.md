---
plan_id: 05-02
phase: 05-pydanticai-migration
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - backend/app/chat/deps.py
  - backend/app/chat/store.py
  - backend/app/chat/models.py
  - backend/app/chat/__init__.py
  - backend/app/llm/base.py
  - backend/app/llm/protocol.py
  - backend/app/llm/__init__.py
  - backend/app/llm/factory.py
  - backend/app/llm/providers/ollama.py
  - backend/app/llm/providers/openai.py
  - backend/app/llm/providers/anthropic.py
  - backend/app/llm/providers/lmstudio.py
  - backend/app/config.py
autonomous: true
requirements:
  - REQ-pydantic-ai-migration
  - REQ-p5-stream-event-abc

must_haves:
  truths:
    - "`inspect.isabstract(app.llm.base.LLMProvider) is True` and the class is no longer a `typing.Protocol`."
    - "`app.llm.base.BoundProvider` does not exist; the two-tier shape is gone (D-01..D-03)."
    - "`StreamEvent` is `class StreamEvent(BaseModel, ABC)` with the five concrete event subclasses inheriting from it; SSE wire bytes are byte-equivalent to Phase 4.7 (per RESEARCH OQ-01)."
    - "`ChatDeps` is a `@dataclass(frozen=True)` carrying `flight_client: FlightAPIClient`, `session_id: str`, `user_id: str`."
    - "`ConversationStore` is an `abc.ABC` with `append`/`load`/`delete`/`list_for_user`; `InMemoryConversationStore` round-trips `list[ModelMessage]` natively."
    - "`Settings.openai_o_series_model_prefixes` exists; `Settings.ollama_reasoning_model_prefixes` is removed."
  artifacts:
    - path: "backend/app/llm/base.py"
      provides: "LLMProvider(ABC) — single-tier abstract surface (D-01..D-03)"
      contains: "class LLMProvider(ABC):"
    - path: "backend/app/chat/deps.py"
      provides: "ChatDeps frozen dataclass (D-05)"
      contains: "@dataclass(frozen=True)"
    - path: "backend/app/chat/store.py"
      provides: "ConversationStore(ABC) + InMemoryConversationStore (D-08, D-11)"
      contains: "class ConversationStore(ABC):"
    - path: "backend/app/chat/models.py"
      provides: "StreamEvent ABC base + 5 concrete subclasses (D-15, D-16)"
      contains: "class StreamEvent(BaseModel, ABC):"
  key_links:
    - from: "backend/app/llm/base.py"
      to: "concrete provider modules (Wave 2)"
      via: "explicit subclass `class OllamaProvider(LLMProvider):` etc."
      pattern: "class \\w+Provider\\(LLMProvider\\):"
    - from: "backend/app/chat/store.py"
      to: "ChatService (Wave 3) and Phase 6 PostgresConversationStore"
      via: "DI via lifespan; ABC method signatures define the swap contract"
      pattern: "ConversationStore\\(ABC\\)"
---

<objective>
Lay the foundational leaf-level modules that every later wave depends on. After this plan completes, the abstract surfaces are in place but no concrete provider has been rewritten yet — that is Wave 2's responsibility.

Specifically:
1. Rename `app/llm/protocol.py` → `app/llm/base.py` and convert `LLMProvider` (Protocol) → `LLMProvider(ABC)`. Delete `BoundProvider` entirely (D-01..D-03). Delete `app/llm/protocol.py` outright (no transitional shim — pure rename + retype per CONTEXT.md anti-pattern lock). Migrate the four concrete providers' import lines and `bind_tools` return-type annotations in the same task so the codebase is importable at end-of-task. Update `app/llm/__init__.py` re-exports.
2. Create `app/chat/deps.py` with the `ChatDeps` frozen dataclass (D-05).
3. Create `app/chat/store.py` with `ConversationStore(ABC)` + `InMemoryConversationStore` (D-08, D-11).
4. Refactor `app/chat/models.py` from the `Annotated[..., Field(discriminator="type")]` alias into `class StreamEvent(BaseModel, ABC)` with the five concrete subclasses inheriting from it (D-15, D-16, REQ-p5-stream-event-abc). Wire format must remain byte-equivalent (verified by Wave 0's golden file).
5. Add `Settings.openai_o_series_model_prefixes` and remove `Settings.ollama_reasoning_model_prefixes` (D-14, OQ-04).

Purpose: Decouple the abstract contracts from any concrete PydanticAI provider construction. Wave 2 can then rewrite each provider against `pydantic_ai.models.*` knowing the ABC is stable.
Output: 4 new/renamed modules + 4 in-place edits.
</objective>

<execution_context>
@/Users/axel/code/trip_planner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/trip_planner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-pydanticai-migration/05-CONTEXT.md
@.planning/phases/05-pydanticai-migration/05-RESEARCH.md
@.planning/phases/05-pydanticai-migration/05-PATTERNS.md
@./CLAUDE.md
@backend/app/llm/protocol.py
@backend/app/llm/__init__.py
@backend/app/llm/factory.py
@backend/app/llm/errors.py
@backend/app/chat/models.py
@backend/app/chat/__init__.py
@backend/app/auth/repository.py
@backend/app/config.py
@backend/app/tools/flight_client.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/app/auth/repository.py (canonical ABC pattern — follow this template):
```python
class UserRepository(ABC):
    @abstractmethod
    def get_user(self, username: str) -> UserInDB: ...
    @abstractmethod
    def verify_password(self, plain: str, hashed: str) -> bool: ...

class EnvUserRepository(UserRepository):
    def __init__(self) -> None: ...
```

From backend/app/llm/errors.py (preserved verbatim — used by validate_config return type):
```python
class ProbeErrorCode(StrEnum): ...
class ProbeError(BaseModel):
    error: ProbeErrorCode
    message: str
    hint: str | None = None
```

From backend/app/tools/flight_client.py (used by ChatDeps):
```python
class FlightAPIClient(ABC):
    @abstractmethod
    async def search(self, query: FlightQuery) -> list[Flight]: ...
```

From pydantic_ai (third-party, used in ConversationStore + LLMProvider signatures):
```python
from pydantic_ai import Agent
from pydantic_ai.messages import ModelMessage
```

Target ABC surface (D-02, RESEARCH § Architectural Responsibility Map):
```python
# app/llm/base.py
class LLMProvider(ABC):
    @abstractmethod
    def get_provider_name(self) -> str: ...
    @abstractmethod
    async def validate_config(self) -> ProbeError | None: ...
    @abstractmethod
    async def list_models(self) -> list[str]: ...
    @abstractmethod
    def build_agent(self, tools: Sequence[Any], deps_type: type[Any]) -> Agent[Any, str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename protocol.py → base.py and convert LLMProvider Protocol → ABC; delete BoundProvider</name>
  <files>
    backend/app/llm/base.py,
    backend/app/llm/protocol.py,
    backend/app/llm/__init__.py
  </files>
  <read_first>
    - backend/app/llm/protocol.py — current Protocol surface (lines 44-92 per PATTERNS.md): `LLMProvider(Protocol)` and `BoundProvider(Protocol)`
    - backend/app/llm/providers/ollama.py — currently imports `from app.llm.protocol import LLMProvider, BoundProvider` (around line 48); `bind_tools` body returns annotated `BoundProvider`
    - backend/app/llm/providers/openai.py — currently imports `from app.llm.protocol import LLMProvider, BoundProvider` (around line 37); same `bind_tools` annotation
    - backend/app/llm/providers/anthropic.py — currently imports `from app.llm.protocol import LLMProvider, BoundProvider` (around line 51); same `bind_tools` annotation
    - backend/app/llm/providers/lmstudio.py — currently imports `from app.llm.protocol import LLMProvider, BoundProvider` (around line 52); same `bind_tools` annotation
    - backend/app/auth/repository.py:35-93 — canonical ABC pattern (the template per PATTERNS.md § "backend/app/llm/base.py")
    - .planning/phases/05-pydanticai-migration/05-PATTERNS.md § "backend/app/llm/base.py" — full target shape
    - .planning/phases/05-pydanticai-migration/05-CONTEXT.md D-01 ("Single LLMProvider(ABC); BoundProvider retires"), D-02 (ABC surface methods), D-03 (ABC not Protocol; closes Known Tech Debt)
    - .planning/phases/05-pydanticai-migration/05-RESEARCH.md § "User Constraints" D-01..D-03 + § "Architectural Responsibility Map"
    - .planning/phases/05-pydanticai-migration/05-CONTEXT.md anti-pattern lock: "Don't ship a `ProtocolProvider` adapter alongside the ABC — pure rename + retype. No backwards-compat shim"
    - ./CLAUDE.md — "Abstract interfaces use `ABC`, never `Protocol`"
    - backend/app/llm/__init__.py — current re-exports
    - backend/tests/unit/llm/test_protocol_abc.py (Wave 0 stub) — these tests must turn green
  </read_first>
  <behavior>
    - `inspect.isabstract(app.llm.base.LLMProvider) is True`
    - `not isinstance(app.llm.base.LLMProvider, type(typing.Protocol))`
    - `not hasattr(app.llm.base, "BoundProvider")` and `not hasattr(app.llm, "BoundProvider")`
    - The four abstract methods (`get_provider_name`, `validate_config`, `list_models`, `build_agent`) are decorated with `@abstractmethod`.
    - `validate_config` is `async def`; `list_models` is `async def`; `get_provider_name` and `build_agent` are sync `def`.
    - `build_agent` signature accepts `tools: Sequence[Any]` and `deps_type: type[Any]` and returns `Agent[Any, str]`.
    - `backend/app/llm/protocol.py` does NOT exist after this task — outright deletion, no shim (CONTEXT.md anti-pattern lock: "pure rename + retype, no backwards-compat shim").
    - All four concrete provider modules (`providers/{ollama,openai,anthropic,lmstudio}.py`) import `LLMProvider` from `app.llm.base`; `BoundProvider` is no longer imported anywhere.
    - The existing `bind_tools` method bodies in those four providers are LEFT INTACT in this task — only the `BoundProvider` return-type annotation is replaced with `Any` (from `typing`); the LangChain `bind_tools` body itself is rewritten in Wave 2 / Plan 05-03.
    - `grep -rEn "BoundProvider" backend/app` returns no results.
    - `from app.llm.providers import ollama, openai, anthropic, lmstudio` succeeds at runtime (the codebase is importable mid-wave).
    - `app/llm/__init__.py` re-exports `LLMProvider` from `app.llm.base`; any `BoundProvider` re-export is deleted.
  </behavior>
  <action>
    Create `backend/app/llm/base.py` with the new ABC surface per the `<interfaces>` block. Imports: `from abc import ABC, abstractmethod`, `from collections.abc import Sequence`, `from typing import Any`, `from pydantic_ai import Agent`, `from app.llm.errors import ProbeError`. Module docstring explains "Phase 5: replaces typing.Protocol with abc.ABC per CLAUDE.md and D-03; the two-tier `LLMProvider` → `BoundProvider` shape collapses (D-01)."

    DELETE `backend/app/llm/protocol.py` outright — no transitional shim. CONTEXT.md anti-pattern lock: "Don't ship a `ProtocolProvider` adapter alongside the ABC — pure rename + retype, no backwards-compat shim." The four concrete providers' import lines are migrated in this same task (next paragraph) so the codebase remains importable at end-of-task.

    Migrate the four concrete provider modules (`backend/app/llm/providers/{ollama,openai,anthropic,lmstudio}.py`) — same task, NOT deferred to Wave 2:
    - Replace `from app.llm.protocol import LLMProvider, BoundProvider` with `from app.llm.base import LLMProvider` (drop the `BoundProvider` name).
    - Add `from typing import Any` if not already present.
    - In each `bind_tools` method's signature, replace the `-> BoundProvider` return-type annotation with `-> Any`. The method BODY is LEFT UNTOUCHED in this task — Wave 2 (plan 05-03 Tasks 1–2) deletes `bind_tools` and adds `build_agent` per provider. The annotation swap exists solely so the modules import without `NameError` mid-wave.
    - Drop any other `BoundProvider` references that surface in those files (e.g. local type-hints, comments referencing the type) — do not preserve `BoundProvider` anywhere.

    Update `backend/app/llm/__init__.py`: replace `from app.llm.protocol import LLMProvider, BoundProvider` (or whatever is currently there) with `from app.llm.base import LLMProvider`; remove any `BoundProvider` reference from `__all__`.

    To keep this task focused: do NOT run `just check` as part of this task's verify. `mypy --strict` will still fail on the LangChain `bind_tools` BODIES inside the four provider files until Wave 2 rewrites them — that is expected and bounded to those four modules. The runtime import path IS clean (verified by the import-smoke acceptance criterion below).

    Per `/dignified-python` skill activation rule: invoke `/dignified-python` mentally for ABC vs Protocol discipline. Public API methods get docstrings (Args/Returns/Raises) per CLAUDE.md.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/llm/test_protocol_abc.py -v && (grep -rEn "BoundProvider" backend/app && echo "FAIL: BoundProvider still present" && exit 1 || echo "OK: BoundProvider removed") && cd backend && uv run python -c "from app.llm.providers import ollama, openai, anthropic, lmstudio; from app.llm.base import LLMProvider; print('OK')" && (test ! -f backend/app/llm/protocol.py && echo "OK: protocol.py deleted" || (echo "FAIL: protocol.py still present" && exit 1))</automated>
  </verify>
  <done>After this task, `backend/app/llm/protocol.py` no longer exists; all four concrete providers import `LLMProvider` from `app.llm.base`; `grep -rEn "BoundProvider" backend/app` returns no results; the import-smoke check `from app.llm.providers import ollama, openai, anthropic, lmstudio` succeeds; `tests/unit/llm/test_protocol_abc.py` is green. `mypy --strict` will still fail on the four providers' `bind_tools` BODIES until Wave 2 (Plan 05-03) rewrites them — that is expected and bounded to those four modules' bodies; the import surface is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ChatDeps and ConversationStore foundations</name>
  <files>
    backend/app/chat/deps.py,
    backend/app/chat/store.py,
    backend/app/chat/__init__.py
  </files>
  <read_first>
    - .planning/phases/05-pydanticai-migration/05-PATTERNS.md § "backend/app/chat/deps.py" + § "backend/app/chat/store.py" — full target shapes
    - .planning/phases/05-pydanticai-migration/05-CONTEXT.md D-05 (ChatDeps fields + frozen), D-08 (ConversationStore ABC), D-09 (metadata stays on ChatService — NOT in store), D-11 (storage = list[ModelMessage] natively)
    - .planning/phases/05-pydanticai-migration/05-RESEARCH.md § "ConversationStore ABC" — reference impl with `_store: dict[str, list[ModelMessage]]` and `_user_sessions: dict[str, set[str]]`
    - .planning/phases/05-pydanticai-migration/05-RESEARCH.md § "ChatDeps dataclass (D-05)" — exact target shape
    - backend/app/auth/repository.py:35-93 — `UserRepository(ABC)` + `EnvUserRepository` template (PATTERNS.md analog)
    - backend/app/llm/factory.py:35-58 — `SessionLLMConfig` `@dataclass(frozen=True)` analog
    - backend/app/tools/flight_client.py — `FlightAPIClient` ABC import for ChatDeps field annotation
    - backend/app/chat/models.py — current `ChatSessionInfo` shape (used by `list_for_user` return type)
    - backend/app/chat/__init__.py — current `__all__` list
    - ~/.claude/rules/common/coding-style.md — immutability ("`existing + messages`" not in-place mutate); Patterns.md § ConversationStore body uses immutable concat
    - backend/tests/unit/chat/test_deps.py + tests/unit/chat/test_conversation_store.py (Wave 0 stubs) — must turn green after this task
  </read_first>
  <behavior>
    - `from dataclasses import is_dataclass; assert is_dataclass(ChatDeps)`; `ChatDeps.__dataclass_params__.frozen is True`.
    - `ChatDeps` field set is exactly `{"flight_client", "session_id", "user_id"}` with annotations `FlightAPIClient`, `str`, `str`.
    - `inspect.isabstract(ConversationStore) is True`; `inspect.isabstract(InMemoryConversationStore) is False`.
    - `ConversationStore` declares four `@abstractmethod async def`: `append(session_id, messages) -> None`, `load(session_id) -> list[ModelMessage]`, `delete(session_id) -> None`, `list_for_user(user_id) -> list[ChatSessionInfo]`.
    - `InMemoryConversationStore.append` uses immutable concat (`existing + messages`), not `.append()`.
    - `await store.load("unknown_id")` returns `[]` (empty list, not `None`/raise).
    - `await store.delete("nonexistent")` is a no-op (uses `dict.pop(..., None)`).
    - `list_for_user` returns `list[ChatSessionInfo]` filtered by `user_id`. Phase 5 metadata (`created_at`, `last_activity`, `provider`, `model`) is NOT carried by the store (D-09 — those live on `ChatService._metadata`); the store tracks only `(user_id, session_id)` pairs and returns minimal `ChatSessionInfo` skeletons. ChatService (Wave 3) merges metadata before returning to the route.
    - Both modules have `async def` methods only (CLAUDE.md "all I/O must be async def").
    - `app/chat/__init__.py` re-exports `ChatDeps`, `ConversationStore`, `InMemoryConversationStore` and adds them to `__all__`.
  </behavior>
  <action>
    Create `backend/app/chat/deps.py`. Imports: `from dataclasses import dataclass`, `from app.tools.flight_client import FlightAPIClient`. Body: `@dataclass(frozen=True)` decorated `class ChatDeps:` with the three fields `flight_client: FlightAPIClient`, `session_id: str`, `user_id: str`. Class docstring per D-05: "Per-turn dependency container threaded through PydanticAI `RunContext` (closes the `_flight_client` monkey-patch back-door)."

    Create `backend/app/chat/store.py`. Imports: `from abc import ABC, abstractmethod`, `from pydantic_ai.messages import ModelMessage`, `from app.chat.models import ChatSessionInfo`. Two classes:

    - `class ConversationStore(ABC)` with the four `@abstractmethod async def` methods (signatures per `<behavior>`).
    - `class InMemoryConversationStore(ConversationStore)`. `__init__` initialises `self._store: dict[str, list[ModelMessage]] = {}` and `self._user_sessions: dict[str, set[str]] = {}`. `append` records the user_id mapping when first seen (the user_id is supplied by `ChatService` when it first calls `append` — for the simplest impl, accept user_id-binding via a separate method `register(session_id: str, user_id: str)` OR ALTERNATIVELY have `ChatService` track the mapping itself and pass it into `list_for_user` differently. Prefer the simpler path: `InMemoryConversationStore` does NOT track user_id (D-09 keeps that on ChatService); `list_for_user` returns `[]` here and ChatService composes the response from its `_metadata` dict. Document this in the docstring as "Phase 5 in-memory store does not index by user_id; ChatService composes user-scoped views via `_metadata`. Phase 6 PostgresConversationStore will own the user→sessions index in SQL." This keeps the ABC signature stable for Phase 6 while keeping Phase 5 minimal.
    - `append`: `existing = self._store.get(session_id, []); self._store[session_id] = existing + messages` (immutable concat per coding-style.md).
    - `load`: `return list(self._store.get(session_id, []))` (defensive copy — caller cannot mutate internal state).
    - `delete`: `self._store.pop(session_id, None)`.
    - `list_for_user`: returns `[]` for Phase 5; docstring explains the Phase 6 hand-off.

    Update `backend/app/chat/__init__.py`: add `from app.chat.deps import ChatDeps`; add `from app.chat.store import ConversationStore, InMemoryConversationStore`; add the three names to the existing `__all__` list (alphabetical order to match the existing style).

    `/dignified-python` skill activation: required for ABC + frozen-dataclass discipline. Public API methods get docstrings.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/chat/test_deps.py tests/unit/chat/test_conversation_store.py -v</automated>
  </verify>
  <done>Both Wave 0 chat-foundation tests are green. `from app.chat import ChatDeps, ConversationStore, InMemoryConversationStore` works. `mypy --strict app/chat/deps.py app/chat/store.py app/chat/__init__.py` passes (these three files are decoupled from the still-LangChain-y `app/chat/service.py`, so they type-check cleanly even though the service rewrite is in Wave 3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor StreamEvent to ABC + 5 subclasses; preserve wire format byte-for-byte; add Settings o-series knob</name>
  <files>
    backend/app/chat/models.py,
    backend/app/config.py,
    backend/app/llm/factory.py
  </files>
  <read_first>
    - backend/app/chat/models.py — current Phase 4.7 alias (lines 42-102 per PATTERNS.md): `Annotated[..., Field(discriminator="type")]` + 5 BaseModel subclasses with `type: Literal[...]` + `session_id: str` repeated on each.
    - .planning/phases/05-pydanticai-migration/05-PATTERNS.md § "backend/app/chat/models.py" — full target shape. Note: `session_id` MOVES to the base class (subclasses no longer redeclare it).
    - .planning/phases/05-pydanticai-migration/05-CONTEXT.md D-15 (StreamEvent ABC), D-16 (REQ-p5-stream-event-abc bundled here)
    - .planning/phases/05-pydanticai-migration/05-RESEARCH.md OQ-01 — "BaseModel + ABC + Field(discriminator='type') works cleanly in Pydantic v2; verified by live execution"
    - .planning/phases/05-pydanticai-migration/05-RESEARCH.md § "StreamEvent ABC Refactor" — exact target class definitions
    - backend/tests/unit/test_stream_events.py — existing tests that exercise the alias; the discriminator test (lines 138-159) must continue to pass.
    - backend/tests/unit/chat/test_stream_event_wire_compat.py (Wave 0) — golden file that locks byte-equivalent serialization.
    - backend/tests/unit/chat/test_stream_event_abc.py (Wave 0) — must turn green here.
    - backend/app/config.py — current `Settings` class; lines 72-83 hold `ollama_reasoning_model_prefixes`.
    - backend/app/llm/factory.py — line ~97 currently calls `OllamaProvider(..., reasoning_model_prefixes=self._settings.ollama_reasoning_model_prefixes, ...)`; this kwarg must be removed in the same task to keep `factory.build()` importable mid-wave.
    - .planning/phases/05-pydanticai-migration/05-PATTERNS.md § "backend/app/config.py" — exact knob to add/remove.
    - ./CLAUDE.md — "tunable thresholds live on Settings, not as module-level constants"
  </read_first>
  <behavior>
    - `inspect.isabstract(StreamEvent) is True`; `StreamEvent` is NOT a Protocol.
    - `class StreamEvent(BaseModel, ABC):` declares `type: str` and `session_id: str`.
    - The five subclasses (`ContentEvent`, `ThinkingEvent`, `ToolCallEvent`, `ToolResultEvent`, `ErrorEvent`) each subclass `StreamEvent` directly and narrow `type: Literal[...]`. They do NOT redeclare `session_id` (it's inherited).
    - `isinstance(ContentEvent(chunk="hi", session_id="s"), StreamEvent) is True` — and same for the other four.
    - `model_dump_json()` output is byte-equivalent to Phase 4.7 for all five subclasses (Wave 0 golden file enforces this — RESEARCH OQ-01 confirms this is true for Pydantic v2 ABC + Literal-discriminator combinations).
    - The `Annotated[ContentEvent | ThinkingEvent | ..., Field(discriminator="type")]` alias is DELETED. Code that previously typed against `StreamEvent` now types against `StreamEvent` directly (the ABC is a real class, not an alias). For TypeAdapter discriminated-union round-trip, callers use `TypeAdapter(StreamEvent)` or build a Union explicitly.
    - DTOs below the alias (`SessionCreateRequest`, `ChatSessionInfo`, `RetryRequest`, `ProviderInfo`, etc.) are UNCHANGED.
    - `Settings.openai_o_series_model_prefixes: tuple[str, ...] = ("o1", "o3")` exists with an explanatory comment mirroring the Phase 4.5 `ollama_reasoning_model_prefixes` style.
    - `Settings.ollama_reasoning_model_prefixes` is REMOVED (per OQ-04 — PydanticAI handles `<think>` tags natively).
    - `backend/app/llm/factory.py`'s `OllamaProvider(...)` construction call no longer passes `reasoning_model_prefixes=...` (the kwarg is dropped here, in the same task as the Settings field removal — Wave 2 / Plan 05-03 Task 3 only handles the broader `OllamaProvider.__init__` signature change).
    - `grep -n "ollama_reasoning_model_prefixes" backend/app` returns no results.
    - `cd backend && uv run python -c "from app.config import Settings; assert not hasattr(Settings(), 'ollama_reasoning_model_prefixes')"` exits 0.
  </behavior>
  <action>
    Edit `backend/app/chat/models.py`. Add `from abc import ABC` to the imports (preserve the existing `from pydantic import BaseModel, Field`, `from typing import Annotated, Any, Literal`, and `ErrorCode` import). Replace lines 42-102 (the 5 event classes + the `StreamEvent = Annotated[...]` alias) with the target shape from PATTERNS.md:

    1. New `class StreamEvent(BaseModel, ABC):` with two fields: `type: str` (no default; subclasses override with `Literal[...] = "<value>"`) and `session_id: str`. Class docstring: "Phase 5 — replaces the Phase 4.7 `Annotated[...]` discriminated-union alias with a real ABC base class. The five concrete subclasses keep their `type: Literal[...]` discriminator fields for SSE wire compatibility (see RESEARCH OQ-01)."
    2. Five concrete subclasses: each declares only its own non-inherited fields. `ContentEvent` and `ThinkingEvent` declare `chunk: str = ""`. `ToolCallEvent` declares `tool_name: str` and `tool_args: dict[str, Any]`. `ToolResultEvent` declares `tool_name: str`, `tool_result: str`, `elapsed_ms: int`. `ErrorEvent` declares `error_code: ErrorCode`, `message: str`, `retryable: bool`, `tool_name: str | None = None`, `raw_detail: str | None = None`. Each subclass declares `type: Literal["..."] = "..."` to narrow the base.
    3. DELETE the `StreamEvent = Annotated[ContentEvent | ThinkingEvent | ToolCallEvent | ToolResultEvent | ErrorEvent, Field(discriminator="type")]` line entirely. Code that needs the discriminated union for `TypeAdapter` validation can build it inline: `TypeAdapter(Annotated[ContentEvent | ThinkingEvent | ToolCallEvent | ToolResultEvent | ErrorEvent, Field(discriminator="type")])`. Audit for any uses of the alias in `app/`: `routes.py` uses `event.model_dump_json()` (no alias dependency); `service.py` only constructs concrete subclasses (no alias dependency). No alias-typed function signatures should remain.
    4. The DTOs below the alias (`SessionCreateRequest`, `ChatSessionInfo`, `RetryRequest`, `ProviderInfo`, etc.) are NOT touched.
    5. `app/chat/__init__.py` re-exports already include `StreamEvent` and the 5 subclasses — no change needed there beyond Task 2's additions.

    Edit `backend/app/config.py`: add `openai_o_series_model_prefixes: tuple[str, ...] = ("o1", "o3")` to `Settings` (with an inline comment "PydanticAI dispatch knob: models matching these prefixes use OpenAIResponsesModel for reasoning support — D-14, RESEARCH OQ-03"). Remove the `ollama_reasoning_model_prefixes` field entirely (PydanticAI handles `<think>` tags natively per OQ-04 — no longer threaded into `OllamaProvider`). Mirror the Phase 4.5 comment style.

    Edit `backend/app/llm/factory.py` IN THE SAME TASK to keep the factory importable mid-wave: locate the `OllamaProvider(...)` construction call (around line 97) and DROP the `reasoning_model_prefixes=self._settings.ollama_reasoning_model_prefixes,` keyword argument. Leave the rest of the factory body untouched — Wave 2 / Plan 05-03 Task 3 still owns the broader `OllamaProvider.__init__` rewrite + the `o_series_prefixes` threading + the `from app.llm.base import LLMProvider` swap. The single-line drop here closes the A2 mid-state breakage where `Settings` no longer has the attribute the factory tries to read.

    Run the Wave 0 wire-compat golden file to confirm byte-equivalence: it must remain green. RESEARCH OQ-01 confirms `BaseModel + ABC + Field(discriminator='type')` produces identical JSON for the same field set — but the field set has subtly changed (`session_id` is now inherited, not declared on each subclass). Verify field ORDER in `model_dump_json()` output: Pydantic v2 emits fields in MRO order (base fields first, then subclass fields). The Phase 4.7 order was `{"type": ..., "session_id": ..., <subclass_fields>}` because `type` and `session_id` were the first two fields declared on each subclass. With the ABC refactor, the order becomes `{"type": ..., "session_id": ..., <subclass_fields>}` because `type` is first declared on the base (then narrowed by the subclass) and `session_id` is second on the base. The order matches — verify by running the golden test.

    If the field order DOES diverge, fix by declaring `type: str` on the base AFTER `session_id`, OR by overriding `model_config = ConfigDict(...)` to enforce ordering. Prefer the first option: declare base fields as `session_id: str` first, `type: str` second — but Phase 4.7's Literal discriminator was first per existing convention. Re-verify against the golden file and adjust whichever ordering keeps Wave 0's `test_stream_event_wire_compat.py` green.

    `/dignified-python` skill: required (ABC mixin discipline + Pydantic v2 idioms).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/chat/test_stream_event_abc.py tests/unit/chat/test_stream_event_wire_compat.py tests/unit/test_stream_events.py -v && (grep -rEn "ollama_reasoning_model_prefixes" backend/app && echo "FAIL: lingering Settings reference" && exit 1 || echo "OK: Settings field cleanly removed") && cd backend && uv run python -c "from app.config import Settings; assert not hasattr(Settings(), 'ollama_reasoning_model_prefixes'); print('OK')"</automated>
  </verify>
  <done>StreamEvent is a real ABC base class with five concrete subclasses; `inspect.isabstract` returns True; the Wave 0 wire-compat golden file passes (byte-equivalent serialization); the existing `test_stream_events.py` discriminator round-trip still passes (TypeAdapter construction inline). `Settings.openai_o_series_model_prefixes` is present; `Settings.ollama_reasoning_model_prefixes` is gone; `backend/app/llm/factory.py` no longer references the deleted Setting (the `OllamaProvider(..., reasoning_model_prefixes=...)` kwarg has been dropped in the same task — closes A2 mid-state breakage).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ChatService → ConversationStore | Untrusted session_id strings cross here (from authenticated user input). |
| StreamEvent.model_dump_json → SSE wire | Server-controlled output; no untrusted input rendered as JSON. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-01 | Tampering | `InMemoryConversationStore.append` could be passed crafted session_ids | mitigate | session_ids are server-issued UUIDs (`ChatService.create_session` uses `uuid.uuid4()`); store accepts arbitrary strings but key collisions only affect the colliding user's session — accepted because Phase 6 PG impl uses FK constraints. |
| T-05.2-02 | Information Disclosure | StreamEvent leaks user data to other sessions | mitigate | `session_id` field is per-event, set by ChatService; the wire format is byte-equivalent to Phase 4.7 (verified by Wave 0 golden file) so existing isolation guarantees carry forward. |
| T-05.2-SC | Tampering | npm/pip install gates | accept | This plan adds zero new packages — `pydantic_ai` import resolves against the lockfile entry that Wave 4 adds. Wave 4 owns the legitimacy gate. |
</threat_model>

<verification>
- All Wave 0 tests created in plan 05-01 that target THIS plan's modules turn green: `test_protocol_abc.py`, `test_deps.py`, `test_conversation_store.py`, `test_stream_event_abc.py`, `test_stream_event_wire_compat.py`.
- `cd backend && uv run python -c "from app.llm.base import LLMProvider; from app.chat.deps import ChatDeps; from app.chat.store import ConversationStore, InMemoryConversationStore; from app.chat.models import StreamEvent, ContentEvent; import inspect; print(inspect.isabstract(LLMProvider), inspect.isabstract(ConversationStore), inspect.isabstract(StreamEvent))"` outputs `True True True`.
- Existing test `tests/unit/test_stream_events.py` continues to pass (Phase 4.7 discriminator semantics preserved via inline `TypeAdapter(Annotated[...])` construction).
</verification>

<success_criteria>
ROADMAP.md Phase 5 success criteria advanced by this plan:
- SC-2 (StreamEvent ABC + wire compat) — DONE structurally; the production producer (`ChatService.chat_stream`) still emits via the legacy LangChain path until Wave 3.
- SC-3 (LLMProvider/BoundProvider → ABC) — DONE for the abstract surface; `app/llm/protocol.py` deleted in this same task; concrete providers will subclass `LLMProvider` explicitly in Wave 2 (currently they import from `app.llm.base` but their `bind_tools` body is still LangChain-shaped).

Plan-level criteria:
- `app/llm/base.py`, `app/chat/deps.py`, `app/chat/store.py` exist.
- `app/chat/models.py` has the `class StreamEvent(BaseModel, ABC):` declaration; the `Annotated[...]` alias is deleted.
- `Settings.openai_o_series_model_prefixes` is added; `Settings.ollama_reasoning_model_prefixes` is removed.
- All Wave 0 tests targeting these modules are green.
- `mypy --strict` passes for the four new/edited foundational files (provider modules will fail until Wave 2 — that is expected and tracked).
</success_criteria>

<output>
Create `.planning/phases/05-pydanticai-migration/05-02-SUMMARY.md` documenting:
- The exact line ranges deleted from `app/chat/models.py` (the alias) and `app/config.py` (the removed Setting).
- Confirmation of byte-equivalent wire format via the Wave 0 golden file output.
- Confirmation that `backend/app/llm/protocol.py` was deleted in this same task (not deferred to Wave 2) and that the four provider files now import from `app.llm.base`.
</output>
