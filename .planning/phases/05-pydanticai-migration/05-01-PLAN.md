---
plan_id: 05-01
phase: 05-pydanticai-migration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/unit/llm/test_protocol_abc.py
  - backend/tests/unit/llm/test_build_agent.py
  - backend/tests/unit/llm/providers/__init__.py
  - backend/tests/unit/llm/providers/test_openai_dispatch.py
  - backend/tests/unit/llm/providers/test_ollama_thinking.py
  - backend/tests/unit/chat/__init__.py
  - backend/tests/unit/chat/test_deps.py
  - backend/tests/unit/chat/test_conversation_store.py
  - backend/tests/unit/chat/test_stream_event_abc.py
  - backend/tests/unit/chat/test_stream_event_wire_compat.py
  - backend/tests/unit/chat/test_stream_event_extraction.py
  - backend/tests/unit/chat/test_stream_error_event.py
  - backend/tests/unit/tools/__init__.py
  - backend/tests/unit/tools/test_flight_search_no_backdoor.py
  - backend/tests/unit/test_dependencies.py
  - backend/tests/unit/test_no_langchain_imports.py
  - backend/tests/integration/llm/__init__.py
  - backend/tests/integration/llm/test_ollama_thinking_live.py
  - backend/tests/integration/llm/test_openai_thinking_live.py
  - backend/tests/integration/llm/test_anthropic_thinking_live.py
autonomous: true
requirements:
  - REQ-pydantic-ai-migration
  - REQ-p5-stream-event-abc

must_haves:
  truths:
    - "Every Wave 0 test file collects cleanly via `pytest --collect-only` even though tests will fail until later waves implement production code."
    - "Phase 4.7 SSE wire bytes are captured into a golden fixture BEFORE the StreamEvent ABC refactor lands, so wire backward-compatibility can be asserted later."
    - "Anti-pattern lock tests exist for both the `_flight_client` back-door (D-06) and the `BoundProvider` two-tier shape (D-01..D-03)."
  artifacts:
    - path: "backend/tests/unit/llm/test_protocol_abc.py"
      provides: "ABC conformance + BoundProvider absence assertion"
    - path: "backend/tests/unit/chat/test_stream_event_wire_compat.py"
      provides: "Phase 4.7 → Phase 5 byte-equivalent wire-format golden file"
    - path: "backend/tests/unit/tools/test_flight_search_no_backdoor.py"
      provides: "Anti-pattern regression guard (D-06)"
    - path: "backend/tests/unit/test_dependencies.py"
      provides: "pyproject manifest assertion for D-20"
  key_links:
    - from: "Wave 0 test stubs"
      to: "Wave 1..4 implementation tasks"
      via: "RED tests turn green as production code lands"
      pattern: "pytest --collect-only succeeds; pytest -x fails on missing imports until prod lands"
---

<objective>
Create the failing test scaffolds (RED stubs) and golden-file fixtures that drive every later wave of Phase 5. Per VALIDATION.md "Wave 0 Requirements" (lines 79-97), these must exist before any production rewrite begins so that:

1. The Phase 4.7 SSE wire format is captured byte-for-byte BEFORE the `StreamEvent` ABC refactor lands (D-15 backward-compat).
2. Anti-pattern locks (`_flight_client` back-door, `BoundProvider` two-tier) have regression tests in place.
3. Every per-task verification command in VALIDATION.md (rows referencing `❌ W0`) has its target file in existence.

Purpose: Prevent silent scope drift during the LangChain → PydanticAI rewrite. Tests fail loudly until each subsequent wave fills them in.
Output: 18 new test files (9 unit, 4 integration, 5 init/scaffolding) under `backend/tests/`.
</objective>

<execution_context>
@/Users/axel/code/trip_planner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/trip_planner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-pydanticai-migration/05-CONTEXT.md
@.planning/phases/05-pydanticai-migration/05-RESEARCH.md
@.planning/phases/05-pydanticai-migration/05-PATTERNS.md
@.planning/phases/05-pydanticai-migration/05-VALIDATION.md
@./CLAUDE.md
@backend/app/chat/models.py
@backend/app/llm/protocol.py
@backend/tests/unit/test_stream_events.py
@backend/tests/unit/llm/test_protocol_conformance.py
@backend/tests/unit/test_chat_stream.py
@backend/tests/unit/test_user_repository.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture Phase 4.7 SSE wire-format golden bytes BEFORE any refactor</name>
  <files>
    backend/tests/unit/chat/__init__.py,
    backend/tests/unit/chat/test_stream_event_wire_compat.py
  </files>
  <read_first>
    - backend/app/chat/models.py — current Phase 4.7 `StreamEvent` discriminated-union alias + 5 concrete event classes (lines 42-102 per PATTERNS.md)
    - backend/tests/unit/test_stream_events.py:51-73 — current `model_dump_json()` discriminator-string assertions (PATTERNS.md analog)
    - .planning/phases/05-pydanticai-migration/05-PATTERNS.md § "backend/tests/unit/chat/test_stream_event_wire_compat.py" — target shape
    - .planning/phases/05-pydanticai-migration/05-CONTEXT.md D-15 — "Wire format unchanged. `model_dump_json()` produces identical output"
    - .planning/phases/05-pydanticai-migration/05-VALIDATION.md row "SSE wire format (`model_dump_json()`) byte-equivalent to Phase 4.7"
    - .planning/phases/05-pydanticai-migration/05-RESEARCH.md OQ-01 resolution — confirms `BaseModel + ABC + discriminator` round-trip is byte-equivalent
  </read_first>
  <action>Create empty `backend/tests/unit/chat/__init__.py` (package marker). Create `backend/tests/unit/chat/test_stream_event_wire_compat.py` with one test per concrete event subclass: `test_content_event_wire_unchanged`, `test_thinking_event_wire_unchanged`, `test_tool_call_event_wire_unchanged`, `test_tool_result_event_wire_unchanged`, `test_error_event_wire_unchanged`. Each test instantiates the Phase 4.7 event with deterministic field values (e.g. `ContentEvent(chunk="hi", session_id="s1")`), calls `event.model_dump_json()`, and asserts the result equals the captured golden string. Capture the golden strings by importing the current production classes from `app.chat.models` and recording the exact bytes — this run executes BEFORE the StreamEvent ABC refactor lands in Wave 1, so the bytes ARE the Phase 4.7 reference. Use `ErrorCode.tool_error` from `app.chat.models` (existing StrEnum). Tests must pass against current Phase 4.7 production code on Wave 0 commit.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/chat/test_stream_event_wire_compat.py -v</automated>
  </verify>
  <done>All 5 wire-compat tests pass against current Phase 4.7 `app.chat.models`. The golden strings are committed verbatim. The file will continue to pass after Wave 1's StreamEvent ABC refactor (per RESEARCH OQ-01 byte-equivalence proof).</done>
</task>

<task type="auto">
  <name>Task 2: Create chat-package Wave 0 test stubs (deps, store, ABC, extraction, error)</name>
  <files>
    backend/tests/unit/chat/test_deps.py,
    backend/tests/unit/chat/test_conversation_store.py,
    backend/tests/unit/chat/test_stream_event_abc.py,
    backend/tests/unit/chat/test_stream_event_extraction.py,
    backend/tests/unit/chat/test_stream_error_event.py
  </files>
  <read_first>
    - .planning/phases/05-pydanticai-migration/05-PATTERNS.md § "backend/tests/unit/chat/test_*" rows — target shape per file
    - .planning/phases/05-pydanticai-migration/05-CONTEXT.md D-05 (ChatDeps), D-08, D-11 (ConversationStore), D-15, D-16 (StreamEvent ABC), D-12 (extraction)
    - .planning/phases/05-pydanticai-migration/05-RESEARCH.md § "ConversationStore ABC" + § "StreamEvent ABC Refactor" + § "Stream Event Type Map"
    - .planning/phases/05-pydanticai-migration/05-VALIDATION.md rows: "ChatDeps frozen dataclass", "ConversationStore ABC + InMemory round-trip", "StreamEvent(BaseModel, ABC) + Field(discriminator='type')", "agent.iter() event loop maps", "Exception in stream → ErrorEvent (with `_scrub`)"
    - backend/tests/unit/test_user_repository.py — UserRepository unit-test shape (analog for ConversationStore)
    - backend/tests/unit/test_stream_events.py:138-159 — TypeAdapter discriminator round-trip (analog for ABC golden test)
    - backend/tests/unit/test_chat_stream.py:18-36 + :38-60 — extraction + error-event analogs
  </read_first>
  <action>
    Create five RED test files, each importing from modules that do not yet exist (Wave 1 will create them). Tests target named pytest functions referenced in VALIDATION.md.

    `test_deps.py`: imports `from app.chat.deps import ChatDeps`. Tests: `test_chatdeps_is_frozen_dataclass` (uses `dataclasses.is_dataclass(ChatDeps)` and `ChatDeps.__dataclass_params__.frozen is True`); `test_chatdeps_has_required_fields` (asserts the three field names `flight_client`, `session_id`, `user_id` per D-05); `test_chatdeps_field_types` (checks annotations include `FlightAPIClient`, `str`, `str`).

    `test_conversation_store.py`: imports `from app.chat.store import ConversationStore, InMemoryConversationStore`. Tests: `test_conversation_store_is_abc` (uses `inspect.isabstract(ConversationStore) is True`); `test_inmemory_round_trip_append_and_load`; `test_load_empty_returns_empty_list`; `test_delete_removes_session`; `test_list_for_user_returns_session_ids`. Storage type assertion: `await store.load(...)` returns `list[ModelMessage]` (import `from pydantic_ai.messages import ModelMessage`). Each test is `async def` with `pytest_asyncio` (project uses `asyncio_mode = "auto"`).

    `test_stream_event_abc.py`: imports `from app.chat.models import StreamEvent, ContentEvent, ThinkingEvent, ToolCallEvent, ToolResultEvent, ErrorEvent`. Tests: `test_streamevent_is_abc` (`inspect.isabstract` semantics — RESEARCH OQ-01 confirms ABC marker without abstract methods); `test_concrete_event_isinstance_streamevent_abc` (asserts `isinstance(ContentEvent(chunk="hi", session_id="s"), StreamEvent)` for all 5 subclasses); `test_typeadapter_round_trip` (asserts `TypeAdapter(StreamEvent).validate_python({"type":"error",...})` returns `ErrorEvent`); `test_streamevent_is_not_a_protocol` (asserts `not isinstance(StreamEvent, type(typing.Protocol))`).

    `test_stream_event_extraction.py`: imports `from app.chat.service import ChatService` and uses `make_chat_service_with_mock_llm` from `tests/fixtures/llm.py`. Four named tests per VALIDATION rows: `test_thinking` (Thinking chunk → ThinkingEvent), `test_text` (Content chunk → ContentEvent), `test_tool_call` (ToolCall chunk → ToolCallEvent), `test_tool_result` (the corresponding `FunctionToolResultEvent` → ToolResultEvent). All four assert event types via `events = [e async for e in service.chat_stream(...)]` and `[e.type for e in events]` membership.

    `test_stream_error_event.py`: imports `from app.chat.service import ChatService`. One test `test_chat_stream_emits_error_event_on_exception` builds a `_MockLLMProvider` whose `stream_function` raises `RuntimeError("test failure 'sk-secret'")`, asserts the resulting `ErrorEvent.raw_detail` does NOT contain `sk-secret` (i.e., `_scrub` from `app.llm.log_scrubbing` ran). Reference Phase 4.7 contract from PATTERNS.md `chat/service.py § Delta — preserved invariants`.

    Each file MUST collect cleanly under `pytest --collect-only`. Tests will fail (red) until Wave 1+ provide the imported modules.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/chat/test_deps.py tests/unit/chat/test_conversation_store.py tests/unit/chat/test_stream_event_abc.py tests/unit/chat/test_stream_event_extraction.py tests/unit/chat/test_stream_error_event.py --collect-only -q</automated>
  </verify>
  <done>All five files compile and collect cleanly under `pytest --collect-only`. Each file's `pytest -v` run fails (red) on missing modules — that's the expected Wave 0 state.</done>
</task>

<task type="auto">
  <name>Task 3: Create LLM-package Wave 0 test stubs (ABC, build_agent, dispatch, thinking)</name>
  <files>
    backend/tests/unit/llm/test_protocol_abc.py,
    backend/tests/unit/llm/test_build_agent.py,
    backend/tests/unit/llm/providers/__init__.py,
    backend/tests/unit/llm/providers/test_openai_dispatch.py,
    backend/tests/unit/llm/providers/test_ollama_thinking.py
  </files>
  <read_first>
    - .planning/phases/05-pydanticai-migration/05-PATTERNS.md § "backend/tests/unit/llm/test_protocol_abc.py", § "test_build_agent.py", § "providers/test_openai_dispatch.py" — target shapes
    - .planning/phases/05-pydanticai-migration/05-CONTEXT.md D-01..D-03 (single ABC, no BoundProvider), D-14 (o-series dispatch), D-12 + RESEARCH OQ-04 (qwen3 thinking)
    - .planning/phases/05-pydanticai-migration/05-RESEARCH.md § "OpenAI Provider" + § "Per-Provider Migration Rules / Ollama Provider"
    - .planning/phases/05-pydanticai-migration/05-VALIDATION.md rows: "LLMProvider(ABC) — single tier, no BoundProvider", "provider.build_agent(...) constructs Agent[ChatDeps, str]", "OpenAIResponsesModel dispatch for o-series", "qwen3 `<think>` tags parse natively into ThinkingPart"
    - backend/tests/unit/llm/test_protocol_conformance.py — file being renamed; analog for ABC conformance assertions
    - backend/tests/unit/llm/test_ollama_provider.py:107-134 — analog for per-provider unit test style
  </read_first>
  <action>
    Create empty `backend/tests/unit/llm/providers/__init__.py` (package marker). Then create 4 RED test files.

    `test_protocol_abc.py`: imports `from app.llm.base import LLMProvider`. Tests: `test_llm_provider_is_abc` (`inspect.isabstract(LLMProvider) is True`); `test_llm_provider_is_not_protocol` (`not isinstance(LLMProvider, type(typing.Protocol))`); `test_ollama_provider_subclasses_llm_provider`, `test_openai_provider_subclasses_llm_provider`, `test_anthropic_provider_subclasses_llm_provider`, `test_lmstudio_provider_subclasses_llm_provider` — each `issubclass(<Provider>, LLMProvider)` per D-03; `test_bound_provider_is_removed_from_module` (`import app.llm.base; assert not hasattr(app.llm.base, "BoundProvider")` per CONTEXT.md anti-pattern lock).

    `test_build_agent.py`: imports `from pydantic_ai import Agent`, `from app.chat.deps import ChatDeps`, `from app.tools.flight_search import search_flights`, and the four concrete provider classes from `app.llm.providers.*`. One test per provider: `test_<provider>_build_agent_returns_pydantic_ai_agent` — instantiates the provider with deterministic args (no live network), calls `provider.build_agent(tools=[search_flights], deps_type=ChatDeps)`, asserts `isinstance(agent, Agent)`.

    `test_openai_dispatch.py`: imports `from pydantic_ai.models.openai import OpenAIChatModel, OpenAIResponsesModel`, `from app.llm.providers.openai import OpenAIProvider`, `from app.chat.deps import ChatDeps`. Tests per D-14: `test_o3_mini_routes_to_openai_responses_model` (model name `"o3-mini"`); `test_o1_mini_routes_to_openai_responses_model` (model name `"o1-mini"`); `test_gpt_4o_mini_routes_to_openai_chat_model` (model name `"gpt-4o-mini"`). Each: `provider = OpenAIProvider(model=..., api_key="sk-test")`; `agent = provider.build_agent(tools=[], deps_type=ChatDeps)`; `assert isinstance(agent.model, OpenAIResponsesModel)` or `OpenAIChatModel` per case.

    `test_ollama_thinking.py`: imports `from pydantic_ai.models.openai import OpenAIChatModel`, `from pydantic_ai.providers.ollama import OllamaProvider as PaiOllamaProvider`, `from app.llm.providers.ollama import OllamaProvider`. One test `test_qwen3_model_profile_includes_default_thinking_tags`: instantiates `OllamaProvider(model="qwen3:4b", base_url="http://x", probe_timeout_seconds=1.0)`, calls `build_agent(tools=[], deps_type=ChatDeps)`, asserts the resulting `agent.model._model_profile.thinking_tags == ('<think>', '</think>')` per RESEARCH OQ-04 (PydanticAI inherits from `qwen_model_profile`). Use `getattr(agent.model, '_model_profile', None)` defensively if the attribute is private; fallback assertion is `agent.model.profile.thinking_tags == ('<think>', '</think>')` — the test is allowed to read whichever attribute PydanticAI 0.8.1 exposes.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/llm/test_protocol_abc.py tests/unit/llm/test_build_agent.py tests/unit/llm/providers/test_openai_dispatch.py tests/unit/llm/providers/test_ollama_thinking.py --collect-only -q</automated>
  </verify>
  <done>All four files collect cleanly. `pytest -v` will fail until `app.llm.base`, `app.chat.deps`, and the rewritten provider modules exist (Wave 1 + Wave 2). The `test_protocol_conformance.py` file is NOT touched here — it is renamed to `test_protocol_abc.py` in Wave 1 once `LLMProvider` becomes an ABC.</done>
</task>

<task type="auto">
  <name>Task 4: Create tools/test stubs + pyproject + import-scan assertions + integration scenarios</name>
  <files>
    backend/tests/unit/tools/__init__.py,
    backend/tests/unit/tools/test_flight_search_no_backdoor.py,
    backend/tests/unit/test_dependencies.py,
    backend/tests/unit/test_no_langchain_imports.py,
    backend/tests/integration/llm/__init__.py,
    backend/tests/integration/llm/test_ollama_thinking_live.py,
    backend/tests/integration/llm/test_openai_thinking_live.py,
    backend/tests/integration/llm/test_anthropic_thinking_live.py
  </files>
  <read_first>
    - .planning/phases/05-pydanticai-migration/05-PATTERNS.md § "test_flight_search_no_backdoor.py" (anti-pattern lock), § "test_dependencies.py", § "test_no_langchain_imports.py", § "test_{ollama,openai,anthropic}_thinking_live.py"
    - .planning/phases/05-pydanticai-migration/05-CONTEXT.md anti-pattern locks: "Don't keep `search_flights._flight_client` 'just in case'", D-13 (gated acceptance), D-20 (dep swap)
    - .planning/phases/05-pydanticai-migration/05-RESEARCH.md § "Common Pitfalls" Pitfall 9 — `langgraph` still in pyproject.toml despite PR #1
    - backend/tests/integration/test_cloud_providers_real.py — Phase 4.5 gated-cloud-key skipif pattern (analog for D-13)
    - backend/pyproject.toml — current deps list (lines 7-22)
    - backend/app/tools/flight_search.py — current `@tool` decorator + `_flight_client` back-door site
  </read_first>
  <action>
    Create empty `backend/tests/unit/tools/__init__.py` and `backend/tests/integration/llm/__init__.py` (package markers).

    `test_flight_search_no_backdoor.py`: imports `from app.tools.flight_search import search_flights`. Tests: `test_search_flights_has_no_flight_client_attribute` (asserts `not hasattr(search_flights, "_flight_client")` per D-06 anti-pattern lock); `test_search_flights_first_param_is_runcontext` — uses `inspect.signature(search_flights)`, takes the first parameter, asserts `first_param.name == "ctx"` and the annotation contains `RunContext` (string match `"RunContext"` in `str(first_param.annotation)` for resilience to PEP 604 vs explicit imports).

    `test_dependencies.py`: imports `tomllib` (stdlib in Py 3.11+) and `pathlib`. Tests: `test_no_langchain_dependencies` — reads `Path(__file__).parent.parent.parent / "pyproject.toml"`, parses with `tomllib.loads(...)`, walks `manifest["project"]["dependencies"]`, splits each entry on `>=`/`==`/`~=`/`<` to get the bare package name (use a small helper or regex `re.split(r"[<>=~!]", dep, 1)[0].strip()`), asserts no name starts with `"langchain"` and no name equals `"langgraph"` (RESEARCH Pitfall 9). `test_pydantic_ai_present`: asserts at least one entry starts with `"pydantic-ai"`. `test_pydantic_floor_at_least_2_12`: asserts the `pydantic` entry version specifier contains `>=2.12` (RESEARCH Pitfall 8). Use a `# type: ignore[union-attr]` on tomllib only if mypy strict complains.

    `test_no_langchain_imports.py`: imports `ast`, `pathlib`. One test `test_no_langchain_imports_in_app_tree`: walks `Path("backend/app").rglob("*.py")` (resolve absolute path from `__file__` parent — three levels up), parses each file with `ast.parse`, walks the AST collecting `ast.ImportFrom` nodes whose `node.module` starts with `"langchain"` and `ast.Import` aliases starting with `"langchain"`. Asserts the collected list is empty with a helpful failure message naming each offender.

    Three gated-acceptance integration files. Each follows the Phase 4.5 `@pytest.mark.skipif` pattern from `test_cloud_providers_real.py`:
      - `test_ollama_thinking_live.py`: `pytest.mark.skipif(not os.getenv("OLLAMA_BASE_URL"), reason="OLLAMA_BASE_URL not set")` — one test that builds an `OllamaProvider(model="qwen3:4b", base_url=os.environ["OLLAMA_BASE_URL"], probe_timeout_seconds=5.0)`, builds an agent, runs `agent.iter("Hello, think briefly before responding.", deps=ChatDeps(...))`, collects `PartDeltaEvent`s, asserts at least one has `delta=ThinkingPartDelta(...)` per D-13.
      - `test_openai_thinking_live.py`: skipif `OPENAI_API_KEY` unset — model `"o3-mini"`, asserts at least one ThinkingPart-derived event in stream.
      - `test_anthropic_thinking_live.py`: skipif `ANTHROPIC_API_KEY` unset — model `"claude-3-7-sonnet-latest"` (or whichever extended-thinking model is current), asserts thinking events.

    All gated tests are SKELETONS — they may be marked `@pytest.mark.skip(reason="awaiting Wave 2 provider rewrite")` if the provider classes do not exist yet, but the file MUST collect.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/tools/test_flight_search_no_backdoor.py tests/unit/test_dependencies.py tests/unit/test_no_langchain_imports.py tests/integration/llm/ --collect-only -q</automated>
  </verify>
  <done>All eight files collect cleanly. `test_dependencies.py` and `test_no_langchain_imports.py` will FAIL on first run because `pyproject.toml` still has `langchain*` and `langgraph` and `app/` still imports them — this is the expected red state. They turn green in Wave 4 when the dep swap completes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → file system | Tests read `pyproject.toml` and AST-walk `app/`. Trusted (CI-only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-01 | Tampering | Wave 0 test scaffolds bypassed by future plans | mitigate | VALIDATION.md sampling-density rule enforced by plan-checker; this plan's tests are referenced by every later wave's `<verify>` block. |
| T-05.1-SC | Tampering | npm/pip/cargo installs | accept | This plan adds zero new packages — no install task. Package legitimacy gate not triggered. |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/unit/chat tests/unit/llm tests/unit/tools tests/integration/llm tests/unit/test_dependencies.py tests/unit/test_no_langchain_imports.py --collect-only -q` collects all 18 test functions across the new files.
- `cd backend && uv run pytest tests/unit/chat/test_stream_event_wire_compat.py -v` passes (Phase 4.7 wire format captured).
- Git status shows 18 new files; zero modifications to production code.
</verification>

<success_criteria>
The following ROADMAP.md success-criterion lines are advanced (not yet completed) by this plan:
- Phase 5 SC-1 (ChatService orchestrates a PydanticAI Agent per session) — test scaffold exists for `_agents` lifecycle assertions.
- Phase 5 SC-2 (StreamEvent ABC + wire compat) — golden file captures Phase 4.7 reference bytes.
- Phase 5 SC-3 (LLMProvider/BoundProvider → ABC) — RED conformance + BoundProvider-absent tests.
- Phase 5 SC-4 (langchain* removed; pydantic-ai added) — `test_dependencies.py` is the canary.
- Phase 5 SC-5 (MockLLMStream drives PydanticAI surface) — extraction tests reference the rewritten fixture API.

Plan-level criteria:
- 18 new files exist.
- All files collect under `pytest --collect-only`.
- The wire-compat test passes against current Phase 4.7 production code (golden bytes recorded).
- No production code is modified — this is purely Wave 0 scaffolding.
</success_criteria>

<output>
Create `.planning/phases/05-pydanticai-migration/05-01-SUMMARY.md` documenting:
- Number of test files created and their pytest node IDs.
- The exact captured Phase 4.7 wire-format golden bytes (5 strings) so they can be recovered if the file is later rewritten.
- The set of test files that are red after this plan (everything except `test_stream_event_wire_compat.py`).
</output>
