---
plan_id: 05-06
phase: 05-pydanticai-migration
plan: 06
type: execute
wave: 5
depends_on: ["05-05"]
files_modified:
  - .planning/adrs/ADR-001-langchain.md
  - .planning/adrs/ADR-007-pydantic-ai.md
  - ARCHITECTURE.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - REQ-pydantic-ai-migration

must_haves:
  truths:
    - "`.planning/adrs/ADR-001-langchain.md` has `Status: Superseded` in its header and a supersession note pointing to ADR-007 (D-21)."
    - "`.planning/adrs/ADR-007-pydantic-ai.md` has `Status: Locked` in its header and cites RESEARCH OQ-01..OQ-05 verifications."
    - "`ARCHITECTURE.md` is updated: the LLMProvider Factory Pattern section reflects ABC + `build_agent`; the StreamEvent section reflects ABC base class; the Known Tech Debt entry for Protocol-vs-ABC is DELETED; the LangChain integration section is replaced with a PydanticAI integration section; an Anti-Pattern Closures (Phase 5) subsection lists `_flight_client` back-door + Protocol-vs-ABC."
    - "`.planning/PROJECT.md` Key Decisions table marks ADR-001 Superseded and ADR-007 Locked, with Phase 5 success-criteria checkboxes ticked."
  artifacts:
    - path: ".planning/adrs/ADR-001-langchain.md"
      provides: "Status: Superseded ADR with supersession note"
    - path: ".planning/adrs/ADR-007-pydantic-ai.md"
      provides: "Status: Locked ADR with RESEARCH-backed rationale"
    - path: "ARCHITECTURE.md"
      provides: "Documentation aligned with Phase 5 architecture"
    - path: ".planning/PROJECT.md"
      provides: "Updated Key Decisions table + ticked Phase 5 success criteria"
  key_links:
    - from: ".planning/adrs/ADR-001-langchain.md"
      to: ".planning/adrs/ADR-007-pydantic-ai.md"
      via: "supersession note in ADR-001"
      pattern: "Superseded by ADR-007"
---

<objective>
Close the documentation half of Phase 5: flip ADR-001 to `Superseded`, flip ADR-007 to `Locked`, rewrite the relevant `ARCHITECTURE.md` sections to reflect the new architecture, and update `.planning/PROJECT.md`'s Key Decisions table.

This is editorial-only — zero code changes, zero behavioral changes. Every assertion in the docs is now backed by code that landed in waves 1-4 and passed `just check && just test` in plan 05-05.

Per RESEARCH § Confidence: ADR-007's rationale is HIGH-confidence (all five Open Questions verified against installed pydantic-ai 0.8.1 source). The locked ADR cites those verifications so future readers know the architectural choices are not speculative.

Purpose: Close the ARCHITECTURE.md "Known Tech Debt" entry and the two anti-pattern entries (`_flight_client` back-door + Protocol-vs-ABC) that have been carried since Phase 4.5.
Output: 4 in-place edits across docs.
</objective>

<execution_context>
@/Users/axel/code/trip_planner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/trip_planner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/phases/05-pydanticai-migration/05-CONTEXT.md
@.planning/phases/05-pydanticai-migration/05-RESEARCH.md
@.planning/phases/05-pydanticai-migration/05-PATTERNS.md
@./CLAUDE.md
@ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip ADR-001 → Superseded; flip ADR-007 → Locked</name>
  <files>
    .planning/adrs/ADR-001-langchain.md,
    .planning/adrs/ADR-007-pydantic-ai.md
  </files>
  <read_first>
    - .planning/adrs/ADR-001-langchain.md (or ARCHITECTURE.md:561-583 if the standalone file doesn't yet exist — see PATTERNS.md note "The file may not exist yet; if so, create it from the ARCHITECTURE.md prose plus a 'Superseded by ADR-007' note")
    - .planning/adrs/ADR-007-pydantic-ai.md (current `Status: Proposed` per CONTEXT.md D-21)
    - .planning/phases/05-pydanticai-migration/05-PATTERNS.md § ".planning/adrs/ADR-001-langchain.md" + § ".planning/adrs/ADR-007-pydantic-ai.md"
    - .planning/phases/05-pydanticai-migration/05-CONTEXT.md D-21 — "ADR-001 transitions Locked → Superseded; ADR-007 (PydanticAI) becomes Locked"
    - .planning/phases/05-pydanticai-migration/05-RESEARCH.md OQ-01..OQ-05 — the verifications ADR-007 cites
    - ARCHITECTURE.md lines 561-583 — current ADR-001 prose; lift verbatim into the standalone file if it doesn't exist
    - ARCHITECTURE.md lines 585-607 — ADR-002 status-flip example; use as editorial template for ADR-007
  </read_first>
  <action>
    Edit `.planning/adrs/ADR-001-langchain.md`. If the file does not exist, create it by lifting the ADR-001 prose verbatim from `ARCHITECTURE.md:561-583` into a new file with this header structure:

    ```
    # ADR-001: LangChain 1.0 with bind_tools() Pattern

    **Date**: 2025-11-10
    **Status**: Superseded by ADR-007 (Phase 5, 2026-06-03)
    ```

    Below the header, preserve the original Context / Decision / Consequences sections verbatim. Append a new section at the bottom titled `## Supersession Note` containing:
    - One paragraph summarizing why LangChain was superseded (lighter, easier to test, single-tier provider abstraction, native tool DI via RunContext, native `<think>` parsing — quote the rationale from CONTEXT.md D-01..D-21).
    - A pointer to `.planning/phases/05-pydanticai-migration/05-CONTEXT.md` D-21 for the locked decision.
    - A pointer to `.planning/adrs/ADR-007-pydantic-ai.md` as the superseding ADR.
    - The migration date: 2026-06-03 (today, per the planning context).

    Edit (or create) `.planning/adrs/ADR-007-pydantic-ai.md`. The standalone file does not currently exist (verified at plan-checker time: `find .planning -name 'ADR-*'` returned empty). Mirror the ADR-001 create-if-missing handling: if `.planning/adrs/ADR-007-pydantic-ai.md` does not exist, CREATE it with the standard ADR header (Title, Status, Date, Context, Decision, Consequences, Supersedes) — drawing the Decision content from `.planning/phases/05-pydanticai-migration/05-CONTEXT.md` D-21 and the per-provider Consequences from `.planning/phases/05-pydanticai-migration/05-RESEARCH.md` § Per-Provider Migration Rules + § "Match block pattern for the agent.iter() streaming loop" + § StreamEvent ABC Refactor. If the file already exists, edit only the header `Status:` line to `Locked` and MERGE any missing sections — preserve rationale notes already present.

    Header structure (use exactly when creating from scratch):

    ```
    # ADR-007: PydanticAI Agent Pattern

    **Date**: 2026-06-03
    **Status**: Locked
    **Supersedes**: ADR-001 (LangChain 1.0 with bind_tools())
    ```

    Below the header, ensure the ADR contains:
    - Context: Why PydanticAI was chosen over LangChain (lighter, RunContext-based DI, single-tier `Agent` shape, native `<think>` parsing for Ollama, native ABC fit per CLAUDE.md).
    - Decision: Use `pydantic-ai>=0.8.1`; `LLMProvider(ABC)` with `build_agent(tools, deps_type) -> Agent[Deps, str]`; `RunContext[ChatDeps]` for tool DI; `agent.iter()` for chat-stream orchestration; `FunctionModel` for offline test mocking.
    - Verification: cite RESEARCH OQ-01 (Pydantic v2 ABC + discriminator), OQ-02 (PydanticAI stream event types), OQ-03 (`OpenAIResponsesModel` for o-series), OQ-04 (qwen3 native `<think>` parsing), OQ-05 (`ModelMessagesTypeAdapter` round-trip stability). Each verification has HIGH confidence per RESEARCH § Metadata.
    - Per-provider rules: refer to RESEARCH § "Per-Provider Migration Rules" verbatim (or summarize as a 4-row table).
    - Consequences: dependency swap (Wave 4), single-tier provider shape (Wave 1+2), back-door closure (Wave 3), test fixture rewrite (Wave 3).
    - Supersedes: ADR-001 (LangChain 1.0 with bind_tools()).
    - Date: 2026-06-03 (Phase 5 lock date — same as today).

    Use the editorial style of ADR-002's status-flip footer (ARCHITECTURE.md:585-607) as the template.
  </action>
  <verify>
    <automated>test -f .planning/adrs/ADR-001-langchain.md && test -f .planning/adrs/ADR-007-pydantic-ai.md && grep -q "Status: Superseded" .planning/adrs/ADR-001-langchain.md && grep -q "Status: Locked" .planning/adrs/ADR-007-pydantic-ai.md && grep -q "Superseded by ADR-007" .planning/adrs/ADR-001-langchain.md && grep -q "ADR-001" .planning/adrs/ADR-007-pydantic-ai.md && echo OK</automated>
  </verify>
  <done>Both ADR files exist on disk. Both have the correct status headers. ADR-001 contains a supersession note pointing to ADR-007. ADR-007 cites all five RESEARCH Open Questions and lists the per-provider migration rules.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ARCHITECTURE.md sections affected by Phase 5</name>
  <files>
    ARCHITECTURE.md
  </files>
  <read_first>
    - ARCHITECTURE.md — entire file (use line numbers from PATTERNS.md): lines 174-251 (LLMProvider Factory Pattern), lines 255-305 (Discriminated StreamEvent Union Pattern), lines 405-456 (LangChain 1.0 Integration), lines 561-583 (ADR-001), lines 585-607 (ADR-002 status-flip example), lines 783-795 (Known Tech Debt: Protocol-vs-ABC)
    - .planning/phases/05-pydanticai-migration/05-PATTERNS.md § "ARCHITECTURE.md" — the four-section update plan
    - .planning/phases/05-pydanticai-migration/05-CONTEXT.md D-03 ("closes the ARCHITECTURE.md 'Known Tech Debt' entry"); anti-patterns to close
    - .planning/phases/05-pydanticai-migration/05-RESEARCH.md § "Match block pattern for the agent.iter() streaming loop" — code shape to embed in the rewritten LangChain integration section
  </read_first>
  <action>
    Edit `ARCHITECTURE.md` with multiple targeted in-place edits. Use the Edit tool for each section — never overwrite the entire file.

    Section 1 — LLMProvider Factory Pattern (lines 174-251 per PATTERNS.md):
    - Change "`LLMProvider` Protocol with `bind_tools` returning `BoundProvider`" prose to "`LLMProvider` ABC with `build_agent(tools, deps_type)` returning `pydantic_ai.Agent[Deps, str]`".
    - Drop any text describing the two-tier shape; replace with "Single-tier ABC; PydanticAI's `Agent` IS the tool-bound thing."
    - Update the example code block (if present) to reference `app.llm.base.LLMProvider` and the four `build_agent`-style methods. Reference RESEARCH § Per-Provider Migration Rules for the four concrete provider behaviors.
    - Reference Phase 5 in a "(Phase 5 — replaces Phase 4.5 two-Protocol shape)" footer note.

    Section 2 — Discriminated StreamEvent Union Pattern (lines 255-305):
    - Replace the alias-based prose with "`StreamEvent(BaseModel, ABC)` base class with five concrete subclasses (`ContentEvent`, `ThinkingEvent`, `ToolCallEvent`, `ToolResultEvent`, `ErrorEvent`)".
    - DELETE the rule "Do not instantiate StreamEvent directly" — the ABC has no `@abstractmethod` declarations (RESEARCH OQ-01 confirms this is supported), so direct instantiation would fail anyway via the ABC marker but the prose was misleading.
    - Add a one-liner: "Wire format is byte-equivalent to Phase 4.7 (verified by `tests/unit/chat/test_stream_event_wire_compat.py` golden file)."

    Section 3 — LangChain 1.0 Integration (lines 405-456):
    - Replace the section heading with "PydanticAI Integration".
    - Replace the LangChain example code (likely showing `bind_tools(...)` + `astream(...)` + `additional_kwargs["reasoning_content"]`) with a PydanticAI example showing `agent.iter(message, message_history=history, deps=deps)` + the per-node loop with `ModelRequestNode.stream(...)` and `CallToolsNode.stream(...)`. Reference RESEARCH § "Match block pattern for the agent.iter() streaming loop" for the canonical structure.
    - Show the `RunContext[ChatDeps]`-based tool body (`async def search_flights(ctx: RunContext[ChatDeps], ...) -> str: client = ctx.deps.flight_client; ...`).
    - Cross-link to ADR-007 ("see `.planning/adrs/ADR-007-pydantic-ai.md` for the locked decision").

    Section 4 — ADR-001 prose (lines 561-583):
    - Add a one-line `Status: Superseded by ADR-007` line if not already present from Task 1's standalone-file creation. The standalone ADR file is the canonical artifact; the in-line ARCHITECTURE.md prose may either mirror the standalone file or simply pointer to it ("See `.planning/adrs/ADR-001-langchain.md` — Status: Superseded by ADR-007").

    Section 5 — Known Tech Debt: Protocol-vs-ABC (lines 783-795):
    - DELETE the entire section per PATTERNS.md § Sections to update. Phase 5 closes this debt.

    Section 6 — NEW subsection: Anti-Pattern Closures (Phase 5). Add a new subsection (above the "Known Tech Debt" location, since that one is gone, OR at the end of the Architectural Patterns section). Content: a 2-row entry table:
    | Anti-pattern | Closed in | How |
    |---|---|---|
    | `search_flights._flight_client` monkey-patched dependency | Phase 5 (D-06) | `RunContext[ChatDeps]` framework-managed DI; back-door deleted |
    | `LLMProvider` / `BoundProvider` Protocol-vs-ABC mismatch | Phase 5 (D-03) | Single `LLMProvider(ABC)`; concrete providers explicit subclass |

    Verify each edit is in the right location by re-reading the immediate surrounding context after each Edit call. The line numbers from PATTERNS.md are approximate; rely on heading/anchor matching, not absolute line numbers.
  </action>
  <verify>
    <automated>grep -q "PydanticAI" ARCHITECTURE.md && grep -q "Anti-Pattern Closures" ARCHITECTURE.md && (grep -c "Known Tech Debt: app/llm/protocol.py uses typing.Protocol" ARCHITECTURE.md | grep -q "^0$") && grep -q "build_agent" ARCHITECTURE.md && echo OK</automated>
  </verify>
  <done>ARCHITECTURE.md no longer contains the "Known Tech Debt: app/llm/protocol.py uses typing.Protocol" section. The LangChain Integration section is renamed to PydanticAI Integration with the new code examples. The LLMProvider Factory Pattern reflects ABC + build_agent. An Anti-Pattern Closures (Phase 5) subsection exists.</done>
</task>

<task type="auto">
  <name>Task 3: Update PROJECT.md Key Decisions table + tick Phase 5 success criteria</name>
  <files>
    .planning/PROJECT.md
  </files>
  <read_first>
    - .planning/PROJECT.md — current state. Look for the "Active" section's Phase 5 entries and the "Key Decisions" table.
    - .planning/ROADMAP.md lines 245-251 — Phase 5 Success Criteria (the six criteria each plan's `must_haves` traces back to)
    - .planning/STATE.md — current project state (Phase 4.9 complete; Phase 5 in progress)
    - .planning/phases/05-pydanticai-migration/05-CONTEXT.md D-21 — "ARCHITECTURE.md updated in the same change; .planning/PROJECT.md Key Decisions table reflects the transitions"
    - PATTERNS.md § ".planning/PROJECT.md" — "mark ADR-001 Superseded; ADR-007 Locked; tick Phase 5 success criteria"
  </read_first>
  <action>
    Edit `.planning/PROJECT.md`. Two areas to update:

    1. Key Decisions table (or equivalent ADR-tracking section): change the row for ADR-001 from `Status: Locked` (or whatever Phase 4.5 left it at) to `Status: Superseded by ADR-007`. Change the row for ADR-007 from `Status: Proposed` (or `Pending`) to `Status: Locked`. If the table doesn't currently list ADR-007, ADD a row for it referencing the locked decision.

    2. Phase 5 success-criteria tracking: locate the section that lists Phase 5 in the "Active" or "Roadmap" subsection. Tick each of the six ROADMAP.md success criteria (lines 246-251 — six numbered items) as completed. The ticking format may be a checkbox (`[x]`), a status column update (`Status: Complete`), or a date-completed field — match whichever convention `.planning/PROJECT.md` already uses for completed phases (Phase 4.9 is the most recent template).

    If `.planning/PROJECT.md` has a "Phases" or "Active" table with status columns, update Phase 5 from `Not started` / `Planning` / `In progress` to `Complete` (or whichever terminal status the table uses for shipped phases).

    DO NOT add or modify any content unrelated to Phase 5 (no roadmap re-shuffling, no future-phase status changes).
  </action>
  <verify>
    <automated>grep -q "ADR-007" .planning/PROJECT.md && grep -q "Superseded" .planning/PROJECT.md && grep -q "Locked" .planning/PROJECT.md && echo OK</automated>
  </verify>
  <done>`.planning/PROJECT.md` Key Decisions table reflects ADR-001 → Superseded and ADR-007 → Locked. Phase 5 success-criteria tracking is updated to reflect completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — docs-only edits) | This plan modifies no executable code, no infrastructure config, and no security-relevant settings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.6-01 | Information Disclosure | ADR text leaks sensitive architecture details | accept | All four files are public project documentation; no credentials, no secrets, no live URLs. |
| T-05.6-SC | Tampering | npm/pip/cargo install gates | accept | This plan adds zero packages. No legitimacy gate triggered. |
</threat_model>

<verification>
- `grep -q "Status: Superseded" .planning/adrs/ADR-001-langchain.md` returns true.
- `grep -q "Status: Locked" .planning/adrs/ADR-007-pydantic-ai.md` returns true.
- `grep -q "Anti-Pattern Closures" ARCHITECTURE.md` returns true.
- `grep -c "Known Tech Debt: app/llm/protocol.py" ARCHITECTURE.md` returns 0.
- `.planning/PROJECT.md` shows ADR-001 Superseded + ADR-007 Locked + Phase 5 success criteria ticked.
</verification>

<success_criteria>
ROADMAP.md Phase 5 success criteria advanced:
- SC-6 (ADR-001 transitions Locked → Superseded; ADR-007 (PydanticAI) becomes Locked) — DONE.

Plan-level criteria:
- ADR-001 status flipped to `Superseded by ADR-007` with a supersession note.
- ADR-007 status flipped to `Locked` with RESEARCH-backed rationale.
- ARCHITECTURE.md updated: LLMProvider section, StreamEvent section, LangChain → PydanticAI section, Known Tech Debt entry deleted, Anti-Pattern Closures subsection added.
- PROJECT.md Key Decisions table reflects the transitions; Phase 5 success criteria ticked.
</success_criteria>

<output>
Create `.planning/phases/05-pydanticai-migration/05-06-SUMMARY.md` documenting:
- The exact set of ARCHITECTURE.md sections edited and their before/after status (heading-level summary, not full diffs).
- The wording of the ADR-001 supersession note.
- The wording of the ADR-007 lock rationale (with RESEARCH OQ citations).
- Confirmation that PROJECT.md Key Decisions table reflects the transitions.
</output>
