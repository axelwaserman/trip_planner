---
id: "05-07"
title: "PR #21 Pre-Merge Fix: Critical + High Review Findings"
objective: "Fix all 13 CRITICAL and HIGH findings from adversarial review before merging PR #21"
wave: 7
depends_on: ["05-06"]
files_modified:
  - backend/app/chat/service.py
  - backend/app/chat/models.py
  - backend/app/api/routes/routes.py
  - backend/app/llm/providers/openai.py
  - backend/app/llm/providers/anthropic.py
  - backend/app/llm/providers/ollama.py
  - backend/app/llm/providers/lmstudio.py
  - backend/app/config.py
  - backend/app/tools/flight_search.py
  - backend/tests/unit/test_chat_service.py
  - backend/tests/unit/chat/test_stream_event_extraction.py
autonomous: true
requirements: []
---

<objective>
Fix every CRITICAL and HIGH finding from the adversarial code review of PR #21 (PydanticAI migration). All 13 findings (C1–C7, H1–H7) must be resolved before the branch merges. No new features, no scope beyond the review items.

Purpose: PR #21 cannot merge safely with CRITICAL findings active. C1 leaks KeyError as 500; C2/C3 means the Postgres conversation table is never populated; C4 silently drops RetryPromptPart; C5 has assert-stripped security guards; C6 has prompt injection via tool_name; C7 accepts unbounded message payloads. HIGH findings degrade encapsulation, violate ADR-008, and leak secrets via repr().

Output:
- `service.py` — KeyError guard, ConversationRepository.create call, RetryPromptPart branch, dead code removed, result captured inside async-with, public is_conversation_owner method, UserRepository injected
- `routes.py` — ownership checks use new public method, tool_name validated before interpolation
- `openai.py` + `anthropic.py` — assert replaced with explicit ValueError
- `ollama.py` + `lmstudio.py` — httpx replaced with pyreqwest (ADR-008), response consumed inside context manager
- `config.py` — openai_api_key + anthropic_api_key typed as SecretStr | None
- `flight_search.py` — explicit passengers > 9 guard
- `models.py` — ChatRequest.message has max_length=32_768
- Tests — RetryPromptPart drop path covered; TOCTOU KeyError path covered
</objective>

<execution_context>
@/Users/axel/code/trip_planner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/trip_planner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-pydanticai-migration/05-06-SUMMARY.md

Key source files (read before touching):
- `backend/app/chat/service.py` — ChatService; _metadata / _agents TOCTOU (C1), create_session missing repo.create (C2/C3), RetryPromptPart silent drop (C4), dead _first_message_preview (H2), result outside async-with (H3), _metadata access in routes (H7)
- `backend/app/chat/repository.py` — ConversationRepository ABC; create() takes user_id: UUID, NOT str (C3 root cause)
- `backend/app/auth/repository.py` — UserRepository ABC; get_user(username: str) → UserInDB; UserInDB.id is UUID (C3 resolution path)
- `backend/app/api/routes/routes.py` — direct _metadata access (H7), tool_name interpolation (C6)
- `backend/app/llm/providers/openai.py` — assert self._api_key is not None (C5)
- `backend/app/llm/providers/anthropic.py` — assert self._api_key is not None (C5)
- `backend/app/llm/providers/ollama.py` — httpx (H1), response.json() outside context manager (H4)
- `backend/app/llm/providers/lmstudio.py` — httpx (H1), response.json() outside context manager (H4)
- `backend/app/config.py` — openai_api_key/anthropic_api_key as str | None (H5)
- `backend/app/tools/flight_search.py` — passengers > 9 not checked (H6)
- `backend/app/chat/models.py` — ChatRequest.message no max_length (C7)
- `backend/app/flights/duffel_client.py` — canonical pyreqwest usage pattern; use ClientBuilder + ConnectError / RequestTimeoutError / StatusError from pyreqwest.exceptions

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/app/chat/repository.py:
```
ConversationRepository.create(*, user_id: UUID, provider: str, model: str) -> UUID
```

From backend/app/auth/repository.py:
```
class UserRepository(ABC):
    async def get_user(self, username: str) -> UserInDB  # raises UserNotFoundError if missing
UserInDB fields: username: str, hashed_password: str, disabled: bool
```
Note: UserInDB does NOT have an `id` field in the current codebase — check
`backend/app/db/models.py` `User` model and `UserInDB` in `backend/app/auth/models.py`
before implementing C3. The approach must resolve username → UUID using the DB User table
or by changing ConversationRepository.create to accept username-as-str.

From backend/app/flights/duffel_client.py (pyreqwest pattern for H1/H4):
```python
from pyreqwest.client import ClientBuilder
from pyreqwest.exceptions import ConnectError, RequestTimeoutError, StatusError

async with ClientBuilder().timeout(timedelta(seconds=<timeout>)).error_for_status(True).build() as client:
    await (
        client.get(url)
        .build()
        .send()
    )
# response consumed inside the async-with; call .json() inside too
```

From backend/app/chat/service.py:
```python
# chat_stream() signature:
async def chat_stream(
    self, message: str, conversation_id: str, *, persist_user_message: bool = True
) -> AsyncGenerator[StreamEvent]:

# Current _handle_tool_event tail (C4 gap — add elif branch here):
elif isinstance(event, FunctionToolResultEvent):
    ret = event.part
    if isinstance(ret, ToolReturnPart):
        ...
    # MISSING: elif isinstance(ret, RetryPromptPart): pop timing + yield ErrorEvent

# Current chat_stream() line 347-349 (C1 gap — access BEFORE try block):
deps = ChatDeps(
    ...
    user_id=self._metadata[conversation_id]["user_id"],  # KeyError possible
)
agent = self._agents[conversation_id]                    # KeyError possible

# Current chat_stream() line 369 (H3 gap):
if persist_user_message and agent_run.result is not None:
    await self._message_store.append(..., agent_run.result.new_messages())
# agent_run.result accessed AFTER `async with agent.iter(...)` block closes
```

From backend/app/api/routes/routes.py:
```python
# Current ownership check pattern (H7 — routes access _metadata directly):
metadata = chat_service._metadata.get(request.conversation_id)
if metadata is None or metadata.get("user_id") != current_user.username:
    raise HTTPException(...)

# Used in three routes: POST /api/chat (line 109), POST /api/chat/retry (line 201),
# DELETE /api/chat/conversation/{id} (line 414)
# Also: metadata["provider"/"model"] read at line 383-384 in create_conversation

# C6 — tool_name interpolation (line 223):
replay_message = f"Please retry the previous {last_inv['tool_name']} call."
# tool_name comes from FunctionToolCallEvent.part.tool_name (LLM-controlled)
```

From backend/app/chat/models.py:
```python
class ChatRequest(BaseModel):
    message: str = Field(..., min_length=1, description="User message to send to the agent")
    # MISSING: max_length=32_768
```

From backend/app/config.py:
```python
# Current:
openai_api_key: str | None = None
anthropic_api_key: str | None = None

# After H5 fix:
openai_api_key: SecretStr | None = None
anthropic_api_key: SecretStr | None = None
# Callers must use .get_secret_value() — check factory and get_available_providers()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: service.py — C1 KeyError guard, C2/C3 repo.create wiring, C4 RetryPromptPart branch, H2 dead code, H3 result capture, H7 public method</name>
  <files>
    backend/app/chat/service.py,
    backend/app/auth/models.py,
    backend/app/db/models.py
  </files>
  <read_first>
    backend/app/chat/service.py,
    backend/app/chat/repository.py,
    backend/app/auth/repository.py,
    backend/app/auth/models.py,
    backend/app/db/models.py,
    backend/app/api/main.py
  </read_first>
  <behavior>
    - C1: chat_stream() accesses _metadata[conversation_id] and _agents[conversation_id] before the try block. Wrapping in KeyError catch must yield ErrorEvent(error_code=ErrorCode.session_error, message="Conversation not found or expired.", retryable=False, tool_name=None, raw_detail=None, conversation_id=conversation_id) and return; no 500.
    - C2/C3: create_session() must call self._conversation_repo.create(...) after building the agent. The repo.create() signature is create(*, user_id: UUID, provider: str, model: str). The user_id arriving is a str (username). Resolution: check whether UserInDB or the DB User model exposes an id: UUID field. If UserInDB has no id field, inject UserRepository into ChatService.__init__ and await self._user_repo.get_user(user_id) to obtain the DB User.id UUID — then pass that UUID to repo.create(). If adding UserRepository injection, update __init__ signature and the in-memory test fixture factory make_chat_service_with_mock_llm to inject a stub UserRepository.
    - C4: _handle_tool_event() has a FunctionToolResultEvent branch that handles ToolReturnPart. Add an elif branch for RetryPromptPart: pop the tool_call_start timing entry using the part's tool_call_id (or pop by best-effort if id unavailable), then yield ErrorEvent(error_code=ErrorCode.tool_error, message="Tool returned a retry prompt.", retryable=True, tool_name=part.tool_name if available else None, raw_detail=None, conversation_id=conversation_id).
    - H2: delete the _first_message_preview private method (lines 223-231) entirely; it is never called.
    - H3: capture new_messages = agent_run.result.new_messages() INSIDE the async-with agent.iter(...) block (before the block exits), then use new_messages in the persist step outside the block. The async-with context is lines 355-376; new_messages must be captured before that block's dedent.
    - H7: add public method is_conversation_owner(self, conversation_id: str, user_id: str) -> bool that returns _metadata.get(conversation_id, {}).get("user_id") == user_id. This replaces the pattern routes currently use.
  </behavior>
  <action>
    Read backend/app/auth/models.py and backend/app/db/models.py first to determine whether a UUID id field exists on UserInDB or User. Choose the less-invasive C3 resolution based on that reading.

    C1 — Wrap the two pre-try accesses (user_id from _metadata and agent from _agents) in a try/except KeyError block. On KeyError: yield the session_error ErrorEvent and return from the generator. The existing outer try/except Exception must remain below this guard.

    C2/C3 — Add the _conversation_repo.create() call inside create_session(), after the agent is stored and the metadata dict is populated, before the return. Pass user_id as UUID (resolved via UserRepository.get_user if needed). If UserRepository injection is required: add user_repo: UserRepository parameter to __init__; add TYPE_CHECKING import for UserRepository from app.auth.repository; update the stored attribute and the ChatService docstring. The InMemoryUserRepository stub needed for tests must be minimal: get_user(username) returning a UserInDB with a deterministic UUID (e.g. uuid5(NAMESPACE_DNS, username)).

    C4 — In _handle_tool_event, after the ToolReturnPart branch closes, add:
    from pydantic_ai.messages import RetryPromptPart (add to top-level imports in the file, not inline).
    elif isinstance(ret, RetryPromptPart):
        tool_call_start.pop(ret.tool_call_id, None)  # pop timing to avoid leak
        yield ErrorEvent(
            error_code=ErrorCode.tool_error,
            message="Tool requested a retry.",
            retryable=True,
            tool_name=ret.tool_name,
            raw_detail=None,
            conversation_id=conversation_id,
        )

    H2 — Delete the entire _first_message_preview method (async def _first_message_preview through its return statement).

    H3 — Inside the async-with agent.iter(...) block, after the async-for node loop closes, add: _new_messages = agent_run.result.new_messages() if agent_run.result is not None else []. Then in the post-block persist step, replace agent_run.result.new_messages() with _new_messages and change the guard to if persist_user_message and _new_messages:.

    H7 — Add is_conversation_owner(self, conversation_id: str, user_id: str) -> bool as a public method. Implementation: return self._metadata.get(conversation_id, {}).get("user_id") == user_id.

    mypy strict compliance: all new parameters need type annotations; UserRepository import must be TYPE_CHECKING-guarded if it creates a cycle.
  </action>
  <verify>
    <automated>cd /Users/axel/code/trip_planner/backend && uv run pytest tests/unit/test_chat_service.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - chat_stream() with unknown conversation_id yields ErrorEvent(error_code="session_error") and does not raise KeyError.
    - create_session() calls self._conversation_repo.create(...) with a UUID user_id.
    - FunctionToolResultEvent with RetryPromptPart yields ErrorEvent(error_code="tool_error", retryable=True).
    - _first_message_preview method is absent from the file.
    - agent_run.result.new_messages() is captured inside async-with scope.
    - is_conversation_owner(conversation_id, user_id) -> bool exists as a public method.
    - uv run pytest tests/unit/test_chat_service.py passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: routes.py — H7 ownership callers + C6 tool_name injection guard</name>
  <files>
    backend/app/api/routes/routes.py
  </files>
  <read_first>
    backend/app/api/routes/routes.py,
    backend/app/chat/service.py
  </read_first>
  <behavior>
    - H7: The three ownership checks at lines 109, 201, and 414 (chat, retry, delete routes) all read chat_service._metadata directly. Each must be replaced by chat_service.is_conversation_owner(conversation_id, current_user.username). The 404 raise shape stays identical; only the condition expression changes.
    - Line 383-384 (create_conversation) also reads chat_service._metadata[conversation_id] for provider/model. This is NOT an ownership check — it reads data after a successful create_session. Add a public get_conversation_metadata(conversation_id: str) -> dict[str, Any] | None method to ChatService in Task 1, OR inline-replace the _metadata read with a direct attribute access that does not cross the encapsulation boundary. The less-invasive approach: since create_session just populated _metadata[conversation_id], the read is safe — add a get_conversation_info(conversation_id: str) -> dict[str, Any] | None method to ChatService in Task 1 and use it here. If adding get_conversation_info is out of scope for Task 1, note it in the task action so the executor adds it during Task 2's read of service.py.
    - C6: The retry route builds replay_message = f"Please retry the previous {last_inv['tool_name']} call.". tool_name comes from FunctionToolCallEvent.part.tool_name which is LLM-controlled. Before the f-string, validate tool_name against the set {"search_flights"} (the only registered tool in Phase 5). If tool_name is not in the registered set, raise HTTPException(status_code=status.HTTP_422_UNPROCESSABLE_CONTENT, detail=f"Unknown tool name in last invocation: {tool_name!r}"). Use a module-level constant _REGISTERED_TOOL_NAMES: frozenset[str] = frozenset({"search_flights"}) at the top of the routes file for clarity.
  </behavior>
  <action>
    Read routes.py fully before editing — three ownership check sites plus the metadata read in create_conversation.

    H7 — Replace each of the three patterns:
      metadata = chat_service._metadata.get(...)
      if metadata is None or metadata.get("user_id") != current_user.username:
    with:
      if not chat_service.is_conversation_owner(request.conversation_id, current_user.username):
    (or conversation_id for the DELETE route which receives it as a path param).

    For the create_conversation metadata read (line 383-384), replace it using the method added in Task 1 (get_conversation_info or similar). If Task 1 did not add that method, add a minimal get_conversation_metadata(self, conversation_id: str) -> dict[str, Any] | None to service.py here, returning _metadata.get(conversation_id).

    C6 — Above the replay_message f-string, add:
      _REGISTERED_TOOL_NAMES: frozenset[str] = frozenset({"search_flights"})  (module-level constant)
      tool_name = last_inv["tool_name"]
      if tool_name not in _REGISTERED_TOOL_NAMES:
          raise HTTPException(status_code=status.HTTP_422_UNPROCESSABLE_CONTENT,
                              detail=f"Unknown tool name in last invocation: {tool_name!r}")
      replay_message = f"Please retry the previous {tool_name} call."

    All existing HTTPException shapes, status codes, and SSE formats must remain byte-identical.
  </action>
  <verify>
    <automated>cd /Users/axel/code/trip_planner/backend && uv run pytest tests/integration/test_chat_service_flow.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - No route accesses chat_service._metadata directly (grep -n "_metadata" backend/app/api/routes/routes.py returns zero lines or only comment references).
    - is_conversation_owner is called in all three auth check sites.
    - _REGISTERED_TOOL_NAMES frozenset is defined at module level.
    - tool_name is validated before f-string interpolation in the retry route.
    - integration tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: openai.py + anthropic.py — C5 assert → explicit ValueError</name>
  <files>
    backend/app/llm/providers/openai.py,
    backend/app/llm/providers/anthropic.py
  </files>
  <read_first>
    backend/app/llm/providers/openai.py,
    backend/app/llm/providers/anthropic.py
  </read_first>
  <action>
    In both files, locate the line `assert self._api_key is not None` inside build_agent() and replace it with:
      if self._api_key is None:
          raise ValueError(
              f"{self.get_provider_name()!r} build_agent() called with api_key=None; "
              "validate_config() must return None before build_agent() is called."
          )

    Update the docstring comment on the assert line in both files to reflect the explicit guard (remove the words "assert" from the inline comment; replace with "explicit guard"). The guard message must NOT include the key value — only the provider name and the call-order expectation.

    No other changes to these files.
  </action>
  <verify>
    <automated>cd /Users/axel/code/trip_planner/backend && uv run pytest tests/unit/ -k "openai or anthropic" -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - grep -n "assert self._api_key" backend/app/llm/providers/openai.py backend/app/llm/providers/anthropic.py returns zero results.
    - Calling build_agent() with api_key=None raises ValueError (not AssertionError).
    - mypy --strict passes on both files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: ollama.py + lmstudio.py — H1 httpx → pyreqwest + H4 response consumed inside context manager</name>
  <files>
    backend/app/llm/providers/ollama.py,
    backend/app/llm/providers/lmstudio.py
  </files>
  <read_first>
    backend/app/llm/providers/ollama.py,
    backend/app/llm/providers/lmstudio.py,
    backend/app/flights/duffel_client.py
  </read_first>
  <behavior>
    - H1: Both providers import httpx and use httpx.AsyncClient for probe requests. ADR-008 mandates pyreqwest exclusively. Replace the httpx dependency in list_models() for both files.
    - H4: Both files call response.json() outside the async-with block (after the context manager exits). The pyreqwest ClientBuilder pattern (see duffel_client.py) consumes the response inside the async-with. Fix this by moving JSON parsing inside the context.
    - Exception types change: httpx.ConnectError → pyreqwest.exceptions.ConnectError; httpx.TimeoutException → pyreqwest.exceptions.RequestTimeoutError; httpx.HTTPStatusError → pyreqwest.exceptions.StatusError. The validate_config() except tuples in both files must update accordingly.
  </behavior>
  <action>
    For ollama.py:
    1. Remove `import httpx` from imports. Add:
       from datetime import timedelta
       from pyreqwest.client import ClientBuilder
       from pyreqwest.exceptions import ConnectError, RequestTimeoutError, StatusError
    2. Rewrite list_models() to use the pyreqwest ClientBuilder pattern per duffel_client.py:
       async with ClientBuilder().timeout(timedelta(seconds=self._probe_timeout)).error_for_status(True).build() as client:
           resp = await client.get(url).build().send()
           payload = resp.json()
       Both the await send() and resp.json() calls must be INSIDE the async-with block. Move models_list extraction and the set comprehension inside the block as well (or assign payload inside, use outside — but ensure resp.json() executes before the context closes).
    3. Update validate_config() except clause:
       except (ConnectError, RequestTimeoutError, StatusError):

    For lmstudio.py:
    Apply the same four changes symmetrically. The URL is f"{self._base_url.rstrip('/')}/models". The JSON parse (payload = resp.json()) and subsequent data extraction must occur inside the async-with block.

    Do NOT change build_agent() in either file. Do NOT change the probe_timeout type — it stays float seconds; use timedelta(seconds=self._probe_timeout) at the call site.

    Update module docstrings in both files to remove the sentence claiming httpx is an ADR-008 exception (the sentence explicitly notes the swap happens in Phase 7 — that phase is now; remove or replace with "uses pyreqwest per ADR-008").
  </action>
  <verify>
    <automated>cd /Users/axel/code/trip_planner/backend && uv run pytest tests/unit/ -k "ollama or lmstudio" -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - grep -n "import httpx" backend/app/llm/providers/ollama.py backend/app/llm/providers/lmstudio.py returns zero results.
    - grep -n "pyreqwest" backend/app/llm/providers/ollama.py backend/app/llm/providers/lmstudio.py shows ClientBuilder imports and usage.
    - resp.json() appears inside (indented under) the async-with block in both files.
    - validate_config() except clauses reference ConnectError, RequestTimeoutError, StatusError from pyreqwest.exceptions.
  </done>
</task>

<task type="auto">
  <name>Task 5: config.py SecretStr (H5) + models.py max_length (C7) + flight_search.py passengers guard (H6)</name>
  <files>
    backend/app/config.py,
    backend/app/chat/models.py,
    backend/app/tools/flight_search.py
  </files>
  <read_first>
    backend/app/config.py,
    backend/app/chat/models.py,
    backend/app/tools/flight_search.py,
    backend/app/llm/factory.py
  </read_first>
  <action>
    H5 — config.py:
    SecretStr is already imported (it is used for duffel_api_token). Change:
      openai_api_key: str | None = None
      anthropic_api_key: str | None = None
    to:
      openai_api_key: SecretStr | None = None
      anthropic_api_key: SecretStr | None = None

    Then audit all call sites that read openai_api_key or anthropic_api_key from settings. Read backend/app/llm/factory.py before editing — the factory reads settings.openai_api_key and settings.anthropic_api_key and passes them as plain str to the provider constructors. At each call site: if the value is not None, call .get_secret_value() before passing to the provider.

    Also update get_available_providers() which reads bool(self.openai_api_key) and bool(self.anthropic_api_key) — SecretStr is truthy when non-empty, so bool() still works correctly for the availability flag. No change needed there.

    C7 — models.py:
    In ChatRequest, change:
      message: str = Field(..., min_length=1, description="...")
    to:
      message: str = Field(..., min_length=1, max_length=32_768, description="...")

    H6 — flight_search.py:
    After the existing `if passengers < 1:` guard (line 228-229), add:
      if passengers > 9:
          return "Error: Number of passengers cannot exceed 9."
    This must appear before the FlightQuery construction.
  </action>
  <verify>
    <automated>cd /Users/axel/code/trip_planner/backend && uv run pytest tests/unit/ -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - settings.openai_api_key and settings.anthropic_api_key are typed SecretStr | None (grep confirms "SecretStr | None" in config.py for both fields).
    - All factory/provider call sites call .get_secret_value() before passing the key.
    - ChatRequest.message Field has max_length=32_768.
    - search_flights() with passengers=10 returns "Error: Number of passengers cannot exceed 9.".
    - uv run mypy --strict backend/app/config.py backend/app/chat/models.py backend/app/tools/flight_search.py passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Tests — RetryPromptPart drop path (C4) + TOCTOU KeyError path (C1) coverage</name>
  <files>
    backend/tests/unit/test_chat_service.py,
    backend/tests/unit/chat/test_stream_event_extraction.py
  </files>
  <read_first>
    backend/tests/unit/test_chat_service.py,
    backend/tests/unit/chat/test_stream_event_extraction.py,
    backend/tests/fixtures/llm.py,
    backend/app/chat/service.py
  </read_first>
  <behavior>
    - C1 test: calling chat_stream() with a conversation_id that is NOT in _metadata must yield exactly one ErrorEvent with error_code="session_error" and must not raise KeyError.
    - C4 test: when _handle_tool_event receives a FunctionToolResultEvent whose .part is a RetryPromptPart, it must yield an ErrorEvent with error_code="tool_error" and retryable=True, and the tool_call_start timing dict must have the corresponding key popped (no leak).
  </behavior>
  <action>
    In test_chat_service.py, add a new test class TestChatStreamKeyErrorGuard (or standalone function) with one test:

    test_chat_stream_unknown_conversation_yields_session_error — construct a service via make_chat_service_with_mock_llm, do NOT call create_session, then collect events from chat_stream("hi", "nonexistent-id"). Assert: events has exactly one element; events[0].type == "error"; events[0].error_code == "session_error" (or ErrorCode.session_error).

    In test_stream_event_extraction.py, add a test:

    test_retry_prompt_part_yields_error_event — construct a RetryPromptPart instance (from pydantic_ai.messages import RetryPromptPart; check its constructor signature by reading pydantic_ai source or using the installed package). Call _handle_tool_event via:
      service = make_chat_service_with_mock_llm(MockLLMStream.greeting())
      conv_id, _ = await service.create_session(default_session_config(), user_id="u")
      timing: dict[str, float] = {"call-1": time.monotonic()}
      events = [e async for e in service._handle_tool_event(
          FunctionToolResultEvent(part=RetryPromptPart(...), tool_call_id="call-1"),
          conv_id,
          timing,
      )]
      assert events[0].type == "error"
      assert events[0].retryable is True
      assert "call-1" not in timing  # timing entry was popped

    Adjust the FunctionToolResultEvent / RetryPromptPart construction to match the installed pydantic_ai API (read the installed package's messages module or check existing test usages).

    If the RetryPromptPart constructor or FunctionToolResultEvent signature is unclear, keep the test as a smoke test that at minimum imports RetryPromptPart and calls _handle_tool_event with a minimal mock.
  </action>
  <verify>
    <automated>cd /Users/axel/code/trip_planner/backend && uv run pytest tests/unit/test_chat_service.py tests/unit/chat/test_stream_event_extraction.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - test_chat_stream_unknown_conversation_yields_session_error passes; no KeyError surfaces.
    - test_retry_prompt_part_yields_error_event passes; timing dict has key removed.
    - All pre-existing tests in both files continue to pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → retry prompt | last_inv["tool_name"] originates from LLM tool dispatch; interpolated into a natural-language prompt sent back to the LLM |
| client → ChatRequest.message | Arbitrary user text crosses the HTTP boundary; no server-side length cap existed before C7 |
| env → settings repr | openai_api_key / anthropic_api_key were plain str and appeared in repr(settings) and debug logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Tampering | retry route — tool_name interpolation | mitigate | C6: validate tool_name against _REGISTERED_TOOL_NAMES frozenset; raise 422 for unknown names |
| T-05-07-02 | Denial of Service | ChatRequest.message — unbounded length | mitigate | C7: max_length=32_768 on ChatRequest.message field via Pydantic Field validator |
| T-05-07-03 | Information Disclosure | config.py — API keys in repr() | mitigate | H5: SecretStr for openai_api_key + anthropic_api_key; .get_secret_value() at factory call sites only |
| T-05-07-04 | Elevation of Privilege | chat_stream — KeyError on _metadata/_agents before try block | mitigate | C1: try/except KeyError → yield session_error ErrorEvent, return; no 500 |
| T-05-07-05 | Repudiation | assert stripped by python -O | mitigate | C5: explicit if/raise ValueError in build_agent() for both OpenAI and Anthropic providers |
| T-05-07-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (pyreqwest already in pyproject.toml via duffel_client) |
</threat_model>

<verification>
Run the full backend test suite to confirm no regressions across all 13 fixes:

```bash
cd /Users/axel/code/trip_planner && just test-unit
```

Spot checks per finding:
- C1: `grep -n "_metadata\[conversation_id\]" backend/app/chat/service.py` — must only appear inside the KeyError guard or after it.
- C2/C3: `grep -n "_conversation_repo.create" backend/app/chat/service.py` — must appear inside create_session().
- C4: `grep -n "RetryPromptPart" backend/app/chat/service.py` — must appear in _handle_tool_event.
- C5: `grep -n "assert self._api_key" backend/app/llm/providers/openai.py backend/app/llm/providers/anthropic.py` — zero results.
- C6: `grep -n "_REGISTERED_TOOL_NAMES" backend/app/api/routes/routes.py` — must exist.
- C7: `grep -n "max_length" backend/app/chat/models.py` — must show 32_768.
- H1/H4: `grep -rn "import httpx" backend/app/llm/providers/` — zero results.
- H2: `grep -n "_first_message_preview" backend/app/chat/service.py` — zero results (the private method is deleted; list_conversations_for_user uses message_store directly).
- H3: `grep -n "new_messages()" backend/app/chat/service.py` — result captured inside async-with scope.
- H5: `grep -n "openai_api_key\|anthropic_api_key" backend/app/config.py` — both must include SecretStr.
- H6: `grep -n "passengers > 9" backend/app/tools/flight_search.py` — must exist.
- H7: `grep -n "_metadata" backend/app/api/routes/routes.py` — zero results for direct dict access; only is_conversation_owner calls remain.
</verification>

<success_criteria>
- All 13 findings (C1–C7, H1–H7) have a concrete implementation and at least one automated test exercising the fix.
- `just test-unit` passes with no failures.
- `just check` (lint + format + typecheck) passes on all modified files — mypy strict mode must not gain any new errors.
- No fenced code blocks inside task actions were written by the executor (plan-level constraint).
- PR #21 branch is ready for final review after these changes land.
</success_criteria>

<output>
Create `.planning/phases/05-pydanticai-migration/05-07-SUMMARY.md` when done.
</output>
