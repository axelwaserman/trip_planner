---
plan_id: 05-05
phase: 05-pydanticai-migration
plan: 05
type: execute
wave: 4
depends_on: ["05-04"]
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
autonomous: true
requirements:
  - REQ-pydantic-ai-migration

must_haves:
  truths:
    - "`backend/pyproject.toml` `[project].dependencies` has zero entries starting with `langchain` and zero entries equal to `langgraph`."
    - "`backend/pyproject.toml` still lists `pydantic-ai>=0.8.1` (added in Wave 2 / Plan 05-03 Task 0b) and now lists `pydantic>=2.12` (Pitfall 8 floor bump)."
    - "`uv.lock` is regenerated and consistent with `pyproject.toml` (no stale `langchain*` resolved)."
    - "`grep -rEn '^(from |import )langchain' backend/app backend/tests` returns zero matches."
    - "Package legitimacy gate for `pydantic-ai` was already cleared in Wave 2 / Plan 05-03 Task 0 — no separate gate task needed in this plan."
    - "`just check` and `just test` pass against the post-swap manifest."
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "uv-managed dependency manifest with langchain* removed and pydantic-ai added"
    - path: "backend/uv.lock"
      provides: "Regenerated lockfile"
  key_links:
    - from: "backend/pyproject.toml dependencies"
      to: "uv.lock resolution"
      via: "uv lock"
      pattern: "uv lock"
---

<objective>
Complete the migration by removing all `langchain*` packages (and the lingering `langgraph` per RESEARCH Pitfall 9) from `pyproject.toml`, bumping `pydantic` to `>=2.12` (Pitfall 8), and regenerating `uv.lock`.

`pydantic-ai>=0.8.1` was already added (and its legitimacy gate already cleared) in Wave 2 / Plan 05-03 Tasks 0 + 0b — closing the hidden ordering dependency where Wave 2's provider rewrite imports `pydantic_ai`. This plan does NOT re-run the legitimacy gate and does NOT re-add `pydantic-ai`.

This plan is intentionally tightly scoped: NO source files change. Wave 3 (plan 05-04) already removed all `langchain*` imports from `app/` and `tests/`. This plan's job is to bring the dependency manifest in line with the source tree.

Purpose: Close the dependency-manifest half of REQ-pydantic-ai-migration / D-20.
Output: An updated `pyproject.toml` (langchain* + langgraph removed; pydantic floor bumped), a regenerated `uv.lock`, and a clean `just check && just test` run.
</objective>

<execution_context>
@/Users/axel/code/trip_planner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/trip_planner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-pydanticai-migration/05-CONTEXT.md
@.planning/phases/05-pydanticai-migration/05-RESEARCH.md
@.planning/phases/05-pydanticai-migration/05-PATTERNS.md
@./CLAUDE.md
@backend/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove langchain* + langgraph and bump pydantic floor via uv</name>
  <files>
    backend/pyproject.toml,
    backend/uv.lock
  </files>
  <read_first>
    - backend/pyproject.toml — current `[project].dependencies` list (lines 7-22 per PATTERNS.md). Note the langchain entries: `langchain>=0.3.0`, `langchain-ollama>=0.2.0`, `langgraph>=1.0.2`, `langchain-openai>=1.2.1`, `langchain-anthropic>=1.4.3`. Also note `pydantic>=2.9.0` (must bump to `>=2.12` per Pitfall 8). At the start of this task, `pydantic-ai>=0.8.1` should already be present (added in Wave 2 / Plan 05-03 Task 0b).
    - .planning/phases/05-pydanticai-migration/05-PATTERNS.md § "backend/pyproject.toml" — exact target dependency list
    - .planning/phases/05-pydanticai-migration/05-RESEARCH.md § "Standard Stack" + Pitfall 8 (pydantic floor) + Pitfall 9 (langgraph still present)
    - .planning/phases/05-pydanticai-migration/05-CONTEXT.md D-20 — exact swap operation
    - ./CLAUDE.md — "Package management: use `uv` exclusively — never `pip`, `poetry`, or `conda`"
    - backend/tests/unit/test_dependencies.py (Wave 0) — manifest assertion test; turns green here
    - backend/tests/unit/test_no_langchain_imports.py (Wave 0) — import-scan test; should already be green after Wave 3 but re-verified here
  </read_first>
  <action>
    Run the canonical uv-managed dependency removal (CLAUDE.md "use `uv` exclusively"). The `uv add 'pydantic-ai>=0.8.1'` step has ALREADY happened in Wave 2 / Plan 05-03 Task 0b — do NOT re-run it. The package legitimacy gate has ALREADY been cleared in Wave 2 / Plan 05-03 Task 0 — do NOT re-run it. All commands must be executed from the `backend/` directory because that's where `pyproject.toml` lives:

    1. `cd backend && uv remove langchain langchain-ollama langchain-openai langchain-anthropic langgraph` — removes all five LangChain packages (note: `langchain-core` was a transitive dep, not a direct dep, per the Phase 4.5 manifest review — confirm by reading the current `pyproject.toml`; if `langchain-core` IS a direct dep, include it in the `uv remove` command). Per RESEARCH Pitfall 9, `langgraph` is still in the manifest despite PR #1 claiming removal — this command finally cleans it up.
    2. After the `uv remove` operation, manually edit `backend/pyproject.toml` to bump `pydantic>=2.9.0` to `pydantic>=2.12` (Pitfall 8). Use the Edit tool to perform a single-line replacement; do NOT use `cat`/heredoc.
    3. Run `cd backend && uv lock` to regenerate the lockfile and resolve transitives. The manual `pydantic` bump in step 2 needs `uv lock` to be picked up.
    4. Run `cd backend && uv sync` to install the new resolution into the project's virtualenv. (Optional but recommended so the next test run sees the updated environment.)

    Verification of the manifest shape after the swap:
    - `[project].dependencies` should contain the entries listed in PATTERNS.md § "backend/pyproject.toml" target shape: `fastapi`, `uvicorn[standard]`, `python-dotenv`, `pydantic>=2.12`, `pydantic-settings`, `pydantic-ai>=0.8.1`, `httpx>=0.27.0` (kept — used by validate_config / list_models), `pyjwt`, `pwdlib[argon2]`, `python-multipart`. Zero `langchain*` entries; zero `langgraph` entry.
    - `uv.lock` reflects the swap (no `langchain*` resolved entries).

    DO NOT modify any source file in this task. The swap is purely declarative.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/test_dependencies.py tests/unit/test_no_langchain_imports.py -v</automated>
  </verify>
  <done>Both Wave 0 tests pass. `grep -E '^"?langchain' backend/pyproject.toml` returns no matches. `grep -E '^"?langgraph' backend/pyproject.toml` returns no matches. `grep -E '^"?pydantic-ai' backend/pyproject.toml` returns one match (carried over from Wave 2). `grep -E '^"?pydantic>=2\.12' backend/pyproject.toml` returns one match (Pitfall 8 floor). `uv.lock` is regenerated.</done>
</task>

<task type="auto">
  <name>Task 2: Full check + test sweep against post-swap manifest</name>
  <files></files>
  <read_first>
    - .planning/phases/05-pydanticai-migration/05-VALIDATION.md — sampling-density rule (no 3 consecutive tasks without an automated verify); end-of-phase requires `just check` + `just test` green
    - ./CLAUDE.md § Commands — `just check` is `lint + format-check + typecheck`; `just test` runs unit + integration + e2e via path discovery
  </read_first>
  <action>
    Run the canonical full-check pipeline against the post-swap manifest to confirm no transitively-resolved package conflicts and no lingering import surprises:

    1. `just check` — runs `ruff check`, `ruff format --check`, and `mypy --strict` per the project's `justfile`. Any failure here is most likely caused by a transitively-pulled package version conflict (e.g., a Pydantic 2.12+ behavior change) — fix the underlying issue (NOT by reverting the dep swap). Common failures and their fixes are documented in RESEARCH § Common Pitfalls.
    2. `just test` — runs the full test suite (unit + integration + e2e via path discovery). Acceptance: zero failures, zero errors. The gated cloud acceptance tests in `tests/integration/llm/test_*_thinking_live.py` will SKIP (no API keys) — that's correct.
    3. (Optional sanity) `cd backend && uv run pytest tests/integration/test_chat_stream.py tests/integration/test_chat_factory.py -v` — re-runs the most behaviorally significant tests to confirm the dep swap didn't introduce a behavioral regression.

    If `just check` or `just test` fails:
    - Identify whether the failure is a NEW issue (caused by Pydantic 2.12 / pydantic-ai resolution) or a pre-existing issue surfaced by the lockfile rebuild.
    - Fix the root cause; do NOT revert the dep swap.
    - Re-run until clean.

    DO NOT commit until both commands are clean.
  </action>
  <verify>
    <automated>cd /Users/axel/code/trip_planner && just check && just test</automated>
  </verify>
  <done>`just check` and `just test` both pass against the post-swap manifest. The phase-5 production code now runs entirely on PydanticAI with no LangChain dependency anywhere in the manifest, source tree, or test tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → PyPI | `uv remove` only deletes existing entries — no new package is downloaded in this plan. The supply-chain legitimacy gate for `pydantic-ai` was cleared earlier in Wave 2 / Plan 05-03 Task 0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-01 | Tampering | Stale `uv.lock` resolves transitive `langchain-core` | mitigate | `uv lock` regenerates from scratch; `uv sync` installs the new resolution. Verify with `grep -i 'langchain\\|langgraph' backend/uv.lock` (acceptable: substring matches in commit hashes/URLs of removed packages — those will be cleaned up by `uv lock`'s pruning). |
| T-05.5-02 | Denial of Service | Pydantic 2.12 floor bump breaks transitively-pulled packages | mitigate | `just check` + `just test` in Task 2 catch regressions. Pydantic 2.12 is already installed (per RESEARCH § Standard Stack — pydantic 2.12.3 is the current installed version) so the floor bump should be a no-op for the resolution. |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/unit/test_dependencies.py tests/unit/test_no_langchain_imports.py -v` passes (both Wave 0 manifest tests).
- `just check && just test` passes from the repo root.
- `grep -E '^"?langchain' backend/pyproject.toml` returns no matches.
- `grep -E '^"?langgraph' backend/pyproject.toml` returns no matches.
- `grep -E '^"?pydantic-ai' backend/pyproject.toml` returns one match.
- `grep -E '^"?pydantic>=2\\.12' backend/pyproject.toml` returns one match.
</verification>

<success_criteria>
ROADMAP.md Phase 5 success criteria advanced:
- SC-4 (`langchain*` removed from `pyproject.toml`; `pydantic-ai` added; `uv lock` reflects the swap) — DONE. (`pydantic-ai` add happened in Wave 2 / Plan 05-03 Task 0b; this plan finishes the SC-4 swap by removing `langchain*` + `langgraph` and bumping the pydantic floor.)

Plan-level criteria:
- `pyproject.toml` has zero `langchain*` and zero `langgraph` entries.
- `pydantic-ai>=0.8.1` is still present (carried over from Wave 2); `pydantic>=2.12` floor is set.
- `uv.lock` regenerated.
- `just check && just test` clean.
</success_criteria>

<output>
Create `.planning/phases/05-pydanticai-migration/05-05-SUMMARY.md` documenting:
- The exact diff of `pyproject.toml` (before/after `[project].dependencies`) for this plan's edit (langchain* + langgraph removal; pydantic floor bump).
- A pointer to Wave 2 / Plan 05-03 Task 0 for the `pydantic-ai` legitimacy gate result and Task 0b for the `uv add pydantic-ai` install diff.
- The pass/fail result of `just check` and `just test` against the post-swap manifest.
- Any transitively-pulled package version differences worth flagging for Phase 6 (e.g., a new transitive dep introduced by `pydantic-ai` that Phase 6's compose stack needs).
</output>
