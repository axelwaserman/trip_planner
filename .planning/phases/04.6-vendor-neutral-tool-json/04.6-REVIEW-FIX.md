---
phase: 04.6-vendor-neutral-tool-json
fixed_at: 2026-05-18T00:00:00Z
review_path: .planning/phases/04.6-vendor-neutral-tool-json/04.6-REVIEW.md
iteration: 1
findings_in_scope: 7
fixed: 7
skipped: 0
status: all_fixed
---

# Phase 04.6: Code Review Fix Report

**Fixed at:** 2026-05-18
**Source review:** `.planning/phases/04.6-vendor-neutral-tool-json/04.6-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope: 7 (CR-01, CR-02, WR-01, WR-02, WR-03, WR-04, WR-05)
- Fixed: 7
- Skipped: 0

All 120 backend tests passed after fixes (`just test`).

## Fixed Issues

### CR-01: `normalize_skyscanner_itinerary` crashes with `IndexError` on empty `legs`

**Files modified:** `backend/app/tools/flight_search.py`, `backend/tests/unit/test_tool_json_normalization.py`
**Commit:** 336f46a (flight_search.py), 6414bef (tests)
**Applied fix:** Added `legs = itin.get("legs", [])` and an early `if not legs: raise ValueError(...)` guard before accessing `legs[0]`. Added `test_skyscanner_empty_legs_raises` test to cover the new guard.

---

### CR-02: Empty `segments` list produces silent `ValidationError` instead of graceful error

**Files modified:** `backend/app/tools/flight_search.py`, `backend/tests/unit/test_tool_json_normalization.py`
**Commit:** 336f46a (flight_search.py), 6414bef (tests)
**Applied fix:** Added `if not leg_segments: raise ValueError(...)` guard after `leg_segments = leg.get("segments", [])`. Added `test_skyscanner_empty_segments_raises` test.

---

### WR-01: Hardcoded stale date in tool docstring misleads the LLM

**Files modified:** `backend/app/tools/flight_search.py`
**Commit:** 336f46a
**Applied fix:** Removed the `IMPORTANT: Today is November 13, 2025. Use dates in 2025 or 2026.` sentence from the `departure_date` argument docstring. The existing instruction to "calculate based on TODAY'S DATE" in the docstring body is sufficient.

---

### WR-02: `number_of_stops` assigned from leg level instead of per-segment data

**Files modified:** `backend/app/tools/flight_search.py`
**Commit:** 336f46a
**Applied fix:** Changed `number_of_stops=leg.get("stopCount", 0)` to `number_of_stops=seg.get("numberOfStops", leg.get("stopCount", 0))` so per-segment stop count is used when available, with leg-level count as fallback.

---

### WR-03: `ToolCallMetadata` and `ToolResultMetadata` redeclared locally in `ToolExecutionCard.tsx`

**Files modified:** `frontend/src/components/ToolExecutionCard.tsx`
**Commit:** 1152be6
**Applied fix:** Removed the two locally-defined `interface ToolCallMetadata` and `interface ToolResultMetadata` blocks (lines 7-19) and added them to the existing import from `../types/chat`. The `ToolExecutionCardProps` interface remains local (component-specific).

---

### WR-04: `MockLLM._astream` raises `StopIteration` when iterator is exhausted

**Files modified:** `backend/tests/fixtures/llm.py`
**Commit:** b1f2a35
**Applied fix:** Wrapped `next(self._streams_iter)` in a `try/except StopIteration` block that raises a `RuntimeError` with a clear diagnostic message explaining how to fix the test setup (add another inner list to `streams=`).

---

### WR-05: `test_amadeus_offer_normalizes_without_lossy_collapse` missing city field assertions

**Files modified:** `backend/tests/unit/test_tool_json_normalization.py`
**Commit:** 6414bef
**Applied fix:** Added two assertions at the end of the test:
```python
assert result.segments[0].departure.city == "LA"   # resolved from dictionaries, not raw IATA
assert result.segments[0].arrival.city == "NYC"    # resolved from dictionaries, not raw IATA
```
These verify that `normalize_amadeus_offer` actually resolves city codes from the `dictionaries.locations` lookup rather than falling back to raw IATA codes.

---

_Fixed: 2026-05-18_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
