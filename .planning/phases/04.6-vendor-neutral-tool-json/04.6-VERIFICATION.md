---
phase: 04.6-vendor-neutral-tool-json
verified: 2026-05-18T09:52:00Z
status: passed
score: 12/12 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: 11/12
  gaps_closed:
    - "ruff format --check passes on all modified files"
  gaps_remaining: []
  regressions: []
---

# Phase 4.6: Vendor-Neutral Tool JSON Verification Report

**Phase Goal:** Replace the search_flights() formatted-text return with a vendor-neutral structured JSON envelope (FlightSearchResult) that frontend components can render as structured data.
**Verified:** 2026-05-18T09:52:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (commit c812358 applied ruff format to test_tool_json_normalization.py)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | search_flights() returns a JSON-serialized FlightSearchResult envelope (string), not formatted text | VERIFIED | `result.model_dump_json()` at line 402 of flight_search.py; spot-check confirmed `starts with {: True`, `has results/status/count/query keys: True`. Old `"\n".join(result_lines)` grep returns 0. |
| 2 | Amadeus FlightOffer + dictionaries normalize into FlightSearchResult without lossy field collapses | VERIFIED | `normalize_amadeus_offer()` exists and is tested by `test_amadeus_offer_normalizes_without_lossy_collapse` + `test_amadeus_offer_carrier_name_comes_from_dictionaries`. Tests pass (12/12). |
| 3 | Skyscanner Itinerary normalizes into FlightSearchResult without lossy field collapses | VERIFIED | `normalize_skyscanner_itinerary()` exists and is tested by `test_skyscanner_itinerary_normalizes_without_lossy_collapse` + `test_skyscanner_price_decimal_precision`. Tests pass. |
| 4 | FlightSearchResult envelope has shape {status, query, results[], count} with IATA + city + optional terminal endpoints, ISO-8601 timestamps with timezone, segments[], price{amount,currency}, carrier IATA + display name | VERIFIED | `FlightSearchResult` class in models.py lines 265-282 has all required fields. Spot-check: `has status key: True`, `has count key: True`, `has query key: True`. `test_flight_search_result_envelope_shape` passes. |
| 5 | Mock-flight adapter (_to_flight_search_result) preserves all current Flight fields when wrapping list[Flight] into FlightSearchResult | VERIFIED | `_to_flight_search_result` at lines 60-117 of flight_search.py preserves price, IATA codes, datetimes, carrier name, booking_class (uppercased). `test_to_flight_search_result_preserves_flight_fields` and `test_to_flight_search_result_empty_list` both pass. |
| 6 | ChatService integration test still passes — tool_result events carry valid FlightSearchResult JSON | VERIFIED | `test_chat_service_flow.py` has `FlightSearchResult.model_validate_json(tool_result_event.tool_result)` assertion; all 3 integration tests pass. |
| 7 | ToolExecutionCard renders structured FlightSearchResult JSON as a Chakra v3 Table for results[], never as raw JSON blob | VERIFIED | `FlightResultsTable` subcomponent uses `Table.Root / Table.Header / Table.Body / Table.Row / Table.ColumnHeader / Table.Cell`. JSON-parse IIFE branches to it when `isFlightSearchResult` is satisfied. |
| 8 | ToolExecutionCard falls back to ReactMarkdown when full_result is not valid FlightSearchResult JSON | VERIFIED | IIFE catch block falls through to ReactMarkdown; confirmed by 3 out of 5 passing tests (non-JSON, shape-mismatch, loading-state paths). |
| 9 | Type guard isFlightSearchResult() narrows unknown JSON to FlightSearchResultData by structural shape check | VERIFIED | `isFlightSearchResult` at lines 12-22 of ToolExecutionCard.tsx checks status/results/count/query presence. |
| 10 | Frontend types in src/types/chat.ts include the structured FlightSearchResultData hierarchy | VERIFIED | All 7 interfaces (FlightEndpointData, CarrierInfoData, PriceInfoData, FlightSegmentData, FlightResultData, FlightSearchQueryData, FlightSearchResultData) present in chat.ts lines 53-100. PriceInfoData.amount is `string`; FlightEndpointData.terminal is `string \| null`. |
| 11 | The LLM still narrates results naturally — useChat.ts is unchanged | VERIFIED | No modification to useChat.ts; chat.py unchanged (tool result wrapped via `str()` which is identity on an already-str value). |
| 12 | ruff format --check passes on all modified backend files | VERIFIED | `uv run ruff format --check app/ tests/` exits 0 after commit c812358. Output: "39 files already formatted". |

**Score:** 12/12 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/app/models.py` | FlightEndpoint, CarrierInfo, PriceInfo, FlightSegment, FlightResult, FlightSearchQuery, FlightSearchResult | VERIFIED | All 7 classes present at lines 137-282. |
| `backend/app/tools/flight_search.py` | _to_flight_search_result, normalize_amadeus_offer, normalize_skyscanner_itinerary, model_dump_json() return | VERIFIED | All 5 functions present; `result.model_dump_json()` at line 402. |
| `backend/tests/unit/test_tool_json_normalization.py` | 12 tests, min 150 lines | VERIFIED | 12 tests (10 required + 2 edge-case extras), 373 lines. All 12 pass. |
| `frontend/src/types/chat.ts` | 7 FlightSearchResultData-hierarchy interfaces | VERIFIED | All 7 interfaces appended at lines 47-100. |
| `frontend/src/components/ToolExecutionCard.tsx` | isFlightSearchResult + FlightResultsTable + Table.Root | VERIFIED | All present at lines 12-82. |
| `frontend/src/components/__tests__/ToolExecutionCard.test.tsx` | 5 executed tests, min 100 lines | VERIFIED | 5 tests, 215 lines, all 5 pass. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `flight_search.py` | `app.models.FlightSearchResult` | `model_dump_json()` call before return | VERIFIED | Line 402: `return result.model_dump_json()` |
| `flight_search.py` | `FlightResult / FlightSegment / FlightEndpoint / CarrierInfo / PriceInfo` | `_to_flight_search_result`, `normalize_amadeus_offer`, `normalize_skyscanner_itinerary` | VERIFIED | Import at lines 11-22; all model classes used in adapter functions |
| `test_tool_json_normalization.py` | `app.tools.flight_search` | imports normalize functions | VERIFIED | Imports at lines 17-26 |
| `ToolExecutionCard.tsx` | `types/chat.ts` | `import type { FlightSearchResultData, FlightResultData }` | VERIFIED | Line 5 |
| `ToolExecutionCard.tsx` | Chakra v3 Table compound API | `Table.Root / Table.ColumnHeader / Table.Cell` | VERIFIED | Lines 44-78 |
| `ToolExecutionCard.test.tsx` | `ToolExecutionCard.tsx` | `from '../ToolExecutionCard'` | VERIFIED | Present in test file |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `ToolExecutionCard.tsx` | `resultMetadata.full_result` | SSE `tool_result` event from backend `search_flights()` | Yes — `_to_flight_search_result(flights, query).model_dump_json()` serialises real `MockFlightAPIClient` results | FLOWING |
| `FlightResultsTable` | `data: FlightSearchResultData` | JSON.parse of `full_result` after `isFlightSearchResult` guard | Yes — passes through real flight data from mock client | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| search_flights() returns `{` JSON string | `asyncio.run(search_flights.ainvoke({...}))` | `starts with {: True`, `has results/status/count/query keys: True` | PASS |
| All 12 unit tests pass | `uv run pytest tests/unit/test_tool_json_normalization.py -v` | 12 passed in 0.02s | PASS |
| All 3 integration tests pass | `uv run pytest tests/integration/test_chat_service_flow.py -v` | 3 passed in 0.09s | PASS |
| All 5 ToolExecutionCard tests pass | `npm test -- --run src/components/__tests__/ToolExecutionCard.test.tsx` | 5 passed | PASS |
| mypy strict passes | `uv run mypy app/` | Success: no issues found in 14 source files | PASS |
| ruff check passes | `uv run ruff check app/ tests/` | All checks passed | PASS |
| ruff format check | `uv run ruff format --check app/ tests/` | 39 files already formatted (exit 0) | PASS |
| TypeScript compiles | `npx tsc --noEmit` | No output (exit 0) | PASS |
| Full backend suite | `uv run pytest tests/unit/ tests/integration/ -q` | 119 passed in 5.08s | PASS |
| Full frontend suite | `npm test -- --run` | 59 passed (6 files passed, 3 skipped) | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| REQ-tool-json-output | Plan 01, Plan 02 | search_flights() returns structured JSON; endpoints carry IATA + city + optional terminal; ISO-8601 with TZ; segments[]; price{amount,currency}; carriers with IATA + display name; count + query echo; ToolExecutionCard renders as table not raw blob; Amadeus + Skyscanner fixtures normalize without lossy collapses; Google Flights fixture explicitly omitted with documented rationale | SATISFIED | All backend models, adapter functions, and frontend renderer verified above. Amadeus + Skyscanner normalization tests pass. Google Flights omission commented at line 118 of test_tool_json_normalization.py with `"Google Flights fixture omitted — no public REST API exists."` |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `backend/app/tools/flight_search.py` | 88, 93 | `# mock placeholder` comments on city field in `_to_flight_search_result` | INFO (intentional) | Documented stubs: mock client does not carry city names; Phase 7 Amadeus client will populate. Not a blocker — behaviour is correct and documented. |

### Human Verification Required

None — all critical behaviors are verifiable programmatically in this phase.

### Gaps Summary

No gaps. The single gap from the initial verification (ruff format check failure in test_tool_json_normalization.py) was closed by commit c812358 which applied `uv run ruff format` to the file. `uv run ruff format --check app/ tests/` now exits 0 (39 files already formatted). All 12/12 must-haves are verified.

---

_Verified: 2026-05-18T09:52:00Z_
_Verifier: Claude (gsd-verifier)_
