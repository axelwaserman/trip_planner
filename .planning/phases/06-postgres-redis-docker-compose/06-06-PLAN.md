---
phase: 06-postgres-redis-docker-compose
plan: 06
type: execute
wave: 5
depends_on:
  - 06-04
files_modified:
  - docker-compose.yml
  - .env.example
  - .gitignore
  - justfile
  - backend/scripts/seed.py
  - backend/scripts/__init__.py
  - backend/seed.toml.example
  - backend/tests/integration/db/test_seed_idempotent.py
  - scripts/test-compose-roundtrip.sh
  - README.md
  - CLAUDE.md
  - .planning/phases/06-postgres-redis-docker-compose/06-06-SUMMARY.md
autonomous: true
requirements:
  - REQ-postgres-redis-compose
  - REQ-p5-db-seed
tags:
  - docker-compose
  - dev-dx
  - seed
  - justfile
  - bootstrap
user_setup:
  - service: docker
    why: "Compose ships the `db` service that backend + tests depend on"
    dashboard_config:
      - task: "Confirm Docker Desktop / engine is running before `just compose-up`"
        location: "Local Docker Desktop / `docker info` returns non-empty"
      - task: "If Homebrew Postgres listens on host port 5432, stop it (`brew services stop postgresql@*`) OR accept the documented Pitfall 4 conflict"
        location: "Local shell — host-side Postgres port"

must_haves:
  truths:
    - "`docker-compose.yml` at repo root exposes a single `db` service (postgres:16-alpine) with named volume `pgdata`, healthcheck `pg_isready -U trip_planner`, and port 5432:5432"
    - "`just compose-up`, `just compose-down`, `just compose-logs`, `just db-shell`, `just migrate`, `just migrate-create MSG`, `just db-seed` recipes exist and execute the documented commands"
    - "`.env.example` is committed and exposes `DATABASE_URL`, `POSTGRES_PASSWORD`, `JWT_SECRET` keys with placeholder values; `.env` remains gitignored"
    - "`backend/seed.toml.example` is committed with placeholder dev creds; `backend/seed.toml` is gitignored"
    - "`backend/scripts/seed.py` parses `seed.toml` via stdlib `tomllib`, hashes via pwdlib argon2, and upserts users via `INSERT ... ON CONFLICT (username) DO UPDATE`; running `just db-seed` twice yields the same row count and refreshes hashes"
    - "`scripts/test-compose-roundtrip.sh` proves data persists across `docker compose down` + `up` (write a row → restart → read the same row); exit 0 on success"
    - "`scripts/test-compose-roundtrip.sh` is executable (`chmod +x`) and fails loudly if `docker info` returns non-zero"
    - "README.md and CLAUDE.md document the new compose-driven boot path (`just compose-up && just migrate && just db-seed && just backend && just frontend`) while preserving the existing fast-iteration `just install && just backend` direct path"
    - "D-04: compose ships only the `db` service (no Redis); v1 documented as single-replica; ABC seams from Plan 06-03 keep the door open for `RedisMessageStore` / `RedisConversationRepository` without ChatService rewrites"
    - "D-08: `just db-seed` runs `backend/scripts/seed.py` which is idempotent (`INSERT ... ON CONFLICT DO UPDATE`); `seed.toml` is gitignored; `seed.toml.example` is committed with placeholder creds; argon2 hashing happens at seed time via pwdlib so plaintext passwords never persist"
    - "D-09: no password migration — `EnvUserRepository` drop is a clean break (no production users yet); `seed.toml.example` ships dev creds for fresh checkouts"
    - "D-10: `docker-compose.yml` at repo root matches the CONTEXT D-10 shape — `postgres:16-alpine` image, envs `POSTGRES_USER=trip_planner`, `POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-trip_planner}`, `POSTGRES_DB=trip_planner`, named volume `pgdata`, healthcheck `pg_isready -U trip_planner` (5s/3s/5-retries); host port binding is `${POSTGRES_HOST_PORT:-5432}:5432` (RESOLVED OQ-1: keep D-10 default with override env var for Pitfall 4)"
    - "D-11 (recipes): `just migrate` and `just migrate-create MSG` justfile recipes exist and call `alembic upgrade head` / `alembic revision --autogenerate -m \"$MSG\"` against the host `uv` env (compose only runs the DB container)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Single-service db compose stack (postgres:16-alpine + pgdata volume + pg_isready healthcheck)"
      contains: "services:\\n  db:\\n    image: postgres:16-alpine"
    - path: ".env.example"
      provides: "Committed env template for DATABASE_URL, POSTGRES_PASSWORD, JWT_SECRET"
    - path: "backend/scripts/seed.py"
      provides: "Idempotent TOML→Postgres user upsert (REQ-p5-db-seed)"
      exports: ["seed"]
    - path: "backend/seed.toml.example"
      provides: "Committed placeholder seed; the real `seed.toml` is gitignored"
    - path: "scripts/test-compose-roundtrip.sh"
      provides: "Acceptance script proving data persistence across compose restart"
    - path: "justfile"
      provides: "compose-up, compose-down, compose-down-clean, compose-logs, db-shell, migrate, migrate-create MSG, db-seed recipes"
    - path: "README.md"
      provides: "Quickstart now references `just compose-up && just migrate && just db-seed`"
  key_links:
    - from: "justfile::db-seed"
      to: "backend/scripts/seed.py"
      via: "cd backend && uv run python scripts/seed.py"
      pattern: "uv run python scripts/seed\\.py"
    - from: "backend/scripts/seed.py"
      to: "backend/seed.toml"
      via: "tomllib.loads(seed_path.read_text())"
      pattern: "tomllib\\.loads"
    - from: "backend/scripts/seed.py"
      to: "Settings.database_url"
      via: "create_async_engine(settings.database_url)"
      pattern: "create_async_engine\\(settings\\.database_url"
    - from: "scripts/test-compose-roundtrip.sh"
      to: "docker-compose.yml"
      via: "docker compose up -d --wait + docker compose down + docker compose up"
      pattern: "docker compose (up|down)"
---

<objective>
Ship the developer-experience layer that makes Phase 6 boot from a fresh checkout: docker-compose with a `db` service, justfile recipes that wrap the lifecycle, an idempotent `scripts/seed.py` for the user table, and an acceptance script that proves data survives `docker compose down && up`. Document the new boot path in README.md and CLAUDE.md while preserving the legacy fast-iteration direct path.

Purpose: Plans 01-04 created the persistence layer; Plan 05 reshaped the API surface. This plan closes the loop so a developer (or CI) can run the entire stack without bespoke setup. ROADMAP success criterion #1 (data persists across compose restart) is locked here. REQ-p5-db-seed (the Phase 5 carry-forward) lands here because the `PostgresUserRepository` from Plan 04 needs a row in the `user` table before any login attempt — `just db-seed` is that bootstrap.
Output: `docker-compose.yml`, `.env.example`, `seed.toml.example`, `backend/scripts/seed.py`, `scripts/test-compose-roundtrip.sh`, justfile recipe additions, README + CLAUDE.md updates, idempotent-seed integration test.
</objective>

<execution_context>
@/Users/axel/code/trip_planner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/trip_planner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-postgres-redis-docker-compose/06-CONTEXT.md
@.planning/phases/06-postgres-redis-docker-compose/06-RESEARCH.md
@.planning/phases/06-postgres-redis-docker-compose/06-PATTERNS.md
@.planning/phases/06-postgres-redis-docker-compose/06-VALIDATION.md
@.planning/phases/06-postgres-redis-docker-compose/06-04-SUMMARY.md
@CLAUDE.md
@README.md
@justfile
@backend/app/config.py
@backend/app/auth/repository.py
@.gitignore

<interfaces>
<!-- D-10 docker-compose shape (CONTEXT.md verbatim, with Pitfall 4 note in research): -->
- service `db`: image `postgres:16-alpine`; envs `POSTGRES_USER=trip_planner`, `POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-trip_planner}`, `POSTGRES_DB=trip_planner`; ports `["${POSTGRES_HOST_PORT:-5432}:5432"]` (Pitfall 4 — collides with Homebrew Postgres on host; override via `POSTGRES_HOST_PORT=5433` in `.env`; document in README); volumes `[pgdata:/var/lib/postgresql/data]`; healthcheck cmd `pg_isready -U trip_planner` interval 5s timeout 3s retries 5
- top-level volume: `pgdata` (named — preserves data across `docker compose down`)

<!-- .env.example (committed; .env stays gitignored). The JWT_SECRET value MUST be a fresh random string distinct from `_DEFAULT_JWT_SECRET = "changeme"` so Phase 6 deploy invalidates Phase 4.2 / Phase 5 tokens per ROADMAP. The ROADMAP "rotate jwt_secret" step is satisfied here as an env-var swap (no code change per RESEARCH §7). -->
- key `DATABASE_URL` default `postgresql+psycopg://trip_planner:trip_planner@localhost:5432/trip_planner`
- key `POSTGRES_PASSWORD` default `trip_planner`
- key `POSTGRES_HOST_PORT` commented-out default (`# POSTGRES_HOST_PORT=5433  # uncomment if host port 5432 is taken (e.g. Homebrew Postgres) — Pitfall 4`)
- key `JWT_SECRET` placeholder `replace-with-random-48-byte-token` with comment hint: generate via `python -c "import secrets; print(secrets.token_urlsafe(48))"`

<!-- seed.toml.example shape (RESEARCH §Pattern 4): -->
- `[[users]]` blocks: `username`, `password`, `disabled` keys; minimum two entries (`admin`, `demo`) with placeholder password text `<change-me>`

<!-- justfile recipes (D-08, D-10, D-11; PATTERNS.md §justfile (extend)): -->
- `compose-up`         → `docker compose up -d --wait`
- `compose-down`       → `docker compose down`
- `compose-down-clean` → `docker compose down -v`
- `compose-logs`       → `docker compose logs -f db`
- `db-shell`           → `docker compose exec db psql -U trip_planner -d trip_planner`
- `migrate`            → `cd backend && uv run alembic upgrade head`
- `migrate-create MSG` → `cd backend && uv run alembic revision --autogenerate -m "{{MSG}}"`
- `db-seed`            → `cd backend && uv run python scripts/seed.py`

<!-- seed.py shape (RESEARCH §Pattern 4 + non-local guard): -->
- stdlib `tomllib` (Python 3.13)
- pwdlib argon2 — REUSES `app.auth.repository._password_hasher` singleton so seeded hashes verify cleanly through `PostgresUserRepository.verify_password`
- bound-parameter SQL: `INSERT INTO "user" (username, hashed_password, disabled) VALUES (:u, :h, :d) ON CONFLICT (username) DO UPDATE SET hashed_password = EXCLUDED.hashed_password, disabled = EXCLUDED.disabled`
- aborts with returncode 2 if `settings.database_url` host is non-localhost AND `settings.seed_allow_non_local is False`

<!-- test-compose-roundtrip.sh acceptance flow (ROADMAP success criterion #1): -->
1. assert `docker info` succeeds
2. `docker compose up -d --wait`
3. `cd backend && uv run alembic upgrade head`
4. ensure `backend/seed.toml` exists (copy from .example if missing); `cd backend && uv run python scripts/seed.py`
5. `BEFORE = SELECT count(*) FROM "user"` (assert > 0)
6. `docker compose down` (preserves volume)
7. `docker compose up -d --wait`
8. `AFTER = SELECT count(*) FROM "user"` (assert == BEFORE)
9. exit 0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docker-compose.yml + .env.example + .gitignore guard + justfile recipes</name>
  <files>docker-compose.yml, .env.example, .gitignore, justfile</files>
  <read_first>
    - justfile (current — confirm `install`, `backend`, `frontend`, `test`, `check`, `fix` recipes; the new compose recipes append to the bottom of the file in their own commented block)
    - .gitignore (lines 42-44 already cover `.env`, `.env.local`, `*.env`; this task verifies — does NOT change — those entries; ADDS `backend/seed.toml` to the ignore list and confirms `backend/seed.toml.example` is NOT ignored)
    - .planning/phases/06-postgres-redis-docker-compose/06-CONTEXT.md (D-10 verbatim compose shape; D-11 alembic recipes; D-08 db-seed recipe)
    - .planning/phases/06-postgres-redis-docker-compose/06-RESEARCH.md (§docker-compose.yml — D-10 verbatim with Pitfall 4 note; §Justfile additions; §Pitfall 4 — port 5432 collision with host PG)
    - .planning/phases/06-postgres-redis-docker-compose/06-PATTERNS.md (§docker-compose.yml; §justfile (extend))
    - backend/app/config.py (`_DEFAULT_JWT_SECRET = "changeme"` constant — the .env.example placeholder MUST differ from this value)
    - CLAUDE.md (uv-only — alembic invoked via `uv run alembic`; `just` is the canonical task runner)
  </read_first>
  <action>
    Create `docker-compose.yml` at the repo root (NOT under `backend/`) with the D-10 shape from CONTEXT.md verbatim (single `db` service, postgres:16-alpine image, named `pgdata` volume, `pg_isready -U trip_planner` healthcheck on 5s/3s/5-retry intervals, port `${POSTGRES_HOST_PORT:-5432}:5432`). Include a top-of-file comment: "Phase 6 — Postgres-only compose. Backend + frontend run on host (just backend / just frontend). Pitfall 4: host port 5432 may collide with Homebrew Postgres — set `POSTGRES_HOST_PORT=5433` in `.env` to override, or `brew services stop postgresql@*` to free 5432."
    Create `.env.example` at the repo root with three keys:
      - `DATABASE_URL=postgresql+psycopg://trip_planner:trip_planner@localhost:5432/trip_planner`
      - `POSTGRES_PASSWORD=trip_planner`
      - `JWT_SECRET=replace-with-random-48-byte-token` (with a one-line comment immediately above noting the secrets.token_urlsafe(48) recipe).
      - Add a top-of-file comment block stating "Copy to .env (gitignored). Phase 6 introduces DATABASE_URL + POSTGRES_PASSWORD; JWT_SECRET MUST differ from the default `changeme` to invalidate any pre-Phase-6 tokens."
    Update `.gitignore` (appended at the bottom in a `# Phase 6 — seed file with dev creds` block):
      - `backend/seed.toml` — gitignored (real dev creds); confirm `backend/seed.toml.example` is NOT covered by any earlier rule via inspection; if a `*.toml` rule exists, add `!backend/seed.toml.example` directly below it.
    Update `justfile` (appended at the bottom in a `# Phase 6 — compose + migrations + seed` block):
      - `compose-up`: `docker compose up -d --wait` (compose v2.18+ flag waits for healthchecks)
      - `compose-down`: `docker compose down` (preserves named volume)
      - `compose-down-clean`: `docker compose down -v` (drops `pgdata` volume — destructive)
      - `compose-logs`: `docker compose logs -f db`
      - `db-shell`: `docker compose exec db psql -U trip_planner -d trip_planner`
      - `migrate`: `cd backend && uv run alembic upgrade head`
      - `migrate-create MSG`: `cd backend && uv run alembic revision --autogenerate -m "{{MSG}}"` (uses just's `{{MSG}}` interpolation)
      - `db-seed`: `cd backend && uv run python scripts/seed.py`
    DO NOT modify any existing recipes (`backend`, `frontend`, `test`, `test-unit`, `test-integration`, `test-e2e`, `lint`, `format`, `typecheck`, `check`, `fix`, `build`, `uat*`, `clean`) — they all stay so the legacy fast-iteration direct path survives per the user requirement.
  </action>
  <verify>
    <automated>test -f docker-compose.yml &amp;&amp; test -f .env.example &amp;&amp; grep -q '^DATABASE_URL=' .env.example &amp;&amp; grep -q '^POSTGRES_PASSWORD=' .env.example &amp;&amp; grep -q '^JWT_SECRET=' .env.example &amp;&amp; ! grep -q '^JWT_SECRET=changeme$' .env.example &amp;&amp; grep -q 'backend/seed.toml$' .gitignore &amp;&amp; just --list 2&gt;/dev/null | grep -E 'compose-up|compose-down|compose-logs|db-shell|migrate|migrate-create|db-seed' | wc -l | tr -d ' ' | grep -qx 8 &amp;&amp; docker compose config --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` exists at repo root and `docker compose config --quiet` exits 0 (compose schema is valid).
    - The compose `db` service uses image `postgres:16-alpine`, named volume `pgdata`, healthcheck command `pg_isready -U trip_planner`, port `${POSTGRES_HOST_PORT:-5432}:5432` (host port overrideable via `POSTGRES_HOST_PORT` env var to dodge Pitfall 4), and reads `POSTGRES_PASSWORD` from env with default `trip_planner`.
    - `.env.example` exists with `DATABASE_URL`, `POSTGRES_PASSWORD`, `JWT_SECRET` keys; the `JWT_SECRET` value is NOT literally `changeme` (the `_DEFAULT_JWT_SECRET` value from `app/config.py`).
    - `.gitignore` includes `backend/seed.toml` (and does NOT ignore `backend/seed.toml.example`).
    - `justfile` exposes eight new recipes: `compose-up`, `compose-down`, `compose-down-clean`, `compose-logs`, `db-shell`, `migrate`, `migrate-create`, `db-seed`. The verify grep counts 8 — `compose-down-clean` matches the `compose-down` alternative but grep prints each justfile line once, so the line count equals the number of distinct recipes printed by `just --list`.
    - All pre-existing recipes (`install`, `backend`, `frontend`, `test`, `test-unit`, `test-integration`, `test-e2e`, `lint`, `format`, `typecheck`, `check`, `fix`, `build`, `uat`, `uat-smoke`, `uat-visual`, `uat-baselines`, `clean`) remain unchanged (verified by `just --list` containing all of them).
  </acceptance_criteria>
  <done>Compose stack defined, environment template committed, justfile drives the lifecycle; legacy direct path preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Idempotent seed script + seed.toml.example + integration test</name>
  <files>backend/scripts/__init__.py, backend/scripts/seed.py, backend/seed.toml.example, backend/tests/integration/db/test_seed_idempotent.py</files>
  <read_first>
    - backend/app/config.py (post Plan 04 — confirm `database_url`, `seed_allow_non_local: bool = False` Settings fields; `_DEFAULT_JWT_SECRET` for the model_post_init pattern reference)
    - backend/app/auth/repository.py (post Plan 04 — confirm `_password_hasher = PasswordHash([Argon2Hasher()])` module-level singleton; reuse the SAME hasher constructor in seed.py to keep Argon2 parameters identical so seeded hashes verify cleanly through `PostgresUserRepository.verify_password`)
    - backend/app/db/models.py (post Plan 02 — `User` table column names: `username`, `hashed_password`, `disabled`)
    - backend/tests/integration/db/conftest.py (post Plan 02 — `pg_database_url` fixture; `postgresql_noproc` factory)
    - .planning/phases/06-postgres-redis-docker-compose/06-RESEARCH.md (§Pattern 4 — TOML + ON CONFLICT DO UPDATE verbatim; §Anti-Patterns — "Trusting `seed.toml` content as a security boundary"; §Security Domain — "Seed script run against prod" mitigation)
    - .planning/phases/06-postgres-redis-docker-compose/06-PATTERNS.md (§`backend/scripts/seed.py` (new) — idempotent seed; pwdlib usage from `_load_users_from_env`)
    - .planning/phases/06-postgres-redis-docker-compose/06-VALIDATION.md (Wave 0 Gaps — `tests/integration/db/test_seed_idempotent.py`)
    - CLAUDE.md (`uv` only; async-only I/O; mypy strict; ruff line length 120; never persist plaintext passwords)
  </read_first>
  <action>
    Create `backend/scripts/__init__.py` (empty file with a one-line module docstring noting "Backend one-shot scripts (seed, ad-hoc maintenance). Run via `cd backend && uv run python scripts/<script>.py`.").
    Create `backend/scripts/seed.py` per RESEARCH §Pattern 4 with the following structure:
      - Module docstring: idempotent dev/test user seed (Phase 6 — REQ-p5-db-seed). Reads `seed.toml`, hashes with pwdlib argon2 (identical constructor to `app.auth.repository._password_hasher`), and upserts via `INSERT ... ON CONFLICT (username) DO UPDATE`. Re-running yields the same row count and refreshes hashes/disabled flags. Aborts when `settings.database_url` host is non-localhost and `settings.seed_allow_non_local is False`.
      - imports: `import asyncio`, `import sys`, `import tomllib`, `from pathlib import Path`, `from urllib.parse import urlparse`, `from sqlalchemy import text`, `from sqlalchemy.ext.asyncio import create_async_engine`, `from app.auth.repository import _password_hasher` (REUSE the module-level singleton — Plan 04 guarantees it survives), `from app.config import settings`.
      - module-level constant: `_LOCAL_HOSTS: frozenset[str] = frozenset({"localhost", "127.0.0.1", "::1"})`.
      - `def _is_local(database_url: str) -> bool`: parse `database_url` via `urlparse`; return `parsed.hostname in _LOCAL_HOSTS` (treat None hostname as non-local conservatively).
      - `async def seed(toml_path: Path) -> int`:
          - if not `toml_path.exists()`: print to stderr `"seed file not found: {toml_path}"`; return 1.
          - parse `data = tomllib.loads(toml_path.read_text())`; `users = data.get("users", [])`. If empty, print `"no users to seed"` and return 0 (idempotent — empty TOML is a no-op).
          - if `not _is_local(settings.database_url) and not settings.seed_allow_non_local`: print `"refusing to seed against non-local database_url; set SEED_ALLOW_NON_LOCAL=true to override"` to stderr; return 2.
          - `engine = create_async_engine(settings.database_url)`; `async with engine.connect() as conn:` — for each entry, build the bound-parameter dict and execute the INSERT-ON-CONFLICT statement (parameter binds `:u`, `:h`, `:d`); `await conn.commit()`. `await engine.dispose()` on the way out.
          - return 0.
      - `def _resolve_seed_path(argv: list[str]) -> Path`: returns `Path(argv[1]) if len(argv) > 1 else Path("seed.toml")` (relative to `backend/` cwd).
      - `if __name__ == "__main__":` invokes `asyncio.run(seed(_resolve_seed_path(sys.argv)))` and exits with the returncode.
      - All function bodies typed for `mypy --strict`; line length ≤ 120; no module-level constants for tunables (use `settings.seed_allow_non_local`; `_LOCAL_HOSTS` is a contract not a tunable).
    Create `backend/seed.toml.example` (the COMMITTED template — gitignored counterpart `seed.toml` is created by developers via `cp seed.toml.example seed.toml`):
      - top-of-file comment: "Copy to seed.toml; replace passwords; run `just db-seed`. Phase 6 — REQ-p5-db-seed."
      - one `[[users]]` block per dev user — at minimum `admin` and `demo`, both `password = "<change-me>"` (the literal placeholder text — developers MUST replace before running seed; the seed script does NOT enforce password policy because dev creds are not a security boundary per RESEARCH §Anti-Patterns).
      - `disabled = false` on each entry.
    Create `backend/tests/integration/db/test_seed_idempotent.py` (uses Plan 02's `pg_database_url` fixture):
      - `test_seed_inserts_and_is_idempotent_on_re_run`: spin up an engine against `pg_database_url`, run `alembic upgrade head` (subprocess pattern from Plan 02 round-trip test); write a temporary `seed.toml` (in `tmp_path`) with one user `{"username":"seedtest","password":"pw","disabled":false}`; monkeypatch `settings.database_url = pg_database_url`; invoke `await seed(tmp_path / "seed.toml")` and assert returncode 0; query `SELECT count(*) FROM "user" WHERE username='seedtest'` returns 1; query `SELECT hashed_password FROM "user" WHERE username='seedtest'` and capture the hash; invoke `seed` AGAIN with the SAME TOML; assert row count is still 1 (no duplicate); assert hashed_password CHANGES (re-hashed via Argon2 random salt); assert it still verifies via `_password_hasher.verify("pw", hashed)`.
      - `test_seed_updates_disabled_flag_in_place`: seed with `disabled=false`, then re-write the TOML with `disabled=true` and re-seed; assert the existing row's `disabled` column is now `True` (ON CONFLICT DO UPDATE — disabled flag refresh).
      - `test_seed_aborts_against_non_local_database_url_without_override`: monkeypatch `settings.database_url = "postgresql+psycopg://x:y@example.com:5432/z"` and `settings.seed_allow_non_local = False`; invoke `seed`; assert returncode 2 AND no DB connection was attempted (cannot fully verify without a live non-local DB; assert via stderr capture that the abort message was printed).
      - `test_seed_returns_1_when_toml_missing`: invoke `seed(Path(tmp_path / "does-not-exist.toml"))`; assert returncode 1.
      - `test_seed_handles_empty_users_table`: write a TOML with no `[[users]]` blocks (just a `[meta]\nphase = "6"` table); invoke `seed`; assert returncode 0 and zero rows changed in the `user` table.
    Path-based selection: `tests/integration/db/test_seed_idempotent.py`; no `@pytest.mark.integration` marker.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -c "import scripts.seed; from pathlib import Path; assert callable(scripts.seed.seed); assert scripts.seed._is_local('postgresql+psycopg://x@localhost:5432/y') is True; assert scripts.seed._is_local('postgresql+psycopg://x@example.com:5432/y') is False; print('OK')" &amp;&amp; uv run mypy scripts/seed.py &amp;&amp; uv run ruff check scripts/seed.py &amp;&amp; test -f seed.toml.example &amp;&amp; uv run pytest tests/integration/db/test_seed_idempotent.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `backend/scripts/seed.py` parses via `tomllib`, hashes via `_password_hasher.hash(...)` (the same singleton `app.auth.repository` exposes — verified by import check), and upserts via parameterised `INSERT ... ON CONFLICT (username) DO UPDATE`.
    - The non-local guard returns exit code 2 when `database_url` host is not in `{localhost, 127.0.0.1, ::1}` and `seed_allow_non_local is False`.
    - `backend/seed.toml.example` is committed and contains at least one `[[users]]` block; `backend/seed.toml` is NOT committed (verified by `git check-ignore backend/seed.toml` returning the path).
    - All five integration tests in `test_seed_idempotent.py` pass against the `pg_database_url` fixture.
    - `mypy --strict scripts/seed.py` passes; `ruff check scripts/seed.py` passes.
    - `_password_hasher.verify("pw", hashed_after_seed)` returns True — proves Argon2 parameters are identical between seed and login paths.
  </acceptance_criteria>
  <done>Seed script idempotent, guarded against non-local execution, and proven by integration test; the .example template is committed.</done>
</task>

<task type="auto">
  <name>Task 3: Compose round-trip acceptance script + README + CLAUDE.md updates</name>
  <files>scripts/test-compose-roundtrip.sh, README.md, CLAUDE.md</files>
  <read_first>
    - README.md (full file — current quickstart instructions; identify the "Run" / "Setup" / "Getting Started" section that needs to grow the compose-driven path)
    - CLAUDE.md (full file — focus on the `## Commands` section at the top; the new compose recipes need a one-line listing alongside the existing `just install` / `just backend` entries)
    - docker-compose.yml (Task 1 output)
    - justfile (Task 1 output — the recipe names the script invokes)
    - backend/scripts/seed.py (Task 2 output — invocation surface for the script)
    - .planning/phases/06-postgres-redis-docker-compose/06-VALIDATION.md (Manual-Only Verifications: "docker compose up reproducibility" + "Restart preserves conversation data")
    - .planning/phases/06-postgres-redis-docker-compose/06-CONTEXT.md (D-10 — "Backend + frontend run on host (`just backend`, `just frontend`)" — preserve the legacy iteration path)
    - .planning/ROADMAP.md (Phase 6 success criterion #1 — `docker compose down && up` preserves data)
  </read_first>
  <action>
    Create `scripts/test-compose-roundtrip.sh` (run `mkdir -p scripts/` first; this directory exists at repo root only after this task lands):
      - shebang `#!/usr/bin/env bash`.
      - `set -euo pipefail` (fail loud, undefined vars are errors, pipe failures propagate).
      - top-of-file comment: "Phase 6 — REQ-postgres-redis-compose acceptance: data persists across compose restart. Exit 0 on success, non-zero on any step failure."
      - step 1: assert `docker info > /dev/null 2>&1` succeeds; on failure print "Docker daemon not running" to stderr and exit 1.
      - step 2: `docker compose up -d --wait` — boots `db` and waits for healthcheck.
      - step 3: `(cd backend && uv run alembic upgrade head)` — applies the Phase 6 schema.
      - step 4: ensure `backend/seed.toml` exists by copying from `.example` if missing (`[ -f backend/seed.toml ] || cp backend/seed.toml.example backend/seed.toml`); then `(cd backend && uv run python scripts/seed.py)`.
      - step 5: `BEFORE=$(docker compose exec -T db psql -U trip_planner -d trip_planner -tAc 'SELECT count(*) FROM "user"')`. Assert `[ "$BEFORE" -gt 0 ]`; on failure print "no users seeded" to stderr and exit 2.
      - step 6: `docker compose down` (preserves the `pgdata` volume — this is the assertion target).
      - step 7: `docker compose up -d --wait`.
      - step 8: `AFTER=$(docker compose exec -T db psql -U trip_planner -d trip_planner -tAc 'SELECT count(*) FROM "user"')`. Assert `[ "$BEFORE" = "$AFTER" ]`; on failure print "data did not persist: before=$BEFORE after=$AFTER" to stderr and exit 3.
      - step 9: print "compose round-trip OK: $AFTER users persisted across restart" and exit 0.
      - the script DOES NOT clean up afterward (developers expect their `pgdata` to survive); a separate `compose-down-clean` recipe handles destructive cleanup.
      - `chmod +x scripts/test-compose-roundtrip.sh` after creation.
    Update `README.md` to document the Phase 6 boot path. Add (or extend) a `## Quickstart` section near the top with two paths:
      - **Compose-driven (recommended; first run on a fresh checkout):** the seven-command sequence `cp .env.example .env` → edit JWT_SECRET → `cp backend/seed.toml.example backend/seed.toml` → edit dev creds → `just compose-up` → `just migrate` → `just db-seed` → `just backend` (one terminal) → `just frontend` (another terminal).
      - **Direct (legacy fast-iteration; assumes Postgres is reachable on localhost:5432):** `just install && just backend && just frontend` — preserved per D-10. Note the prerequisite that something MUST provide a Postgres on `localhost:5432` (compose, Homebrew, or a remote DB via `DATABASE_URL`).
      - Add a "Pitfall 4" callout noting the host-port-5432 collision with Homebrew Postgres + the resolution (`brew services stop postgresql@*` OR change compose port to `5433:5432` and update `DATABASE_URL`).
      - Add a "Compose round-trip test" subsection: `bash scripts/test-compose-roundtrip.sh` — proves data survives restart (ROADMAP success criterion #1).
    Update `CLAUDE.md` `## Commands` section: append the new compose + migrate + seed recipes to the existing `just` listing. Add ONE line per recipe with a concise description. Preserve the existing line ordering above; insert the new lines in a logical group (after `just install`, before `just check`).
  </action>
  <verify>
    <automated>test -x scripts/test-compose-roundtrip.sh &amp;&amp; bash -n scripts/test-compose-roundtrip.sh &amp;&amp; grep -q 'just compose-up' README.md &amp;&amp; grep -q 'just db-seed' README.md &amp;&amp; grep -q 'compose-up' CLAUDE.md &amp;&amp; grep -q 'db-seed' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/test-compose-roundtrip.sh` exists, is executable (`-x` permission bit set), and `bash -n` (syntax-check only) exits 0.
    - The script's nine logical steps are present (verified by grep for `docker compose up -d --wait`, `alembic upgrade head`, `python scripts/seed.py`, `docker compose down`, `count\\(\\*\\) FROM "user"`).
    - `README.md` documents BOTH the compose-driven and direct boot paths; the compose path mentions `just compose-up`, `just migrate`, `just db-seed` in order; the legacy path mentions `just install`.
    - `CLAUDE.md` `## Commands` section lists the new recipes alongside the existing ones; pre-existing commentary on `uv` only / async I/O / etc. is preserved verbatim.
    - Manual-only verification: a developer running `bash scripts/test-compose-roundtrip.sh` against a fresh checkout exits 0 (covered by VALIDATION.md §Manual-Only Verifications — phase gate, not in this plan's automated verify).
  </acceptance_criteria>
  <done>End-to-end acceptance script committed; documentation updated; both boot paths documented and the compose path is the recommended first run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host shell → docker daemon | `docker compose` spawns containers with the env vars in `.env`; the `POSTGRES_PASSWORD` value crosses |
| `.env` file → process memory | `pydantic-settings` loads `DATABASE_URL` (with password) and `JWT_SECRET` into `Settings`; secrets never log |
| `seed.toml` file → seed.py process | Plaintext dev passwords cross briefly to be Argon2-hashed; never written to DB |
| Compose DB port (5432) → host network | Bound port is reachable from any process on the host; localhost-only by default but no firewall rule |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Information Disclosure | `seed.toml` plaintext credentials in git | mitigate | `.gitignore` excludes `backend/seed.toml`; only the `.example` placeholder ships. Task 1's verify gate asserts `git check-ignore backend/seed.toml` returns the path. The seed script does NOT enforce password policy because dev creds are not a security boundary — the gate is checkout-time hygiene. |
| T-06-06-02 | Tampering | `scripts/seed.py` SQL upsert | mitigate | Bound parameters (`:u`, `:h`, `:d`) prevent SQL injection; no f-string SQL. The non-local guard (`_is_local` + `seed_allow_non_local`) prevents accidental invocation against a non-localhost `DATABASE_URL` (RESEARCH §Anti-Pattern "Trusting `seed.toml` content as a security boundary"). |
| T-06-06-03 | Information Disclosure | Compose `db` port 5432 exposed on host | accept | Local dev only. The bind is `5432:5432` (D-10 verbatim) — listens on `0.0.0.0:5432` by default in Docker. Production deploy MUST remove the `ports:` block (documented in README's "Compose round-trip test" subsection). For Phase 6 v1 this is acceptable because production deploy is out of phase scope; documenting the removal step closes the audit trail. |
| T-06-06-04 | Repudiation | JWT_SECRET rotation | mitigate | `.env.example` ships a placeholder distinct from `_DEFAULT_JWT_SECRET = "changeme"`. Developers who copy `.env.example` to `.env` and edit the value invalidate any pre-Phase-6 JWTs (ROADMAP requirement). The Settings `model_post_init` warning that fires on the default value is preserved from earlier phases — provides a runtime hint when the rotation step is missed. |
| T-06-06-05 | Spoofing | seed.py reuses `_password_hasher` from `app.auth.repository` | mitigate | Importing the module-level singleton GUARANTEES Argon2 parameters (memory cost, time cost, parallelism) match between seed-time hashing and login-time verification. Test `test_seed_inserts_and_is_idempotent_on_re_run` asserts `_password_hasher.verify("pw", hashed_after_seed)` returns True — locks the parameter alignment. |
| T-06-06-06 | Denial of Service | Compose healthcheck flapping | accept | `pg_isready -U trip_planner` runs every 5s with 3s timeout and 5 retries. If Postgres fails the healthcheck the container restarts; backend on host gets connection-refused errors and bubbles them up via the existing FastAPI error path. No automated mitigation — Phase 6 v1 is single-replica dev. |
| T-06-06-07 | Tampering | `compose-down-clean` data loss | mitigate | The destructive `-v` flag is on a SEPARATE recipe (`compose-down-clean`) — the default `compose-down` preserves the volume. Task 1 verify asserts both recipes exist; documentation in README's Pitfall 4 callout warns that `compose-down-clean` drops data. |
| T-06-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all dependencies pinned in Plan 01. Audit table covers them. |
</threat_model>

<verification>
- `docker compose config --quiet` exits 0 (compose schema valid).
- `cd backend && uv run pytest tests/integration/db/test_seed_idempotent.py -x --tb=short` passes against a live compose db.
- `cd backend && uv run mypy scripts/seed.py app/` passes; `cd backend && uv run ruff check scripts/seed.py app/` passes.
- `bash -n scripts/test-compose-roundtrip.sh` exits 0 (script syntax valid).
- Manual: `bash scripts/test-compose-roundtrip.sh` exits 0 against a fresh checkout (proves ROADMAP success criterion #1; gate is the phase /gsd:verify-work, not this plan's automated verify).
- `grep -q 'just compose-up' README.md && grep -q 'just db-seed' README.md` (documentation gate).
- `grep -q 'compose-up' CLAUDE.md && grep -q 'db-seed' CLAUDE.md` (CLAUDE.md gate).
- `git check-ignore backend/seed.toml` returns the path (gitignore gate); `! git check-ignore backend/seed.toml.example` confirms the example is tracked.
</verification>

<success_criteria>
- `docker compose up -d --wait` boots a single `db` service that the host-side backend connects to via `postgresql+psycopg://localhost:5432`.
- `just compose-up && just migrate && just db-seed && just backend` boots the full stack on a fresh checkout (with `.env` and `backend/seed.toml` copied from their `.example` siblings).
- `scripts/test-compose-roundtrip.sh` proves data survives `docker compose down && up` — ROADMAP success criterion #1 locked by acceptance script.
- `seed.py` is idempotent (re-running yields the same row count, refreshes hashes), guarded against non-local execution, and reuses the same Argon2 hasher as `PostgresUserRepository` so seeded passwords verify cleanly through `POST /api/auth/token`.
- `.env.example` documents the JWT_SECRET rotation step; `backend/seed.toml.example` ships placeholder dev creds with a `<change-me>` literal.
- README + CLAUDE.md document both the compose-driven and legacy-direct boot paths; the compose path is recommended for first runs.
- `backend/seed.toml` is gitignored; the `.example` template is committed.
</success_criteria>

<output>
Create `.planning/phases/06-postgres-redis-docker-compose/06-06-SUMMARY.md` when done.
</output>
