---
phase: 06-postgres-redis-docker-compose
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/app/db/__init__.py
  - backend/app/db/session.py
  - backend/app/config.py
  - backend/tests/unit/db/__init__.py
  - backend/tests/unit/db/test_db_session.py
  - .planning/adrs/ADR-006-postgres.md
  - .planning/phases/06-postgres-redis-docker-compose/06-01-SUMMARY.md
autonomous: true
requirements:
  - REQ-postgres-redis-compose
tags:
  - postgres
  - psycopg
  - sqlmodel
  - alembic
  - spike
user_setup:
  - service: docker
    why: "Wave 0 spike requires a running Postgres for the smoke test"
    dashboard_config:
      - task: "Confirm Docker daemon is running before executing the spike test (`docker info` returns non-empty)"
        location: "Local Docker Desktop / engine"

must_haves:
  truths:
    - "`uv lock` resolves with `psycopg[binary,pool]==3.3.4`, `sqlmodel==0.0.38`, `alembic==1.18.4`, and dev-dep `pytest-postgresql==8.1.0` pinned in pyproject.toml"
    - "`Settings.database_url`, `Settings.db_pool_size`, `Settings.db_pool_overflow`, `Settings.seed_allow_non_local` exist and are overridable via env vars"
    - "`app/db/session.py` exports a module-level `engine` (AsyncEngine), `_async_sessionmaker` (async_sessionmaker[AsyncSession]), and an async `get_session()` FastAPI dep yielding `sqlmodel.ext.asyncio.session.AsyncSession`"
    - "Spike smoke test opens a connection to a live Postgres 16 container via `postgresql+psycopg://`, executes `SELECT 1`, and closes cleanly with no `MissingGreenlet` warnings"
    - "ADR-006-postgres.md exists with status `Locked`, recording driver = psycopg async, ORM = SQLModel + SQLAlchemy 2.0, migrations = alembic, Redis = deferred"
    - "D-01: pre-phase spike confirms `postgresql+psycopg://` async + SQLModel + SQLAlchemy 2.0 compatibility; spike outputs are `app/db/session.py` and ADR-006 amendment"
    - "D-04: Redis is deferred; ADR-006 records the deferral and the ABC-seam rationale (D-05/D-06) keeps the door open for `RedisMessageStore` / `RedisConversationRepository` without ChatService rewrites"
    - "D-11 (deps): `alembic==1.18.4` is pinned in `pyproject.toml`; alembic init/configuration is owned by Plan 06-02"
  artifacts:
    - path: "backend/app/db/session.py"
      provides: "Async engine + sessionmaker + get_session FastAPI dep"
      exports: ["engine", "_async_sessionmaker", "get_session"]
    - path: "backend/app/db/__init__.py"
      provides: "db package marker"
    - path: "backend/tests/unit/db/test_db_session.py"
      provides: "Spike smoke test asserting AsyncSession round-trip against postgresql+psycopg"
    - path: ".planning/adrs/ADR-006-postgres.md"
      provides: "Locked architectural decision record for the Phase 6 DB stack"
    - path: "backend/pyproject.toml"
      provides: "Pinned new dependencies (psycopg, sqlmodel, alembic, pytest-postgresql)"
  key_links:
    - from: "backend/app/db/session.py"
      to: "backend/app/config.py::Settings.database_url"
      via: "create_async_engine(settings.database_url, ...)"
      pattern: "create_async_engine\\(settings\\.database_url"
    - from: "backend/app/db/session.py"
      to: "sqlmodel.ext.asyncio.session.AsyncSession"
      via: "async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)"
      pattern: "async_sessionmaker\\(.*class_=AsyncSession.*expire_on_commit=False"
---

<objective>
Wave 0 spike: pin dependencies, prove `postgresql+psycopg://` async + SQLModel + SQLAlchemy 2.0 work end-to-end against a live Postgres 16 container, ship the application's async session factory, and lock the architectural decision in ADR-006.

Purpose: Per D-01 (CONTEXT.md), the entire phase rests on this stack working. A failing spike here forces a fallback to `asyncpg` and reshapes Plans 02-06. By executing the spike before the schema and stores, downstream plans receive a working `engine` + `_async_sessionmaker` and a recorded driver decision.
Output: pinned deps in `pyproject.toml`, working `app/db/session.py`, passing smoke test, ADR-006 in Locked state.
</objective>

<execution_context>
@/Users/axel/code/trip_planner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/trip_planner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-postgres-redis-docker-compose/06-CONTEXT.md
@.planning/phases/06-postgres-redis-docker-compose/06-RESEARCH.md
@.planning/phases/06-postgres-redis-docker-compose/06-PATTERNS.md
@.planning/phases/06-postgres-redis-docker-compose/06-VALIDATION.md
@.planning/adrs/ADR-007-pydantic-ai.md
@CLAUDE.md
@backend/app/config.py
@backend/pyproject.toml

<interfaces>
<!-- Settings field shape executor must extend (current — backend/app/config.py): -->
- `class Settings(BaseSettings)` with `model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")`
- Existing fields include `jwt_secret`, `auth_users` (will be deleted in Plan 04), `cors_allowed_origins` validator.
- New required fields: `database_url: str`, `db_pool_size: int`, `db_pool_overflow: int`, `seed_allow_non_local: bool`.

<!-- AsyncSession import path (NOTE: SQLModel's subclass, NOT sqlalchemy's): -->
- `from sqlmodel.ext.asyncio.session import AsyncSession`
- `from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine`
- The application uses SQLModel's AsyncSession subclass so `.exec()` ergonomics match the sync API across the rest of the codebase.

<!-- Database URL format (RESEARCH §1, [CITED: docs.sqlalchemy.org/en/20/dialects/postgresql.html]): -->
- `postgresql+psycopg://trip_planner:trip_planner@localhost:5432/trip_planner`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin Phase 6 dependencies and add Settings DB knobs</name>
  <files>backend/pyproject.toml, backend/uv.lock, backend/app/config.py</files>
  <read_first>
    - backend/pyproject.toml (current dep block + existing dev-deps group)
    - backend/app/config.py (full file — Settings class shape, env_file config, model_post_init pattern, _DEFAULT_AUTH_USERS constant)
    - .planning/phases/06-postgres-redis-docker-compose/06-RESEARCH.md (§Standard Stack table; §Settings additions; §Package Legitimacy Audit table)
    - .planning/phases/06-postgres-redis-docker-compose/06-PATTERNS.md (§`backend/app/config.py` — add `database_url`, remove `auth_users` — note that `auth_users` removal is deferred to Plan 04 to keep this Wave 0 spike non-breaking)
    - CLAUDE.md (uv-only constraint; Tunable thresholds live on Settings; ABC over Protocol)
  </read_first>
  <action>
    Add runtime deps `psycopg[binary,pool]>=3.3.4`, `sqlmodel>=0.0.38`, `alembic>=1.18.4` to `backend/pyproject.toml` `[project] dependencies` (uv-managed).
    Add dev dep `pytest-postgresql>=8.1.0` to `[dependency-groups] dev`.
    Run `cd backend && uv lock` to regenerate `uv.lock`; commit both files.
    DO NOT delete `auth_users` from `Settings` in this plan — Plan 04 owns that deletion alongside `EnvUserRepository` removal so the running app stays bootable while Plan 02/03 land.
    Extend `app/config.py` `Settings` with four new fields:
      - `database_url: str = "postgresql+psycopg://trip_planner:trip_planner@localhost:5432/trip_planner"`
      - `db_pool_size: int = 5`
      - `db_pool_overflow: int = 10`
      - `seed_allow_non_local: bool = False`
    Place these in a `# Database (Phase 6 — D-01, D-11)` block immediately above the existing `provider_probe_timeout_seconds` knob to mirror the existing grouped-comment style.
    Do NOT add a `model_post_init` warning for these new fields in this plan; revisit during Plan 06 when the seed flow lands and `seed_allow_non_local` becomes load-bearing.
    Per the Package Legitimacy Audit in 06-RESEARCH.md (slopcheck OK + multi-million weekly downloads + official source repos for psycopg/sqlmodel/alembic/pytest-postgresql), no checkpoint:human-verify gate is required before install.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv lock --check &amp;&amp; uv run python -c "from app.config import settings; assert settings.database_url.startswith('postgresql+psycopg://'); assert settings.db_pool_size == 5; assert settings.db_pool_overflow == 10; assert settings.seed_allow_non_local is False; print('Settings OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '^\s*"(psycopg\[binary,pool\]|sqlmodel|alembic)' backend/pyproject.toml` returns three matches (one per package).
    - `grep -E '"pytest-postgresql' backend/pyproject.toml` returns one match in the dev group.
    - `uv lock --check` exits 0 (lockfile in sync with pyproject).
    - `Settings()` instantiation reads `DATABASE_URL`/`DB_POOL_SIZE`/`DB_POOL_OVERFLOW`/`SEED_ALLOW_NON_LOCAL` env vars when set (verified by passing one through and asserting the override took).
    - `auth_users` field is still present on `Settings` (its deletion belongs to Plan 04).
  </acceptance_criteria>
  <done>Dependencies pinned to research-verified versions; Settings has `database_url` + pool knobs + `seed_allow_non_local`; lockfile committed.</done>
</task>

<task type="auto">
  <name>Task 2: Create app/db/session.py async engine + sessionmaker + get_session dep</name>
  <files>backend/app/db/__init__.py, backend/app/db/session.py</files>
  <read_first>
    - backend/app/config.py (after Task 1 — confirm Settings has `database_url`, `db_pool_size`, `db_pool_overflow`)
    - backend/app/auth/repository.py (placeholder-dep + DI override pattern — Wave 1 will reuse this for `get_session`)
    - .planning/phases/06-postgres-redis-docker-compose/06-RESEARCH.md (§Pattern 1 — async session with sqlmodel.ext.asyncio.session.AsyncSession; §Pitfall 8 — expire_on_commit=False is mandatory)
    - .planning/phases/06-postgres-redis-docker-compose/06-PATTERNS.md (§`backend/app/db/session.py` — async engine + sessionmaker + FastAPI dep)
    - CLAUDE.md (async-only I/O; mypy --strict; ruff line length 120)
  </read_first>
  <action>
    Create `backend/app/db/__init__.py` (empty package marker; one-line module docstring noting "Phase 6 DB infrastructure — engine, models, session.").
    Create `backend/app/db/session.py` containing:
      - module docstring referencing D-01 spike + Pitfall 8 rationale for `expire_on_commit=False`
      - imports: `from collections.abc import AsyncGenerator`; `from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine`; `from sqlmodel.ext.asyncio.session import AsyncSession`; `from app.config import settings`
      - module-level `engine = create_async_engine(settings.database_url, pool_size=settings.db_pool_size, max_overflow=settings.db_pool_overflow, echo=settings.debug)`
      - module-level `_async_sessionmaker = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)`
      - `async def get_session() -> AsyncGenerator[AsyncSession, None]:` placeholder dep with body `async with _async_sessionmaker() as session: yield session`. Add a docstring noting it is the FastAPI request-scoped session dep, NOT the per-store sessionmaker (stores in Plan 03 receive `_async_sessionmaker` directly so they own session lifecycles independent of the request).
    Type the module to pass `mypy --strict` (no bare `# type: ignore`); the SQLModel AsyncSession import path is correct per RESEARCH §Pattern 1.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -c "from app.db.session import engine, _async_sessionmaker, get_session; from sqlalchemy.ext.asyncio import AsyncEngine; from sqlalchemy.ext.asyncio import async_sessionmaker as _asm; assert isinstance(engine, AsyncEngine); assert isinstance(_async_sessionmaker, _asm); assert callable(get_session); print('OK')" &amp;&amp; uv run mypy app/db/</automated>
  </verify>
  <acceptance_criteria>
    - `app/db/__init__.py` and `app/db/session.py` exist.
    - `engine`, `_async_sessionmaker`, `get_session` import cleanly from `app.db.session`.
    - `engine` is an `AsyncEngine` instance whose URL renders as `postgresql+psycopg://...` (assert via `str(engine.url)`).
    - `_async_sessionmaker.kw["expire_on_commit"] is False` (Pitfall 8 lock).
    - `mypy --strict app/db/` passes with zero errors.
    - `ruff check app/db/` passes with zero errors.
  </acceptance_criteria>
  <done>Async engine + sessionmaker + request-scoped get_session dep wired to Settings.</done>
</task>

<task type="auto">
  <name>Task 3: Spike smoke test against live Postgres + write ADR-006</name>
  <files>backend/tests/unit/db/__init__.py, backend/tests/unit/db/test_db_session.py, .planning/adrs/ADR-006-postgres.md</files>
  <read_first>
    - backend/app/db/session.py (just created — confirm export shape)
    - backend/tests/conftest.py + backend/tests/unit/llm/conftest.py (existing autouse / fixture conventions; path-based test layout)
    - .planning/phases/06-postgres-redis-docker-compose/06-RESEARCH.md (§Validation Architecture — Test Framework + Wave 0 Gaps; §Sources for ADR-006 citations)
    - .planning/adrs/ADR-007-pydantic-ai.md (existing ADR style — `Status: Locked` line at top, structured `Context / Decision / Consequences` sections)
    - .planning/phases/06-postgres-redis-docker-compose/06-CONTEXT.md (`<specifics>` — ADR-006 must capture: driver choice, event-log schema, ABC seams D-05/D-06, Redis deferral D-04)
    - CLAUDE.md (path-based test selection — no `pytest.mark.unit`/`integration`)
  </read_first>
  <action>
    Create `backend/tests/unit/db/__init__.py` (empty) and `backend/tests/unit/db/test_db_session.py`.
    The smoke test file MUST contain three tests, all `async def` running under `asyncio_mode = "auto"`:
      - `test_engine_url_uses_psycopg_async_dialect`: assert `str(engine.url).startswith("postgresql+psycopg://")` (synchronous; pure inspection — no DB needed).
      - `test_async_sessionmaker_disables_expire_on_commit`: assert `_async_sessionmaker.kw["expire_on_commit"] is False` (Pitfall 8 lock; pure inspection).
      - `test_async_session_select_one_round_trip`: open a session via `async with _async_sessionmaker() as session`, run `await session.execute(text("SELECT 1"))`, assert `.scalar_one() == 1`. This test connects to a live Postgres on `localhost:5432`. Skip with `pytest.skip("Postgres unavailable on localhost:5432 — start with `docker run --rm -p 5432:5432 -e POSTGRES_PASSWORD=trip_planner -e POSTGRES_USER=trip_planner -e POSTGRES_DB=trip_planner postgres:16-alpine`")` if `OperationalError` is raised on connect (use `try/except sqlalchemy.exc.OperationalError`).
    Path-based selection: file lives under `tests/unit/db/` per CLAUDE.md; no `@pytest.mark.unit` decorator.
    Create `.planning/adrs/ADR-006-postgres.md` mirroring the structure of ADR-007:
      - frontmatter / header line `Status: Locked` (date today)
      - sections: Context, Decision, Consequences, Alternatives Considered, References
      - Decision section MUST capture: driver = `psycopg[binary,pool]>=3.3.4` async via `postgresql+psycopg://`; ORM = `sqlmodel>=0.0.38` + `sqlalchemy>=2.0.50`; migrations = `alembic>=1.18.4`; event-log schema per CONTEXT.md D-02 (cite the row-shape verbatim); ABC seams per D-05 (`MessageStore`) + D-06 (`ConversationRepository`); Redis deferred per D-04
      - References: cite RESEARCH.md §Sources entries for SQLAlchemy 2.0 async docs, alembic async cookbook, SQLModel AsyncSession source, PydanticAI ModelMessagesTypeAdapter docs
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/unit/db/test_db_session.py -x --tb=short &amp;&amp; test -f ../.planning/adrs/ADR-006-postgres.md &amp;&amp; grep -q '^Status: Locked' ../.planning/adrs/ADR-006-postgres.md</automated>
  </verify>
  <acceptance_criteria>
    - All three tests in `tests/unit/db/test_db_session.py` exist and the first two pass deterministically without any DB.
    - `test_async_session_select_one_round_trip` either passes (when Postgres is up on `localhost:5432` with the spike credentials) or skips with the documented message (when it isn't) — never errors.
    - `ADR-006-postgres.md` exists with `Status: Locked`, references D-01/D-02/D-04/D-05/D-06, and cites SQLAlchemy + alembic + SQLModel official sources.
    - `mypy --strict tests/unit/db/` passes (or matches existing test-tree mypy posture).
  </acceptance_criteria>
  <done>D-01 spike answer recorded as a passing/skipping smoke test; ADR-006 locked; downstream plans can rely on `engine` + `_async_sessionmaker` working.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend process → Postgres TCP (psycopg async) | Database credentials cross this boundary in the connection string |
| `Settings` env loader → process memory | `DATABASE_URL` env var (may contain a password) is read into `Settings.database_url` and held for engine construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure | `app/db/session.py` engine repr | mitigate | SQLAlchemy's `AsyncEngine.__repr__` masks the password by default; do not log `engine` directly. The existing Phase 4.5 `ApiKeyScrubber` does NOT cover DB password patterns — note for Plan 06 to add a scrub pattern when the connection string flows through logs. |
| T-06-01-02 | Tampering | new dependencies (psycopg, sqlmodel, alembic, pytest-postgresql) | accept | Per Phase 6 RESEARCH §Package Legitimacy Audit, all four packages are >5 years old, multi-million weekly downloads, official source repos, slopcheck OK. No `[ASSUMED]`/`[SUS]` packages → no blocking checkpoint required. |
| T-06-01-SC | Tampering | npm/pip/cargo installs (none added in this plan beyond the audited Python packages) | mitigate | Audit table in RESEARCH.md is the gate; this plan only installs from the Approved row of that table. |
| T-06-01-03 | Repudiation | ADR-006 status drift | mitigate | ADR-006 includes a date-stamped `Status: Locked` line; future ADR transitions land via separate commits, preserving git history. |
| T-06-01-04 | Denial of Service | spike smoke test against `localhost:5432` | accept | Test skips cleanly when Postgres is absent; cannot wedge CI because path is `tests/unit/db/` and the connect-or-skip pattern matches Phase 4.5's auto-stub conftest. |
</threat_model>

<verification>
- `cd backend && uv lock --check` passes.
- `cd backend && uv run pytest tests/unit/db/test_db_session.py -x` passes (with Postgres up: 3/3; without: 2 pass + 1 skip).
- `cd backend && uv run mypy app/db/ app/config.py` passes under strict mode.
- `cd backend && uv run ruff check app/db/ app/config.py tests/unit/db/` passes.
- `grep -E "^Status:\s*Locked" .planning/adrs/ADR-006-postgres.md` matches.
- `python -c "from app.db.session import engine, _async_sessionmaker, get_session"` exits 0.
</verification>

<success_criteria>
- New deps pinned per RESEARCH §Standard Stack and present in `uv.lock`.
- `Settings.database_url`, `db_pool_size`, `db_pool_overflow`, `seed_allow_non_local` exposed and overridable via env vars.
- `app/db/session.py` exports a working async `engine`, `_async_sessionmaker` (with `expire_on_commit=False`), and `get_session` dep.
- Spike smoke test demonstrates the stack works against a live Postgres 16 container.
- ADR-006-postgres.md is `Status: Locked` and cross-references CONTEXT.md D-01/D-02/D-04/D-05/D-06.
</success_criteria>

<output>
Create `.planning/phases/06-postgres-redis-docker-compose/06-01-SUMMARY.md` when done.
</output>
