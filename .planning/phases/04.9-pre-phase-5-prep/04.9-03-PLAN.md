---
phase: "04.9"
plan: "03"
type: execute
wave: 3
depends_on:
  - "04.9-01"
  - "04.9-02"
files_modified:
  - frontend/src/hooks/useChat.ts
  - frontend/src/components/ChatInterface.tsx
  - frontend/src/components/Sidebar.tsx
autonomous: true
requirements:
  - REQ-frontend-bug-fixes

must_haves:
  truths:
    - "Switching provider/model on an empty session does NOT create a second new session when the provider and model are unchanged"
    - "ChatInterface.tsx uses theme tokens (bg.canvas, fg.primary, accent.solid, border.subtle) — no raw gray.50/blue.500 literals"
    - "Sidebar model-name text has overflow=hidden, textOverflow=ellipsis, whiteSpace=nowrap"
    - "Frontend unit tests pass unchanged"
    - "just check passes"
  artifacts:
    - path: "frontend/src/hooks/useChat.ts"
      provides: "empty-session guard in handleProviderChange; isStreamingThinking reset on tool_result"
      contains: "messages.length"
    - path: "frontend/src/components/ChatInterface.tsx"
      provides: "theme-token-correct styles"
      contains: "bg.canvas"
    - path: "frontend/src/components/Sidebar.tsx"
      provides: "overflow-safe model name text"
      contains: "textOverflow"
  key_links:
    - from: "frontend/src/hooks/useChat.ts"
      to: "session store"
      via: "getSessionSnapshot(sessionId).messages.length check before initSession"
      pattern: "messages\\.length"
---

<objective>
Fix three frontend bugs in batch A: (1) sidebar model-name overflow, (2) empty-session guard in handleProviderChange, (3) font/color harmonization in ChatInterface.tsx. Also apply the double-think fix (isStreamingThinking reset) here as it touches the same useChat.ts file.

Purpose: Removes visual and UX regressions before Phase 5. All fixes are surgical — no architectural changes.

Output: Updated useChat.ts, ChatInterface.tsx, Sidebar.tsx. All frontend tests still pass.
</objective>

<execution_context>
@/Users/axel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/axel/code/trip_planner/.planning/ROADMAP.md
@/Users/axel/code/trip_planner/.planning/phases/04.9-pre-phase-5-prep/04.9-RESEARCH.md
</context>

<interfaces>
<!-- Extracted from RESEARCH.md codebase inspection -->

From frontend/src/hooks/useChat.ts (read fully before editing):
- handleProviderChange: useCallback at line ~390; calls initSession unconditionally
- isStreamingThinking: local `let` variable declared inside sendMessage and inside retryLastTool
- case 'tool_result': branch at line ~622 — sets isStreamingAssistant = false but does NOT reset isStreamingThinking
- getSessionSnapshot(sessionId): returns session state including messages array

From frontend/src/components/ChatInterface.tsx (read fully before editing):
- Line 186: bg="gray.50" → bg="bg.canvas"
- Line 188: bg="white" → bg="bg.surface"
- Line 188: borderColor="gray.200" → borderColor="border.subtle"
- Line 191: fontSize="xl" fontWeight="bold" inline → use Heading with fontFamily="display"
- Line 194: color="gray.600" → color="fg.secondary"
- Lines 344+: bg="blue.500" color="white" (user bubbles) → bg="accent.solid" color="white"
- Lines 347+: bg="white" (assistant bubbles) → bg="bg.surface"
- Lines 347+: borderColor="gray.200" → borderColor="border.subtle"
- Lines 347+: color="gray.800" → color="fg.primary"
- Line 406: bg="white" (input bar) → bg="bg.surface"
- Line 406: borderColor="gray.200" → borderColor="border.subtle"
- Line 419: colorScheme="blue" → colorPalette="accent"

From frontend/src/components/Sidebar.tsx (read lines 212-260 before editing):
- The `{s.provider} · {s.model}` Text at line ~249 is missing overflow control props
- The parent Box has h="48px" — adding overflow props to the Text prevents content overflow

From frontend/src/theme/index.ts (read to confirm token names):
- Tokens in use: bg.canvas, bg.surface, fg.primary, fg.secondary, fg.muted, accent.solid, border.subtle
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: useChat.ts — empty-session guard + double-think isStreamingThinking reset</name>
  <files>
    frontend/src/hooks/useChat.ts
  </files>
  <read_first>
    frontend/src/hooks/useChat.ts — read fully; focus on handleProviderChange (~line 390), the sendMessage closure's case 'tool_result' branch (~line 622), and the retryLastTool closure's case 'tool_result' branch (search for the second occurrence)
    frontend/src/theme/index.ts — skip (not relevant to this task)
  </read_first>
  <action>
    Fix 1 — Empty-session guard in handleProviderChange (Bug 1 per REQ-frontend-bug-fixes):
    In `handleProviderChange`, before calling `initSession`, read the current session's message count via `getSessionSnapshot`. If the current provider and model match the incoming `provider` and `model` arguments AND the session has zero messages, return early without calling initSession. The guard condition is: `provider === currentProvider && model === currentModel && sessionId !== null && getSessionSnapshot(sessionId).messages.length === 0`. When provider OR model changes on an empty session, still call `initSession` (the user is switching provider — the backend needs to create a session bound to the new provider config). Exact check: `if (provider === currentProvider && model === currentModel && sessionId && getSessionSnapshot(sessionId).messages.length === 0) return`.
    Add `currentProvider` and `currentModel` to the `useCallback` dependency array for `handleProviderChange` if they are not already there.

    Fix 2 — Double-think isStreamingThinking reset (Bug 3 per REQ-frontend-bug-fixes):
    In the sendMessage closure's `handleStreamEvent` switch, find `case 'tool_result':` — it currently sets `isStreamingAssistant = false`. Add `isStreamingThinking = false` on the next line (before the `break`).
    Search for the second `case 'tool_result':` inside the `retryLastTool` closure and apply the identical fix there.
    There are exactly 2 occurrences per RESEARCH.md (one per closure). Verify with grep before editing.
  </action>
  <verify>
    <automated>cd /Users/axel/code/trip_planner/frontend && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>Frontend unit tests pass; handleProviderChange has the early-return guard; both tool_result case branches reset isStreamingThinking.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ChatInterface.tsx theme tokens + Sidebar.tsx overflow fix</name>
  <files>
    frontend/src/components/ChatInterface.tsx
    frontend/src/components/Sidebar.tsx
  </files>
  <read_first>
    frontend/src/components/ChatInterface.tsx — read fully; identify every bg=, borderColor=, color=, colorScheme= occurrence
    frontend/src/components/Sidebar.tsx — read lines 200-260; find the `{s.provider} · {s.model}` Text element
    frontend/src/theme/index.ts — read to confirm available semantic token names
  </read_first>
  <action>
    ChatInterface.tsx — Replace all hardcoded palette references with theme tokens per the mapping documented in RESEARCH.md Bug 2 section. Exact changes:
    - All `bg="gray.50"` → `bg="bg.canvas"`
    - All `bg="white"` → `bg="bg.surface"`
    - All `borderColor="gray.200"` → `borderColor="border.subtle"`
    - `color="gray.600"` → `color="fg.secondary"`
    - `color="gray.800"` → `color="fg.primary"`
    - `bg="blue.500" color="white"` (user message bubbles) → `bg="accent.solid" color="white"`
    - `colorScheme="blue"` on Button → `colorPalette="accent"` (Chakra v3 uses colorPalette, not colorScheme — see RESEARCH.md Pitfall 4)
    - For the header title: if it uses `fontSize="xl" fontWeight="bold"`, replace with a `Heading` element using `as="h1"` and `fontFamily="display"` if a display token exists in the theme; otherwise use `fontSize="xl" fontWeight="semibold"` with the fg.primary color token. Check theme/index.ts for the exact heading font token name before deciding.
    No functional changes — purely prop value substitution.

    Sidebar.tsx — Find the second `Text` element in the session row that renders `{s.provider} · {s.model}` (approximately line 249). Add these props to it: `overflow="hidden"` `textOverflow="ellipsis"` `whiteSpace="nowrap"`. The parent Box already has `h="48px"` — confirm it also has `overflow="hidden"` or `overflowY="hidden"`; if not, add `overflow="hidden"` to the Box as well to clip the row to its height.

    Run `cd /Users/axel/code/trip_planner && just check` after edits to verify TypeScript types pass.
  </action>
  <verify>
    <automated>cd /Users/axel/code/trip_planner && just check && cd frontend && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <done>just check passes; frontend tests pass; no raw gray.50/blue.500 literals remain in ChatInterface.tsx; Sidebar Text for provider·model has overflow control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend display | No new user input handling — pure rendering and hook logic changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.9.03-SC | Tampering | No new packages | accept | No frontend packages installed in this plan |
</threat_model>

<verification>
1. `cd /Users/axel/code/trip_planner/frontend && npm test -- --run` — all tests pass.
2. `just check` passes (TypeScript + ESLint).
3. `grep -n "gray\.50\|blue\.500\|gray\.200\|colorScheme" /Users/axel/code/trip_planner/frontend/src/components/ChatInterface.tsx` — returns empty or only comments.
4. `grep -n "textOverflow" /Users/axel/code/trip_planner/frontend/src/components/Sidebar.tsx` — returns a line.
5. `grep -n "messages\.length" /Users/axel/code/trip_planner/frontend/src/hooks/useChat.ts` — returns the guard line.
6. Searching `isStreamingThinking = false` in useChat.ts returns 2 occurrences in tool_result branches.
</verification>

<success_criteria>
- useChat.ts handleProviderChange has early-return guard for same provider/model on empty session
- Both tool_result branches in useChat.ts reset isStreamingThinking = false
- ChatInterface.tsx has no raw Chakra palette references (gray.50, blue.500, etc.); uses semantic tokens
- Sidebar.tsx provider·model Text has overflow control properties
- All frontend tests pass; just check passes
</success_criteria>

<output>
Create `.planning/phases/04.9-pre-phase-5-prep/04.9-03-SUMMARY.md` when done.
</output>
