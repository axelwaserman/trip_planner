---
phase: "04.9"
plan: "02"
type: execute
wave: 2
depends_on:
  - "04.9-01"
files_modified:
  - backend/app/auth/repository.py
  - backend/app/api/routes/auth.py
  - backend/app/api/main.py
  - backend/tests/unit/test_user_repository.py
  - backend/tests/unit/test_auth_di.py
autonomous: true
requirements:
  - REQ-user-repository

must_haves:
  truths:
    - "backend/app/auth/repository.py exports UserRepository Protocol and EnvUserRepository"
    - "get_current_user and login route receive UserRepository via Annotated[UserRepository, Depends(get_user_repository)]"
    - "Module-global _users_db dict in auth.py is removed"
    - "mypy --strict passes with no new type: ignore comments"
    - "Existing auth integration tests (test_auth_routes.py) continue to pass"
    - "New unit tests in test_user_repository.py pass"
  artifacts:
    - path: "backend/app/auth/repository.py"
      provides: "UserRepository Protocol, EnvUserRepository"
      contains: "class UserRepository(Protocol)"
    - path: "backend/tests/unit/test_user_repository.py"
      provides: "Unit tests for EnvUserRepository"
      contains: "def test_get_user"
    - path: "backend/tests/unit/test_auth_di.py"
      provides: "Unit tests for DI injection of UserRepository into get_current_user"
      contains: "def test_get_current_user"
  key_links:
    - from: "backend/app/api/routes/auth.py"
      to: "backend/app/auth/repository.py"
      via: "Annotated[UserRepository, Depends(get_user_repository)]"
      pattern: "Depends\\(get_user_repository\\)"
    - from: "backend/app/api/main.py"
      to: "backend/app/api/routes/auth.py"
      via: "app.dependency_overrides[get_user_repository]"
      pattern: "dependency_overrides\\[.*get_user_repository"
---

<objective>
Extract `UserRepository` Protocol + `EnvUserRepository` class from the module-global `_users_db` dict in `auth.py`. Wire via FastAPI DI (same pattern as `get_chat_service`). Remove the module-level dict. Write unit tests before the implementation (Wave 0 from RESEARCH.md was folded here since the tests are light).

Purpose: Phase 5 swaps `EnvUserRepository` for `PostgresUserRepository` by overriding `get_user_repository` in the lifespan — zero route changes needed.

Output: `auth/repository.py`, updated `auth.py`, updated `main.py`, two new test files.
</objective>

<execution_context>
@/Users/axel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/axel/code/trip_planner/.planning/ROADMAP.md
@/Users/axel/code/trip_planner/.planning/phases/04.9-pre-phase-5-prep/04.9-RESEARCH.md
@/Users/axel/code/trip_planner/.planning/phases/04.9-pre-phase-5-prep/04.9-01-SUMMARY.md
</context>

<interfaces>
<!-- Extracted from codebase for executor reference -->

From backend/app/api/routes/auth.py (current state, read before editing):
- _password_hasher: PasswordHash([Argon2Hasher()]) — module-level singleton; must move into EnvUserRepository
- load_users_from_env() -> dict[str, UserInDB] — move into EnvUserRepository.__init__
- _users_db: dict[str, UserInDB] — module-level; to be REMOVED
- verify_password(plain: str, hashed: str) -> bool — move into EnvUserRepository.verify_password
- get_current_user(token: Annotated[str, Depends(oauth2_scheme)]) -> User — gains UserRepository DI param
- login(form_data: ...) -> dict[str, str] — gains UserRepository DI param

Proposed interface (from RESEARCH.md):
  class UserRepository(Protocol):
      def get_user(self, username: str) -> UserInDB | None: ...
      def verify_password(self, plain: str, hashed: str) -> bool: ...

  class EnvUserRepository:
      def __init__(self) -> None:  # calls load_users_from_env logic internally
      def get_user(self, username: str) -> UserInDB | None:
      def verify_password(self, plain: str, hashed: str) -> bool:

DI placeholder pattern (mirrors get_chat_service in routes.py):
  def get_user_repository() -> UserRepository:
      raise RuntimeError("UserRepository not configured")
  # Override in main.py lifespan

From backend/app/api/main.py (current wiring pattern):
  app.dependency_overrides[routes.get_chat_service] = get_chat_service_override
  app.dependency_overrides[routes.get_llm_factory] = get_llm_factory_override
  # New: app.dependency_overrides[auth.get_user_repository] = lambda: _user_repo

From backend/app/auth/models.py (after Plan 01):
  class User(BaseModel): username, email, full_name, disabled
  class UserInDB(User): hashed_password
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for UserRepository and DI injection</name>
  <files>
    backend/tests/unit/test_user_repository.py
    backend/tests/unit/test_auth_di.py
  </files>
  <read_first>
    backend/app/api/routes/auth.py — read fully to understand current auth logic
    backend/app/auth/models.py — read for User/UserInDB shapes
    backend/tests/integration/test_auth_routes.py — read for existing auth test patterns
  </read_first>
  <behavior>
    test_user_repository.py:
    - test_get_user_returns_correct_userinfo: EnvUserRepository built with MONKEYPATCH of AUTH_USERS="alice:secret" → get_user("alice") returns UserInDB with username="alice"
    - test_get_user_returns_none_for_unknown: get_user("unknown") returns None
    - test_verify_password_correct: verify_password("secret", hashed) returns True where hashed was hashed from "secret"
    - test_verify_password_wrong: verify_password("wrong", hashed) returns False

    test_auth_di.py:
    - test_get_current_user_rejects_missing_user: a stub UserRepository that always returns None from get_user → get_current_user raises 401
    - test_get_current_user_accepts_valid_token: stub UserRepository returns a valid User for the username in the JWT → get_current_user returns the User
  </behavior>
  <action>
    Create `backend/tests/unit/test_user_repository.py`:
    - Import `EnvUserRepository` from `app.auth.repository` (this will fail until Plan 01 is done and the file is created in Task 2 of this plan)
    - Use monkeypatch or os.environ patching to control the AUTH_USERS env var
    - Test 4 behaviors listed above using the AAA pattern

    Create `backend/tests/unit/test_auth_di.py`:
    - Import `get_current_user` from `app.api.routes.auth`
    - Create a minimal stub class implementing the UserRepository Protocol
    - Test that `get_current_user` uses the injected repo, not a global dict
    - Use `pytest.mark.asyncio` or `anyio` for async tests (check existing test patterns in the integration suite)

    Run `cd /Users/axel/code/trip_planner/backend && uv run pytest tests/unit/test_user_repository.py tests/unit/test_auth_di.py -x` — tests MUST FAIL at import (RED phase, as UserRepository does not exist yet).
  </action>
  <verify>
    <automated>cd /Users/axel/code/trip_planner/backend && uv run pytest tests/unit/test_user_repository.py tests/unit/test_auth_di.py --tb=no -q 2>&1 | tail -5</automated>
  </verify>
  <done>Test files exist; running pytest shows import errors or assertion failures (RED) — not "passed".</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement UserRepository, wire DI, make tests GREEN</name>
  <files>
    backend/app/auth/repository.py
    backend/app/api/routes/auth.py
    backend/app/api/main.py
  </files>
  <read_first>
    backend/app/api/routes/auth.py — read fully (current state after Plan 01)
    backend/app/api/main.py — read fully (current lifespan wiring)
    backend/app/auth/models.py — read for UserInDB shape
    backend/tests/unit/test_user_repository.py — read the expectations
    backend/tests/unit/test_auth_di.py — read the expectations
  </read_first>
  <action>
    Step 1 — Create `backend/app/auth/repository.py`:
    - Define `UserRepository(Protocol)` with `get_user(self, username: str) -> UserInDB | None` and `verify_password(self, plain: str, hashed: str) -> bool`
    - Define `EnvUserRepository` class: `__init__` reads AUTH_USERS via the existing `load_users_from_env` logic (inline it or import the function — the function will be removed from auth.py after), `get_user` returns `self._users.get(username)`, `verify_password` delegates to `_password_hasher.verify(plain, hashed)`
    - Move `_password_hasher = PasswordHash([Argon2Hasher()])` into the module scope of `repository.py` (used by EnvUserRepository.verify_password)
    - Move or copy `load_users_from_env` logic into `EnvUserRepository.__init__` body
    - Add `from app.auth.models import UserInDB` import; add `from typing import Protocol`
    - Ensure `runtime_checkable=False` — the Protocol is structural only (mypy strict compliance)

    Step 2 — Update `backend/app/api/routes/auth.py`:
    - Add `from app.auth.repository import UserRepository` import
    - Add `get_user_repository() -> UserRepository` placeholder function that raises `RuntimeError("UserRepository not configured")` (mirrors `get_chat_service` pattern in routes.py)
    - Update `get_current_user` signature: add `repo: Annotated[UserRepository, Depends(get_user_repository)]` parameter; replace `_users_db.get(username)` with `repo.get_user(username)`
    - Update `login` function signature: add `repo: Annotated[UserRepository, Depends(get_user_repository)]`; replace `_users_db.get(form_data.username)` with `repo.get_user(form_data.username)`; replace `verify_password(form_data.password, ...)` with `repo.verify_password(form_data.password, ...)`
    - Remove `_users_db: dict[str, UserInDB] = load_users_from_env()` module-level line
    - Remove the `verify_password(plain: str, hashed: str) -> bool` standalone function
    - Remove `load_users_from_env` function definition (it now lives inside repository.py)
    - Keep `_password_hasher` removal clean — it moves to repository.py
    - Keep `User` and `UserInDB` class definitions in auth.py for now (they are also duplicated in auth/models.py — this duplication is intentional for Plan 02; Plan 05 or a later cleanup removes the duplication when Postgres lands)

    Step 3 — Update `backend/app/api/main.py` lifespan:
    - Import `auth` from `app.api.routes`
    - In the lifespan startup block, after the existing overrides, add:
      `_user_repo = EnvUserRepository()` (import `EnvUserRepository` from `app.auth.repository`)
      `app.dependency_overrides[auth.get_user_repository] = lambda: _user_repo`

    Run tests GREEN: `cd /Users/axel/code/trip_planner/backend && uv run pytest tests/unit/test_user_repository.py tests/unit/test_auth_di.py -x`
    Then run full suite check: `just check && just test-unit && just test-integration`
  </action>
  <verify>
    <automated>cd /Users/axel/code/trip_planner/backend && uv run pytest tests/unit/test_user_repository.py tests/unit/test_auth_di.py -x -q && cd /Users/axel/code/trip_planner && just check</automated>
  </verify>
  <done>Both new unit test files pass; just check passes; just test-integration passes (existing auth route tests continue to work).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AUTH_USERS env → UserInDB | Env var parsing; same logic as before, moved into EnvUserRepository.__init__ |
| JWT Bearer token → User | Unchanged; still jwt.decode then repo.get_user lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.9.02-A | Tampering | EnvUserRepository.verify_password | mitigate | verify_password MUST delegate to PasswordHash([Argon2Hasher()]).verify — same hasher as before; changing the hasher constructor would invalidate all stored hashed passwords |
| T-4.9.02-B | Spoofing | get_user_repository placeholder | mitigate | Placeholder raises RuntimeError if lifespan override is missing — fail-fast is correct; unauthenticated access is impossible before the override is registered |
| T-4.9.02-SC | Tampering | No new packages | accept | No new dependencies installed |
</threat_model>

<verification>
1. `cd /Users/axel/code/trip_planner/backend && uv run pytest tests/unit/test_user_repository.py tests/unit/test_auth_di.py -v` — all tests pass.
2. `grep "_users_db" /Users/axel/code/trip_planner/backend/app/api/routes/auth.py` — returns empty.
3. `grep "get_user_repository" /Users/axel/code/trip_planner/backend/app/api/main.py` — returns a line with `dependency_overrides`.
4. `just check` passes.
5. `just test-integration` passes (existing auth integration tests unchanged).
</verification>

<success_criteria>
- backend/app/auth/repository.py exists with UserRepository Protocol and EnvUserRepository
- auth.py has no _users_db module-level dict; get_current_user and login use repo.get_user and repo.verify_password
- main.py overrides get_user_repository with EnvUserRepository instance in lifespan
- Unit tests test_user_repository.py and test_auth_di.py both pass
- just check and just test-integration both pass
</success_criteria>

<output>
Create `.planning/phases/04.9-pre-phase-5-prep/04.9-02-SUMMARY.md` when done.
</output>
