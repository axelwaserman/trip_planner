---
phase: "04.9"
plan: "04"
type: execute
wave: 3
depends_on:
  - "04.9-01"
  - "04.9-02"
files_modified:
  - frontend/src/components/ProviderCard.tsx
  - backend/app/api/routes/routes.py
autonomous: true
requirements:
  - REQ-frontend-bug-fixes

must_haves:
  truths:
    - "Entering an invalid base URL in ProviderCard shows an inline error message before navigation"
    - "POST /api/providers/refresh always refreshes local models regardless of TTL age"
    - "GET /api/providers lazy-load TTL logic is unchanged"
    - "Frontend unit tests pass; just check passes"
  artifacts:
    - path: "frontend/src/components/ProviderCard.tsx"
      provides: "validateBaseUrl helper, baseUrlError state, Field.ErrorText display"
      contains: "validateBaseUrl"
    - path: "backend/app/api/routes/routes.py"
      provides: "refresh_providers endpoint with no TTL gating"
      contains: "refresh_providers"
  key_links:
    - from: "frontend/src/components/ProviderCard.tsx"
      to: "handleSave"
      via: "validateBaseUrl(baseUrl) called before onSave; return early if error"
      pattern: "validateBaseUrl"
    - from: "backend/app/api/routes/routes.py"
      to: "factory.refresh_local_models()"
      via: "refresh_providers endpoint calls refresh unconditionally"
      pattern: "refresh_local_models"
---

<objective>
Fix two remaining frontend/backend bugs: (4) settings URL error — invalid base URL shows an inline error in ProviderCard instead of silently navigating away; (5) LM Studio stale cache — POST /api/providers/refresh bypasses the TTL cache and always calls factory.refresh_local_models().

Purpose: Both bugs make the settings page unreliable. Users entering bad URLs get no feedback; model lists show stale data after ejecting a model.

Output: Updated ProviderCard.tsx with client-side URL validation state; updated routes.py with TTL bypass on the explicit refresh endpoint.
</objective>

<execution_context>
@/Users/axel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/axel/code/trip_planner/.planning/ROADMAP.md
@/Users/axel/code/trip_planner/.planning/phases/04.9-pre-phase-5-prep/04.9-RESEARCH.md
</context>

<interfaces>
<!-- Extracted from RESEARCH.md codebase inspection -->

From frontend/src/components/ProviderCard.tsx (read fully before editing):
- handleSave function at line ~237: calls onSave(kind, updated) and navigates; no URL validation
- Base URL Input: `<Input type="url" ... />` inside a Field.Root for local providers
- Field.Root already has Field.Label; needs Field.ErrorText for error display
- useState pattern already used in the file (read to confirm import)

URL validation helper (per RESEARCH.md Pattern 4):
  function validateBaseUrl(url: string): string | null
  - empty string → return null (empty is valid, use default)
  - try new URL(url): if protocol not in ['http:', 'https:'] → error message
  - if hostname not in ['localhost', '127.0.0.1', 'host.docker.internal'] → error message
  - catch → 'Invalid URL format'
  - success → return null

Field.ErrorText usage pattern (Chakra v3, from existing ProviderCard and SettingsProviders patterns):
  <Field.Root invalid={!!baseUrlError}>
    <Field.Label>Base URL</Field.Label>
    <Input ... />
    {baseUrlError && <Field.ErrorText>{baseUrlError}</Field.ErrorText>}
  </Field.Root>

From backend/app/api/routes/routes.py (read lines 490-540 before editing):
- refresh_providers endpoint at line ~488
- TTL gating at lines 508-516:
  stale = [name for name in _LOCAL_PROVIDER_NAMES if name not in timestamps or (now - timestamps[name]) > ttl]
  if stale: fresh = await factory.refresh_local_models()
    for name, models in fresh.items():
      if name not in stale: continue  ← this skips non-stale providers
- Fix: remove the stale filter; always call factory.refresh_local_models(); update all entries

The GET /api/providers lazy-load code (lines ~435-490 area) also uses TTL gating — do NOT touch that code path. Only the refresh_providers (POST) endpoint changes.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ProviderCard.tsx — client-side base URL validation with inline error</name>
  <files>
    frontend/src/components/ProviderCard.tsx
  </files>
  <read_first>
    frontend/src/components/ProviderCard.tsx — read fully; focus on handleSave function, the base URL Field.Root, and existing useState calls
    frontend/src/theme/index.ts — skip
  </read_first>
  <action>
    Step 1 — Add a `validateBaseUrl(url: string): string | null` function at module scope (above the component). Logic per RESEARCH.md Pattern 4: empty string → null; try new URL(url) → check protocol in ['http:', 'https:'] and hostname in ['localhost', '127.0.0.1', 'host.docker.internal']; catch any URL parse error → 'Invalid URL format'. Return null on success.

    Step 2 — Add `const [baseUrlError, setBaseUrlError] = useState<string | null>(null)` inside the component (alongside the existing useState calls).

    Step 3 — In `handleSave`, add at the top: `setBaseUrlError(null)`. Then, for local providers only (guard with `if (isLocal && baseUrl.trim().length > 0)` or equivalent): call `const urlErr = validateBaseUrl(baseUrl.trim()); if (urlErr) { setBaseUrlError(urlErr); return; }`. The early return prevents navigation to /app.

    Step 4 — Find the base URL Field.Root in JSX. Add the `invalid={!!baseUrlError}` prop to the Field.Root wrapper. Below the Input, add: `{baseUrlError && <Field.ErrorText>{baseUrlError}</Field.ErrorText>}`. Import `Field` from `@chakra-ui/react` if not already imported (check the existing imports in the file).

    Confirm that the `isLocal` flag or equivalent local-provider detection is already in the component (it controls whether the base URL input is shown). If the flag is named differently, read the component first and use the correct variable name.
  </action>
  <verify>
    <automated>cd /Users/axel/code/trip_planner && just check && cd frontend && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <done>just check passes; frontend tests pass; validateBaseUrl function exists in ProviderCard.tsx; Field.ErrorText is wired to baseUrlError state.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: routes.py — remove TTL gating from POST /api/providers/refresh</name>
  <files>
    backend/app/api/routes/routes.py
  </files>
  <read_first>
    backend/app/api/routes/routes.py — read lines 480-545; identify refresh_providers function body and the stale-filter block
  </read_first>
  <action>
    In `refresh_providers` (the POST endpoint), replace the TTL-gated stale-filter pattern with an unconditional refresh:

    Remove these lines (approximately 508-516):
      stale = [name for name in _LOCAL_PROVIDER_NAMES if name not in timestamps or (now - timestamps[name]) > ttl]
      if stale:
          fresh = await factory.refresh_local_models()
          for name, models in fresh.items():
              if name not in stale:
                  continue
              cache[name] = [] if models is None else models
              timestamps[name] = now

    Replace with:
      fresh = await factory.refresh_local_models()
      for name, models in fresh.items():
          cache[name] = [] if models is None else models
          timestamps[name] = now

    The `ttl`, `now` variables may no longer be needed in this function — remove any now-unused assignments. Keep the rest of the function unchanged (the entry-building loop that reads from cache).

    CRITICAL: Do NOT touch the GET /api/providers handler. That handler's TTL logic (the `needs_lazy_load` check or equivalent) must remain intact — it only calls refresh when cache is genuinely cold, not on every GET request.

    Run `just check` and `just test-unit && just test-integration` to verify.
  </action>
  <verify>
    <automated>cd /Users/axel/code/trip_planner && just check && just test-unit && just test-integration</automated>
  </verify>
  <done>just check passes; just test-unit && just test-integration pass; refresh_providers calls factory.refresh_local_models() unconditionally without TTL gating.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (base URL) → backend session create | Client-side validation mirrors backend SSRF guard; no new attack surface |
| POST /api/providers/refresh → local daemons | Already auth-protected; removing TTL means more frequent Ollama/LM Studio polling on explicit user request only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.9.04-A | Spoofing | validateBaseUrl client-side check | accept | Client-side validation is UX only; backend SessionCreateRequest._validate_base_url is the authoritative SSRF guard — it remains unchanged |
| T-4.9.04-B | DoS | refresh_providers no TTL | accept | Route is auth-protected; a logged-in user refreshing frequently is acceptable for a demo-grade app |
| T-4.9.04-SC | Tampering | No new packages | accept | No new dependencies installed |
</threat_model>

<verification>
1. `just check` passes.
2. `just test-unit && just test-integration` pass.
3. `cd /Users/axel/code/trip_planner/frontend && npm test -- --run` passes.
4. `grep -n "validateBaseUrl" /Users/axel/code/trip_planner/frontend/src/components/ProviderCard.tsx` returns a function definition line.
5. `grep -n "stale" /Users/axel/code/trip_planner/backend/app/api/routes/routes.py | grep "refresh_providers" ` — stale filter no longer inside refresh_providers.
6. `grep -n "needs_lazy_load\|stale" /Users/axel/code/trip_planner/backend/app/api/routes/routes.py` — TTL logic still present in GET /api/providers handler.
</verification>

<success_criteria>
- ProviderCard.tsx: invalid base URL shows Field.ErrorText and blocks save/navigate
- routes.py refresh_providers: calls factory.refresh_local_models() unconditionally
- GET /api/providers TTL logic is untouched
- just check, just test-unit, just test-integration all pass
- Frontend tests pass
</success_criteria>

<output>
Create `.planning/phases/04.9-pre-phase-5-prep/04.9-04-SUMMARY.md` when done.
</output>
