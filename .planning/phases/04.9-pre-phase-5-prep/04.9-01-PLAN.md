---
phase: "04.9"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/flights/__init__.py
  - backend/app/flights/models.py
  - backend/app/providers/__init__.py
  - backend/app/providers/models.py
  - backend/app/auth/__init__.py
  - backend/app/auth/models.py
  - backend/app/chat/models.py
  - backend/app/api/routes/routes.py
  - backend/app/chat/service.py
  - backend/app/tools/flight_client.py
  - backend/app/tools/flight_search.py
  - backend/tests/fixtures/flights.py
  - backend/tests/integration/test_chat_service_flow.py
  - backend/tests/unit/test_flight_models.py
  - backend/tests/unit/test_mock_client.py
  - backend/tests/unit/test_tool_json_normalization.py
  - backend/app/models.py
autonomous: true
requirements:
  - REQ-model-restructure

must_haves:
  truths:
    - "backend/app/models.py is deleted and does not exist on disk"
    - "just check passes with zero mypy errors and zero ruff errors"
    - "just test-unit && just test-integration both pass"
    - "All 9 production import sites reference domain modules, not app.models"
    - "All 5 test import sites reference domain modules, not app.models"
  artifacts:
    - path: "backend/app/flights/models.py"
      provides: "FlightQuery, Flight, FlightEndpoint, CarrierInfo, PriceInfo, FlightSegment, FlightResult, FlightSearchQuery, FlightSearchResult, BookingClass, SortBy"
      contains: "class FlightQuery"
    - path: "backend/app/providers/models.py"
      provides: "ProviderInfo, ProviderRefreshEntry, ProviderRefreshResponse, ProviderTestRequest, ProviderTestResponse"
      contains: "class ProviderInfo"
    - path: "backend/app/auth/models.py"
      provides: "User, UserInDB"
      contains: "class UserInDB"
    - path: "backend/app/chat/models.py"
      provides: "StreamEvent hierarchy + ChatRequest + SessionCreateRequest + ChatSessionInfo + all chat DTOs"
      contains: "class SessionCreateRequest"
  key_links:
    - from: "backend/app/tools/flight_search.py"
      to: "backend/app/flights/models.py"
      via: "from app.flights.models import ..."
      pattern: "from app\\.flights\\.models import"
    - from: "backend/app/api/routes/routes.py"
      to: "backend/app/chat/models.py + backend/app/providers/models.py"
      via: "from app.chat.models import ... / from app.providers.models import ..."
      pattern: "from app\\.chat\\.models import|from app\\.providers\\.models import"
---

<objective>
Split the 558-line monolithic `backend/app/models.py` into four domain modules: `auth/models.py`, `chat/models.py` (existing, gains new names), `providers/models.py`, `flights/models.py`. Delete `app.models` after updating all 9 production and 5 test import sites.

Purpose: Phase 5's `User` and `Message` SQLModels land in `auth/models.py` and `chat/models.py` respectively — no surgery on flight models required. Clean domain ownership before Postgres lands.

Output: Four domain model files, `app.models` gone, `just check` green, tests green.
</objective>

<execution_context>
@/Users/axel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/axel/code/trip_planner/.planning/PROJECT.md
@/Users/axel/code/trip_planner/.planning/ROADMAP.md
@/Users/axel/code/trip_planner/.planning/phases/04.9-pre-phase-5-prep/04.9-RESEARCH.md
</context>

<interfaces>
<!-- Key interfaces extracted from the codebase for executor reference. -->

From backend/app/models.py (the file being split — read it first):
- BookingClass, SortBy: Literal type aliases
- FlightQuery, FlightEndpoint, CarrierInfo, PriceInfo, FlightSegment, Flight, FlightResult, FlightSearchQuery, FlightSearchResult: Pydantic models (flights domain)
- ProviderInfo, ProviderRefreshEntry, ProviderRefreshResponse, ProviderTestRequest, ProviderTestResponse: Pydantic models (providers domain)
- SessionCreateRequest, SessionCreateError, ChatRequest, RetryRequest, ChatResponse, ChatSessionInfo, ChatSessionsListResponse, ChatHistoryMessage, ChatSessionHistoryResponse: Pydantic models (chat domain)
- Note: SessionCreateError references ProbeErrorCode from app.llm.errors — the import must follow it to providers/models.py

From backend/app/chat/models.py (already exists; gains the chat DTOs above):
- Existing exports: ContentEvent, ThinkingEvent, ToolCallEvent, ToolResultEvent, ErrorEvent, ErrorCode, StreamEvent (union alias)
- These stay in place — no move needed

From backend/app/api/routes/auth.py (imports from app.models NOT needed; User/UserInDB live there):
- User, UserInDB are currently defined in auth.py itself (NOT in app.models) — they move to auth/models.py

From backend/app/api/routes/routes.py line 18 — current import:
  from app.models import (
    ChatRequest, ChatSessionHistoryResponse, ChatSessionsListResponse,
    ProviderInfo, ProviderRefreshEntry, ProviderRefreshResponse,
    ProviderTestRequest, ProviderTestResponse, RetryRequest, SessionCreateRequest,
  )

After split becomes:
  from app.chat.models import (ChatRequest, ChatSessionHistoryResponse, ChatSessionsListResponse, RetryRequest, SessionCreateRequest)
  from app.providers.models import (ProviderInfo, ProviderRefreshEntry, ProviderRefreshResponse, ProviderTestRequest, ProviderTestResponse)
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create three new domain model files and extend chat/models.py</name>
  <files>
    backend/app/auth/__init__.py
    backend/app/auth/models.py
    backend/app/flights/__init__.py
    backend/app/flights/models.py
    backend/app/providers/__init__.py
    backend/app/providers/models.py
    backend/app/chat/models.py
  </files>
  <read_first>
    backend/app/models.py — read every line; this is the source file being split
    backend/app/chat/models.py — read fully; it already exports the StreamEvent hierarchy
    backend/app/api/routes/auth.py — read fully; User/UserInDB are defined here, not in app.models
    backend/app/api/main.py — check lifespan for any app.models references
  </read_first>
  <action>
    Step 1 — Create `backend/app/auth/__init__.py` (empty) and `backend/app/auth/models.py`.
    `auth/models.py` exports: `User`, `UserInDB`. Copy the two class definitions from `auth.py` verbatim — do NOT delete them from `auth.py` yet (that happens in Plan 02 when UserRepository is extracted). Import `BaseModel` from pydantic. No other imports needed.

    Step 2 — Create `backend/app/flights/__init__.py` (empty) and `backend/app/flights/models.py`.
    `flights/models.py` exports: `BookingClass`, `SortBy`, `FlightQuery`, `FlightEndpoint`, `CarrierInfo`, `PriceInfo`, `FlightSegment`, `Flight`, `FlightResult`, `FlightSearchQuery`, `FlightSearchResult`. Copy these 11 names verbatim from `app.models`. Keep all validators (`validate_iata_code`, `validate_dates`, `validate_origin_destination`, `validate_departure_date`, `validate_arrival_after_departure`). Standard header imports: `re`, `date`, `datetime`, `Decimal`, `Literal`, `Self`, `BaseModel`, `Field`, `field_validator`, `model_validator`.

    Step 3 — Create `backend/app/providers/__init__.py` (empty) and `backend/app/providers/models.py`.
    `providers/models.py` exports: `ProviderInfo`, `ProviderRefreshEntry`, `ProviderRefreshResponse`, `ProviderTestRequest`, `ProviderTestResponse`, `SessionCreateError`. Copy from `app.models`. Add `from app.llm.errors import ProbeErrorCode` — `SessionCreateError` references it. Do NOT move `SessionCreateRequest` here; it goes in `chat/models.py` (it is a chat-session creation DTO per RESEARCH.md recommendation).

    Step 4 — Extend `backend/app/chat/models.py`. Append to the end of the existing file: `SessionCreateRequest`, `SessionCreateError` (import `SessionCreateError` from `app.providers.models` to avoid duplication — OR copy it in full here if it does not reference flight or provider types). Check: does `SessionCreateError` reference `ProbeErrorCode`? Yes. Option: define it once in `providers/models.py` and import it into `chat/models.py` with `from app.providers.models import SessionCreateError`. Then add to `chat/models.py`: `ChatRequest`, `RetryRequest`, `ChatResponse`, `ChatSessionInfo`, `ChatSessionsListResponse`, `ChatHistoryMessage`, `ChatSessionHistoryResponse`. Copy these 8 names verbatim from `app.models`. Add the imports they need (`Optional`, `datetime`, etc.).

    Do NOT delete `backend/app/models.py` yet — that happens in Task 2 after all import sites are updated.

    Run `cd /Users/axel/code/trip_planner && uv run --project backend python -c "from app.flights.models import FlightQuery; from app.providers.models import ProviderInfo; from app.chat.models import SessionCreateRequest, ChatRequest; print('imports ok')"` to verify no import errors before proceeding.
  </action>
  <verify>
    <automated>cd /Users/axel/code/trip_planner/backend && uv run python -c "from app.flights.models import FlightQuery, Flight, FlightSearchResult; from app.providers.models import ProviderInfo, ProviderRefreshResponse; from app.chat.models import SessionCreateRequest, ChatRequest, ChatSessionInfo, RetryRequest; print('all domain imports ok')"</automated>
  </verify>
  <done>All three new domain model files exist and import cleanly; chat/models.py exports the new DTOs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update all 9 production + 5 test import sites; delete app/models.py</name>
  <files>
    backend/app/api/routes/routes.py
    backend/app/chat/service.py
    backend/app/tools/flight_client.py
    backend/app/tools/flight_search.py
    backend/tests/fixtures/flights.py
    backend/tests/integration/test_chat_service_flow.py
    backend/tests/unit/test_flight_models.py
    backend/tests/unit/test_mock_client.py
    backend/tests/unit/test_tool_json_normalization.py
    backend/app/models.py
  </files>
  <read_first>
    backend/app/api/routes/routes.py — read the full import block at lines 1-30
    backend/app/chat/service.py — read line 44 import
    backend/app/tools/flight_client.py — read line 10 import
    backend/app/tools/flight_search.py — read line 11 import
    backend/tests/fixtures/flights.py — read line 6
    backend/tests/integration/test_chat_service_flow.py — read line 10
    backend/tests/unit/test_flight_models.py — read line 9
    backend/tests/unit/test_mock_client.py — read line 9
    backend/tests/unit/test_tool_json_normalization.py — read line 8
  </read_first>
  <action>
    Update each file's `from app.models import ...` to the new domain paths. Apply each change:

    routes.py: Replace the single `from app.models import (...)` block with:
      `from app.chat.models import (ChatRequest, ChatSessionHistoryResponse, ChatSessionsListResponse, RetryRequest, SessionCreateRequest)`
      `from app.providers.models import (ProviderInfo, ProviderRefreshEntry, ProviderRefreshResponse, ProviderTestRequest, ProviderTestResponse)`

    chat/service.py line 44: Change `from app.models import ChatHistoryMessage, ChatSessionHistoryResponse, ChatSessionInfo` to `from app.chat.models import ChatHistoryMessage, ChatSessionHistoryResponse, ChatSessionInfo`.

    tools/flight_client.py: Change `from app.models import BookingClass, Flight, FlightQuery, SortBy` to `from app.flights.models import BookingClass, Flight, FlightQuery, SortBy`.

    tools/flight_search.py: Change `from app.models import (CarrierInfo, Flight, FlightEndpoint, FlightQuery, FlightResult, FlightSearchQuery, FlightSearchResult, FlightSegment, PriceInfo, SortBy)` to `from app.flights.models import (...)` with the same names.

    tests/fixtures/flights.py: Change `from app.models import Flight, FlightQuery` to `from app.flights.models import Flight, FlightQuery`.

    tests/integration/test_chat_service_flow.py: Change `from app.models import FlightSearchResult` to `from app.flights.models import FlightSearchResult`.

    tests/unit/test_flight_models.py: Change `from app.models import Flight, FlightQuery` to `from app.flights.models import Flight, FlightQuery`.

    tests/unit/test_mock_client.py: Change `from app.models import FlightQuery` to `from app.flights.models import FlightQuery`.

    tests/unit/test_tool_json_normalization.py: Change `from app.models import (...)` to `from app.flights.models import (...)` for all flight model names.

    After all sites are updated, run `grep -r "from app.models" /Users/axel/code/trip_planner/backend/` to confirm zero remaining references. Only then delete `backend/app/models.py`.

    Run `just check` from the repo root to verify mypy strict and ruff pass. Then run `just test-unit && just test-integration`.
  </action>
  <verify>
    <automated>grep -r "from app.models" /Users/axel/code/trip_planner/backend/ 2>/dev/null | grep -v "__pycache__" | wc -l</automated>
  </verify>
  <done>grep returns 0 lines; `just check` passes; `just test-unit && just test-integration` pass; backend/app/models.py does not exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import graph | Refactoring only — no new external inputs; trust boundary unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.9.01-SC | Tampering | No new packages installed | accept | No npm/pip/cargo installs in this plan; no slopcheck needed |
</threat_model>

<verification>
1. `grep -r "from app.models" /Users/axel/code/trip_planner/backend/ | grep -v __pycache__` returns empty output.
2. `ls /Users/axel/code/trip_planner/backend/app/models.py` returns "No such file".
3. `just check` passes (ruff + mypy strict).
4. `just test-unit && just test-integration` both pass.
</verification>

<success_criteria>
- backend/app/models.py is deleted
- Four domain model files exist and export the correct names
- All import sites updated; zero `from app.models` references remain
- mypy --strict passes with zero new ignores
- just check passes; just test-unit && just test-integration pass
</success_criteria>

<output>
Create `.planning/phases/04.9-pre-phase-5-prep/04.9-01-SUMMARY.md` when done.
</output>
