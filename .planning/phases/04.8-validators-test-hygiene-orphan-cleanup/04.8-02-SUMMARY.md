---
phase: 04.8-validators-test-hygiene-orphan-cleanup
plan: 02
subsystem: testing
tags: [test-hygiene, fixtures, dead-code, pytest, pydantic, sse]

# Dependency graph
requires:
  - phase: 04.8-01
    provides: "Three additive Pydantic v2 validators on FlightQuery and Flight (origin!=dest, departure>=today, arrival>departure)"
provides:
  - "create_mock_flight() and create_mock_flight_query() keyword-only factories in backend/tests/fixtures/flights.py"
  - "parse_sse_events(str|list[str]) -> list[StreamEvent] utility in backend/tests/utils/sse.py"
  - "9 unit tests for parse_sse_events covering all edge cases"
  - "Refactored test_flight_models.py using factories instead of inline Flight/FlightQuery constructions"
  - "Refactored test_chat.py using parse_sse_events instead of inline SSE list comprehension"
  - "Deleted frontend orphans ToolCallCard.tsx and ToolResultCard.tsx"
affects:
  - future-test-authoring (pattern established: use factories for Flight/FlightQuery, use parse_sse_events for SSE assertions)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Factory pattern for test domain objects (keyword-only, sensible defaults, override by name)"
    - "Central SSE parse helper for typed StreamEvent assertions in integration tests"

key-files:
  created:
    - backend/tests/fixtures/flights.py
    - backend/tests/utils/__init__.py
    - backend/tests/utils/sse.py
    - backend/tests/unit/test_sse_utils.py
  modified:
    - backend/tests/unit/test_flight_models.py
    - backend/tests/integration/test_chat.py
  deleted:
    - frontend/src/components/ToolCallCard.tsx
    - frontend/src/components/ToolResultCard.tsx

key-decisions:
  - "Refactored test_chat.py rather than test_session.py — the actual inline SSE pattern was in test_chat.py lines 121-123, not test_session.py (plan referenced older version)"
  - "Inline Flight/FlightQuery constructions kept inside pytest.raises() blocks for validator-rejection tests — factory overrides would obscure the intent of intentionally-invalid inputs"

patterns-established:
  - "Test factories: keyword-only factory functions with future-dated defaults that pass all current validators"
  - "SSE test helpers: parse_sse_events() converts raw response.text to typed list[StreamEvent] for attribute access"

requirements-completed:
  - REQ-test-fixture-dedup

# Metrics
duration: 7min
completed: 2026-05-19
---

# Phase 04.8 Plan 02: Centralise test scaffolding + delete frontend orphans Summary

**Keyword-only Flight/FlightQuery factory functions, a typed SSE parser utility, and deletion of two zero-caller frontend components; test_flight_models.py and test_chat.py refactored to consume them**

## Performance

- **Duration:** 7 min
- **Started:** 2026-05-19T03:14:44Z
- **Completed:** 2026-05-19T03:21:50Z
- **Tasks:** 2
- **Files modified:** 8 (4 created, 2 modified, 2 deleted)

## Accomplishments

- Created `create_mock_flight()` and `create_mock_flight_query()` factories with keyword-only signatures and future-dated defaults that satisfy the Plan 01 validators
- Created `parse_sse_events()` utility that converts raw SSE response text into a `list[StreamEvent]` — replacing the fragile `data_lines` list-comprehension pattern
- 9 unit tests for `parse_sse_events` covering happy path, list input, blank/keepalive skipping, typed return, empty input, malformed JSON, and Pydantic validation error propagation (TDD RED→GREEN with ruff formatting fix)
- Refactored `test_flight_models.py` to use factories for all non-validator-rejection tests (22 tests still green)
- Refactored `test_chat.py` to use `parse_sse_events()` (5 integration tests still green)
- Deleted `ToolCallCard.tsx` and `ToolResultCard.tsx` — confirmed zero callers via grep + tsc + production build

## Task Commits

1. **Task 1: Create fixture and utility modules (TDD)** - `1105045` (feat)
2. **Task 2: Refactor tests + delete frontend orphans** - `2babc9e` (refactor)

**Plan metadata:** (docs commit below)

_Note: Task 1 followed TDD: test file created first (RED: ImportError), then implementation files created (GREEN: 9 tests passing)_

## Files Created/Modified

- `backend/tests/fixtures/flights.py` — `create_mock_flight()` and `create_mock_flight_query()` factories
- `backend/tests/utils/__init__.py` — new test utility package init
- `backend/tests/utils/sse.py` — `parse_sse_events(str | list[str]) -> list[StreamEvent]` helper
- `backend/tests/unit/test_sse_utils.py` — 9 unit tests for `parse_sse_events`
- `backend/tests/unit/test_flight_models.py` — refactored to use factories (22 tests)
- `backend/tests/integration/test_chat.py` — refactored SSE pattern + removed unused `json` import
- ~~`frontend/src/components/ToolCallCard.tsx`~~ — deleted (orphan)
- ~~`frontend/src/components/ToolResultCard.tsx`~~ — deleted (orphan)

## Decisions Made

- The plan referenced `test_session.py` as the file with inline SSE patterns (lines 60-63), but the actual inline pattern was in `test_chat.py` (lines 121-123). The `test_session.py` in this branch already had no SSE parsing. The refactor was applied to the correct file.
- Validator-rejection tests (`pytest.raises()` blocks) keep inline `Flight(...)` and `FlightQuery(...)` constructions rather than using factories. Factories with intentionally-invalid overrides would obscure the test's purpose.

## Deviations from Plan

**1. [Rule 1 - Bug] Refactored test_chat.py instead of test_session.py**
- **Found during:** Task 2 (step C — scanning for inline SSE patterns)
- **Issue:** Plan said inline SSE pattern is in `test_session.py` lines 60-63, but `test_session.py` has no SSE parsing in this branch. The actual pattern `data_lines = [line[len("data: ") :] for line in body.strip().split("\n") if line.startswith("data: ")]` was in `test_chat.py` lines 121-123.
- **Fix:** Applied the `parse_sse_events()` refactor to `test_chat.py` instead. Zero `data_lines` patterns remain in the test suite.
- **Files modified:** `backend/tests/integration/test_chat.py`
- **Verification:** `grep -rn "data_lines" backend/tests/` returns 0 matches; 5 chat integration tests pass
- **Committed in:** `2babc9e`

**2. [Rule 2 - Minor] ruff SIM108 lint fix in sse.py**
- **Found during:** Task 1 (GREEN phase, `just check`)
- **Issue:** ruff flagged the `if isinstance(text, str): ... else: ...` block as SIM108 (use ternary instead)
- **Fix:** Replaced with ternary operator; then ran `ruff format` to fix trailing whitespace in the slice expression
- **Files modified:** `backend/tests/utils/sse.py`
- **Verification:** `just check` exits 0
- **Committed in:** `1105045` (same task commit)

---

**Total deviations:** 2 auto-fixed (1 incorrect file reference corrected, 1 lint fix)
**Impact on plan:** Both deviations necessary for correctness. No scope creep.

## Issues Encountered

None — all tests passed after each fix.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- REQ-test-fixture-dedup closed
- Phase 04.8 Plans 01 and 02 complete — test hygiene objectives achieved
- Factory pattern established for future phases that add FlightSegment/FlightResult models (Phase 4.6 follow-up)
- Full backend test suite: 234 passed, 2 skipped

---
*Phase: 04.8-validators-test-hygiene-orphan-cleanup*
*Completed: 2026-05-19*

## Self-Check: PASSED

- `backend/tests/fixtures/flights.py` exists and contains both factories: PASS
- `backend/tests/utils/__init__.py` exists: PASS
- `backend/tests/utils/sse.py` contains `parse_sse_events`: PASS
- `backend/tests/unit/test_sse_utils.py` contains 9 test functions: PASS
- `backend/tests/unit/test_flight_models.py` imports from `tests.fixtures.flights`: PASS
- `backend/tests/integration/test_chat.py` imports `parse_sse_events`: PASS
- `frontend/src/components/ToolCallCard.tsx` deleted: PASS
- `frontend/src/components/ToolResultCard.tsx` deleted: PASS
- `cd backend && uv run pytest tests/ -x -q` exits 0 (234 passed, 2 skipped): PASS
- `cd frontend && npm run build` exits 0: PASS
- `just check` exits 0: PASS
- Task 1 commit `1105045` exists: PASS
- Task 2 commit `2babc9e` exists: PASS
