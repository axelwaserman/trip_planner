---
phase: 04.8-validators-test-hygiene-orphan-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models.py
  - backend/tests/unit/test_flight_models.py
autonomous: true
requirements:
  - REQ-pydantic-validators
tags:
  - pydantic
  - validators
  - business-rules
must_haves:
  truths:
    - "FlightQuery construction with origin == destination raises ValidationError"
    - "FlightQuery construction with departure_date in the past raises ValidationError"
    - "FlightQuery construction with departure_date == today succeeds (same-day boundary)"
    - "Flight construction with arrival <= departure raises ValidationError"
    - "Existing FlightQuery.validate_dates (return_date > departure_date) still rejects bad return dates"
    - "POST /api/* endpoints accepting these models return HTTP 422 for invalid bodies (delegated to FastAPI)"
  artifacts:
    - path: "backend/app/models.py"
      provides: "Three new @model_validator(mode='after') methods"
      contains: "validate_origin_destination"
    - path: "backend/app/models.py"
      provides: "Departure-not-in-past validator"
      contains: "validate_departure_not_in_past"
    - path: "backend/app/models.py"
      provides: "Flight arrival-after-departure validator"
      contains: "validate_arrival_after_departure"
    - path: "backend/tests/unit/test_flight_models.py"
      provides: "New test cases covering all four validator behaviors"
      contains: "test_flight_query_rejects_same_origin_destination"
  key_links:
    - from: "backend/app/models.py FlightQuery"
      to: "FastAPI route handlers (existing)"
      via: "Pydantic ValidationError -> FastAPI 422 response"
      pattern: "@model_validator\\(mode=\"after\"\\)"
    - from: "backend/app/models.py FlightQuery.validate_dates"
      to: "Existing return_date > departure_date enforcement"
      via: "Preserved alongside new validators"
      pattern: "Return date must be after departure date"
---

<objective>
Add three additive Pydantic v2 business-rule validators to `FlightQuery` and `Flight` so invalid domain objects are rejected at the model boundary with a clear 422 response. Cover each validator with unit tests using deterministic past/future date constants (no `freezegun`).

Purpose: Close REQ-pydantic-validators. Three rules currently rely on downstream callers being well-behaved: `origin != destination`, `departure_date >= today`, `arrival > departure`. Pydantic enforcement removes the trust assumption.

Output:
- `backend/app/models.py` with three new validators (additive — `validate_dates` preserved unchanged).
- `backend/tests/unit/test_flight_models.py` with new test cases for each validator (valid + invalid + boundary).

Scope constraint: `FlightSegment` does NOT exist in this branch (cut before Phase 4.6 merged). D-03 mentions `FlightSegment` but it cannot be implemented here. Plan scope is `Flight` + `FlightQuery` ONLY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.8-validators-test-hygiene-orphan-cleanup/04.8-CONTEXT.md
@.planning/phases/04.8-validators-test-hygiene-orphan-cleanup/04.8-RESEARCH.md
@backend/app/models.py
@backend/tests/unit/test_flight_models.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from backend/app/models.py. -->

`FlightQuery` (backend/app/models.py lines 22-72):
- Fields: `origin: str (3 letters)`, `destination: str (3 letters)`, `departure_date: date`, `return_date: date | None`, `passengers: int (1-9)`
- Existing `@field_validator("origin", "destination") validate_iata_code` — uppercases + regex-validates IATA codes (RUNS BEFORE model validators)
- Existing `@model_validator(mode="after") validate_dates` — raises `"Return date must be after departure date"` if `return_date <= departure_date`. PRESERVE UNCHANGED.

`Flight` (backend/app/models.py lines 75-129):
- Fields include `departure: datetime` (with timezone), `arrival: datetime` (with timezone)
- Existing `@field_validator("booking_class", mode="before") normalize_booking_class`
- No existing model_validator on Flight.

Imports already in models.py: `from datetime import date, datetime`, `from typing import Self`, `from pydantic import BaseModel, Field, field_validator, model_validator`. Add `datetime.now()` usage in validator body — no new imports required.

Conventions (from .planning/codebase/CONVENTIONS.md and CLAUDE.md):
- Cross-field validators: `@model_validator(mode="after")` returning `Self`
- Error messages: sentence form, NO trailing period — e.g. `"Arrival must be after departure"`
- Python 3.13 native: do NOT add `from __future__ import annotations`
- Use `str | None`, never `Optional[str]`
- mypy strict — full return-type annotations
- ruff line length 120
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add three validators to FlightQuery and Flight (TDD: RED step first)</name>
  <files>backend/app/models.py, backend/tests/unit/test_flight_models.py</files>
  <read_first>
    backend/app/models.py
    backend/tests/unit/test_flight_models.py
    .planning/phases/04.8-validators-test-hygiene-orphan-cleanup/04.8-CONTEXT.md
    .planning/phases/04.8-validators-test-hygiene-orphan-cleanup/04.8-RESEARCH.md
  </read_first>
  <behavior>
    - FlightQuery(origin="LAX", destination="LAX", departure_date=FUTURE_DATE) raises ValidationError matching "Origin and destination must be different airports"
    - FlightQuery(origin="lax", destination="LAX", departure_date=FUTURE_DATE) raises ValidationError matching "Origin and destination must be different airports" (lowercase normalized first by validate_iata_code, so this becomes LAX==LAX)
    - FlightQuery(origin="LAX", destination="JFK", departure_date=PAST_DATE) raises ValidationError matching "Departure date cannot be in the past"
    - FlightQuery(origin="LAX", destination="JFK", departure_date=date_today) succeeds (boundary: same-day == today is valid per D-02)
    - FlightQuery(origin="LAX", destination="JFK", departure_date=FUTURE_DATE) succeeds (happy path)
    - Flight(..., departure=DT, arrival=DT) raises ValidationError matching "Arrival must be after departure" (equal datetimes rejected)
    - Flight(..., departure=DT, arrival=DT_minus_1h) raises ValidationError matching "Arrival must be after departure"
    - Flight(..., departure=DT, arrival=DT_plus_1h) succeeds
    - Existing FlightQuery test "test_flight_query_return_before_departure" still passes (validate_dates preserved)
  </behavior>
  <action>
    Step 1 (RED) — Add new test functions in `backend/tests/unit/test_flight_models.py`. At the top of the test module add module-level constants:
      - `PAST_DATE = date(2020, 1, 1)`
      - `FUTURE_DATE = date(2099, 1, 1)`
    Add these test functions (AAA pattern, descriptive names, no `from __future__` import):
      - `test_flight_query_rejects_same_origin_destination` — uses pytest.raises(ValidationError, match="Origin and destination must be different airports") with origin="LAX" destination="LAX" departure_date=FUTURE_DATE
      - `test_flight_query_rejects_same_origin_destination_after_iata_normalization` — origin="lax" destination="LAX" departure_date=FUTURE_DATE; verifies that field-validator IATA uppercasing runs first so LAX==LAX collision fires the model_validator
      - `test_flight_query_rejects_past_departure` — pytest.raises(ValidationError, match="Departure date cannot be in the past") with departure_date=PAST_DATE
      - `test_flight_query_accepts_today_departure` — uses `today = datetime.now().date()`; constructs FlightQuery successfully; asserts query.departure_date == today (boundary per D-02)
      - `test_flight_query_accepts_future_departure` — happy path with FUTURE_DATE
      - `test_flight_rejects_arrival_equals_departure` — pytest.raises(ValidationError, match="Arrival must be after departure") with departure==arrival (use timezone-aware datetimes via `datetime(2099, 6, 1, 10, 0, tzinfo=UTC)`)
      - `test_flight_rejects_arrival_before_departure` — pytest.raises(ValidationError, match="Arrival must be after departure") with arrival = departure - 1h
      - `test_flight_accepts_arrival_after_departure` — happy path with arrival = departure + 1h (this is what existing `test_flight_valid` already covers, but add an explicit test that documents the model_validator path)
    Run `cd backend && uv run pytest tests/unit/test_flight_models.py -x -q` and verify the eight new tests fail (RED). Existing tests must still pass; if any pre-existing test breaks, the change is wrong.

    Step 2 (GREEN) — Add three `@model_validator(mode="after")` methods to `backend/app/models.py`:
      In `FlightQuery` (after the existing `validate_dates` method, NOT replacing it):
        - `validate_origin_destination(self) -> Self` — if `self.origin == self.destination` raise `ValueError("Origin and destination must be different airports")`; else return self.
        - `validate_departure_not_in_past(self) -> Self` — if `self.departure_date < datetime.now().date()` raise `ValueError("Departure date cannot be in the past")`; else return self.
      In `Flight` (new method on the class, after existing `normalize_booking_class`):
        - `validate_arrival_after_departure(self) -> Self` — if `self.arrival <= self.departure` raise `ValueError("Arrival must be after departure")`; else return self.
    Each method gets a docstring matching the surrounding style (Returns / Raises sections).
    Do NOT modify or remove the existing `validate_dates` method on FlightQuery. Do NOT modify the existing `validate_iata_code` field validator.
    Re-run the unit test command — all eight new tests AND all existing tests must pass (GREEN).

    Step 3 (REFACTOR if needed) — Run `just check` from repo root. Fix any ruff or mypy strict issues. The validators are short enough that no refactor should be required; do not introduce new helper functions.

    Note: `test_flight_valid` (line 126) currently uses `datetime(2025, 6, 1, ...)` which becomes a past date as of 2026. The new `validate_arrival_after_departure` validator does NOT inspect the absolute date, only the relative ordering, so this test continues to pass. The existing `test_flight_query_valid` (line 12) uses `date(2025, 6, 1)` — that IS now in the past relative to today (2026-05-18). After Step 2 lands, that existing test will fail. Update those date literals to use `FUTURE_DATE` (or `date(2099, 6, 1)` / `date(2099, 6, 8)`) so the existing tests stay green. Touch only the date constants — do not change assertions or fields under test.
  </action>
  <acceptance_criteria>
    <code>backend/app/models.py contains "def validate_origin_destination(self) -> Self:"</code>
    <code>backend/app/models.py contains "def validate_departure_not_in_past(self) -> Self:"</code>
    <code>backend/app/models.py contains "def validate_arrival_after_departure(self) -> Self:"</code>
    <code>backend/app/models.py contains "Origin and destination must be different airports"</code>
    <code>backend/app/models.py contains "Departure date cannot be in the past"</code>
    <code>backend/app/models.py contains "Arrival must be after departure"</code>
    <code>backend/app/models.py contains "def validate_dates(self) -> Self:" (preserved)</code>
    <code>backend/app/models.py contains "Return date must be after departure date" (preserved)</code>
    <behavior>FlightQuery(origin="LAX", destination="LAX", departure_date=date(2099,1,1)) raises ValidationError</behavior>
    <behavior>FlightQuery(origin="LAX", destination="JFK", departure_date=date(2020,1,1)) raises ValidationError</behavior>
    <behavior>FlightQuery(origin="LAX", destination="JFK", departure_date=datetime.now().date()) constructs successfully</behavior>
    <behavior>Flight(... departure=DT, arrival=DT) raises ValidationError</behavior>
    <behavior>FlightQuery(origin="LAX", destination="JFK", departure_date=date(2099,6,1), return_date=date(2099,5,1)) raises ValidationError matching "Return date must be after departure date"</behavior>
    <test-command>cd backend &amp;&amp; uv run pytest tests/unit/test_flight_models.py -x -q exits 0</test-command>
    <test-command>cd backend &amp;&amp; uv run pytest tests/unit/test_flight_models.py -k "rejects_same_origin_destination or rejects_past_departure or accepts_today_departure or rejects_arrival_equals_departure or rejects_arrival_before_departure" -v exits 0</test-command>
    <cli>just check exits 0 (ruff lint + format-check + mypy strict)</cli>
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/unit/test_flight_models.py -x -q</automated>
  </verify>
  <done>
    Three new validators present on FlightQuery and Flight; existing validate_dates preserved verbatim; eight new unit tests pass; existing test_flight_models.py tests still pass; `just check` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the full backend test suite to confirm no regressions outside test_flight_models.py</name>
  <files>backend/tests/</files>
  <read_first>
    backend/tests/conftest.py
    backend/tests/unit/test_mock_client.py
    backend/tests/integration/test_chat.py
  </read_first>
  <action>
    Run the full unit + integration suite. The validators are additive so no existing tests should break, BUT some existing tests use stale date literals (e.g. `date(2025, 6, 1)` is now in the past). Identify and fix any test that constructs a FlightQuery with a past departure_date or a Flight with `arrival <= departure`. The fix is mechanical: replace past-date literals with future-date literals (year 2099 or `datetime.now().date() + timedelta(days=N)`). Do NOT change the assertion bodies; do NOT change fields-under-test. Touch ONLY the date/datetime literals that the new validators now reject.

    Specifically inspect:
      - `backend/tests/unit/test_mock_client.py` — calls `FlightQuery(origin=..., destination=..., departure_date=...)`; if any departure_date is in the past, bump to a future year.
      - `backend/tests/integration/test_chat.py` and other integration tests — same pattern.
      - `backend/tests/integration/test_session.py` — should not need changes (no FlightQuery/Flight construction).

    Run `cd backend &amp;&amp; uv run pytest tests/ -x -q` and iterate until green. If any failure is NOT a stale-date issue (e.g. a test constructs origin == destination intentionally as part of a different rule), revert the test change and document the conflict in PR notes — but per CONTEXT.md no such conflict is expected.

    Final step: run `just check` once more from repo root.
  </action>
  <acceptance_criteria>
    <test-command>cd backend &amp;&amp; uv run pytest tests/unit/ tests/integration/ -x -q exits 0</test-command>
    <cli>just check exits 0</cli>
    <code>No test in backend/tests/ constructs FlightQuery with departure_date earlier than today (grep-checked)</code>
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/unit/ tests/integration/ -x -q</automated>
  </verify>
  <done>
    Full unit + integration suite green; `just check` clean; any stale-date literals updated to future dates (mechanical fix only); no behavioral test changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → FastAPI | Untrusted JSON body crosses here; Pydantic models are the validation gate |
| LLM tool call → search_flights | LLM-generated tool args are validated by FlightQuery before reaching the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.8.1-01 | Tampering | FlightQuery via POST body | mitigate | New `@model_validator validate_origin_destination` rejects same-airport queries at the boundary; FastAPI returns 422 |
| T-04.8.1-02 | Tampering | FlightQuery via POST body / LLM tool args | mitigate | New `@model_validator validate_departure_not_in_past` rejects historical dates at the boundary; FastAPI returns 422 |
| T-04.8.1-03 | Tampering | Flight model construction (downstream from API/mock client) | mitigate | New `@model_validator validate_arrival_after_departure` rejects time-inverted flights at construction time; raises ValidationError before nonsensical data reaches business logic |
| T-04.8.1-04 | Information Disclosure | ValidationError message content | accept | Error messages are short, deterministic, contain no PII or system internals — safe to surface to clients |
| T-04.8.1-SC | Tampering | npm/pip installs | n/a | No new packages installed in this plan |
</threat_model>

<verification>
- All eight new validator tests pass.
- Existing FlightQuery and Flight tests still pass (validate_dates and other validators preserved).
- Full backend test suite (`tests/unit/` + `tests/integration/`) is green.
- `just check` is green (ruff + format + mypy strict).
- `models.py` contains exactly THREE new `@model_validator(mode="after")` methods (two on FlightQuery, one on Flight). No `FlightSegment` work — out of branch scope.
</verification>

<success_criteria>
- A `POST /api/*` request with `{"origin": "JFK", "destination": "JFK", ...}` returns HTTP 422 with detail mentioning "Origin and destination must be different airports".
- A `POST /api/*` request with `{"departure_date": "2020-01-01", ...}` returns HTTP 422 with detail mentioning "Departure date cannot be in the past".
- Constructing `Flight(arrival=DT, departure=DT)` in Python raises `ValidationError` mentioning "Arrival must be after departure".
- The existing `validate_dates` validator (return_date > departure_date) still rejects bad return dates.
- mypy strict passes.
</success_criteria>

<output>
Create `.planning/phases/04.8-validators-test-hygiene-orphan-cleanup/04.8-01-SUMMARY.md` when done.
</output>
