---
phase: 4.8
slug: validators-test-hygiene-orphan-cleanup
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-18
---

# Phase 4.8 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest + vitest |
| **Config file** | `backend/pyproject.toml` / `frontend/vitest.config.ts` |
| **Quick run command** | `cd backend && uv run pytest tests/unit/ -x -q` |
| **Full suite command** | `just test` |
| **Estimated runtime** | ~30 seconds (unit only) / ~60 seconds (full) |

---

## Sampling Rate

- **After every task commit:** Run `cd backend && uv run pytest tests/unit/ -x -q`
- **After every plan wave:** Run `just test`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 4.8-01-01 | 01 | 1 | REQ-pydantic-validators | — | Invalid FlightQuery rejected with 422 | unit | `cd backend && uv run pytest tests/unit/test_flight_models.py -x -q` | ❌ W0 | ⬜ pending |
| 4.8-01-02 | 01 | 1 | REQ-pydantic-validators | — | Invalid Flight (arrival <= departure) rejected with 422 | unit | `cd backend && uv run pytest tests/unit/test_flight_models.py -x -q` | ❌ W0 | ⬜ pending |
| 4.8-02-01 | 02 | 2 | REQ-test-fixture-dedup | — | create_mock_flight() exists and is consumed | unit | `cd backend && uv run pytest tests/ -x -q` | ❌ W0 | ⬜ pending |
| 4.8-02-02 | 02 | 2 | REQ-test-fixture-dedup | — | parse_sse_events() exists and is consumed | unit | `cd backend && uv run pytest tests/ -x -q` | ❌ W0 | ⬜ pending |
| 4.8-02-03 | 02 | 2 | REQ-test-fixture-dedup | — | Dead components deleted; frontend builds clean | build | `cd frontend && npm run build` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `backend/tests/unit/test_flight_models.py` — stubs for validator tests (REQ-pydantic-validators)
- [ ] `backend/tests/fixtures/flights.py` — create_mock_flight() and create_mock_flight_query() factories (REQ-test-fixture-dedup)
- [ ] `backend/tests/utils/__init__.py` — create utils directory
- [ ] `backend/tests/utils/sse.py` — parse_sse_events() utility (REQ-test-fixture-dedup)

*Existing pytest infrastructure covers all other phase requirements.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Frontend builds with no dead-import warnings | REQ-test-fixture-dedup | Build output inspection | Run `cd frontend && npm run build` and verify no import errors for ToolCallCard/ToolResultCard |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
