---
phase: 04.8-validators-test-hygiene-orphan-cleanup
verified: 2026-05-19T03:29:44Z
status: gaps_found
score: 8/10 must-haves verified
overrides_applied: 0
gaps:
  - truth: "Existing test_session.py SSE inline parse pattern is replaced by parse_sse_events()"
    status: failed
    reason: "test_session.py lines 60-63 still contain the inline data_lines list-comprehension and json.loads pattern. parse_sse_events is NOT imported in test_session.py. The SUMMARY claims the refactor was applied to test_chat.py instead (citing an 'incorrect file reference'), but test_chat.py has no SSE parsing at all and is only 86 lines — the executor's claimed deviation is false. The only integration test containing inline SSE parsing remains test_session.py::TestChatInvalidSession::test_invalid_session_streams_empty_error_event."
    artifacts:
      - path: "backend/tests/integration/test_session.py"
        issue: "Lines 60-63 still use inline data_lines list-comprehension and json.loads instead of parse_sse_events()"
    missing:
      - "Add 'from tests.utils.sse import parse_sse_events' import to test_session.py"
      - "Replace lines 60-66 (data_lines / json.loads / dict-subscript assertions) with 'events = parse_sse_events(response.text)' and typed StreamEvent attribute access"
      - "Remove the now-unused 'import json' import from test_session.py"
  - truth: "No test redefines a mock-flight factory inline (SC 2 partial: 'no test redefines a mock-flight factory inline')"
    status: failed
    reason: "SC 2 states 'existing tests consume them; no test redefines a mock-flight factory inline'. The test_session.py SSE inline parse is a reuse-gap for the SSE utility — the helper exists but integration tests still bypass it. This directly contradicts the 'existing tests consume them' requirement for parse_sse_events."
    artifacts:
      - path: "backend/tests/integration/test_session.py"
        issue: "Integration test does not consume parse_sse_events despite it being available"
    missing:
      - "Same fix as gap above — refactor test_session.py to use parse_sse_events()"
---

# Phase 4.8: Validators + Test Hygiene + Orphan Cleanup Verification Report

**Phase Goal:** Business rules are enforced at the model boundary, the test suite shares fixtures, and dead components are gone.
**Verified:** 2026-05-19T03:29:44Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | FlightQuery construction with origin == destination raises ValidationError | VERIFIED | `models.py:75-86` `validate_origin_destination` raises `ValueError("Origin and destination must be different airports")`; test `test_flight_query_rejects_same_origin_destination` passes |
| 2 | FlightQuery construction with departure_date in the past raises ValidationError | VERIFIED | `models.py:88-102` `validate_departure_not_in_past` raises `ValueError("Departure date cannot be in the past")`; test `test_flight_query_rejects_past_departure` passes |
| 3 | FlightQuery construction with departure_date == today succeeds (same-day boundary) | VERIFIED | Validator uses `< datetime.now().date()` (strict less-than); test `test_flight_query_accepts_today_departure` constructs with today and asserts success |
| 4 | Flight construction with arrival <= departure raises ValidationError | VERIFIED | `models.py:161-173` `validate_arrival_after_departure` raises `ValueError("Arrival must be after departure")`; tests `test_flight_rejects_arrival_equals_departure` and `test_flight_rejects_arrival_before_departure` pass |
| 5 | Existing FlightQuery.validate_dates (return_date > departure_date) still rejects bad return dates | VERIFIED | `models.py:60-72` `validate_dates` preserved verbatim; test `test_flight_query_return_before_departure` passes |
| 6 | `backend/tests/fixtures/flights.py::create_mock_flight()` exists and is consumed by tests | VERIFIED | File exists, keyword-only signature, sensible defaults (LAX→JFK, 2099-06-01, arrival +8h30m); `test_flight_models.py` line 10 imports it; 10+ call sites confirmed |
| 7 | `backend/tests/fixtures/flights.py::create_mock_flight_query()` exists and is consumed by tests | VERIFIED | Exported from `flights.py` with keyword-only signature, defaults passing all Plan 01 validators; `test_flight_models.py` line 10 imports it; multiple call sites confirmed |
| 8 | `backend/tests/utils/sse.py::parse_sse_events()` exists and accepts str or list[str] | VERIFIED | File exists with correct `def parse_sse_events(text: str | list[str]) -> list[StreamEvent]` signature; 9 unit tests in `test_sse_utils.py` all pass |
| 9 | Existing test_session.py SSE inline parse pattern is replaced by parse_sse_events() | FAILED | `test_session.py` lines 60-63 still contain the raw `data_lines` list-comprehension and `json.loads` pattern. `parse_sse_events` is not imported. SUMMARY claimed the refactor went to `test_chat.py`, but `test_chat.py` has no SSE parsing at all (only 86 lines). |
| 10 | Orphaned ToolCallCard.tsx and ToolResultCard.tsx are deleted; frontend builds clean | VERIFIED | Both files absent from `frontend/src/components/`; `tsc --noEmit` exits 0; zero references remain in `frontend/src/` |

**Score:** 8/10 truths verified

### Deferred Items

None.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/app/models.py` | Three new `@model_validator(mode="after")` methods + preserved `validate_dates` | VERIFIED | `validate_origin_destination` (line 75), `validate_departure_not_in_past` (line 88), `validate_arrival_after_departure` (line 161), `validate_dates` (line 60) all present with correct error messages |
| `backend/tests/fixtures/flights.py` | `create_mock_flight()` and `create_mock_flight_query()` factories | VERIFIED | Both present, keyword-only signatures, future-dated defaults, full type annotations |
| `backend/tests/utils/__init__.py` | Package init for new utils directory | VERIFIED | Exists with module docstring |
| `backend/tests/utils/sse.py` | `parse_sse_events()` returning `list[StreamEvent]` | VERIFIED | Correct signature, handles str and list[str], skips blank/keep-alive lines |
| `backend/tests/unit/test_flight_models.py` | Imports from fixtures and covers new validators | VERIFIED | Imports `create_mock_flight, create_mock_flight_query` from `tests.fixtures.flights`; 8 new validator tests present |
| `backend/tests/unit/test_sse_utils.py` | 9 unit tests for parse_sse_events | VERIFIED | All 9 tests present covering happy path, list input, blank skip, keep-alive skip, typed return, empty input, malformed JSON, validation error |
| `backend/tests/integration/test_session.py` | Must consume parse_sse_events (inline pattern eliminated) | FAILED | Lines 60-63 still use `data_lines` list-comprehension and `json.loads`; `parse_sse_events` not imported |
| `frontend/src/components/ToolCallCard.tsx` | Must NOT exist | VERIFIED | File does not exist |
| `frontend/src/components/ToolResultCard.tsx` | Must NOT exist | VERIFIED | File does not exist |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `test_flight_models.py` | `fixtures/flights.py::create_mock_flight` | `from tests.fixtures.flights import create_mock_flight` (line 10) | WIRED | 10+ call sites in test file |
| `test_flight_models.py` | `fixtures/flights.py::create_mock_flight_query` | `from tests.fixtures.flights import create_mock_flight_query` (line 10) | WIRED | Multiple call sites in test file |
| `test_sse_utils.py` | `utils/sse.py::parse_sse_events` | `from tests.utils.sse import parse_sse_events` (line 9) | WIRED | 9 call sites for unit tests |
| `test_session.py` | `utils/sse.py::parse_sse_events` | should be `from tests.utils.sse import parse_sse_events` | NOT_WIRED | No import; inline pattern at lines 60-63 still used |
| `FlightQuery` | FastAPI 422 response | Pydantic ValidationError → FastAPI handler | WIRED | `@model_validator(mode="after")` on FlightQuery; all three validators present |
| `frontend/src/components/` | ToolCallCard/ToolResultCard deleted | Zero references remain | WIRED (deletion confirmed) | `grep -rn "ToolCallCard\|ToolResultCard" frontend/src/` returns 0 matches; `tsc --noEmit` exits 0 |

### Data-Flow Trace (Level 4)

Not applicable — this phase modifies Pydantic validation logic and test scaffolding, not components that render dynamic data.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All unit + integration tests pass | `uv run pytest tests/unit/ tests/integration/ -q` | 120 passed in 5.04s | PASS |
| ruff + format-check + mypy strict | `just check` | All checks passed, 40 files formatted, no mypy issues | PASS |
| Frontend tsc --noEmit | `node_modules/.bin/tsc --noEmit` (main repo bin against worktree) | Exit 0, no output | PASS |

### Probe Execution

No probe scripts declared or present for this phase.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| REQ-pydantic-validators | 04.8-01 | Business-rule validators at model boundary | SATISFIED | Three `@model_validator(mode="after")` methods present and tested; 8 validator tests pass |
| REQ-test-fixture-dedup | 04.8-02 | Centralise repeated test scaffolding | PARTIAL | Factories (`create_mock_flight`, `create_mock_flight_query`) and SSE helper (`parse_sse_events`) created and used by unit tests; but `test_session.py` integration test still uses inline SSE pattern — one call site remains un-deduplicated |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `backend/tests/integration/test_session.py` | 60-63 | Inline `data_lines` list-comprehension + `json.loads` SSE parsing that `parse_sse_events` was created to replace | Warning | REQ-test-fixture-dedup not fully closed; `parse_sse_events` utility is an orphan from the integration test's perspective |

No debt markers (`TBD`, `FIXME`, `XXX`) found in any phase-modified file.

### Human Verification Required

None — all checks were programmatically verifiable.

### Gaps Summary

**One gap blocks full goal achievement.**

The SUMMARY.md for Plan 02 claims the inline SSE parse in `test_session.py` was actually in `test_chat.py`, and that the refactor was correctly applied there. This claim is false. `test_chat.py` is 86 lines with no SSE parsing at all — there was nothing to refactor there. `test_session.py` lines 60-63 still contain the exact inline pattern (`data_lines = [line for line in response.text.strip().split("\n") if line.startswith("data: ")]` / `json.loads`) that `parse_sse_events` was created to eliminate.

**Root cause:** The executor appears to have grep'd for `data_lines` in `test_session.py` of an older version and then got confused about which file contained the pattern. After creating `parse_sse_events`, they refactored `test_chat.py` (which needed no refactoring) instead of `test_session.py` (which does).

**Fix required:** Refactor `test_session.py::TestChatInvalidSession::test_invalid_session_streams_empty_error_event` to use `parse_sse_events()`:
1. Add `from tests.utils.sse import parse_sse_events` import
2. Replace the `data_lines` / `json.loads` / dict-subscript block with `events = parse_sse_events(response.text)` and typed StreamEvent attribute access (`events[0].type`, `events[0].chunk`, `events[0].session_id`)
3. Remove the now-unused `import json` import

**Impact on Success Criteria:**
- SC 1 (model boundary validators): FULLY MET
- SC 2 (fixtures exist and existing tests consume them): PARTIALLY MET — factories are consumed by `test_flight_models.py`; `parse_sse_events` is created but NOT consumed by any integration test
- SC 3 (orphan deletion + clean build): FULLY MET

---

_Verified: 2026-05-19T03:29:44Z_
_Verifier: Claude (gsd-verifier)_
