# Phase 4.8: Validators + Test Hygiene + Orphan Cleanup - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-18
**Phase:** 4.8-validators-test-hygiene-orphan-cleanup
**Areas discussed:** Clock anchoring, parse_sse_events() shape, create_mock_flight() scope

---

## Clock anchoring (departure_date >= today validator)

| Option | Description | Selected |
|--------|-------------|----------|
| Live `datetime.now().date()` | No extra deps; tests use hardcoded PAST/FUTURE dates | ✓ |
| Injectable clock via Settings | `Settings.today_factory = date.today`; overridable in tests | |
| You decide | Leave to planner | |

**User's choice:** Live `datetime.now().date()`
**Notes:** None — clean choice, matches the simplest correct approach.

---

| Option | Description | Selected |
|--------|-------------|----------|
| `>= today` (same-day valid) | Matches REQ-pydantic-validators literally | ✓ |
| `> today` (strictly future) | Tighter but departs from spec | |

**User's choice:** `>= today`
**Notes:** Requirement says `>= today`; no reason to deviate.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Both Flight and FlightSegment | Both carry departure+arrival; complete coverage | ✓ |
| Flight only | REQ names Flight explicitly; leaves FlightSegment unchecked | |
| You decide | Let planner assess | |

**User's choice:** Both Flight and FlightSegment

---

## parse_sse_events() shape

| Option | Description | Selected |
|--------|-------------|----------|
| Raw SSE text (`str` or `list[str]`) | Decoupled; works in unit + integration | ✓ |
| TestClient StreamingResponse directly | Convenient but tightly coupled to FastAPI test client | |

**User's choice:** Raw SSE text
**Notes:** User clarified mid-question that this helper is backend only (not frontend). Confirmed `backend/tests/utils/sse.py`.

---

| Option | Description | Selected |
|--------|-------------|----------|
| `list[StreamEvent]` (typed) | Type-safe; Phase 4.7 updates the annotation in one line | ✓ |
| `list[dict[str, Any]]` (raw dicts) | Flexible but no type checking or Pydantic validation | |

**User's choice:** `list[StreamEvent]`

---

## create_mock_flight() scope

| Option | Description | Selected |
|--------|-------------|----------|
| Flight only | REQ names this function specifically | |
| Flight + FlightQuery + FlightResult/FlightSegment | Centralises all flight-domain test data | ✓ |

**User's choice:** Full scope covering all flight-domain models
**Notes:** User chose broader coverage over minimum-spec. Factory functions: `create_mock_flight()`, `create_mock_flight_query()`, `create_mock_flight_segment()`, `create_mock_flight_result()`.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Keyword overrides with defaults | `create_mock_flight(origin="JFK")` — most reusable | ✓ |
| Fixed fixture + separate factory | More files, less flexible | |

**User's choice:** Keyword overrides with sensible defaults

---

## Claude's Discretion

None — user made explicit choices for every question.

## Deferred Ideas

None — discussion stayed within phase scope.
