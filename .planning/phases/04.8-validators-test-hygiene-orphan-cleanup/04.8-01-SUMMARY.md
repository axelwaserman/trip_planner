---
phase: 04.8-validators-test-hygiene-orphan-cleanup
plan: 01
subsystem: api
tags: [pydantic, validators, business-rules, testing]

# Dependency graph
requires:
  - phase: 04.6-vendor-neutral-tool-json
    provides: FlightQuery and Flight Pydantic models the validators extend

provides:
  - Three additive @model_validator(mode="after") methods on FlightQuery and Flight
  - Unit test coverage for all three new validators plus boundary conditions
  - Stale 2025 date literals updated to 2099 across test_mock_client.py

affects:
  - backend/app/models.py (FlightQuery, Flight)
  - backend/tests/unit/test_flight_models.py
  - backend/tests/unit/test_mock_client.py

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "@model_validator(mode='after') returning Self for cross-field business-rule enforcement"
    - "Module-level PAST_DATE / FUTURE_DATE constants instead of freezegun for deterministic date tests"

key-files:
  created: []
  modified:
    - backend/app/models.py
    - backend/tests/unit/test_flight_models.py
    - backend/tests/unit/test_mock_client.py

key-decisions:
  - "Use datetime.now().date() directly in validator body (no injectable clock, no freezegun) per D-01"
  - "Same-day departure (departure_date == today) is valid — boundary is >= today per D-02"
  - "Validators are additive — validate_dates preserved unchanged"

patterns-established:
  - "@model_validator(mode='after') pattern: PAST_DATE/FUTURE_DATE constants, no freezegun"

requirements-completed:
  - REQ-pydantic-validators

# Metrics
duration: 4min
completed: 2026-05-19
---

# Phase 4.8 Plan 01: Pydantic Validators Summary

**Three additive @model_validator(mode="after") methods enforce origin != destination, departure >= today, and arrival > departure at the model boundary; eight new unit tests cover all branches.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-19T03:07:07Z
- **Completed:** 2026-05-19T03:11:18Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Added `FlightQuery.validate_origin_destination` — rejects `origin == destination` with "Origin and destination must be different airports"
- Added `FlightQuery.validate_departure_not_in_past` — rejects `departure_date < today` with "Departure date cannot be in the past"; same-day accepted per D-02
- Added `Flight.validate_arrival_after_departure` — rejects `arrival <= departure` with "Arrival must be after departure"
- Added module-level `PAST_DATE = date(2020, 1, 1)` and `FUTURE_DATE = date(2099, 1, 1)` constants to test file; added UTC datetime constants
- Updated stale `date(2025, ...)` literals across `test_flight_models.py` and `test_mock_client.py` to `date(2099, ...)`
- Full TDD cycle: RED commit (`12c3b62`), GREEN commit (`fcc2909`), fix commit (`73cb38c`)

## Task Commits

Each task was committed atomically:

1. **Task 1 RED: Add 8 failing tests** - `12c3b62` (test)
2. **Task 1 GREEN: Implement validators + fix stale test dates** - `fcc2909` (feat)
3. **Task 2: Fix stale dates in test_mock_client.py + ruff C408** - `73cb38c` (fix)

## Files Created/Modified

- `backend/app/models.py` — Added three `@model_validator(mode="after")` methods to FlightQuery (validate_origin_destination, validate_departure_not_in_past) and Flight (validate_arrival_after_departure); validate_dates preserved unchanged
- `backend/tests/unit/test_flight_models.py` — Added PAST_DATE/FUTURE_DATE/UTC datetime constants; added 8 new test functions; updated 12 stale `date(2025, ...)` literals to FUTURE_DATE
- `backend/tests/unit/test_mock_client.py` — Updated 14 stale `date(2025, ...)` literals to `date(2099, ...)` so FlightQuery construction passes the new departure validator

## Decisions Made

- Used `datetime.now().date()` directly in the validator body (no injectable clock, no `pytest-freezegun`) per D-01. Tests use `PAST_DATE = date(2020, 1, 1)` for invalid and `FUTURE_DATE = date(2099, 1, 1)` for valid cases.
- Same-day departure boundary: `departure_date < today` rejects (strictly less-than), so today == valid. Matches REQ-pydantic-validators literal: "departure_date >= today".

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Ruff C408 in test_flight_models.py**
- **Found during:** Task 1 GREEN (just check)
- **Issue:** `base_kwargs = dict(...)` call flagged by ruff C408 (unnecessary dict() call)
- **Fix:** Rewrote as typed `base_kwargs: dict[str, object] = {...}` dict literal
- **Files modified:** `backend/tests/unit/test_flight_models.py`
- **Verification:** `just check` passes with no ruff errors
- **Committed in:** `73cb38c`

---

**Total deviations:** 1 auto-fixed (1 blocking/lint)
**Impact on plan:** Trivial style fix. No behavioral changes.

## Issues Encountered

None — plan executed as written with one lint auto-fix.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- REQ-pydantic-validators is complete; all three business-rule validators enforce domain invariants at the model boundary
- `validate_dates` (return_date > departure_date) preserved unchanged
- 22 unit tests pass in `test_flight_models.py`; 224 total unit + integration tests pass
- `just check` clean (ruff + mypy strict)
- Ready for Plan 02: shared test fixtures + orphan cleanup

## Self-Check

**Files exist:**
- PASS: `backend/app/models.py` contains `def validate_origin_destination`, `def validate_departure_not_in_past`, `def validate_arrival_after_departure`, `def validate_dates`
- PASS: `backend/tests/unit/test_flight_models.py` contains 22 test functions
- PASS: `backend/tests/unit/test_mock_client.py` contains no `date(2025, ...)` literals

**Commits exist:**
- PASS: `12c3b62` — test(04.8-01): add failing tests
- PASS: `fcc2909` — feat(04.8-01): implement validators
- PASS: `73cb38c` — fix(04.8-01): stale dates + ruff C408

**Test suite:**
- PASS: `cd backend && uv run pytest tests/unit/test_flight_models.py -x -q` → 22 passed
- PASS: `cd backend && uv run pytest tests/unit/ tests/integration/ -x -q` → 224 passed, 2 skipped
- PASS: `just check` → ruff + mypy strict both pass

## Self-Check: PASSED

---
*Phase: 04.8-validators-test-hygiene-orphan-cleanup*
*Completed: 2026-05-19*
