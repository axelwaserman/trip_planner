---
phase: 07-real-flight-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/config.py
  - backend/app/llm/log_scrubbing.py
  - backend/tests/unit/test_config_duffel.py
  - backend/tests/unit/llm/test_log_scrubbing.py
  - .env.example
autonomous: true
requirements: [REQ-real-flight-api]
requirements_addressed: [REQ-real-flight-api]
tags: [duffel, config, secrets, log-scrubbing]

must_haves:
  truths:
    - "Settings exposes duffel_api_token (SecretStr | None, default None) and duffel_env (Literal['test','live','mock'], default 'test') per D-01."
    - "A fresh checkout boots without DUFFEL_API_TOKEN set (Pitfall 6 — no boot-time ValidationError)."
    - "ApiKeyScrubber redacts both duffel_test_<token> and duffel_live_<token> substrings from log lines, leaving short prefixes alone."
    - ".env.example documents DUFFEL_API_TOKEN and DUFFEL_ENV with exact env var names that match Settings field aliases."
  artifacts:
    - path: "backend/app/config.py"
      provides: "duffel_api_token + duffel_env Settings fields"
      contains: "duffel_api_token: SecretStr | None"
    - path: "backend/app/llm/log_scrubbing.py"
      provides: "Duffel bearer token regex inside SECRET_PATTERNS"
      contains: "duffel_(test|live)_"
    - path: "backend/tests/unit/test_config_duffel.py"
      provides: "Locks Settings.duffel_* contract"
      exports: []
    - path: "backend/tests/unit/llm/test_log_scrubbing.py"
      provides: "Extended scrubber coverage for duffel tokens"
      exports: []
    - path: ".env.example"
      provides: "Documented DUFFEL_API_TOKEN + DUFFEL_ENV rows"
      contains: "DUFFEL_API_TOKEN"
  key_links:
    - from: "backend/app/config.py::Settings"
      to: "DUFFEL_API_TOKEN env var"
      via: "pydantic-settings BaseSettings env_file load"
      pattern: "duffel_api_token"
    - from: "backend/app/llm/log_scrubbing.py::SECRET_PATTERNS"
      to: "Duffel bearer token shape"
      via: "regex tuple entry"
      pattern: "duffel_\\(test\\|live\\)_"
---

<objective>
Add the Duffel credentials surface to the project: two `Settings` fields (D-01) and a log-scrubber regex covering `duffel_test_*` / `duffel_live_*` (Pitfall 7). Document both env vars in `.env.example`. This is the prerequisite plan for every downstream plan in the phase — D-02 lifespan branch and the e2e suite both consume `Settings.duffel_api_token` / `Settings.duffel_env`, and the scrubber regex must already be live before any Duffel client logs a request.

Purpose: Land the credential plumbing in isolation so a developer with no Duffel account still gets a bootable stack (Pitfall 6) and so any leaked bearer-token substring is redacted from logs before formatter dispatch.

Output: Two Settings fields, one regex line + supporting tests, one `.env.example` block.
</objective>

<execution_context>
@/Users/axel/code/trip_planner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/trip_planner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-real-flight-api/07-CONTEXT.md
@.planning/phases/07-real-flight-api/07-RESEARCH.md
@.planning/phases/07-real-flight-api/07-PATTERNS.md
@backend/app/config.py
@backend/app/llm/log_scrubbing.py
@.env.example
@CLAUDE.md

<interfaces>
<!-- Existing Settings shape — match this convention exactly. -->
From backend/app/config.py:
```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")
    # ... existing fields including:
    openai_api_key: str | None = None
    anthropic_api_key: str | None = None
    # Add Duffel block adjacent to these.
```

From backend/app/llm/log_scrubbing.py:
```python
SECRET_PATTERNS: tuple[tuple[re.Pattern[str], str], ...] = (
    (re.compile(r"sk-ant-[A-Za-z0-9_-]{20,}"), "sk-ant-[REDACTED]"),
    (re.compile(r"sk-(?:[a-z]+-)*[A-Za-z0-9_-]{20,}"), "sk-[REDACTED]"),
    (re.compile(r'("[A-Za-z0-9_]*api_key"\s*:\s*)"[^"]+"'), r'\1"[REDACTED]"'),
)

def _scrub(text: str) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add duffel_api_token + duffel_env Settings fields with tests (D-01, Pitfall 6)</name>
  <files>backend/app/config.py, backend/tests/unit/test_config_duffel.py</files>
  <read_first>
    - backend/app/config.py (full file — match existing optional-credential pattern shape)
    - .planning/phases/07-real-flight-api/07-CONTEXT.md (D-01 spec)
    - .planning/phases/07-real-flight-api/07-RESEARCH.md (Pitfall 6 — token MUST default to None)
    - .planning/phases/07-real-flight-api/07-PATTERNS.md (sections "backend/app/config.py (config)" and "backend/tests/unit/test_config_duffel.py (test, unit)")
    - CLAUDE.md (StrEnum vs Literal rule, mypy strict, line length 120)
  </read_first>
  <behavior>
    - Test 1: `Settings()` instantiated with `duffel_api_token=None` returns object with `duffel_api_token is None` (Pitfall 6 — no ValidationError on missing token).
    - Test 2: setting env var `DUFFEL_API_TOKEN=duffel_test_xxxxxxxxxxxxxxxxxxxx` produces `Settings()` whose `duffel_api_token` is a `SecretStr` instance and whose `repr()` does not contain the literal substring `duffel_test_xxxx` (T-07-02-class — SecretStr opacity in repr).
    - Test 3: default `Settings().duffel_env == "test"`.
    - Test 4: setting `DUFFEL_ENV=staging` raises `pydantic.ValidationError` (Literal narrowing of D-01).
    - Test 5: setting `DUFFEL_ENV=mock` produces `Settings()` whose `duffel_env == "mock"` (third allowed literal).
  </behavior>
  <action>
    Per D-01: extend `backend/app/config.py::Settings` with TWO new fields placed adjacent to the existing `anthropic_api_key` block (preserving alphabetical-by-feature grouping and existing comment-section style):

    1. `duffel_api_token: SecretStr | None = None` — optional bearer token. `SecretStr` from `pydantic` (add to existing `from pydantic import field_validator` import: `from pydantic import SecretStr, field_validator`). Default `None` is mandatory (Pitfall 6 — fresh checkout must boot without creds; D-02 lifespan auto-fallback consumes the `None` branch).
    2. `duffel_env: Literal["test", "live", "mock"] = "test"` — env selector. Add `from typing import Literal` (or extend existing typing import). Three literals exact and ordered as `"test"`, `"live"`, `"mock"`. The `"mock"` literal is honored by the lifespan in Plan 07-03 to force the mock client even when a token is present (useful for tests with real creds in env).

    Add a leading block comment matching the existing comment-block style (e.g. `# Duffel Configuration (Phase 7 — D-01).` followed by 1-3 lines explaining auto-fallback behaviour and the SecretStr scrubbing guarantee). Do NOT add `_DEFAULT_DUFFEL_TOKEN` or any module-level constant — the field default lives on the model.

    Then create `backend/tests/unit/test_config_duffel.py` covering the 5 tests in `<behavior>` above. Use `monkeypatch.setenv` to drive env-var-based tests; for the no-env-var `None`-default test, instantiate `Settings(duffel_api_token=None)` (do NOT rely on absent env — pytest may inherit a developer's local env). Imports: `pytest`, `pydantic.SecretStr`, `pydantic.ValidationError` (or generic `Exception` per the PATTERNS sketch), `app.config.Settings`. Use AAA structure with descriptive test names per /Users/axel/.claude/rules/common/testing.md.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/test_config_duffel.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/unit/test_config_duffel.py -x -v` passes with 5 tests collected and 0 failed.
    - `cd backend && uv run python -c "from app.config import Settings; s = Settings(duffel_api_token=None); assert s.duffel_api_token is None; assert s.duffel_env == 'test'"` exits 0.
    - `cd backend && uv run python -c "from app.config import Settings; from pydantic import SecretStr; import os; os.environ['DUFFEL_API_TOKEN']='duffel_test_abcdefghij1234567890'; s = Settings(); assert isinstance(s.duffel_api_token, SecretStr); assert 'abcdefghij' not in repr(s.duffel_api_token)"` exits 0.
    - `cd backend && uv run mypy app/config.py` reports zero errors (strict mode; no new `type: ignore` comments).
    - `grep -nE 'duffel_api_token: SecretStr \| None = None' backend/app/config.py` returns exactly 1 match.
    - `grep -nE 'duffel_env: Literal\["test", "live", "mock"\] = "test"' backend/app/config.py` returns exactly 1 match.
  </acceptance_criteria>
  <done>
    Both Settings fields land with the exact types from D-01; all 5 unit tests pass; mypy clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ApiKeyScrubber to redact Duffel bearer tokens (Pitfall 7)</name>
  <files>backend/app/llm/log_scrubbing.py, backend/tests/unit/llm/test_log_scrubbing.py</files>
  <read_first>
    - backend/app/llm/log_scrubbing.py (full file — preserve `SECRET_PATTERNS` ordering rules and `_scrub` contract)
    - backend/tests/unit/llm/test_log_scrubbing.py (existing test groups A/B/C — append to Group A only)
    - .planning/phases/07-real-flight-api/07-CONTEXT.md (Pitfall 7 + D-04 context: status-only error messages still need defense-in-depth log redaction)
    - .planning/phases/07-real-flight-api/07-RESEARCH.md (Pitfall 7 spec — `duffel_(test|live)_[A-Za-z0-9_-]{20,}` regex)
    - .planning/phases/07-real-flight-api/07-PATTERNS.md (sections "backend/app/llm/log_scrubbing.py" and "backend/tests/unit/llm/test_log_scrubbing.py")
  </read_first>
  <behavior>
    - Test 1 `test_scrub_redacts_duffel_test_token`: `_scrub("token=duffel_test_abcdefghij1234567890")` returns `"token=duffel_[REDACTED]"`.
    - Test 2 `test_scrub_redacts_duffel_live_token`: `_scrub("token=duffel_live_abcdefghij1234567890")` returns `"token=duffel_[REDACTED]"`.
    - Test 3 `test_scrub_short_duffel_prefix_does_not_match`: `_scrub("debug duffel_test_short was here")` returns the input unchanged (the `{20,}` lower bound prevents false positives on partial dumps).
    - Test 4 `test_scrub_duffel_token_inside_authorization_header_format`: `_scrub("Authorization: Bearer duffel_live_xyz1234567890abcdefgh")` substitutes only the token, leaving `"Authorization: Bearer duffel_[REDACTED]"`.
    - Test 5 `test_scrub_existing_openai_key_still_redacted_after_addition`: regression-lock — `_scrub("key=sk-abcdefghij1234567890")` STILL returns `"key=sk-[REDACTED]"` (Duffel pattern must not break OpenAI/Anthropic ordering).
  </behavior>
  <action>
    Per Pitfall 7 / RESEARCH §"Duffel bearer-token format `duffel_(test|live)_[A-Za-z0-9_-]{20,}`": append exactly ONE regex tuple to `SECRET_PATTERNS` in `backend/app/llm/log_scrubbing.py`. The tuple is:

    `(re.compile(r"duffel_(test|live)_[A-Za-z0-9_-]{20,}"), "duffel_[REDACTED]")`

    Append it AFTER the three existing entries — order does not matter for correctness because `duffel_*` and `sk-*` shapes are disjoint, but appending preserves the existing comment block stating "Order matters: ``sk-ant-`` MUST match before bare ``sk-``". Update the SECRET_PATTERNS comment block to add one short line noting the Duffel addition (Phase 7 / Pitfall 7) — keep it terse (per CLAUDE.md "comments explain *why*, not *what*").

    Add a sibling `Duffel bearer (Phase 7 / Pitfall 7).` mini-block to the existing top-level rationale comment so future readers see why this pattern joined the tuple.

    Extend `backend/tests/unit/llm/test_log_scrubbing.py` Group A direct-regex tests with the 5 tests listed in `<behavior>` above. Use the existing `_scrub` import path. Match the existing test naming convention (`test_scrub_*`). Do NOT modify Group B (logging.Filter) or Group C (install/uninstall) tests — Duffel coverage at the regex layer is sufficient because the scrubber pipeline already exercises both layers in existing tests.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/llm/test_log_scrubbing.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/unit/llm/test_log_scrubbing.py -x -v` passes with all preexisting tests still green AND 5 new Duffel tests added.
    - `grep -cE 'duffel_\(test\|live\)_\[A-Za-z0-9_-\]\{20,\}' backend/app/llm/log_scrubbing.py` returns `1`.
    - `cd backend && uv run python -c "from app.llm.log_scrubbing import _scrub; assert _scrub('Authorization: Bearer duffel_test_xxxxxxxxxxxxxxxxxxxx') == 'Authorization: Bearer duffel_[REDACTED]'"` exits 0.
    - `cd backend && uv run python -c "from app.llm.log_scrubbing import _scrub; assert _scrub('debug duffel_test_short was here') == 'debug duffel_test_short was here'"` exits 0 (short-prefix false-positive guard).
    - `cd backend && uv run python -c "from app.llm.log_scrubbing import _scrub; assert _scrub('key=sk-abcdefghij1234567890') == 'key=sk-[REDACTED]'"` exits 0 (regression-lock for OpenAI ordering).
    - `cd backend && uv run mypy app/llm/log_scrubbing.py` reports zero errors.
  </acceptance_criteria>
  <done>
    Duffel bearer tokens are redacted by `_scrub` and `ApiKeyScrubber.filter` for both `duffel_test_*` and `duffel_live_*` shapes; regression-lock test ensures OpenAI/Anthropic redaction unchanged; all tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document DUFFEL_API_TOKEN + DUFFEL_ENV in .env.example</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (existing file — match the comment style and field-block layout used for DATABASE_URL / JWT_SECRET)
    - .planning/phases/07-real-flight-api/07-CONTEXT.md (D-01 + D-11 — Duffel signup flow lives in Plan 07-04 README, but env-var presence belongs here)
    - .planning/phases/07-real-flight-api/07-RESEARCH.md ("Environment Availability" + "Runtime State Inventory" — confirms DUFFEL_API_TOKEN is the canonical env-var name; DUFFEL_ENV is the literal selector)
  </read_first>
  <action>
    Append a `# Duffel Flight API (Phase 7 — D-01)` comment block to `.env.example` after the JWT_SECRET block, with two env-var rows:

    1. A leading 2-3 line comment block explaining: token is OPTIONAL (lifespan auto-falls-back to MockFlightAPIClient when unset, per D-02), token format is `duffel_test_*` (sandbox) or `duffel_live_*` (production), pointer to README "Duffel credential setup" section that Plan 07-04 will populate.
    2. `DUFFEL_API_TOKEN=` (left empty — developer fills in from dashboard; the empty value is fine because `Settings.duffel_api_token` defaults to `None`).
    3. `# DUFFEL_ENV=test` commented-out — note that valid values are `test`, `live`, `mock` and that the default in `Settings` is `test`. Comment-out the entry so absent env doesn't surprise pydantic-settings (the default already covers it; the comment is documentation).

    Do NOT add anything else (e.g. CORS, OPENAI_API_KEY) — the scope is strictly two new lines + a header comment block.
  </action>
  <verify>
    <automated>grep -c '^DUFFEL_API_TOKEN=' .env.example | grep -q '^1$' && grep -cE '^# DUFFEL_ENV=' .env.example | grep -q '^1$' && grep -cE 'Phase 7' .env.example | grep -qE '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^DUFFEL_API_TOKEN=' .env.example` returns `1` (uncommented row).
    - `grep -cE '^# DUFFEL_ENV=' .env.example` returns `1` (commented row documenting the literal options).
    - `grep -nE '(test|live|mock)' .env.example` returns at least one line within the new Duffel block listing the three valid `DUFFEL_ENV` values.
    - Block comment includes the literal substring `Phase 7` AND `D-01` for traceability.
    - File still ends with a single trailing newline (POSIX text-file convention; check `tail -c 1 .env.example | od -c` shows `\n`).
  </acceptance_criteria>
  <done>
    `.env.example` documents both Duffel env vars; `Settings()` continues to load successfully with the example file as a literal `.env`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-shell → process | Untrusted env vars cross here on `uvicorn` startup; `Settings.duffel_api_token` is the controlled ingestion point. |
| process → log handler | LogRecord text/args cross here; `ApiKeyScrubber.filter` is the redaction point. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Information Disclosure | `Settings.duffel_api_token` repr in stack traces / debug dumps | mitigate | Use `pydantic.SecretStr` per D-01 — `repr()` returns `"SecretStr('**********')"`, never the raw token; tested in Task 1 Test 2. |
| T-07-01-02 | Information Disclosure | bearer token leakage to log handlers (uvicorn access logs, app loggers) | mitigate | Append regex `duffel_(test|live)_[A-Za-z0-9_-]{20,}` to `SECRET_PATTERNS` per Pitfall 7; covered in Task 2. The `{20,}` lower bound prevents false positives on harmless prefix substrings. |
| T-07-01-03 | Information Disclosure | bearer token leakage via git (committed `.env`) | accept | `.env` is already in `.gitignore` (verified across the repo for prior Phase 6 work); `.env.example` introduces only an empty `DUFFEL_API_TOKEN=` placeholder — no secret committed. The README credential-setup section in Plan 07-04 reinforces this. |
| T-07-01-04 | Denial of Service (self) | `Settings(...)` raising `ValidationError` on missing token at boot (Pitfall 6) | mitigate | `duffel_api_token: SecretStr | None = None` — default `None` makes the field optional; lifespan auto-fallback in Plan 07-03 consumes the `None` branch. Tested in Task 1 Test 1. |
| T-07-01-05 | Tampering | invalid `DUFFEL_ENV` value (e.g. `staging`, `prod`, empty string) | mitigate | `Literal["test", "live", "mock"]` narrowing rejects unknown values at Settings construction time; tested in Task 1 Test 4. |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/unit/test_config_duffel.py tests/unit/llm/test_log_scrubbing.py -x -v` — all tests pass.
- `cd backend && uv run mypy app/` — zero new errors after Settings + log_scrubbing edits.
- `cd backend && uv run ruff check app/config.py app/llm/log_scrubbing.py` — clean.
- `cd backend && uv run ruff format --check app/config.py app/llm/log_scrubbing.py` — formatted.
- `grep -E 'duffel_api_token|duffel_env' backend/app/config.py` returns both fields.
- `grep -E 'duffel_\(test\|live\)_' backend/app/llm/log_scrubbing.py` returns one regex.
- `grep '^DUFFEL_API_TOKEN=' .env.example` returns one row.
</verification>

<success_criteria>
- `Settings.duffel_api_token: SecretStr | None` and `Settings.duffel_env: Literal["test","live","mock"]` exist with the exact types and defaults from D-01.
- A fresh checkout (no `DUFFEL_API_TOKEN` env var, no `.env` row) instantiates `Settings()` without `ValidationError`.
- `ApiKeyScrubber._scrub` redacts `duffel_test_<20+ chars>` and `duffel_live_<20+ chars>` substrings in log lines, leaving short prefixes alone and preserving existing OpenAI/Anthropic redaction.
- `.env.example` documents `DUFFEL_API_TOKEN` (empty placeholder) and `DUFFEL_ENV` (commented default with literal options) under a `Phase 7 — D-01` header block.
- All new and existing unit tests pass; mypy strict + ruff lint + ruff format-check are clean.
</success_criteria>

<output>
Create `.planning/phases/07-real-flight-api/07-01-SUMMARY.md` when done — capture:
- Final field declarations (with exact types).
- Regex tuple added (verbatim).
- Test count delta (Group A in `test_log_scrubbing.py` + new `test_config_duffel.py`).
- Confirmation that `Settings()` boots with no `DUFFEL_*` env vars present.
</output>
