---
phase: 07-real-flight-api
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - backend/app/api/main.py
  - backend/app/tools/flight_search.py
  - backend/tests/unit/test_tool_json_normalization.py
  - backend/tests/integration/test_lifespan_flight_provider.py
  - backend/tests/integration/test_health.py
autonomous: true
requirements: [REQ-real-flight-api]
requirements_addressed: [REQ-real-flight-api]
tags: [duffel, lifespan, health, cleanup, integration-tests]

must_haves:
  truths:
    - "Lifespan branches on Settings.duffel_env + token presence: env=='mock' OR token is None → MockFlightAPIClient + flight_provider='mock' + WARN log when token is missing in non-mock env."
    - "When env in {'test','live'} AND token is set, lifespan constructs DuffelFlightClient(api_token=settings.duffel_api_token.get_secret_value(), base_url='https://api.duffel.com') and sets flight_provider='real'."
    - "/health returns flight_provider in {'real','mock'} matching the lifespan branch (verified via integration TestClient)."
    - "Boot NEVER raises a ValidationError when DUFFEL_API_TOKEN is unset (Pitfall 6 lock)."
    - "flight_search.py no longer defines or exports normalize_amadeus_offer / normalize_skyscanner_itinerary (D-08 dead-code cleanup)."
    - "flight_search.py still defines search_flights, _to_flight_search_result, _extract_carrier_iata, _to_iso_duration — vendor-neutral helpers preserved."
  artifacts:
    - path: "backend/app/api/main.py"
      provides: "Lifespan auto-fallback branch for Duffel (D-02)"
      contains: "DuffelFlightClient"
    - path: "backend/app/tools/flight_search.py"
      provides: "Cleaned tool entry-point — vendor-specific normalizers removed (D-08)"
      contains: "search_flights"
    - path: "backend/tests/integration/test_lifespan_flight_provider.py"
      provides: "Lifespan branch coverage: mock literal, missing token, real token"
    - path: "backend/tests/integration/test_health.py"
      provides: "Updated /health flight_provider assertions"
  key_links:
    - from: "backend/app/api/main.py::lifespan"
      to: "app.flights.duffel_client.DuffelFlightClient"
      via: "Settings-driven branch (D-02)"
      pattern: "duffel_env.*mock|duffel_api_token is None"
    - from: "backend/app/api/main.py::lifespan"
      to: "app.state.flight_client + app.state.flight_provider"
      via: "DI seam consumed by /health and search_flights tool"
      pattern: "app\\.state\\.flight_(client|provider)"
---

<objective>
Wire the new `DuffelFlightClient` into the FastAPI lifespan with the D-02 auto-fallback branch, update `/health` integration coverage to verify both lifespan branches, and complete the D-08 dead-code cleanup of `flight_search.py` (delete `normalize_amadeus_offer` + `normalize_skyscanner_itinerary` plus their unit tests). This is the plan that makes the Duffel client usable by the rest of the stack and removes the last carry-over of the Amadeus implementation from the tool entry-point module.

Purpose: A developer running `just backend` with no Duffel creds gets a bootable mock-backed stack with one WARN log; a developer with `DUFFEL_API_TOKEN` set gets the real client; `/health` accurately reports which branch was taken; `flight_search.py` is materially smaller because vendor-specific normalization now lives on the client (D-08).

Output: One lifespan branch (~15 lines), one cleanup pass on `flight_search.py` (~150 LOC removed), one rebuilt integration-test module, one updated existing integration-test module, one cleanup of `test_tool_json_normalization.py`.
</objective>

<execution_context>
@/Users/axel/code/trip_planner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/trip_planner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-real-flight-api/07-CONTEXT.md
@.planning/phases/07-real-flight-api/07-RESEARCH.md
@.planning/phases/07-real-flight-api/07-PATTERNS.md
@.planning/phases/07-real-flight-api/07-01-SUMMARY.md
@.planning/phases/07-real-flight-api/07-02-SUMMARY.md
@backend/app/api/main.py
@backend/app/tools/flight_search.py
@backend/app/tools/flight_client.py
@backend/tests/integration/test_health.py
@backend/tests/unit/test_tool_json_normalization.py
@CLAUDE.md

<interfaces>
Existing lifespan shape — extend the flight-client construction block; keep everything else intact.

From backend/app/api/main.py (current Mock-only branch — replace lines 56-61):
```
flight_client: FlightAPIClient = MockFlightAPIClient(seed=42)
flight_provider = "mock"
```
The lifespan currently sets `app.state.flight_client = flight_client` and `app.state.flight_provider = flight_provider` (lines 90-91 per grep). Preserve those assignments — they are the DI seam the rest of the app reads.

From backend/app/flights/duffel_client.py (Plan 07-02 output):
```
class DuffelFlightClient(FlightAPIClient):
    def __init__(self, api_token: str, base_url: str) -> None: ...
    async def health_check(self) -> bool: ...  # returns True
    async def search(self, query, ...) -> list[Flight]: ...
```

From backend/app/tools/flight_search.py (current — to clean):
- `normalize_amadeus_offer` at line 141 — DELETE
- `normalize_skyscanner_itinerary` at line 221 — DELETE
- `_to_flight_search_result`, `_extract_carrier_iata`, `_to_iso_duration`, `search_flights` — KEEP (vendor-neutral)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reintroduce D-02 lifespan auto-fallback branch + DuffelFlightClient construction</name>
  <files>backend/app/api/main.py</files>
  <read_first>
    - backend/app/api/main.py (full file — find the lifespan function, currently constructs MockFlightAPIClient unconditionally per grep at lines 56-61)
    - backend/app/config.py (Plan 07-01 output — Settings.duffel_api_token + Settings.duffel_env confirmed in place)
    - backend/app/flights/duffel_client.py (Plan 07-02 output — confirm DuffelFlightClient signature)
    - .planning/phases/07-real-flight-api/07-PATTERNS.md (section "backend/app/api/main.py::lifespan" — exact branch shape to reintroduce)
    - .planning/phases/07-real-flight-api/07-CONTEXT.md (D-02 spec — WARN copy, fallback semantics, never-fail invariant)
    - .planning/phases/07-real-flight-api/07-RESEARCH.md (Pattern 1, Pitfall 6, OQ-1)
  </read_first>
  <action>
    Replace the current `flight_client = MockFlightAPIClient(seed=42); flight_provider = "mock"` two-liner inside `lifespan()` with the D-02 branch.

    1. Add a top-of-file import: `from app.flights.duffel_client import DuffelFlightClient`. Place it alphabetically inside the existing `from app.flights.*` import block; if there are no other `app.flights` imports yet in `main.py`, add it next to `from app.tools.flight_client import FlightAPIClient, MockFlightAPIClient`.
    2. Locate the existing block (around lines 56-61 per grep result) starting with the comment `# Phase 7 (vendor switch): Amadeus integration removed; ...` and ending with `flight_provider = "mock"`.
    3. Replace it with the D-02 branch (preserve indentation level — it is inside the `async def lifespan` body):
       - Read `settings` (already imported / accessible via the existing lifespan; if not, import `from app.config import settings`).
       - Branch: `if settings.duffel_env == "mock" or settings.duffel_api_token is None:` then mock construction, else `DuffelFlightClient(api_token=settings.duffel_api_token.get_secret_value(), base_url="https://api.duffel.com")` with `flight_provider = "real"`.
       - Inside the mock branch, ALSO check `if settings.duffel_env != "mock":` — if true (i.e. token is missing in a non-mock env), emit `logger.warning("DUFFEL_API_TOKEN missing — falling back to MockFlightAPIClient")`. The literal substring `DUFFEL_API_TOKEN missing` is asserted by Task 3 integration test, so do NOT paraphrase. Use the existing `logger` defined at module scope (verify via grep before assuming).
       - The `if settings.duffel_env == "mock"` no-WARN branch is intentional — when the developer explicitly selects mock via env they have already opted in.
    4. Keep the existing `app.state.flight_client = flight_client` and `app.state.flight_provider = flight_provider` assignments downstream.
    5. Make sure `flight_client: FlightAPIClient` annotation lives on the FIRST assignment in the branch so mypy narrows correctly across both branches; the second assignment uses bare `flight_client = DuffelFlightClient(...)`.

    Do NOT change anything else in `main.py` — chat service construction, conversation store, app.state.chat_service, lifespan yield/cleanup, lifespan registration on the FastAPI constructor are all untouched.

    The WARN copy must match exactly (including the em-dash U+2014): `DUFFEL_API_TOKEN missing — falling back to MockFlightAPIClient`.
  </action>
  <verify>
    <automated>cd backend && uv run mypy app/api/main.py && uv run ruff check app/api/main.py && uv run ruff format --check app/api/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run mypy app/api/main.py` returns zero errors.
    - `cd backend && uv run ruff check app/api/main.py` clean; `ruff format --check` clean.
    - `grep -cE 'from app\.flights\.duffel_client import DuffelFlightClient' backend/app/api/main.py` returns `1`.
    - `grep -cE 'settings\.duffel_env == .mock.' backend/app/api/main.py` returns `1` AND `grep -cE 'settings\.duffel_api_token is None' backend/app/api/main.py` returns `1`.
    - `grep -F 'DUFFEL_API_TOKEN missing' backend/app/api/main.py | wc -l` returns 1 (literal substring including em-dash).
    - `grep -cE 'DuffelFlightClient\(' backend/app/api/main.py` returns at least `1` (constructor call).
    - `grep -cE 'get_secret_value\(\)' backend/app/api/main.py` returns at least `1` (token unwrapped via SecretStr.get_secret_value at construction site).
    - Boot smoke test: `cd backend && DUFFEL_API_TOKEN= uv run python -c "from app.api.main import app; print('boot ok')"` exits 0 with output `boot ok` (Pitfall 6 — never fails on missing creds).
  </acceptance_criteria>
  <done>
    Lifespan branches per D-02; boot is robust against missing creds; DuffelFlightClient construction site is the only place the bearer token leaves SecretStr.
  </done>
</task>

<task type="auto">
  <name>Task 2: D-08 dead-code cleanup — delete normalize_amadeus_offer + normalize_skyscanner_itinerary and their tests</name>
  <files>backend/app/tools/flight_search.py, backend/tests/unit/test_tool_json_normalization.py</files>
  <read_first>
    - backend/app/tools/flight_search.py (full file — identify exact line ranges for the two functions and their internal helpers)
    - backend/tests/unit/test_tool_json_normalization.py (full file — identify which tests reference the deleted functions per the grep result lines 18, 19, 84, 247, 253, 271, 277, 286, 291, 302)
    - .planning/phases/07-real-flight-api/07-CONTEXT.md (D-08 — normalization moves onto each FlightAPIClient impl; module-level normalizers have no live caller)
    - .planning/phases/07-real-flight-api/07-PATTERNS.md (section "backend/app/tools/flight_search.py")
    - .planning/phases/07-real-flight-api/07-RESEARCH.md (§"State of the Art" — confirms D-08 deletion is design-correct)
  </read_first>
  <action>
    In `backend/app/tools/flight_search.py`:

    1. Delete `normalize_amadeus_offer` (currently at line 141 per grep) — entire function definition, docstring, and any helpers used ONLY by it.
    2. Delete `normalize_skyscanner_itinerary` (currently at line 221 per grep) — same scope.
    3. After deletion, scan unused imports: `from datetime import UTC` (used inside `normalize_skyscanner_itinerary` per PATTERNS notes) — remove if no other callers; `from typing import Any` — keep iff still used by any remaining symbol; otherwise drop.
    4. Find any internal helper imported and used ONLY by the deleted normalizers. Delete those too. Do NOT delete `_extract_carrier_iata`, `_to_iso_duration`, `_to_flight_search_result`, or `search_flights` — those are vendor-neutral and remain in use.
    5. Update the comment in `_to_flight_search_result` (lines 86-88 per PATTERNS) — replace any "Phase 7's real Amadeus client will populate `city`..." prose with a short Duffel-aware note: `Flight.origin`/`destination` come from segment IATA codes; `city` stays None for v1 (airport-IATA→city lookup deferred per CONTEXT.md). Keep it terse — single comment block, ≤4 lines.
    6. Update the module docstring at the top of the file: replace any Amadeus mentions with Duffel where appropriate, OR delete stale references entirely. Module purpose is unchanged: vendor-neutral tool entry point + FlightResult shaping.

    In `backend/tests/unit/test_tool_json_normalization.py`:

    1. Delete tests that reference `normalize_amadeus_offer` or `normalize_skyscanner_itinerary` (per grep these are at lines 247, 253, 271, 277, 286, 291, 302). Identify each `def test_*` block that uses these names and remove the entire block including its docstring and fixtures.
    2. Keep tests that exercise the vendor-neutral models (`Flight`, `FlightSegment`, `FlightResult`) directly — these remain valid.
    3. Drop the now-unused imports at lines 18-19 (`normalize_amadeus_offer`, `normalize_skyscanner_itinerary` from `app.tools.flight_search`).
    4. Add a regression-lock test at the top of the surviving file:
       ```
       def test_flight_search_module_no_amadeus_normalizers() -> None:
           """D-08 lock: vendor-specific module-level normalizers are gone."""
           from app.tools import flight_search
           assert not hasattr(flight_search, "normalize_amadeus_offer")
           assert not hasattr(flight_search, "normalize_skyscanner_itinerary")
       ```
    5. If after deletion the file has fewer than 2 surviving tests besides the regression-lock, that is acceptable — coverage of those vendor-neutral models lives in `test_flight_models.py` already.

    Do NOT delete the file outright. After deletion, run mypy + the unit suite to confirm no broken imports anywhere else in `tests/`.
  </action>
  <verify>
    <automated>cd backend && uv run mypy app/tools/flight_search.py tests/unit/test_tool_json_normalization.py && uv run pytest tests/unit/test_tool_json_normalization.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'def normalize_amadeus_offer|def normalize_skyscanner_itinerary' backend/app/tools/flight_search.py` returns `0` (both deleted).
    - `grep -cE 'normalize_amadeus_offer|normalize_skyscanner_itinerary' backend/tests/unit/test_tool_json_normalization.py` returns `0` (every reference gone).
    - `cd backend && uv run pytest tests/unit/test_tool_json_normalization.py -x -v` passes (every surviving test green; regression-lock test passes).
    - `cd backend && uv run mypy app/tools/flight_search.py` zero errors; `ruff check` and `ruff format --check` clean.
    - `cd backend && uv run pytest tests/unit/ -x` (full unit suite) green — confirms no other test imports the deleted symbols.
    - `grep -cE 'def search_flights|def _to_flight_search_result|def _extract_carrier_iata|def _to_iso_duration' backend/app/tools/flight_search.py` returns `4` (preserved helpers + tool entry).
  </acceptance_criteria>
  <done>
    Vendor-specific module-level normalizers and their tests are gone; vendor-neutral helpers preserved; regression-lock test prevents reintroduction.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration tests for lifespan flight-provider branches + /health flight_provider invariant</name>
  <files>backend/tests/integration/test_lifespan_flight_provider.py, backend/tests/integration/test_health.py</files>
  <read_first>
    - backend/app/api/main.py (Task 1 output — confirm lifespan branch shape)
    - backend/tests/integration/test_health.py (current — Task will update the comment + assertions)
    - backend/tests/integration/conftest.py (existing fixtures — confirm whether httpx autouse stub exists; lifespan tests must NOT inherit it if it would block real `app.state` setup)
    - .planning/phases/07-real-flight-api/07-PATTERNS.md (section "backend/tests/integration/test_lifespan_flight_provider.py" — full template body to follow)
    - .planning/phases/07-real-flight-api/07-CONTEXT.md (D-02 + /health invariant)
  </read_first>
  <behavior>
    `test_lifespan_flight_provider.py` covers three lifespan branches via FastAPI `TestClient(app)` context-manager (the bare module-level constructor does NOT trigger lifespan — `with TestClient(app) as client:` is required):

    - `test_lifespan_uses_mock_when_duffel_env_is_mock`: monkeypatch `app.config.settings.duffel_env` to `"mock"`, monkeypatch `app.config.settings.duffel_api_token` to `None`, enter lifespan via `TestClient`, hit `/health`, assert `app.state.flight_provider == "mock"` AND `isinstance(app.state.flight_client, MockFlightAPIClient)`. Assert NO WARN log fires (caplog at WARNING level returns no records containing `DUFFEL_API_TOKEN missing`) — opting into mock explicitly is silent.
    - `test_lifespan_falls_back_to_mock_when_token_missing`: monkeypatch `duffel_env` to `"test"`, `duffel_api_token` to `None`, capture WARN logs at level WARNING for logger `app.api.main`, enter lifespan, assert mock client + flight_provider == 'mock' + at least one log record contains the literal substring `DUFFEL_API_TOKEN missing`. This is the Pitfall 6 + D-02 fallback path.
    - `test_lifespan_constructs_duffel_client_with_real_token`: monkeypatch `duffel_env` to `"test"`, `duffel_api_token` to `SecretStr("duffel_test_fake_xxxxxxxxxxxxxxxx")`, enter lifespan, hit `/health`, assert `app.state.flight_provider == "real"` AND `isinstance(app.state.flight_client, DuffelFlightClient)` AND `app.state.flight_client._base_url == "https://api.duffel.com"`. The token is NEVER unwrapped into a logged string by this test — assertion uses `_base_url` only.

    `test_health.py` updates:
    - `test_health_flight_provider_is_mock_without_credentials`: rewrite the docstring/comment block. Currently says "Phase 7 vendor switch in progress; the real-client branch will return when the Duffel integration lands." — replace with: "Default test env has no real-flight-API creds AND duffel_env defaults to 'test', so D-02 fallback selects MockFlightAPIClient. Real-client branch is exercised in test_lifespan_flight_provider.py." Assertion logic stays unchanged: `assert response.json()["flight_provider"] == "mock"`.
    - `test_health` (the unconditional shape test): no change required — the post-Plan-07-05 invariant `flight_provider in {"real", "mock"}` is preserved by D-02.
  </behavior>
  <action>
    Create `backend/tests/integration/test_lifespan_flight_provider.py` per the verbatim template in 07-PATTERNS.md §"backend/tests/integration/test_lifespan_flight_provider.py", with these substitutions confirmed:
    - `monkeypatch.setattr("app.config.settings.duffel_env", "mock")` — pydantic-settings `Settings` instances support attribute assignment when defined as a singleton; if `app.config` does not export a module-level `settings` symbol, fall back to monkeypatching the relevant getter or constructing a fresh `Settings(_env_file=None, duffel_env="mock", duffel_api_token=None)` and patching `app.config.settings` to point to it.
    - Use `caplog.at_level(logging.WARNING, logger="app.api.main")` to capture the D-02 WARN log. The logger name MUST match the actual `logger = logging.getLogger(__name__)` location in `main.py` (verify via `grep -n 'logger = logging.getLogger' backend/app/api/main.py`).
    - Imports: `logging`, `pytest`, `fastapi.testclient.TestClient`, `pydantic.SecretStr`, `app.api.main.app`, `app.flights.duffel_client.DuffelFlightClient`, `app.tools.flight_client.MockFlightAPIClient`.
    - Module docstring states: "Lifespan flight-provider branch tests (Phase 7 / Plan 07-03, D-01 + D-02)." with a short summary of the three tested branches.
    - No pytest markers (Phase 4.4 removed them).

    Update `backend/tests/integration/test_health.py`:
    - Rewrite the docstring on `test_health_flight_provider_is_mock_without_credentials` per the `<behavior>` block.
    - Optionally tighten the top-level module docstring to remove the stale "vendor switch in progress" line.
    - Do not add new test cases here — `test_lifespan_flight_provider.py` is the new canonical location for branch coverage; this file just retains the unconditional-shape sanity check.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/test_lifespan_flight_provider.py tests/integration/test_health.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/integration/test_lifespan_flight_provider.py tests/integration/test_health.py -x -v` passes with at least 3 tests in `test_lifespan_flight_provider.py` (one per branch) and 2 tests in `test_health.py` and 0 failures.
    - `grep -cE 'def test_lifespan_uses_mock_when_duffel_env_is_mock|def test_lifespan_falls_back_to_mock_when_token_missing|def test_lifespan_constructs_duffel_client_with_real_token' backend/tests/integration/test_lifespan_flight_provider.py` returns `3` (all three branch tests present).
    - `cd backend && uv run mypy tests/integration/test_lifespan_flight_provider.py tests/integration/test_health.py` zero errors.
    - `cd backend && uv run ruff check tests/integration/test_lifespan_flight_provider.py tests/integration/test_health.py` clean.
    - `cd backend && uv run pytest tests/integration/ -x` (full integration suite) green — no regressions in chat / auth / db tests.
    - `grep -cF 'DUFFEL_API_TOKEN missing' backend/tests/integration/test_lifespan_flight_provider.py` returns at least `1` (asserts the WARN literal).
  </acceptance_criteria>
  <done>
    Lifespan-branch invariants are now under integration test coverage; `/health` test docstring no longer references "vendor switch in progress"; full integration suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Settings.duffel_api_token` (SecretStr) → DuffelFlightClient constructor (str) | Bearer token leaves SecretStr opacity at exactly one call site (lifespan); no other code path unwraps. |
| process → /health response body | Lifespan-derived `flight_provider` value crosses here; must NOT leak token state beyond `"real" \| "mock"`. |
| LLM tool layer → app.state.flight_client | DI seam; the tool layer must keep treating `FlightAPIClient` as a black-box ABC (no isinstance branching on Duffel vs Mock). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Information Disclosure | bearer-token leakage at the lifespan unwrap site | mitigate | The single `settings.duffel_api_token.get_secret_value()` call in `lifespan` is the only place the token leaves SecretStr opacity. The unwrapped string passes directly to `DuffelFlightClient.__init__` and is stored on `self._api_token`. Plan 07-01 `ApiKeyScrubber` regex provides defense in depth on any accidental log line. Tested in Task 3 by asserting `_base_url` only — never inspecting the stored token. |
| T-07-03-02 | Denial of Service (self) | boot-time hard-fail when `DUFFEL_API_TOKEN` env is unset (Pitfall 6) | mitigate | D-02 lifespan checks `settings.duffel_api_token is None` and falls through to mock; `Settings.duffel_api_token` already defaults to `None` (Plan 07-01). Task 1 `acceptance_criteria` includes a literal boot smoke test; Task 3 includes the `test_lifespan_falls_back_to_mock_when_token_missing` integration test. |
| T-07-03-03 | Tampering | attacker-controlled `flight_provider` field on `/health` enabling fingerprinting | accept | `flight_provider` is a literal `"real" \| "mock"` (per `app.state.flight_provider` assignments) — no user input flows into this field; it reflects internal lifespan choice only. Acceptable disclosure level for a demo health endpoint; Phase 8 `RequestLoggingMiddleware` will not change this contract. |
| T-07-03-04 | Repudiation | silent fallback to mock with no operator visibility | mitigate | D-02 specifies `logger.warning("DUFFEL_API_TOKEN missing — falling back to MockFlightAPIClient")` exactly once at lifespan startup when `duffel_env != "mock"` and token is `None`. Captured by `test_lifespan_falls_back_to_mock_when_token_missing` (caplog assertion). The explicit `duffel_env == "mock"` branch is intentionally silent — the operator opted in. |
| T-07-03-05 | Tampering | dead-code `normalize_amadeus_offer` / `normalize_skyscanner_itinerary` becoming stale shims | mitigate | Task 2 deletion + regression-lock test `test_flight_search_module_no_amadeus_normalizers` prevents future reintroduction. Closes the D-08 attack surface (no caller invoking a half-maintained vendor-specific path). |
| T-07-03-SC | Tampering | npm/pip installs introducing untrusted code | accept | Phase 7 plans introduce zero new packages (RESEARCH §"Package Legitimacy Audit": 3 OK on existing pins). |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/integration/test_lifespan_flight_provider.py tests/integration/test_health.py tests/unit/test_tool_json_normalization.py -x -v` — green.
- `cd backend && uv run pytest tests/unit/ tests/integration/ -x` — full default-collection suite green.
- `cd backend && uv run mypy app/api/main.py app/tools/flight_search.py tests/integration/test_lifespan_flight_provider.py tests/integration/test_health.py tests/unit/test_tool_json_normalization.py` — zero errors.
- `cd backend && uv run ruff check app/api/main.py app/tools/flight_search.py` — clean.
- Boot smoke: `cd backend && DUFFEL_API_TOKEN= uv run python -c "from app.api.main import app; print('boot ok')"` exits 0.
- `grep -cE 'def normalize_amadeus_offer|def normalize_skyscanner_itinerary' backend/app/tools/flight_search.py` returns `0`.
- `grep -cF 'DuffelFlightClient' backend/app/api/main.py` returns at least `1`.
</verification>

<success_criteria>
- Lifespan auto-fallback per D-02: missing creds → mock with WARN, mock-env → silent mock, real creds → DuffelFlightClient targeting `https://api.duffel.com`.
- `/health` shape preserved (`flight_provider in {"real","mock"}`); branch coverage now lives in `test_lifespan_flight_provider.py`.
- D-08 cleanup complete: vendor-specific normalizers gone from `flight_search.py` and from `test_tool_json_normalization.py`; regression-lock test prevents reintroduction.
- Boot smoke test exits 0 with no `DUFFEL_*` env vars set (Pitfall 6 invariant).
- All unit + integration tests pass; mypy + ruff clean across modified files.
</success_criteria>

<output>
Create `.planning/phases/07-real-flight-api/07-03-SUMMARY.md` when done — capture:
- Final lifespan branch shape (paste the 8-12 line excerpt for the record).
- LOC delta for `flight_search.py` (before vs after — should drop ~150).
- Test count delta for `test_tool_json_normalization.py` (before vs after).
- Confirmation of the `DUFFEL_API_TOKEN missing` literal substring at the exact em-dash codepoint.
- Boot smoke test result.
</output>
