---
phase: 07-real-flight-api
plan: 05
type: execute
wave: 4
depends_on:
  - 07-04
files_modified:
  - backend/app/api/main.py
  - backend/app/api/routes/routes.py
  - backend/tests/integration/test_health.py
  - backend/tests/integration/test_lifespan_flight_provider.py
autonomous: true
requirements:
  - REQ-real-flight-api
must_haves:
  truths:
    - "Lifespan branches on `Settings.amadeus_env` AND credential presence: `mock` env or missing creds → `MockFlightAPIClient` + `flight_provider='mock'`."
    - "Real-mode lifespan path uses `https://test.api.amadeus.com` for `amadeus_env=='test'` and `https://api.amadeus.com` for `amadeus_env=='prod'` (no other URL is constructable — T-07-01 mitigation)."
    - "When credentials are missing in non-mock mode, lifespan logs WARN `\"AMADEUS_* creds missing — falling back to MockFlightAPIClient\"` (D-05). Startup never fails because of missing creds."
    - "`GET /health` returns `{\"status\":\"healthy\",\"flight_provider\":\"mock\"|\"real\"}` (D-06). Field is always present when lifespan has run."
    - "`app.state.flight_client` and `app.state.flight_provider` are set after startup."
  artifacts:
    - path: backend/app/api/main.py
      provides: "lifespan auto-fallback branch + app.state.flight_provider stash"
      contains: "flight_provider"
    - path: backend/app/api/routes/routes.py
      provides: "/health endpoint extended with flight_provider field"
      contains: "flight_provider"
    - path: backend/tests/integration/test_health.py
      provides: "/health field-shape assertion + flight_provider='mock' default"
      contains: "flight_provider"
    - path: backend/tests/integration/test_lifespan_flight_provider.py
      provides: "lifespan-branch tests: mock env → MockFlightAPIClient; missing creds → fallback + WARN; real creds → AmadeusFlightClient"
      contains: "test_lifespan"
  key_links:
    - from: backend/app/api/main.py
      to: app.flights.amadeus_client.AmadeusFlightClient
      via: "conditional construction inside lifespan"
      pattern: "AmadeusFlightClient\\("
    - from: backend/app/api/main.py
      to: app.state.flight_provider
      via: "string assignment"
      pattern: "app\\.state\\.flight_provider"
    - from: backend/app/api/routes/routes.py
      to: app.state.flight_provider
      via: "request.app.state read in /health"
      pattern: "request\\.app\\.state\\.flight_provider"
---

<objective>
Wire `AmadeusFlightClient` (Plan 04) into the FastAPI lifespan with the auto-fallback branch (D-04 + D-05): if `Settings.amadeus_env == "mock"` OR either credential is missing, log WARN and instantiate `MockFlightAPIClient`; otherwise instantiate `AmadeusFlightClient` against the env-appropriate base URL. Stash `app.state.flight_provider` ("real" | "mock") and extend `GET /health` to surface that field per D-06. With this plan complete, the running app exposes the choice from outside the process and degrades gracefully when credentials are absent — a developer with no Amadeus account still gets a working stack.

Purpose: This is the integration seam that makes Plan 04's client observable end-to-end. Plan 06's e2e_amadeus tests rely on the real client being constructed when `AMADEUS_*` env vars are present; Plan 06's CI gating relies on the WARN log + fallback when they aren't.
Output: Modified `lifespan`, modified `/health`, integration test extension proving the fallback path + the field shape, new lifespan test using FastAPI test client to verify both branches.
</objective>

<execution_context>
@/Users/axel/code/trip_planner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/trip_planner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-real-flight-api/07-CONTEXT.md
@.planning/phases/07-real-flight-api/07-RESEARCH.md
@.planning/phases/07-real-flight-api/07-PATTERNS.md
@.planning/phases/07-real-flight-api/07-VALIDATION.md
@.planning/phases/07-real-flight-api/07-04-SUMMARY.md
@CLAUDE.md
@backend/app/api/main.py
@backend/app/api/routes/routes.py
@backend/app/config.py
@backend/app/flights/amadeus_client.py
@backend/app/tools/flight_client.py
@backend/tests/integration/test_health.py
@backend/tests/integration/conftest.py

<interfaces>
<!-- Lifespan invariant: chat_service receives the FlightAPIClient (mock or real) -->
From backend/app/api/main.py (current):
- Lifespan constructs `MockFlightAPIClient(seed=42)` unconditionally (line 57) and passes it to `ChatService(flight_client=flight_client, ...)` (line 71).
- After Plan 05, the construction is conditional and `app.state.flight_provider: str` is set BEFORE `yield` so route handlers can read it.

From backend/app/api/routes/routes.py (current /health, line 429-439):
- `async def health_check() -> dict[str, str]:` — no `Request` parameter. After Plan 05 it accepts `Request` and reads `app.state.flight_provider`.

From backend/app/flights/amadeus_client.py (post-Plan-04):
- `AmadeusFlightClient(api_key: str, api_secret: str, base_url: str)` — three required positional arguments. SecretStr values must be unpacked via `.get_secret_value()` at the lifespan call site.

From backend/app/config.py (post-Plan-02):
- `settings.amadeus_env`, `settings.amadeus_api_key`, `settings.amadeus_api_secret`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modify lifespan auto-fallback + extend /health with flight_provider</name>
  <files>backend/app/api/main.py, backend/app/api/routes/routes.py</files>
  <read_first>
    backend/app/api/main.py (entire file — currently 162 lines)
    backend/app/api/routes/routes.py (lines 1-15 imports, 429-439 /health endpoint, 446-475 example of `request.app.state` read pattern)
    backend/app/flights/amadeus_client.py (post-Plan-04 constructor signature)
    backend/app/config.py (post-Plan-02 amadeus_* fields with SecretStr)
    .planning/phases/07-real-flight-api/07-RESEARCH.md (Pattern 6: Lifespan auto-fallback, Pattern 8: Health endpoint extension)
    .planning/phases/07-real-flight-api/07-PATTERNS.md (`backend/app/api/main.py` section, lines 387-433; `backend/app/api/routes/routes.py` section, lines 436-462)
    .planning/phases/07-real-flight-api/07-CONTEXT.md (D-04, D-05, D-06)
  </read_first>
  <behavior>
    - Lifespan replaces the unconditional `flight_client = MockFlightAPIClient(seed=42)` (line 57) with a conditional branch:
      - If `settings.amadeus_env == "mock"` OR `settings.amadeus_api_key is None` OR `settings.amadeus_api_secret is None`: instantiate `MockFlightAPIClient(seed=42)`, set `flight_provider = "mock"`. If `amadeus_env != "mock"` and creds were missing, log `logger.warning("AMADEUS_* creds missing — falling back to MockFlightAPIClient")`.
      - Else: compute `base_url = "https://test.api.amadeus.com"` if `amadeus_env == "test"` else `"https://api.amadeus.com"`. Instantiate `AmadeusFlightClient(api_key=settings.amadeus_api_key.get_secret_value(), api_secret=settings.amadeus_api_secret.get_secret_value(), base_url=base_url)`. Set `flight_provider = "real"`.
    - `app.state.flight_provider = flight_provider` is assigned BEFORE `yield` so all routes can read it.
    - Type annotation on the local variable: `flight_client: FlightAPIClient` (the abstract base type), so both branches type-check identically. Add `FlightAPIClient` to the existing `from app.tools.flight_client import MockFlightAPIClient` import.
    - The `chat_service = ChatService(flight_client=flight_client, ...)` call is unchanged — `ChatService` already accepts `FlightAPIClient` polymorphically.
    - `GET /health` route handler accepts `request: Request` (the symbol is already imported in `routes.py` line 8) and returns `{"status": "healthy", "flight_provider": getattr(request.app.state, "flight_provider", "mock")}`. The `getattr` default guards against TestClient invocations that don't run the lifespan (defensive but correct — the real CI path always runs lifespan via FastAPI TestClient context manager).
  </behavior>
  <action>
    Open `backend/app/api/main.py`. Add `AmadeusFlightClient` to the imports: change `from app.tools.flight_client import MockFlightAPIClient` to `from app.flights.amadeus_client import AmadeusFlightClient` AND `from app.tools.flight_client import FlightAPIClient, MockFlightAPIClient` (add `FlightAPIClient` for type annotation). Order imports per ruff isort: app.* imports stay grouped together.
    Replace line 57 (`flight_client = MockFlightAPIClient(seed=42)`) with the conditional branch from PATTERNS lines 401-426 / RESEARCH Pattern 6:
    ```
    if (
        settings.amadeus_env == "mock"
        or settings.amadeus_api_key is None
        or settings.amadeus_api_secret is None
    ):
        if settings.amadeus_env != "mock":
            logger.warning("AMADEUS_* creds missing — falling back to MockFlightAPIClient")
        flight_client: FlightAPIClient = MockFlightAPIClient(seed=42)
        flight_provider = "mock"
    else:
        base_url = (
            "https://test.api.amadeus.com"
            if settings.amadeus_env == "test"
            else "https://api.amadeus.com"
        )
        flight_client = AmadeusFlightClient(
            api_key=settings.amadeus_api_key.get_secret_value(),
            api_secret=settings.amadeus_api_secret.get_secret_value(),
            base_url=base_url,
        )
        flight_provider = "real"
    ```
    Below the existing `app.state.user_repo = ...` line (≈ line 88), add `app.state.flight_provider = flight_provider` (place it grouped logically with the other flight-related state — actually best to place it right after `chat_service` is constructed and before `app.state.user_repo` since flight state is more closely related to the flight client).
    Open `backend/app/api/routes/routes.py`. Modify `health_check` (line 429-439) per PATTERNS lines 449-460:
    Change signature from `async def health_check() -> dict[str, str]:` to `async def health_check(request: Request) -> dict[str, str]:`. Body becomes:
    ```
    flight_provider = getattr(request.app.state, "flight_provider", "mock")
    return {"status": "healthy", "flight_provider": flight_provider}
    ```
    `Request` is already imported at line 8 (`from fastapi import APIRouter, Depends, HTTPException, Request, status`).
    Run `cd backend && uv run mypy app/api/main.py app/api/routes/routes.py` — strict pass.
    Run `cd backend && uv run ruff check app/api/main.py app/api/routes/routes.py`.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from fastapi.testclient import TestClient; from app.api.main import app
with TestClient(app) as client:
    r = client.get('/health')
    assert r.status_code == 200, r.status_code
    body = r.json()
    assert body['status'] == 'healthy', body
    assert 'flight_provider' in body, body
    assert body['flight_provider'] in ('real', 'mock'), body
    print('ok', body)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nv '^#' backend/app/api/main.py | grep -c 'AmadeusFlightClient'` returns ≥ 2 (import + construction).
    - `grep -nv '^#' backend/app/api/main.py | grep -c 'app\.state\.flight_provider'` returns ≥ 1.
    - `grep -nv '^#' backend/app/api/main.py | grep -c 'amadeus_env'` returns ≥ 1.
    - `grep -nv '^#' backend/app/api/main.py | grep -c 'AMADEUS_\* creds missing'` returns ≥ 1 (WARN string locked).
    - `grep -nv '^#' backend/app/api/main.py | grep -c 'get_secret_value'` returns ≥ 2 (key + secret unpacked).
    - `grep -nv '^#' backend/app/api/routes/routes.py | grep -c 'flight_provider'` returns ≥ 1.
    - `cd backend && uv run mypy app/api/main.py app/api/routes/routes.py` passes.
    - `cd backend && uv run ruff check app/api/main.py app/api/routes/routes.py` passes.
    - Verification command above prints `ok` with `'flight_provider': 'mock'` (default test env has no AMADEUS_* set).
  </acceptance_criteria>
  <done>
    Lifespan auto-fallback works; `/health` exposes `flight_provider`; mypy + ruff clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add /health field-shape test + lifespan-branch integration tests</name>
  <files>backend/tests/integration/test_health.py, backend/tests/integration/test_lifespan_flight_provider.py</files>
  <read_first>
    backend/tests/integration/test_health.py (current 14 lines)
    backend/tests/integration/conftest.py (autouse httpx stub — note the fixture)
    backend/app/api/main.py (post-Task-1 lifespan)
    backend/app/api/routes/routes.py (post-Task-1 /health)
    backend/app/flights/amadeus_client.py (constructor signature)
    .planning/phases/07-real-flight-api/07-PATTERNS.md (`backend/tests/integration/test_health.py` section, lines 636-664)
  </read_first>
  <behavior>
    File `test_health.py` (extension):
    - The existing `test_health()` is updated so it asserts the new shape: `data["status"] == "healthy"`, `"flight_provider" in data`, `data["flight_provider"] in ("real", "mock")`. The exact-equality assertion `assert response.json() == {"status": "healthy"}` from line 14 must be removed and replaced.
    - New test `test_health_flight_provider_is_mock_without_credentials()`: in default test env (no `AMADEUS_API_KEY`/`AMADEUS_API_SECRET` set), `/health` returns `flight_provider == "mock"`.
    File `test_lifespan_flight_provider.py` (new):
    - `test_lifespan_uses_mock_when_amadeus_env_is_mock`: force `Settings(amadeus_env="mock")` via env-var monkeypatch, build app, run TestClient context (which triggers lifespan), assert `app.state.flight_provider == "mock"` AND `isinstance(app.state.chat_service.flight_client, MockFlightAPIClient)`.
    - `test_lifespan_falls_back_to_mock_when_credentials_missing`: set `AMADEUS_ENV=test`, leave `AMADEUS_API_KEY` unset; run TestClient; assert `flight_provider == "mock"` AND a WARN log line containing `"AMADEUS_* creds missing"` was emitted (use `caplog.at_level(logging.WARNING)`).
    - `test_lifespan_constructs_amadeus_client_with_real_credentials`: set `AMADEUS_ENV=test`, `AMADEUS_API_KEY=fake_key_value`, `AMADEUS_API_SECRET=fake_secret_value`; run TestClient; assert `app.state.flight_provider == "real"` AND `isinstance(app.state.chat_service.flight_client, AmadeusFlightClient)` AND the constructed client's `_base_url == "https://test.api.amadeus.com"`. NO real HTTP traffic occurs (the lifespan only constructs the client; it doesn't fetch a token until `_get_token()` is awaited).
  </behavior>
  <action>
    Edit `backend/tests/integration/test_health.py`:
      - Replace the body of `test_health` per `<behavior>` so it asserts the new shape and adds the membership check on `flight_provider`.
      - Add a new `test_health_flight_provider_is_mock_without_credentials` test. The `monkeypatch` fixture (from pytest) is unnecessary here — the conftest in `tests/integration/conftest.py` doesn't pre-populate `AMADEUS_*` so the default test environment naturally has no creds. Just call `client.get("/health")` and assert.
      - Keep the existing `client = TestClient(app)` module-level fixture, but switch the new tests to use `with TestClient(app) as client:` form because lifespan must run for `app.state.flight_provider` to be set. The module-level `TestClient(app)` does NOT trigger lifespan — the context-manager form does. Update both tests accordingly.
    Create `backend/tests/integration/test_lifespan_flight_provider.py`:
      - Imports: `import logging`, `import pytest`, `from fastapi.testclient import TestClient`, `from app.flights.amadeus_client import AmadeusFlightClient`, `from app.tools.flight_client import MockFlightAPIClient`.
      - In each test: use `monkeypatch.setenv` (and `monkeypatch.delenv` with `raising=False`) to set the desired `AMADEUS_*` values. Then importantly: the `Settings` instance in `app.config` is module-level and frozen at import time. To re-read env vars per test, the test must construct a fresh app via `importlib.reload` of `app.api.main` or use a session-scoped Settings reset. The cleaner approach: build a fresh app via dependency override-style setup. The simplest reliable approach: use `pytest.MonkeyPatch.setattr("app.config.settings.amadeus_env", "mock")` (or `"test"`) and the corresponding credential fields directly on the singleton `settings` object before constructing `TestClient(app)` with the context-manager.
      - For each test: `monkeypatch.setattr("app.config.settings.amadeus_env", <value>)`; `monkeypatch.setattr("app.config.settings.amadeus_api_key", None)` or a `SecretStr("fake_key_value")` instance; same for `_secret`. Then `with TestClient(app) as client:` ... assert `app.state.flight_provider == ...` and `type(app.state.chat_service.flight_client).__name__ == "AmadeusFlightClient"` (or `MockFlightAPIClient`).
      - For the WARN-log test: use `caplog` fixture with `caplog.at_level(logging.WARNING, logger="app.api.main")`. After the `with TestClient(app) as client:` block, assert `any("AMADEUS_* creds missing" in record.message for record in caplog.records)`.
      - For the real-credentials test: imports `from pydantic import SecretStr`; sets `settings.amadeus_api_key = SecretStr("fake_key_value")` and `settings.amadeus_api_secret = SecretStr("fake_secret_value")`; assert `app.state.chat_service.flight_client._base_url == "https://test.api.amadeus.com"`.
    Run `cd backend && uv run pytest tests/integration/test_health.py tests/integration/test_lifespan_flight_provider.py -v` and confirm all pass.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/test_health.py tests/integration/test_lifespan_flight_provider.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/integration/test_health.py -v` runs ≥ 2 tests, all green.
    - `cd backend && uv run pytest tests/integration/test_lifespan_flight_provider.py -v` runs ≥ 3 tests, all green.
    - `grep -n 'flight_provider' backend/tests/integration/test_health.py` returns ≥ 2 matches.
    - `grep -n 'AmadeusFlightClient\|MockFlightAPIClient' backend/tests/integration/test_lifespan_flight_provider.py` returns ≥ 3 matches.
    - `grep -n 'AMADEUS_\* creds missing' backend/tests/integration/test_lifespan_flight_provider.py` returns ≥ 1 match (WARN log assertion).
    - `cd backend && uv run ruff check tests/integration/test_health.py tests/integration/test_lifespan_flight_provider.py` passes.
    - Full integration suite still passes: `cd backend && uv run pytest tests/integration/ -x --tb=short`.
  </acceptance_criteria>
  <done>
    `/health` field-shape regression test exists; lifespan-branch tests cover all three branches (mock env, missing creds with WARN, real creds → AmadeusFlightClient with correct base_url); all green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment → process | `AMADEUS_*` env vars cross from host into Settings; `Literal` (Plan 02) + `SecretStr` (Plan 02) are the gates. |
| Process → outside observers | `/health` exposes `flight_provider` ("real"/"mock") — a coarse capability fingerprint, not a credential. Acceptable for an internal demo project. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Lifespan base_url construction | mitigate | The base_url branch is `if amadeus_env == "test" else "https://api.amadeus.com"` — only two known URLs are constructable, both hardcoded. The Literal type on `amadeus_env` (Plan 02) prevents arbitrary string injection. Verified by `test_lifespan_constructs_amadeus_client_with_real_credentials` asserting the exact base_url. |
| T-07-02 | Information Disclosure | `flight_provider` field on `/health` | accept | The field is a coarse "real"/"mock" boolean — it does not leak credentials, base URLs, or rate-limit state. Acceptable observability for an internal demo (CONTEXT.md confirms). Phase 8 may gate `/health` further if a public deployment surface ever materializes. |
| T-07-03 | Information Disclosure | WARN log "AMADEUS_* creds missing" | accept | The log line names the env var by name only — never echoes any value. The existing `ApiKeyScrubber` is installed (Plan 04 lifespan ordering preserved) before lifespan-spawned tasks would format any record. Verified by `test_lifespan_falls_back_to_mock_when_credentials_missing` which inspects `caplog.records` and confirms no credential substring leaks. |
| T-07-SC | Tampering (supply chain) | No new packages | n/a | This plan adds no new dependencies. |
</threat_model>

<verification>
## Plan-Level Verification

1. `cd backend && uv run pytest tests/integration/test_health.py tests/integration/test_lifespan_flight_provider.py -v` — all pass.
2. `cd backend && uv run mypy app/api/main.py app/api/routes/routes.py` — strict, no errors.
3. `cd backend && uv run ruff check app/api/main.py app/api/routes/routes.py tests/integration/test_health.py tests/integration/test_lifespan_flight_provider.py` — clean.
4. Full unit + integration suite passes: `cd backend && uv run pytest tests/unit tests/integration -x --tb=short`.
5. Boot smoke: `cd backend && uv run uvicorn app.api.main:app --host 127.0.0.1 --port 8001 &` (background), then `curl -s http://127.0.0.1:8001/health` — returns `{"status":"healthy","flight_provider":"mock"}`. Kill the process. (Optional manual sanity, not required if the TestClient verification passes.)
</verification>

<success_criteria>
- Lifespan branches correctly across all three input combinations (mock env, real env without creds, real env with creds).
- WARN log line is emitted exactly once when falling back; startup never fails because of missing creds (D-05).
- `/health` carries `flight_provider` field of type `str` ∈ {"real", "mock"} (D-06).
- Plan 06's e2e_amadeus suite can rely on `app.state.flight_client` being an `AmadeusFlightClient` instance when `AMADEUS_*` env vars are present in CI.
</success_criteria>

<output>
Create `.planning/phases/07-real-flight-api/07-05-SUMMARY.md` documenting:
- The exact placement of `app.state.flight_provider = ...` relative to other `app.state.*` assignments.
- Whether `monkeypatch.setattr("app.config.settings.amadeus_env", ...)` worked or whether the test had to import-reload `app.api.main` (record the chosen approach for Plan 06's reference).
- Confirmation that the WARN log line includes the literal substring `"AMADEUS_* creds missing"` (Plan 06 CI inspection may parse for it).
</output>
